# 内部供应链模块落地方案（2026-07-26）

## 1. 已确认的业务定位

- 当前只有一家内部供应链公司，由内部员工使用 PC 后台。
- 内部供应链统一管理全部供应商、商品、订货单、配送单和默认仓库存。
- 门店仍按原流程下单、确认改单和收货；店长端本批次不改。
- 允许部分发货，配送单按实际发货数量生成；下单门店的下单者接收站内及企微通知。
- 门店实收少于实发时按实际收到数量入库，并自动形成报损事实。
- 总厨现有 BOM、报损裁决、门店库存和消耗查看保留；价格和商品变更直接生效，只通知总厨。
- 仓库模型保留多仓接口，当前只启用一个默认仓；库存增加由供应链手工入库完成。
- 外部供应商小程序属于后续阶段，届时只允许查看和处理自己绑定供应商的数据。

## 2. 权限边界

| 领域 | 内部供应链 | 外部供应商（后续） |
| --- | --- | --- |
| 订货单、配送履约 | 全租户可查询、接单、改单、实发、送达 | 仅绑定供应商 |
| 商品、分类 | 管理全部供应商商品，变更直接生效 | 仅自己的商品 |
| 默认仓库存 | 按供应商查看、手工入库、查看流水 | 仅自己的库存 |
| 供应商档案 | 新增、编辑基础档案、启停 | 不可管理其他供应商 |
| 门店收货、库存、消耗 | 跨店只读 | 不开放 |
| 账期、对账、发票 | 只读 | 仅自己的数据 |
| 付款、审核、银行账户 | 不开放 | 不开放 |
| 营业额、成本率 | 不开放 | 不开放 |

所有查询必须携带认证 tenant 范围；供应商维度写操作必须显式选择 supplierId，禁止缺失绑定时退化为全租户写入。

## 3. PC 信息架构

统一入口为 `/v2/supply-chain/home`，固定左侧导航分为三组：

1. 履约作业：供应链总览、订单履约、订货单查询、配送单查询、收货与差异。
2. 商品与仓库：商品管理、分类管理、仓库库存、供应商管理。
3. 核算与分析：账务查询、经营分析、我的账户。

旧供应商端的接单、改单、实际发货、商品和分类能力并入此入口，不删除底层独立订货单与配送单模型。

## 4. 本批次实现

- 新增统一 PC 工作台和左侧全功能导航。
- 恢复并复用原供应商订单履约、配送单、商品和分类能力。
- 内部角色可以跨供应商选择分类和默认仓库存，库存写入必须显式选择供应商。
- 新增默认仓库存页：SKU、低库存、缺货、库存金额、预占/可用和最近流水，并提供手工入库。
- 新增供应商管理入口；内部角色可维护基础档案、账期和状态，但不能读取或修改银行及自动付款字段。
- 新增账务只读页：账期、对账单、发票；不提供付款、审核和银行操作。
- 新增经营分析页：确认收货采购趋势、门店采购分布和低库存；不展示营业额和成本率。
- 供应链订单操作收敛到 tenant 范围；未来外部供应商仍保持 supplierId 强隔离。

## 5. 发布门禁与后续

- 本批次只进入 `integration/20260725-unified-rc`，不合并 main、不部署生产、不写生产数据。
- 发布前继续执行 API/Web 全量测试、数据库集成测试、构建、TypeScript、浏览器逐页验收、
  `git diff --check` 和敏感信息扫描。
- 多仓实体与真实 warehouseId 切换仍按独立批次推进；在所有写路径守恒核对完成前，不移除
  Product.stock 兼容桥。
- 供应链手工入库“更正/撤销”和门店已确认实收更正仍是冻结业务边界，不在本批次自行定义。

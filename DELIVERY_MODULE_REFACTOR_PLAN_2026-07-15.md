# 滇界 V4 配送/送货单模块整改与验证计划

日期：2026-07-15
状态：P0 本地实施与验收完成，待生产发布审批
前置：订货单 P0 冻结、修订和审计已完成
环境：仅本地隔离库，生产环境不修改

## 1. 已确认规则

1. 一张订货单的数据结构允许生成多张配送单，覆盖分车、缺货补送和分次配送。
2. 页面默认按剩余未配送数量一次全部发完，不强迫普通供应商理解分批概念。
3. 配送不能修改价格；配送单价从已确认订货单快照继承。
4. 本期按供应商单一虚拟仓库处理，配送单预留 `warehouseId`，暂不建设多仓页面。
5. 已发货配送单冻结；更正必须走冲销/补送，不直接覆盖历史记录。

## 2. 本轮 P0 范围

- 新增独立 `DeliveryOrder`、`DeliveryOrderItem`、`DeliveryOrderEvent`。
- 配送单独立编号、状态、金额、发货/送达/收货责任人和时间。
- 配送数量支持实际称重，并校验累计配送上限。
- 发货库存流水关联配送单，不再只关联订货单。
- 一张订货单允许多张配送单和多张对应入库单。
- 订购、累计实发、累计实收三种口径永久分离。
- 现有 `/orders/:id/ship|deliver|receive` 保持兼容，但内部改为驱动配送单。
- 提供配送单列表/详情及按日期、门店、供应商、商品、单号查询基础。
- 历史已发货订单生成兼容配送快照。

## 3. 暂不包含

- 多仓库库存余额和库间调拨。
- 司机、车辆、线路和运费管理。
- 配送单冲销审批完整 UI。
- 批次 FIFO 自动扣减。
- 配送导出综合报表。

## 4. 数据口径

| 口径 | 权威来源 |
|---|---|
| 原始订货 | `PurchaseOrder.submittedSnapshot` |
| 当前有效订货 | 已批准修订后的 `PurchaseOrderItem.quantity` |
| 本次实发 | `DeliveryOrderItem.shippedQty` |
| 累计实发 | 同订货单所有非作废配送明细之和 |
| 本次实收 | `DeliveryOrderItem.receivedQty` |
| 累计实收 | 同订货单所有已收货配送明细之和 |
| 财务入库 | 每张配送单对应的 `Receipt` 实收金额 |

`PurchaseOrderItem.shippedQty/receivedQty` 在兼容期保存累计值，不能再作为单次配送权威值。

## 5. 状态规则

配送单：`DRAFT → SHIPPED → DELIVERED → RECEIVED`，异常可进入 `CANCELLED`。

- 草稿只允许供应商本人修改。
- 发货后明细、单价和金额冻结。
- 送达后启动 24 小时门店确认倒计时。
- 本次收货完成但订货仍有剩余时，订货单回到 `CONFIRMED` 等待下一次配送。
- 全部订货数量完成且无报损时，订货单进入 `COMPLETED`；存在待处理差异时保留 `RECEIVED` 供后续流程处理。

## 6. API 落地

- `GET /api/deliveries`：组合查询配送单。
- `GET /api/deliveries/:id`：配送单详情与事件。
- `PATCH /api/orders/:id/ship`：按剩余数量直接创建并冻结一张 `SHIPPED` 配送单，扣减供应商库存。
- `PATCH /api/orders/:id/deliver`：推进当前在途配送单为已送达。
- `PATCH /api/orders/:id/receive`：门店按当前已送达配送单确认实收并生成独立入库单。
- 兼容接口已切换到配送单权威数据；独立草稿编辑 API 和冲销 UI 留在后续增强，不阻塞当前真实业务流程。

## 7. 迁移策略

1. 只新增表、字段和索引；Receipt 的订货单关联从一对一放宽为一对多。
2. 已发货、待确认和已收货历史订单各生成一张 `LEGACY_MIGRATED` 配送单。
3. 历史配送数量取 `shippedQty ?? quantity`，实收取 `receivedQty`。
4. 现有 Receipt 自动关联到生成的历史配送单。
5. 迁移完成后核对订单、配送单、入库单、金额与库存流水数量。

## 8. 自动化验证

- 同一订货单可顺序生成两张配送单。
- 默认草稿数量等于剩余未配送数量。
- 配送请求传入单价字段返回 400。
- 多次配送累计量不得超过商品配送上限。
- 发货只扣一次库存，重复请求幂等。
- 配送库存流水 `sourceType=DeliveryOrder` 且关联正确。
- 发货后配送明细不可修改。
- 送达前不能收货；越权门店/供应商返回 403/404。
- 第一张部分收货后订货单回到待继续配送。
- 最后一张收货后订货单完成，并产生两张独立 Receipt。
- 原始订货快照和原始金额在全流程保持不变。

## 9. 发布门槛

- 本地迁移、schema diff、API/Web 编译通过。
- 单元、API 端到端和浏览器流程通过。
- 历史迁移 dry-run 无未解释差异。
- 财务凭证、账期、报损、自动收货和库存回归通过。
- 生产结构副本验证、备份和回滚方案通过后，才可申请生产发布。

## 10. 2026-07-15 本地验收结果

- Prisma schema 格式化、校验和 Client 生成通过。
- 两个配送迁移已应用到本地隔离库，`migrate diff` 返回 `No difference detected`。
- API TypeScript 构建、Web TypeScript 检查和 Next.js 正式构建均通过，`git diff --check` 通过；构建仅保留 Sentry/OpenTelemetry 既有动态依赖警告。
- 订货单完整性单测 5/5 通过。
- 配送端到端脚本通过：一张 5kg 订货单先配送/收货 2kg，再默认配送/收货剩余 3kg；最终 2 张配送单、2 张入库单，累计实发/实收均为 5kg。
- 幂等重试、配送禁止改单价、库存流水关联配送单、订货原始快照不变均已由脚本断言。
- 浏览器验收通过：供应商订单页显示 2 张配送单及对应入库单；独立送货单显示本次 3kg、来源订货单 5kg、原订货单总额 31.25 元；页面无控制台错误。
- API 全量测试为 41/42：唯一失败是既有美团同步游标用例 `cursor 推进`，与本次配送改造无关，需在美团模块单独处理。
- 本地保留验收样例：订货单 `PO202607000011`，配送单 `DO202607000009`、`DO202607000010`。
- 生产环境未执行迁移、未部署代码、未改数据。

# 上游采购闭环执行计划

> 技术总负责：Codex
> 执行负责人：Codex（核心与供应商门户统一负责）
> 基线：`d9e09ee90cb337de1608c1ec9b1323980d113241`

## 1. 分工原则

- Codex 持有业务真相源、数据库、状态机、权限、库存和财务一致性。
- 数据库、状态机、API、供应商门户、测试和集成都由 Codex 顺序执行。
- 不再启用外部 AI 并行开发，避免接口契约漂移和共享工作区冲突。
- 生产部署仍需经过自动化测试、端到端验收和灰度闸门。

## 2. 工作包

| 编号 | 工作包 | 状态 | 交付物 |
|---|---|---|---|
| A0 | 修复主干 5 个 API 基线测试 | 本地完成 | 全量测试可信基线 |
| A1 | Upstream 数据模型、枚举、迁移 | 本地完成 | 独立上游领域模型、向前迁移、本地升级验证 |
| A2 | 上游供应商账号受限邀请与能力 | 本地完成 | 供应链仅可邀请上游供应商 OWNER/STAFF、对象级隔离 |
| A3 | 合同与供货商品正式价格 | 本地完成 | 合同、合同版本、合同商品 API |
| A4 | 采购单、改单、接单状态机 | 本地完成 | 统一领域服务、供应链/供应商命令 API |
| A5 | 分批发货 | 本地完成 | 发货单、剩余量、批次/效期、幂等 |
| A6 | 总仓验收与自动入库 | 本地完成 | 独立收货事务、台账/批次原子写入、复核闸门 |
| A7 | 到货差异、补报、仲裁 | 本地完成 | 索赔状态机、收货后补报、库存损耗过账 |
| A8 | 月度对账与发票分配 | 本地完成 | 对账生成、双方确认/异议、锁定、发票分配接口 |
| K1 | 上游供应商自助门户 | 本地完成 | 接单、改单、发货、差异、月结页面和测试 |
| V1 | 不变量、权限、并发与端到端验收 | 本地完成 | 1898 项自动化测试、5 条浏览器测试覆盖 E2E-00～E2E-07、截图与数据不变量证据 |
| R1 | 影子模式和单供应商灰度 | 未开始 | 生产备份/迁移、3 日影子一致、5 日真实过账零差异 |

## 3. 冻结架构边界

### 复用

- `Supplier`、`ProductUpstreamSource`。
- 供应商账号、邀请、JWT `supplierId` 绑定。
- `WarehouseLedgerBalance / Movement / Lot` 的锁、移动均价、批次和冲销内核。
- `Invoice / InvoicePayment`、OSS、操作日志和通知基础设施。

### 隔离

- 不复用带 `storeId` 的 `PurchaseOrder / DeliveryOrder / Receipt / LossClaim`。
- 不复用只能关联门店收货的 `ReconciliationItem`。
- 不把“制单即过账”的 `WarehouseDoc` 当成采购收货草稿。
- 不调用 `recordBatchManualWarehouseInbound` 冒充采购入库。

## 4. API 命名约定

统一前缀 `/api/upstream`，供应链端与供应商端调用同一领域服务：

- `/api/upstream/contracts`
- `/api/upstream/purchase-orders`
- `/api/upstream/shipments`
- `/api/upstream/receipts`
- `/api/upstream/arrival-claims`
- `/api/upstream/settlement-statements`

状态变化全部使用命令接口，禁止前端任意 PATCH `status`。供应商范围始终来自登录账号绑定，前端不得传 `supplierId` 决定归属。

## 5. 上线闸门

功能开关：

- `UPSTREAM_PROCUREMENT_ENABLED`
- `UPSTREAM_RECEIPT_POSTING_ENABLED`
- `UPSTREAM_MANUAL_INBOUND_RESTRICTED`

顺序：

1. 测试租户完整演练。
2. 生产 dark launch，全部写入开关关闭。
3. 单个供应商影子运行 3 个工作日，和现有手工流程逐行对比达到 100%。
4. 单个供应商真实入库 5 个工作日，重复入账、库存差异、应付差异均为 0。
5. 扩到 3～5 家供应商，并跑完第一个自然月对账。
6. 全量启用后，普通手工采购入库关闭，只保留审批制例外入口。

## 6. 本地完成情况与剩余上线工作

本地已经完成并验证：

1. API 107 个测试文件、1114 项测试全部通过；Web 60 个测试文件、784 项测试全部通过。
2. API/Web 生产构建通过；`git diff --check` 通过。
3. 本地数据库迁移成功；浏览器真实链路从“新建上游供应商 → 邀请负责人激活和首次登录 → 绑定供货商品”开始，继续完成合同 → 采购单 → 供应商接单/改单 → 分批发货 → 总仓收货/独立复核 → 收货后补报 → 供应商响应/仲裁 → 库存损耗 → 月度对账 → 财务锁定。
4. 台账产生独立的 `UPSTREAM_RECEIPT` 和 `LOSS` 流水，真实闭环没有借用手工入库或门店履约单据。
5. 真实演练发现并修复 Prisma 嵌套创建误传 `tenantId` 的问题；修复后全链路通过。
6. 创建采购单、发货单、收货单和收货后补报均带幂等键；视觉验收验证重放返回原单，并发确认稳定为一次成功、一次 409，不会重复库存流水、扣款或报损。
7. 分批履约同时覆盖短缺、破损、拒收：最终采购状态按“实到 + 已确认短缺”正确完成，不会永久停在部分收货。
8. 未消耗收货支持整单冲销：原始流水保留，反向流水后库存数量和价值净变化为 0；已消耗批次会被拒绝冲销。

上线前仍必须完成：

1. 备份生产数据库并演练迁移，首次部署保持 `UPSTREAM_PROCUREMENT_ENABLED=false`、`UPSTREAM_RECEIPT_POSTING_ENABLED=false`。
2. 建立一个真实上游供应商测试账号，只做 dark launch 和权限验收。
3. 选择单一供应商影子运行 3 个工作日，再开启真实库存过账 5 个工作日。
4. 账实、重复流水和应付差异均为 0 后，才逐步限制普通手工入库。
5. 不能随本次开发自动切换仓库 `STRICT` 模式；库存口径切换仍需独立盘点和评审。

## 7. 原外部任务处理

- `coordination/packets/KIMI_UPSTREAM_SUPPLIER_PORTAL_2026-09-16.md` 未发送，仅作为门户范围和验收要求参考。
- 后续不创建 Kimi 分支或工作区，也不接受外部 AI 未经核验的代码。

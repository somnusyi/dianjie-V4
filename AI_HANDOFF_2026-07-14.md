# 滇界 V4：AI 开发交接说明（2026-07-14）

> 用途：交给下一位 AI/开发者继续开发与验收。本文只记录可安全共享的项目、数据和运行信息；**不得将账号、密码、Token、银行密钥或生产数据库连接串写入 Git、文档或聊天记录**。

## 1. 项目目标与当前原则

滇界 V4 是南京云洱之境餐饮集团自用的多角色餐饮经营系统，当前以 **合肥瑶海店** 的真实经营数据为试点。核心目标是把采购、收货、库存、菜品销售、经营指标和财务数据连成可追溯闭环，后续再扩展到其他门店。

当前已经确认的业务原则：

- 系统处于真实使用场景，金额、库存、付款相关功能必须以可审计和可回滚为优先。
- 历史阶段没有完整的销售扣库存记录，因此**不能根据历史采购减去历史销售去反推当前库存**。
- `2026-07-13` 晚盘点应作为库存基准；基准后的库存再按“收货 + 调整/报损 - 销售理论消耗”滚动计算。
- 历史销售数据可以导入营业与菜品销量，但当前不应补生成历史库存消耗或凭证。
- 先在本地独立数据库验证，再由人工确认是否发布生产；当前没有授权直接发布生产。
- 银行自动付款/转账相关能力应保持关闭，除非生产环境配置和负责人再次确认。

## 2. 代码仓库与当前状态

| 项目 | 当前信息 |
| --- | --- |
| 工作目录 | `/Users/somnusyi/Documents/Codex/dianjie-V4-rebuilt` |
| Git 分支 | `rebuild/production-20260713` |
| 当前基线提交 | `915def0 rebuild production source from verified runtime` |
| GitHub 目标仓库 | `somnusyi/dianjie-V4` |
| 运行形态 | pnpm monorepo：Fastify API + Next.js Web + Prisma/PostgreSQL + Flask CMB 服务 |
| 生产形态 | 阿里云 ECS 上 PM2 进程；生产目录与真实密钥不得在本文档中记录 |

当前工作树**不是干净状态**。以下变动尚未提交，接手者需要先审查、构建、在本地数据库验证后，再决定分组提交：

- 经营页营业指标与同比/环比：`apps/api/src/routes/profit.ts`、`apps/web/src/app/v2/manager/ops/page.tsx`
- 店长首页库存摘要：`apps/api/src/routes/v2Dashboard.ts`、`apps/web/src/app/v2/manager/home/page.tsx`、`apps/web/src/components/v2/use-dashboard.tsx`
- 实物盘点快照与导入：`apps/api/src/services/storeInventory.ts`、`apps/api/src/routes/inventory.ts`、`apps/api/scripts/import-store-inventory-snapshot.ts`、`apps/web/src/app/v2/manager/inventory/`
- Prisma 数据模型和迁移：`packages/db/prisma/schema.prisma`、`packages/db/prisma/migrations/20260714113000_store_inventory_snapshots/`
- 历史瑶海营业/菜品销量导入脚本：`apps/api/scripts/import-yaohai-history.ts`
- 另有 `index.ts`、`auth.ts`、`cmb.ts` 等未提交改动；先逐项 diff 审查，不要随盘点功能一起盲目提交。

建议接手第一步执行：

```bash
cd /Users/somnusyi/Documents/Codex/dianjie-V4-rebuilt
git status --short
git diff --check
git diff --stat
git log --oneline -12
```

## 3. 架构速览

```text
apps/web          Next.js 14，移动端 V2 页面位于 src/app/v2/
apps/api          Fastify + TypeScript，业务 API、导入脚本和服务层
apps/cmb          Python/Flask 招行服务；默认应停用自动付款能力
packages/db       Prisma schema、迁移、seed 与数据库客户端
docker-compose.yml PostgreSQL / Redis 开发辅助配置
scripts/          部署与验证脚本
```

关键运行约束：

- API 源码默认端口和 Web 代理默认端口曾不一致。**本地统一使用 API `4444`、Web `3200`**，并在 Web 的 `.env.local` 显式设定 `NEXT_PUBLIC_API_BASE=http://localhost:4444`。
- API 的环境文件预期为 `apps/api/.env`；只能在本机或服务器保存，禁止提交。
- 数据库 schema 变更后要先生成 Prisma Client，再构建 API/Web。不要随意执行 `prisma format`：当前版本可能造成大范围纯格式 diff。
- 旧交接文档可能含过期基础设施和敏感信息。以当前 `.env.example`、部署脚本、实际服务器配置为准；发现明文敏感信息应删除/轮换并从历史文档移除。

## 4. 已实现但尚待本地验收的功能

### 4.1 店长“营业”页面

用户要求展示并对比以下四项：**营业额、营业收入、订单量、优惠金额**。

当前未提交实现已完成以下方向：

- `profit` API 从 `RevenueRecord.rawData` 读取历史 POS 导入的折前金额、折后金额、订单量、优惠金额；旧手工数据缺字段时回退到 `amount`。
- 当前月自动按已有营业日截取“上月同期”，避免把半个月与完整上月错误对比。
- `manager/ops` 页仅保留“本月 / 上月”切换，展示四项经营指标和比较百分比。

验收重点：

1. 核对“营业额 / 营业收入”的业务定义。当前实现将折前金额作为营业额、折后实收作为营业收入；如集团口径不同，统一修改字段命名和计算。
2. 没有 POS 原始字段的旧记录应显示可理解的降级结果，不能伪造订单量和优惠金额。
3. 当前月和上月的比较期、跨月天数、时区应做接口测试。

### 4.2 实物盘点库存基准与店长库存入口

新增模型：`InventorySnapshot` / `InventorySnapshotItem`，用于保留某一时点的实物盘点原样数据（名称、规格、单位、数量、单价、金额、分区），不会强行匹配错误 SKU。

当前实现状态：

- 新增迁移 `20260714113000_store_inventory_snapshots`。
- 新增 `GET /api/inventory/snapshot/latest`，为店长返回最近一份盘点摘要。
- 店长首页在任务区下方新增“库存盘点”卡片；无基准时明确显示“盘点基准待导入”，不再把错误推算库存当成真实库存。
- 新增 `/v2/manager/inventory`：可按全部、在库、零库存、未匹配筛选，并按盘点分区查看。
- 导入脚本默认为 dry-run；只有 `--commit` 才写数据库，覆盖既有盘点还需要 `--replace`。

已验证的盘点源数据：`瑶海7.13盘点.xlsx`（7 月 13 日结束后盘点）共 167 行，126 行数量大于 0、41 行为零，盘点金额合计约 `¥31,649.586`。该文件应作为 7 月 13 日收盘 / 7 月 14 日开盘的基准，不是实时库存。

仍需完成：

1. 在本地库执行迁移，导入该基准，登录店长端检查卡片和详情页。
2. 通过真实 `Product` 主数据逐项确认匹配；当前脚本只接受唯一的标准化名称匹配，歧义项保留未匹配，不能自动猜测。
3. 定义后续滚动规则：收货、报损、调拨、销售理论消耗、周期盘点差异如何进入库存台账。
4. 低库存提醒应等单位换算与商品匹配可靠后再启用；“零库存”不等于“必须采购”。

## 5. 数据资产与导入边界

| 数据 | 当前用途 | 可否直接导入/替换 |
| --- | --- | --- |
| 开业以来综合营业统计（截至 7/13 前版本） | 日粒度营业额、折扣、订单等 | 可经历史导入脚本导入 `RevenueRecord` |
| 开业以来菜品销售明细（截至 7/12 前版本） | 日粒度 `DishSale`、菜品销量 | 可导入；**不生成历史库存消耗** |
| `瑶海7.13盘点.xlsx` | 实物库存基准 | 可导入 `InventorySnapshot`，先本地 dry-run |
| 菜品利润分析/BOM 表 | 理论食材消耗和理论成本 | 可用于 BOM 映射；先做菜品名、商品名、单位的映射治理 |
| 新提供的“菜品销售统计（单品+套餐明细）” | 实际出货菜品的汇总参考 | 不能直接覆盖现有日粒度 `DishSale` |

### 新“实际出货菜品”表的关键结论

新文件覆盖约 `2026-04-01` 至 `2026-07-13`，是按菜品名称合并的汇总表，并非每日明细；其汇总值约为：销售数量 `22,755`、折前销售额 `¥738,676.10`、折后菜品收入 `¥587,588.81`、优惠 `¥151,087.29`。

它非常适合做该区间的整体菜品结构、BOM 理论用量和理论成本分析；但不能直接替换已导入/待导入的日粒度 `DishSale`，否则会破坏日经营趋势、日成本和未来库存消耗时间序列。

下一位开发者应二选一：

1. 向门店索取同一“单品+套餐明细”口径的**按日导出**，再替换日粒度销量；或
2. 新建“销售汇总批次/实际出货汇总”模型，单独保存本表并只用于区间分析，绝不与每日销量混写。

在未决定前，禁止以这张汇总表覆盖历史销售记录。

### BOM 与成本建议

推荐成本口径是：**BOM 标准用量 × 食材移动平均采购成本 = 理论食材成本**，并定期与实物盘点差异对账。采购记录完整但历史销售消耗不完整，所以不能用“累计采购 - 累计库存”当作已验证成本或库存。

已知难点是菜品/套餐/规格命名与主数据不完全一致，需建立：菜品别名、套餐拆分规则、食材 SKU 映射、单位换算四类基础映射。映射未完成前，成本报表必须显示覆盖率和未匹配项。

## 6. 本地验证环境（尚未完整跑通）

目标是建立独立本地 PostgreSQL，不连接生产库。已创建隔离容器，宿主机仅绑定到 `127.0.0.1:5434`；不要复用或删除机器上其他旧项目容器。

建议流程：

```bash
# 仅在本地 .env 配置好 DATABASE_URL 后执行
pnpm --filter @dianjie/db generate
pnpm --filter @dianjie/db exec prisma migrate deploy
pnpm --filter @dianjie/api build
pnpm --filter @dianjie/web build

# 盘点导入先验证，不写入
pnpm --filter @dianjie/api exec tsx scripts/import-store-inventory-snapshot.ts --offline
# 确认门店、文件和映射后，才增加 --commit
```

本地还缺少可重复的“瑶海测试租户/门店/店长”初始化。建议新增一个仅开发环境使用的 bootstrap 脚本，创建测试租户、瑶海门店和本地测试账号；不得复制生产人员或真实凭据。之后按顺序导入历史营业数据和盘点基准。

此前已完成的构建验证：Prisma schema 校验、Prisma client 生成、API TypeScript 构建、Web Next.js 构建均通过。Web 构建存在 Sentry/OpenTelemetry 的非阻塞依赖警告，发布前仍应重新跑一次完整构建。

## 7. 已知风险与待审计项

1. **采购月份统计错误风险**：历史采购导入的 `createdAt` 集中在导入日；一些聚合逻辑可能错误按 `createdAt` 归属月份。采购经营分析应按 `deliveryDate`（或经确认的业务日期）计算。
2. **历史库存不可追溯**：销售扣库存并未从开业日起完整记录。盘点快照上线前，原库存推算仅可作参考，不可用于补货或财务结论。
3. **销售口径混用风险**：套餐销售、单品实际出货、折前/折后/到账金额必须分表或显式标识；禁止默认等价。
4. **多租户与权限**：每个按 ID 更新/查询的 API 都需 tenant 和 store scope；禁止只用裸 ID 更新。
5. **财务/银行安全**：自动付款、转账、回单等涉及真实资金，必须做环境开关、审批、审计日志和人工验收；当前默认关闭。
6. **敏感信息治理**：历史文档和脚本可能含明文连接/访问资料。接手后应开展一次秘密扫描和清理，密钥放入服务器环境变量或受控密钥服务，并规划后续轮换；本次不自行轮换生产凭据。
7. **旧交接内容过期**：早期文档中的路径、端口、部署方式、账号信息可能与当前重建分支不一致，不能照抄执行。

## 8. 发布与提交策略

不应把当前所有未提交改动一次性推送。建议拆为三组可回滚提交：

1. `feat(manager-ops): add operating metrics and comparable month-over-month data`
2. `feat(inventory): add physical inventory snapshot baseline and manager views`
3. `chore(import): add Yaohai historical revenue and inventory import tooling`

每组至少完成：`git diff --check`、Prisma 校验（涉及 schema 时）、API build、Web build、关键 API/页面的本地冒烟测试。数据库迁移必须先在本地库跑通，再由负责人确认生产发布窗口。

生产部署只使用仓库已有的受控部署脚本（`scripts/deploy-worktree.sh`）和部署验证步骤；不要手工只同步某一个 `dist`、schema 或 `.next` 目录。部署前确认：备份、迁移可执行、PM2 健康检查、回滚版本、真实密钥未被写入提交。

## 9. 下一位开发者的优先级清单

### P0：先确保数据正确

- [ ] 审查当前未提交 diff，拆分并在本地库验证。
- [ ] 初始化独立本地瑶海测试数据，迁移数据库。
- [ ] dry-run 后导入 7/13 实物盘点，核对 167 行、金额及门店展示。
- [ ] 明确新“实际出货菜品汇总表”采用“索要日明细”还是“独立汇总模型”的方案；未决前不得覆盖日销量。
- [ ] 修正所有采购月度分析中按 `createdAt` 而非业务日期归集的查询。

### P1：构建可用库存与成本闭环

- [ ] 完成 BOM 菜品、套餐拆分、食材 SKU、单位换算的映射表与维护界面/导入模板。
- [ ] 实现基准日后的理论库存流水，并支持盘点差异调整与审计。
- [ ] 以移动平均采购成本计算理论成本，展示匹配覆盖率和未匹配清单。
- [ ] 每日要求店长提交营业汇总和菜品销售明细；允许批量上传、校验日期重复与覆盖策略。

### P2：系统性代码审计

- [ ] 权限/租户隔离审计。
- [ ] 金额精度、时区、日期边界和幂等性审计。
- [ ] 导入任务的重复导入、撤销、审计日志与异常报告。
- [ ] API 错误处理、监控、备份恢复演练。
- [ ] 明文敏感信息扫描、清理和安全发布流程。

## 10. 接手时建议先问业务负责人确认的三件事

1. “营业额、营业收入”在集团报表中的最终定义是否分别为折前 GMV 与折后实收；平台抽成是否另列还是计入收入。
2. 是否能导出“单品+套餐明细”的日粒度报表；如不能，是否同意建立独立的实际出货汇总批次。
3. 7/13 盘点的时间点、门店范围、单位/规格是否已确认；之后的盘点频率和报损/调拨流程由谁负责录入。

---

最后更新：2026-07-14。本文描述的是当前本地重建分支与未提交工作树，不等同于生产已上线状态。

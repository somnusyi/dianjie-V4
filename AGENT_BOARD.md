# 滇界 V4 任务板

> 本文件是仓库内唯一任务真相源。状态改变必须和代码提交一起更新。

状态：`待认领 → 进行中 → 待验收 → 已发布`；业务信息不足的任务进入 `待外部`。

## 进行中

当前无已认领开发任务。P1-002 在服务器 GitHub 写权限补齐后进入实现。

## 待认领

### P1-002 Qwen 全栈自治第二阶段

- 支持核心 API 但必须管理员二次批准。
- 允许新增向前兼容 migration。
- migration 必须通过空库验证、升级验证、备份验证和人工终审。

### P1-003 DJ002 上线准备

- 导入初始盘点；
- 校验账号、SKU、供应商、BOM、日报导入；
- 完成订货到财务结算端到端验收。

### P1-004 主数据主动校验

- BOM 单位换算异常预警；
- SKU 采购/库存/成本/订货单位完整性；
- 配方发布前 dry-run。

### P1-005 经营与库存通知

- 老板晨报；
- 安全库存预警；
- 日报缺失及异常提醒。

## 待外部

### EXT-001 GitHub 自动发布写权限

- 服务器 key `/root/.ssh/dianjie_github_deploy` 当前只能读取仓库。
- 需要在 GitHub 仓库 Deploy keys 中为对应 key 开启 `Allow write access`。

### EXT-002 DJ002 初始盘点

- 等待包河万达店厨师长实盘数据。

### EXT-003 六项 SKU 单位换算

- 等待总厨确认茶、毛肚、黄喉、辣椒、盐冻虾、冰激凌的业务换算口径。

## 暂缓

- 百家蘸料 BOM。
- 打包盒 BOM。

## 已发布

- 2026-07-30 P1-001 Qwen 全栈自治第一阶段：
  - 已完成确定性的变更分类与验证计划底座；
  - 已接入 Web + 非核心 API 的 Qwen 开发任务与范围化独立验证；
  - API 发布已加入构建、生产 dist 备份、同步、进程重启、健康检查和同组件回滚；
  - Web/API 混合发布严格执行“全部构建 → 统一备份 → 同步 → 重启”；
  - 资金、权限、认证、核心库存写入/成本和数据库仍强制拒绝；
  - 管理员二次上线批准保持不变；
  - API 单元测试 682 项、集成测试 186 项、类型检查和构建通过；
  - Gitea 与 GitHub 均已包含 P1 生产发布基线 `f5563576`。
- 2026-07-29 P0-001 接管与发布链路治理：
  - 修复反馈集成测试和隔离测试库 migration 前置；
  - `AUTO_FIX_MODE=off` 时不再创建 Qwen 任务；
  - 自动部署改为“远程候选源码持久化成功后才允许触碰生产”；
  - 生产、GitHub、本地和服务器源码完成版本核验；
  - 交接材料、任务板和历史 worktree 审计纳入仓库；
  - API/Web 单测、186 项集成测试、类型检查、构建和生产健康检查通过。
- 2026-07-29 前历史发布见 Git 日志和部署记录。

# 多标签页工作台方案（美团管家式）

> 编制：2026-08-18，待审批
> 需求来源：负责人截图（美团管家·火锅版的多标签工作方式）——顶部一排可切换可关闭的标签，各标签页面状态保留（填一半的表单、选好的筛选不丢），同时开着订单/库存/差异单对照操作

## 一、技术选型

| 方案 | 说明 | 结论 |
|---|---|---|
| **A. iframe 多页签框架（推荐）** | 新增一个"工作台"页面：顶部标签栏 + 内容区，每个标签是一个 iframe 加载现有路由 | **采用**。页面状态天然保留；现有 196 个页面零改动；同域登录态共享；美团管家本身就是这种架构；1-2 天可用 |
| B. 客户端 keep-alive | 自研组件树缓存，路由变"假路由" | 否决。196 页面全要适配，破坏 URL 直达/刷新语义，工程量不可控 |
| C. 轻量标签栏（不保状态） | 只记录历史快速切换 | 否决。不满足"表单填一半不丢"，体验差距大 |

## 二、方案 A 设计

### 结构

```
/v2/workbench（新框架页，PC 端）
├── 左侧：角色菜单（复用现有菜单配置）
├── 顶部：标签栏 [首页 ×][采购订单 ×][库存 ×][差异单 ×]…
└── 内容区：每个标签一个隐藏切换的 <iframe src="现有路由?embed=1">
```

### 关键机制

1. **开标签**：点菜单 → 已开过则激活，没开过则新增标签 + iframe（上限 8 个，超出提示先关）。
2. **状态保留**：切换标签只切 iframe 显隐，不销毁——表单、筛选、滚动位置全保留。
3. **embed 模式**：iframe 加载的页面带 `?embed=1`，AppLayout 检测后隐藏自己的侧边栏（页面本身一行配置不改，只改布局组件的判断）。直接输 URL 正常访问的页面不受影响。
4. **登录态**：回滚后仍是 localStorage，同域 iframe 自动共享 ✓——工作台里所有标签同身份。若将来需要"双开不同身份"，用浏览器的多用户配置文件即可。
5. **登出联动**：任一 iframe 跳到 /login 时（检测 self !== top），postMessage 通知父框架整体清理标签并跳登录页。
6. **标签恢复**：标签列表存 sessionStorage——工作台页刷新后标签全部恢复（页面状态随 iframe 刷新重载，这是 iframe 方案边界，刷新即回到各页初始态；不刷新则一直保留）。
7. **移动端不启用**：窄屏检测自动跳转普通单页模式（iframe 多标签是 PC 场景）。

### 入口

- 各角色 PC 端登录后默认进工作台（可配置回普通模式）；或先做成一个入口按钮（"多窗口模式"）供你和会计先用，稳定后再默认。

## 三、实施清单

1. `/v2/workbench` 框架页 + 标签栏组件（含关闭/激活/上限）
2. AppLayout 的 embed 判断（隐藏侧栏）
3. 登出联动 postMessage
4. 角色菜单配置抽取复用（与现有侧边栏同源，避免两套菜单漂移）
5. 测试：组件测试 + 手工冒烟（开 4 个标签填表单切换验证状态保留、登出联动、刷新恢复）

## 四、附带说明

- 误解期间做的"每标签页独立登录"改造已回滚（`327b3d5` 已部署），自动登录行为恢复原状。
- 本方案不影响手机端和现有任何页面的直达访问。

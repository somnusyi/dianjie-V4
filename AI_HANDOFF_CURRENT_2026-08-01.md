# 滇界 V4 项目完整交接文档

> 更新时间：2026-08-01（Asia/Shanghai）  
> 适用对象：接手项目的 Codex、架构师、全栈开发、测试及运维人员  
> 当前可信代码版本：`a03cca5ea6c9c03488e5751e16844ae206d2892c`  
> 安全说明：本文不保存账号密码、数据库连接串、云密钥、SSH 私钥或员工个人凭证。

## 0. 接手者先读

1. 这是一个已经在生产真实使用的连锁餐饮多租户系统，不是演示项目。
2. 2026-08-01 已现场核验：本地 `main`、GitHub `origin/main`、Gitea `gitea/main`、服务器源码 `server/main`、生产部署标记全部指向 `a03cca5e`。
3. 生产 API、数据库、Web 登录页正常，PM2 的 API/Web 进程在线。
4. 当前没有已认领开发任务，但并不代表没有待办。下一阶段最高优先级是“供应链美团库存快照 + 多门店每日出货明细导入”，该需求尚未完成开发。
5. 生产真实租户已有两家店：DJ001 瑶海店在真实使用；DJ002 包河万达店已建档，但没有初始库存、日报和完整上线验收，不能把其预计库存当作可信数据。
6. 自动修复系统已具备 Web、非核心 API 的自主开发/测试/发布能力；核心经营 API 仅可在专用闸门和隔离数据库下临时放行；认证、权限、资金、数据库迁移、依赖和部署配置仍是永久红线。
7. 接手后先做只读核验，不要直接改生产数据、不要整分支覆盖、不要修改历史 migration，也不要清理本地未跟踪文件。

## 1. 项目定位

滇界 V4 是南京云洱之境餐饮集团使用的多角色、可扩展连锁餐饮经营管理系统。当前以合肥瑶海店为真实业务基线，目标扩展到至少 5 家门店。

核心业务链路：

```text
商品/供应商/单位主数据
        ↓
门店查看预计库存并订货
        ↓
内部供应链接单、改单、实发、配送
        ↓
门店验收，确认实收与到货差异
        ↓
门店入库 + 移动平均成本 + 供应商应付
        ↓
店长每日上传营业统计和菜品销售双表
        ↓
菜品销量 × 已发布 BOM = 食材理论消耗
        ↓
预计库存、报损、盘点、月结和财务分析
```

系统继续采用“模块化单体”。除招行国密辅助服务外，当前没有拆微服务的必要；扩店阶段优先保证租户隔离、业务原子性、可审计和可回滚。

## 2. 代码仓库、环境与版本真相

### 2.1 仓库

| 对象 | 地址/路径 | 当前状态 |
| --- | --- | --- |
| 本地工作仓 | `/Users/somnusyi/Documents/Codex/dianjie-V4-rebuilt` | `main = a03cca5e` |
| GitHub | `git@github.com:somnusyi/dianjie-V4.git` | `origin/main = a03cca5e` |
| 内网 Gitea | `http://192.168.1.129:1606/weilun/dianjie-V4.git` | `gitea/main = a03cca5e` |
| 服务器源码仓 | `ssh://root@116.62.32.162/app/dianjie-src` | `server/main = a03cca5e`，干净 |
| 生产应用目录 | `/app/dianjie-v4` | `.deployed-commit = a03cca5e` |

凭证不在本文记录。需要 Git、SSH、生产账号或数据库权限时，从项目负责人或本机安全凭证存储获取。

### 2.2 生产

- 域名：`https://njdianjie.com`
- 服务器：`116.62.32.162`
- API：PM2 `dianjie-v4-api`，内部端口 4004
- Web：PM2 `dianjie-v4-web`，内部端口 3204
- 生产源码：`/app/dianjie-src`
- 生产应用：`/app/dianjie-v4`
- 运行时：Node.js 20、pnpm 10.32.1、Prisma 5.22.0、PostgreSQL 15
- migration：73 条已应用，无已知失败记录
- 2026-08-01 健康检查：`/api/health` 返回 `status=ok, db=ok`；`/v2/login` 返回 200

### 2.3 本地运行

- 常用 Web：`http://localhost:3200/v2/login`
- 本地必须使用 Node.js 20。当前机器可将 `/opt/homebrew/opt/node@20/bin` 放到 `PATH` 前部。
- 本地和集成测试数据库必须与生产隔离；集成库名称必须以 `_test` 或 `_ci` 结尾。
- 不要在业务正在使用的本地 `.next` 目录上直接做生产构建；用干净 worktree。

## 3. 技术架构

```text
浏览器 / PWA / Capacitor
          │
          ▼
Next.js 14 + React 18 + Tailwind
          │ REST / JWT
          ▼
Fastify 4 + TypeScript
          │
          ▼
Prisma 5 + PostgreSQL 15

辅助：Redis/BullMQ、阿里云 OSS、企业微信、招行 CMB Python 服务、
美团接口代码（生产未启用）、Qwen Code 自动开发管线
```

### 3.1 Monorepo

| 路径 | 作用 |
| --- | --- |
| `apps/web` | Next.js 多角色 Web/PWA、移动端和 PC 工作台 |
| `apps/api` | Fastify API、业务服务、调度器、导入器和外部集成 |
| `apps/cmb` | 招行国密辅助服务；自动付款仍受控 |
| `packages/db` | Prisma schema、73 条 migration、客户端和 seed |
| `scripts` | 环境校验、构建、发布、备份、迁移核验和专项验收 |

### 3.2 工程硬规则

- 所有业务查询必须带 `tenantId`；门店角色再限定 `storeId`，外部供应商再限定 `supplierId`。
- 写接口用 zod 校验；关键写入必须写 OpLog，且 `entityType/targetId` 完整。
- 多步业务事实、金额、状态、库存和审计日志必须同事务；并发敏感写入使用数据库锁/幂等键。
- 金额和数量使用 Decimal，禁止用 JavaScript 浮点累计关键财务事实。
- 历史单据读取冻结快照，商品改名、单位或价格变化不能重写历史单据。
- 已确认库存/消耗/盘点/入库需要纠错时，使用冲销 + 补记，不直接篡改历史。
- 禁止修改历史 migration；禁止用 `prisma db push` 代替 migration。
- OSS 附件读取前使用现有 `resignOssUrls()` 重新签名。
- 兼容旧 Android/微信 WebView，不直接依赖 `crypto.randomUUID()`；使用项目 `clientRequestId()`。

## 4. 角色和当前能力

| 角色 | 已上线能力 | 主要入口 | 当前缺口/注意事项 |
| --- | --- | --- | --- |
| 老板/超管 | 集团经营、门店、审批、资金、报表、反馈、AI 助手、自动修复审批 | `/v2/boss/home`、`/v2/boss/stores`、`/v2/boss/autofix` | 自动修复历史转人工任务需逐条判断，不能批量重跑 |
| 财务 | 各店、应付、对账、发票、付款申请、凭证、资金、月结、新建店 | `/v2/finance/home`、`/v2/finance-pc/home` | 真实财务 UAT 和历史月份展示仍需加强；财务域是 AI 永久红线 |
| 总厨 | 菜品档案、默认/规格 BOM、配方版本、发布、上下架、别名、缺 BOM 待办、报损/争议 | `/v2/chef-director/home`、`/v2/chef-director/dishes` | 仍有 BOM、别名、单位换算和套餐拆解数据治理工作 |
| 店长 | 营业看板、日报双表上传、预计库存、门店运营、费用、团队 | `/v2/manager/home`、`/v2/manager/upload-platform`、`/v2/manager/inventory` | 美团 API 未开，日报仍需每天人工上传 |
| 厨师长 | 查看门店预计库存、订货、收货/验收、盘点入口 | `/v2/chef/home`、`/v2/chef/inventory`、`/v2/chef/purchase` | 新店必须先建立初始库存基准 |
| 内部供应链 | PC 工作台、订单、配送、商品、分类、供应商、默认仓库存、收货只读、差异、分析、账务只读 | `/v2/supply-chain/home` | 美团库存快照和多门店每日出货文件的专用导入尚未实现 |
| 外部供应商 | 自己的商品/分类、订单履约、库存、差异、账单、发票、对账 | `/v2/supplier/home` | 必须保持 supplierId 强隔离；不能访问银行/付款审批 |
| 工程人员 | 工程工作台和任务 | `/v2/engineer/home` | 仅按授权使用 |

## 5. 业务规则真相

### 5.1 订货、实发、收货

1. 当前所有货物直接送门店；供应链只有一个默认仓，但数据结构保留未来多仓扩展。
2. 门店订货后，内部供应链可接单、调整本次实发数量并配送。
3. 原始订购量、实发量、实收量必须分别保存；原订货金额、实发金额、应付金额不得混用。
4. 允许部分发货；剩余未发部分按当前业务规则原子关闭，不能留下无法处理的悬空数量。
5. 门店在收货时一次性确认短缺、破损、品质和证据。收货确认是供应商责任截止点。
6. 收货后不再向供应商补报，不建立“免费补送”；后续发现的问题进入门店内部报损或盘点差异。
7. 合格实收形成门店库存和应付；应付按冻结单价 × 合格实收数量计算。
8. 已确认入库单不得直接改商品、数量、价格和应付；必须走审计更正/冲销方案。

### 5.2 两本库存账

- 供应链/供应商仓库存与门店库存是两本独立账，不能互相覆盖。
- 供应链严格库存时：接单预占，取消/拒单释放，发货核销预占并扣物理库存。
- 未完成供应链期初库存时，可使用“不跟踪库存”模式，不能因为系统库存 0 阻断真实发货。
- 门店预计库存口径：

```text
最近可信盘点/初始快照
+ 后续确认实收入库
- 日报 BOM 理论消耗
- 人工消耗
- 门店内部报损
± 审计调整/冲销补记
```

- 门店工作台和厨师长工作台展示的是实时预计库存，不是首次盘点金额。
- 盘点是门店内部行为，与供应商库存和供应商应付无关。
- 已确认盘点不可编辑；录错时整单冲销后重做。

### 5.3 日报和经营数据

1. 美团 API 未正式启用前，店长每天 11:00 前上传前一营业日两份表：综合营业统计、菜品销售明细/品项销售明细。
2. 上传流程为“选两份文件 → 生成预览 → 核对 → 确认”。
3. 确认必须原子写入营业数据、菜品销量和当日可执行 BOM 消耗；任一步失败都不能部分成功。
4. 系统每天 11:00 检查前一营业日缺报，并向店长/厨师长发送提醒。
5. 缺菜品档案或缺 BOM 可以显式暂缓，生成总厨待办；总厨补齐后幂等回补历史消耗。
6. 套餐不能直接作为最终扣库对象，必须使用实际出货菜品；后续要求销售明细本身不再包含套餐。

### 5.4 BOM、单位和成本

- BOM 按毛重口径；退菜不补回库存。
- 百家蘸料不扣库存；赠品按对应库存商品直接扣减。
- 鲜鸡/冻品只按已确认映射替代，不能模糊串货。
- 配方使用不可变版本：已发布版本不能原地改写，先复制草稿、修改、再发布生效。
- 先匹配 POS 规格 BOM；没有精确规格时才回退默认 BOM。
- 四单位合同：采购单位、库存单位、成本单位、订货单位。单位换算必须明确且可审计。
- 成本单位固定为最小库存单位；采购包装价需换算为最小单位移动平均成本。
- BOM 的职责是定义“每份菜消耗多少最小库存单位”，不是手工维护一个独立成本真相。
- 菜品成本 = 生效 BOM 用量 × 当时移动平均库存成本；不要把采购整箱价格直接当单份菜品成本。

## 6. 当前生产数据基线（2026-08-01 只读核验）

生产有两个租户：真实租户 `dianjie` 和预览/测试租户 `test`。不要把测试租户数据计入经营统计。

### 6.1 真实租户门店

| 门店 | 状态 | 账号绑定 | 日报导入 | 库存快照 | 判断 |
| --- | --- | ---: | ---: | ---: | --- |
| DJ001 合肥瑶海店 | ENABLED / OPERATING | 5 | 16 | 1 | 真实运行基线，可继续使用 |
| DJ002 合肥包河万达店 | ENABLED / OPERATING | 2 | 0 | 0 | 尚未完成库存与日报上线，预计库存不可信 |

测试租户另有一条 DJ001，仅用于预览/测试，不属于真实门店数量。

### 6.2 自动修复任务

| 状态 | 数量 |
| --- | ---: |
| RESOLVED | 14 |
| ESCALATED | 9 |
| 运行中状态 | 0 |

九个 ESCALATED 是历史转人工记录，不应直接全部重跑。应先判断需求是否仍存在、是否已被后续代码覆盖、是否命中永久红线，再只重跑仍有效的任务。

## 7. 近期已经完成的重要改进

### 7.1 供应链 V5

- 内部供应链角色与外部供应商角色分离；内部供应链可跨供应商操作，外部供应商严格绑定自身 supplierId。
- PC 供应链统一工作台、商品/分类、订单/配送、收货只读、仓库库存、供应商管理、差异、账务只读和分析页面已上线。
- 商品支持搜索、分类侧栏、图片预览、过滤导出、跨页选择、批量分类/停售/恢复。
- 供应链商品变更直接生效并通知总厨；外部供应商继续受自身范围和能力控制。
- 已加入四单位合同、三位小数数量、历史单据快照和默认仓库模型。
- 订单价格按成本单位/订货单位正确折算，历史订单保存冻结快照。
- 部分发货关闭、实发/配送数量、通知对象、收货查询和库存分页均完成过多轮并发与回归加固。
- 供应链有 Excel 增量入库页面；外部供应商有全量库存快照导入页面。两者都要求商品先建档，不会静默创建未知 SKU。

### 7.2 门店、日报、库存和 BOM

- 店长/厨师长可查看实时预计库存和库存健康，而非只显示初始盘点金额。
- 日报双表支持预览、确认、营业/销量/BOM 原子写入、缺 BOM 暂缓和后续回补。
- 总厨菜品档案支持菜品上新/下架、POS 别名、默认/规格 BOM、版本记录和发布生效。
- 移动平均成本、库存基础单位、包装换算和同日“先收货、后闭店扣耗”的口径已进入代码。
- 财务各店页已有新建门店入口。

### 7.3 AI 反馈、自主开发和自动发布

- 用户可在 App 内提交反馈和截图；Qwen 参与澄清、分类和任务书生成。
- 超管可在手机审批中心批准开发，也可从 AI 助手发起开发任务。
- `approved_auto` 低风险任务一次批准即授权开发、独立验证和自动发布；`suggest` 模式仍需要上线终审。
- Web、普通非核心 API、Web/API 混合变更都有范围化测试、构建、备份、发布、健康检查和自动回滚。
- 自动发布顺序为“远程候选源码持久化 → 生产部署 → 健康检查 → 晋升 main”，远程不可写时不得触碰生产。
- 核心库存/订单/采购/收货/配送/盘点/报损/BOM 消耗/结算/成本 API 只有在 `AUTO_FIX_CORE_API_ENABLED=true` 且设置隔离测试库时才可进入自动开发。
- 认证、权限、资金/支付/财务、Prisma schema/migration、依赖和部署配置始终拒绝自动修改。
- 2026-08-01 已修复误转人工问题：
  - 测试进程隔离生产域名变量，避免无关环境导致假失败；
  - Qwen 明确返回 `NO_CHANGE`、`无源码改动`、`当前代码已满足` 时，独立复验通过即可直接解决；
  - 网络、超时、限流等临时故障自动重试一次；
  - ESCALATED 页面可基于最新代码手工重试；
  - 供应链等角色页面壳不再被过宽红线误伤。
- 两个生产验证案例：
  - 商品四单位/成本单位规则由 Qwen 修改 3 个 Web 文件，独立测试通过后自动部署，提交 `5e744448`；
  - 供应链分类过滤任务由 Qwen 判断当前代码已满足，系统独立验证后以“无改动”解决，没有制造空提交。

## 8. 当前验证基线

2026-08-01 在本地 `a03cca5e` 上重新执行：

| 验证 | 结果 |
| --- | --- |
| API 单元测试 | 66 个文件，791 项通过 |
| Web 单元测试 | 30 个文件，545 项通过 |
| API TypeScript | 通过 |
| Web TypeScript | 通过 |
| 隔离数据库集成测试 | 最近完整基线 186/186 通过 |
| production migration | 73 条已应用 |
| 生产 API/DB/Web 健康 | 通过 |

常用命令：

```bash
export PATH=/opt/homebrew/opt/node@20/bin:$PATH
pnpm env:check
pnpm --filter @dianjie/api test
pnpm --filter @dianjie/api exec tsc --noEmit
pnpm --filter @dianjie/web test
pnpm --filter @dianjie/web exec tsc --noEmit
```

涉及核心业务 API 时，除单测/类型/构建外还必须使用名称以 `_test` 或 `_ci` 结尾的隔离 PostgreSQL 执行 migration 和领域集成测试。禁止继承生产 `DATABASE_URL`。

## 9. 当前明确未完成事项

### P0：供应链美团库存与多门店出货导入

业务方已经确认：

- 供应链负责人会从美团导出最新仓库库存；
- 后续每天会导出“除瑶海店外”的各门店出货明细；
- 系统需要专用上传入口，并据此维护供应链库存和出货流水。

当前已有可复用底座：

- 外部供应商全量快照接口：`POST /api/supplier/stock/import-snapshot`；
- 内部供应链 Excel 增量入库页：`/v2/supply-chain/inventory/import`；
- 默认仓库、库存流水、批次、预占、实发扣减、SKU 主数据和四单位合同；
- 未匹配 SKU 会整批阻断，不会静默跳过。

但“美团专用文件适配 + 导入批次 + 门店出货幂等 + 排除瑶海规则 + 日结对账”尚未实现。接手者应先拿到一份真实库存文件和一份真实出货文件，再确定字段和单位。推荐实现原则：

1. 文件先上传到暂存批次，只解析、匹配和预览，不直接写库存。
2. 以文件哈希 + 业务日期 + 来源门店/仓库做幂等，同一文件重复上传不能重复扣减。
3. 库存文件按“目标快照”处理；每日出货按“增量出库”处理，两种语义不能混用。
4. 出货必须生成独立库存流水和来源单号；不能伪造成门店订货单或配送单。
5. 瑶海店已有系统内订货/配送链路，默认排除外部出货导入，避免重复扣库；若文件中出现瑶海行，应在预览中明确阻断或标红。
6. 商品按美团编码优先匹配，其次审核后的别名；仅凭模糊品名不得自动写入。
7. 单位必须先按四单位合同换算；待确认换算阻断整批确认。
8. 确认动作采用单事务或可证明的分块原子策略，并写导入批次、行明细、库存流水和 OpLog。
9. 支持整批冲销，不直接删除历史；冲销后允许修正文件形成新版本。
10. 上线前覆盖重复上传、部分错误、跨门店、跨供应商、并发确认、负库存、瑶海重复扣减和冲销 E2E。

### P0/P1：扩展到 5 家店

- DJ002 需要导入初始盘点、校验账号、SKU、供应商、BOM、日报，并完成从订货到财务结算的 E2E 验收。
- 后续每家新店必须满足上线门槛：门店档案、角色账号、供应商品范围、商品单位、初始库存、日报模板、菜品别名/BOM、安全库存和权限隔离。
- 禁止仅“新建一条门店记录”就宣布上线；没有初始库存时，预计库存必须明确显示不可信。

### P1：主数据和 BOM 主动治理

- 六项 SKU 单位换算仍等待业务确认：茶、毛肚、黄喉、辣椒、盐冻虾、冰激凌。
- 百家蘸料和打包盒 BOM 暂缓。
- 持续检查缺菜品档案、缺 BOM、未发布 BOM、规格未覆盖、套餐未拆实际菜品、POS 别名冲突和采购 SKU 不匹配。
- 配方发布前增加 dry-run：按近 7/30 天销量模拟消耗、库存和成本异常，阻止数量级错误发布。

### P1：经营和库存通知

- 老板晨报；
- 安全库存/缺货预警；
- 日报缺失和异常提醒完善；
- 供应链库存文件、出货文件漏传提醒；
- 5 店运营时的跨店异常汇总。

### P1/P2：盘点和历史

- 盘点业务规则已定义，页面/状态机有历史实现；项目负责人此前要求盘点阶段暂缓。
- 重新启动前先核对当前 `main` 实际可用程度和生产数据，不要依据旧分支文档直接部署。
- 最终应保留盘点历史、确认基准、差异、证据和整单冲销。

### P2：数据库自治和观测

- 数据库自动修改尚未开放。未来只允许新增、向前兼容 migration，并需要空库验证、升级验证、备份和人工终审。
- Sentry、正式 CI、staging、告警和证书治理仍需继续完善；不要因观测缺口阻断当前业务，但上线前必须保留健康检查和回滚。

## 10. 自动修复系统当前边界

### 可自动处理

- Web 页面、交互、展示、非敏感前端业务逻辑；
- 普通非核心 API；
- 明确开闸后的核心经营 API，但必须使用隔离数据库并通过额外领域验证；
- 测试、类型检查、构建、源码持久化、备份、部署、健康检查和回滚。

### 永久人工/红线

- 登录认证、JWT、权限模型和跨租户范围；
- 资金、支付、财务、银行账户、自动付款；
- Prisma schema、migration、生产数据库结构；
- 依赖、锁文件、环境变量和部署配置；
- 业务规则不清晰、需要业务负责人判断的数据纠错。

### 转人工任务处理原则

1. 先读 `error`、`planSummary` 和变更范围。
2. 判断是临时故障、假失败、已经满足、还是永久红线。
3. 临时故障或旧代码基线可使用“基于最新代码重试”。
4. 无改动任务必须独立复验，不允许只听模型自述。
5. 资金/权限/数据库类不要为了降低人工参与而放宽红线。

## 11. 接手后的推荐动作

### 第一步：只读同步核验

```bash
cd /Users/somnusyi/Documents/Codex/dianjie-V4-rebuilt
export PATH=/opt/homebrew/opt/node@20/bin:$PATH
git fetch --all --prune
git rev-parse main origin/main gitea/main server/main
git status --short
```

四个提交应一致。生产还需只读核对 `/app/dianjie-v4/.deployed-commit`、`/app/dianjie-src` HEAD/dirty 状态、API 健康和 PM2 状态。

### 第二步：阅读顺序

1. 本文件 `AI_HANDOFF_CURRENT_2026-08-01.md`；
2. `AGENT_BOARD.md`；
3. `QWEN.md`；
4. `README.md`；
5. `SUPPLY_CHAIN_MODULE_PLAN_2026-07-26.md`；
6. `docs/WORKTREE_AUDIT_2026-07-29.md`；
7. 对应业务模块代码和测试。

旧交接文件仅作为历史证据，其中版本号、测试数量、生产地址和待办可能已经过期。

### 第三步：先拿文件样例，再开发供应链导入

不要先猜美团文件字段。向项目负责人索取：

- 最新库存导出原文件 1 份；
- 一天完整出货明细原文件 1 份；
- 文件覆盖的供应商、仓库、门店范围；
- 是否含退货、撤销、赠品、单位换算、跨日补录；
- 美团商品编码与系统商品编码的可用映射。

拿到样例后先产出字段映射、幂等键、错误分级、预览和冲销设计，再实施。

### 第四步：扩店门禁

先完成 DJ002 的全链路演练，再复制为 5 店模板。每一家店都要独立验证 tenant/store scope、主数据、库存基线、日报、BOM、订货、收货、应付和角色路由。

## 12. Git、发布和工作区注意事项

- 当前本地 `main` 存在用户历史未跟踪文件，属于用户资料，不得删除、覆盖或擅自提交：
  - `.codex-artifacts/`
  - `AI_HANDOFF_CURRENT_2026-07-20.docx`
  - `AI_HANDOFF_CURRENT_2026-07-20.md`
  - `KIMI_WORK_REVIEW_2026-07-24.md`
  - `apps/api/scripts/_tmp-vision-cleanup.js`
  - `apps/api/scripts/_tmp-vision-close.js`
  - `apps/api/scripts/_tmp-vision-e2e.js`
  - `库存主数据映射审计_2026-07-15.xlsx`
- 本地残留多条历史 feature/worktree 分支。先读 `docs/WORKTREE_AUDIT_2026-07-29.md`，不要整分支合并或批量删除。
- 正式开发使用独立 worktree 和小批次提交；提交前检查 diff、测试、类型和敏感信息。
- 生产只使用项目受控发布脚本；禁止手工 rsync 半套产物、直接编辑 `/app/dianjie-src` 或 `/app/dianjie-v4`。
- 生产写入前备份；排查默认只读；任何数据纠错都要有业务确认、影响清单、幂等保护和回滚/冲销方案。

## 13. 可直接交给另一个 Codex 的启动提示

```text
你现在接手“滇界 V4”连锁餐饮系统。请先完整阅读：
/Users/somnusyi/Documents/Codex/dianjie-V4-rebuilt/AI_HANDOFF_CURRENT_2026-08-01.md

然后进入：
/Users/somnusyi/Documents/Codex/dianjie-V4-rebuilt

先做只读核验：git status、main/origin/main/gitea/main/server/main、生产部署标记、生产源码是否干净、API/Web/DB 健康。不要删除未跟踪文件，不要修改历史 migration，不要直接写生产数据，不要把账号密码或密钥写入仓库。

当前最高优先级是供应链负责人从美团导出的“最新库存快照”和“除瑶海店外每日多门店出货明细”的专用导入闭环。现有系统已经有默认仓、商品四单位、库存流水、供应商全量快照导入、内部供应链 Excel 增量入库、订货/配送/收货链路；新功能尚未完成。必须先向负责人取得两份真实文件样例，再设计预览、SKU 映射、单位换算、文件幂等、瑶海排除、库存流水、整批冲销和 E2E 验证。

每次准备改代码前先说明根因、范围、数据影响、验证与发布计划；涉及认证、权限、资金、数据库 schema/migration、依赖和部署配置必须停下来请求明确授权。
```

## 14. 当前结论

项目已经从单店试运行进入“稳定真实链路、强化供应链、准备复制到多店”的阶段。代码和生产基线目前一致且健康，自动开发能力已打通并经真实任务验证。接下来的关键不是继续堆页面，而是把供应链外部文件导入、商品/单位/BOM 主数据、门店初始库存和全链路验收标准固化成可复制的上线模板，先让 DJ002 通过，再扩展到 5 店。

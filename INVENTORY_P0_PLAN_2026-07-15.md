# 滇界 V4 单门店库存 P0 方案

日期：2026-07-15
状态：P0 第一阶段已完成并通过本地验收；主数据映射与完整批次账待继续
环境：仅本地隔离库；生产环境不修改

## 1. 已确认规则

1. 所有货物由供应商直接送到门店，不建设集团总仓、门店调拨或配送中心库存。
2. 门店暂定每月闭店后进行一次全量实物盘点。
3. 所有食材纳入批次和保质期管理。
4. 包装食材优先填写生产日期/到期日期；散装、蔬菜和鲜活食材缺少生产日期时，以到货日作为批次日期，按商品默认保质天数自动生成预计到期日。
5. 月内库存只能称为“预计库存”，不能宣传为实时实物库存。

## 2. 库存边界

系统保留两本相互独立的库存账：

- 供应商库存：供应商入库、配送出库、供应商盘点和供应商报损。
- 门店库存：实物盘点期初、采购实收入库、BOM 理论消耗、门店报损和盘点调整。

`Product.stock` 只作为供应商商品余额兼容字段，门店消耗不得再修改它。门店库存必须按 `storeId + productId` 独立核算。

## 3. 门店库存口径

预计数量：

> 最近盘点实物数量 + 盘点后采购实收 - BOM 理论消耗 - 门店报损 ± 盘点调整

移动平均成本：

> 新平均成本 = (原数量 × 原平均成本 + 本次入库数量 × 本次采购单价) / (原数量 + 本次入库数量)

- 出库按出库时移动平均成本计价，出库不改变平均单价。
- 盘盈按当前移动平均成本计价；没有成本时使用盘点单价。
- 盘亏按当前移动平均成本减少库存价值。
- 采购到货短量没有进入门店库存，不能作为门店报损再次扣库存。

## 4. P0 页面

1. 库存总览：预计数量、移动平均成本、库存价值、低库存、临期、数据截止时间。
2. 入库记录：采购收货自动生成，显示批次和保质期。
3. 消耗与报损：区分 BOM 理论消耗、人工消耗、店内报损。
4. 月度盘点：实物数量、账面数量、盘盈盘亏、确认人。
5. 库存流水：按商品、日期、类型和来源检索，不允许删除。

暂不建设生产、寄售、部门领退料、仓位移库、客户销售出库、多仓调拨和复杂锁库。

## 5. 实施顺序

1. 建立门店库存余额、不可删除流水和批次表。
2. 修复门店消耗误扣供应商库存。
3. 将 2026-07-13 瑶海盘点作为可追溯期初。
4. 采购收货在同一事务中写门店入库流水、移动平均成本和批次。
5. BOM 销售消耗及人工消耗写门店出库流水。
6. 月度盘点生成盘盈盘亏流水并重置可信基准。
7. 页面统一使用“预计库存/最近盘点”，不再显示虚假“实时”。

## 6. 验收标准

- 门店消耗不改变任何供应商 `Product.stock`。
- 同一采购收货重试不会重复入库。
- 盘点 100kg，之后入库 20kg、消耗 15kg，预计库存为 105kg。
- 两次不同价格入库后移动平均成本计算准确。
- 未实际入库的采购短量不影响门店库存。
- 每次收货产生批次；缺少生产日期时自动使用到货日和 `shelfDays` 计算到期日。
- 门店、供应商和跨租户数据均有权限隔离。
- API/Web 构建、迁移 diff、端到端和浏览器流程通过后才能申请生产发布。

## 7. 2026-07-15 当前完成情况

已完成：

1. 门店预计库存改为从最近实物盘点向后滚动，口径为“期初盘点＋实收入库－消耗－门店报损”。
2. 门店消耗不再修改供应商 `Product.stock`，两本库存账已在代码层隔离。
3. 收货时已有生产日期/到期日期则保留；缺失时以到货日作为批次日期，并按商品 `shelfDays` 生成预计到期日。
4. 店长库存页同时展示预计库存和最近实物盘点；厨师长库存页统一改称“预计库存”，并明确非实时。
5. 未匹配品项不再伪装为可计算库存。完成主数据导入前页面曾明确显示“盘点品项匹配 0/167”；现已在本地按盘点行逐项建立并绑定 167 个商品，完整保留 31,649.586 元盘点期初。
6. 已清理本地隔离库中的自动化配送遗留单据，避免测试品虚增预计库存；生产环境无修改。
7. 167 个商品均以盘点名称、规格和单位为准；鲜品/冻品及不同包装不合并。供应商暂为空、供应商库存 `stock=0`，默认保质期暂为 7 天，等待供应商和完整保质期资料补齐。

主数据审计结果：

- 盘点品项：167 个。
- BOM：7 个分类、109 个菜品、429 条原料明细、240 个原料名称。
- 销售统计：149 个菜品。
- 盘点名称与 BOM 原料可直接同名对应：29 个；销售菜品与 BOM 可直接同名对应：81 个。
- 鲜品/冻品、简称、复合料、包装单位和基础单位仍需逐项确认，不能自动合并。
- 本地商品主数据绑定：167/167；这只解决盘点期初和商品身份，不代表 BOM 已完整。
- 审计文件：`库存主数据映射审计_2026-07-15.xlsx`。

本地验证结果：

- 订货单不可篡改端到端：通过。
- 一张订货单分两次配送、两次收货：通过。
- 盘点 100、入库 20、消耗 15，预计库存 105：通过。
- 移动平均成本：通过。
- API TypeScript 构建：通过。
- Web TypeScript 检查：通过。
- 全量 API 测试：42/42 通过；美团同步游标用例已修复为在每次同步时读取当前机构配置。
- 店长/厨师长库存页面桌面与移动端浏览器验收：通过。
- 主数据导入后预计库存：167 项，金额 31,649.586 元；有库存 126 项，盘点为零 41 项；新增商品供应商库存合计 0。

下一步：

1. 补充每个商品的真实供应商、采购单位换算、安全库存和默认保质期。
2. 收到完整销售菜品 BOM 后，将菜品、原料和用量导入正式结构，处理套餐、赠菜、改名菜和复合料。
3. 增加独立批次余额/批次流水，使临期数量能够按批次真实扣减，而不只是展示最近到期日。
4. 完成盘点差异确认与盘盈盘亏流水后，再申请生产发布。

## 8. 发布候选补充验证（2026-07-15 晚）

- 本地 Prisma 迁移账本已补齐，34 条迁移全部应用，`migrate status` 正常。
- 当前本地数据库与 `schema.prisma` 对比为零差异。
- 34 条迁移已在全新空数据库从头执行成功，完成后与 `schema.prisma` 零差异。
- API 42/42、Web TypeScript、API/Web 生产构建均通过。
- 订货不可变、分批配送收货、供应商商品、分类、门店预计库存、167 项主数据绑定端到端验证均通过。
- 供应商库存、分类、商品、订单和店长库存页面已完成真实浏览器回归，无页面错误。
- 以上均为本地隔离环境结果；生产发布仍需生产备份副本迁移演练和业务方 UAT 确认。

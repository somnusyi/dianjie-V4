# P0 修复方案（2026-08-14）

> 编制：接手评审 AI（ZCode）
> 基线提交：`e0e3b83c9feb2d9c12bacd82c448648bd4729741`（本地 = origin/main = 生产）
> 状态：**已获项目负责人批准并实施完毕**，分支 `fix/p0-timezone-meituan-manual-sale`，未合并 main、未部署
> 实施提交：修复 A `de31c4d`（时区统一）、修复 B `f6ec50a`（美团隔离）、修复 C `4edf886`（双扣闸门）、补漏 `305a842`（import/类型）
> 验证结果：API 测试 956/956 通过（TZ=America/Los_Angeles / TZ=UTC / 宿主默认三环境）；API `tsc --noEmit` + `pnpm build` 通过；Web 测试 580/580、`tsc --noEmit`、`WEB_PORT=3299 pnpm build` 全部通过
> 生产只读核验（2026-08-15）：`.deployed-commit` = `e0e3b83` 与本地/GitHub 一致；API/Web/CMB online，`db=ok`；服务器时区 CST（华东）确认；在途单 PO202608000011（合肥瑶海店，¥5175，24 行）与 DO202608000017 自 08-13 14:43 发货后停留超 2 天，判定为"忘点送达"卡单，需供应链侧推进或超时提醒（见第二批建议 6）；08-11 导入仍 STAGED 未恶化；Gitea 镜像 192.168.1.129:1606 本机不可达
> 前置结论：生产服务器与门店均为华东时区（Asia/Shanghai），时区缺陷降级为"边界风险 + 跨时区开发机测试失败"，但仍是必修项。
> 本机注意：默认 node 为 v25.9.0，构建需 `export PATH="/opt/homebrew/opt/node@20/bin:$PATH"` 切到 20.x（项目 engines 要求）。

## 修复 A：业务时区统一

### 根因

`documentNo.ts:11` 等处直接 `dayjs(at).format('YYYYMM')`，按**宿主机本地时区**解释时间。两类受害场景：

1. 开发/CI 机时区非华东 → 单号月份错（本机 PDT 已复现，`DOC202607000001` ≠ `DOC202608000001`）。
2. 生产虽为华东时区，但传入 `receivedAt`/`confirmedAt` 等 UTC 时间戳时，月末 16:00Z 之后（上海次月 1 日 0 点起）的记录会错月。

### 影响面（全量替换清单）

| 文件:行 | 用途 | 替换为 |
|---|---|---|
| `apps/api/src/services/documentNo.ts:11` | 单据 DOC 单号 | `businessMonthKey(at)` |
| `apps/api/src/utils/no.ts:13` | RK/PO/DC/PY/LC 通用单号 | `businessMonthKey()` |
| `apps/api/src/routes/orders.ts:561` | 采购单单号 | `businessMonthKey()` |
| `apps/api/src/routes/orders.ts:1522` | 发货单号 | `businessMonthKey()` |
| `apps/api/src/routes/orders.ts:1966` | 收货单号（按 receivedAt） | `businessMonthKey(receivedAt)` |
| `apps/api/src/routes/receipts.ts:340` | 收货单号 | `businessMonthKey()` |
| `apps/api/src/routes/receipts.ts:526` | 收货单号（按 confirmedAt） | `businessMonthKey(confirmedAt)` |
| `apps/api/src/routes/lossClaims.ts:497` | 差异单号（按 now） | `businessMonthKey()` |
| `apps/api/src/routes/lossClaims.ts:622` | 手工报损单号 | `businessMonthKey()` |
| `apps/api/src/routes/payments.ts:132` | 付款单号 | `businessMonthKey()` |
| `apps/api/src/routes/reconciliations.ts:202` | 对账单号 | `businessMonthKey()` |
| `apps/api/src/services/scheduler.ts:57` | 自动收货单号（按 receivedAt） | `businessMonthKey(receivedAt)` |
| `apps/api/src/services/paymentSchedule.ts:102` | 付款计划单号（按 confirmedAt） | `businessMonthKey(confirmedAt)` |
| `apps/api/src/routes/cashbook.ts:262` | 导出文件名时间戳 | `businessTimestampKey()` |
| `apps/api/src/routes/vouchers.ts:340,357` | 凭证导出文件名 | `businessDateKey()` / `businessTimestampKey()` |

### 实现

新建 `apps/api/src/lib/businessTime.ts`（与已有 `lib/dateRange.ts` 同层级，遵循项目"日期工具集中化"先例）：

```ts
import dayjs from 'dayjs'
import utc from 'dayjs/plugin/utc'
import timezone from 'dayjs/plugin/timezone'

dayjs.extend(utc)
dayjs.extend(timezone)

/** 业务时区：全部门店与仓库均在华东；单号月份、期间、归属日均按此时区切分。
 *  刻意不做成环境变量：业务月份契约必须全局唯一，不允许按部署环境漂移。 */
export const BUSINESS_TZ = 'Asia/Shanghai'

/** 单号月份键 YYYYMM（业务时区）。所有月度序列号必须经由本函数取月份。 */
export function businessMonthKey(at: Date | string | number = new Date()): string {
  return dayjs(at).tz(BUSINESS_TZ).format('YYYYMM')
}

/** 业务日期键 YYYY-MM-DD（业务时区），用于导出文件名与归属日展示。 */
export function businessDateKey(at: Date | string | number = new Date()): string {
  return dayjs(at).tz(BUSINESS_TZ).format('YYYY-MM-DD')
}

/** 业务时间戳 YYYYMMDD_HHmmss（业务时区），用于导出文件名。 */
export function businessTimestampKey(at: Date | string | number = new Date()): string {
  return dayjs(at).tz(BUSINESS_TZ).format('YYYYMMDD_HHmmss')
}
```

dayjs 的 utc/timezone 插件随 dayjs 标准包分发，**无新增依赖**。

### 测试计划

1. 修复 `tests/services/documentNo.test.ts` 现有用例。
2. 新增边界用例（与宿主机时区无关，任何 TZ 下结果一致）：
   - `2026-07-31T15:59:59Z`（上海 7/31 23:59:59）→ `202607`
   - `2026-07-31T16:00:00Z`（上海 8/1 00:00:00）→ `202608`
   - `2026-08-31T15:59:59Z` → `202608`、`2026-08-31T16:00:00Z` → `202609`
3. 新增 `tests/lib/businessTime.test.ts` 覆盖三个函数。
4. 验证命令含时区矩阵：`TZ=UTC` 与 `TZ=America/Los_Angeles` 各跑一次 `pnpm --filter @dianjie/api test`。

### 兼容性评估

- 历史单号**不重编、不迁移**：月份键只在生成瞬间使用，存量单号不动。
- 生产（华东时区）下 `new Date()` 场景行为**不变**；`receivedAt`/`confirmedAt` 边界场景从"可能错月"变为"正确"。无 schema 变更，回滚 = revert。

## 修复 B：美团模块租户/门店过滤

### 现状

`apps/api/src/routes/meituanData.ts` 全部 6 个端点无任何租户/门店过滤；`MtOrder` 模型本身无 `tenantId`（schema:3264），但有 `storeId`（由美团 poiId → V4 Store 映射写入，未映射时为 null）。`allowedRoles` 含门店级 `MANAGER` → 任一店长可读**全集团**订单流水（金额、桌台备注、收银员）。

### 方案（不动 schema，短期版）

在 `meituanData.ts` 内新增统一 scope 解析：

```ts
// MANAGER（门店角色）：仅本店
// FINANCE/BOSS：租户内全部已映射门店
// ADMIN/SUPER_ADMIN：同上 + storeId 为 null 的未映射历史订单
// resolveMeituanScope(req) → { storeIds: string[] | null /* null=仅未映射 */, includeUnmapped: boolean }
```

- `storeIds` 来源：`prisma.store.findMany({ where: { tenantId }, select: { id } })`（租户门店数量个位数，开销可忽略）。
- 各端点改造：
  - `stats/today`、`stats/business-type-breakdown`：aggregate/groupBy 的 where 加 `storeId: { in: scope.storeIds }`。
  - `stats/payment-breakdown`、`stats/trend`（raw SQL）：加 `AND o."storeId" = ANY(${storeIds}::text[])`。
  - `orders` 列表：where 加同样条件；未映射订单仅 ADMIN/SUPER_ADMIN 可见。
  - `orders/:mtOrderId`（当前纯 IDOR）：查到后校验 `order.storeId` 属于 scope，否则 404。
- `allowedRoles` 保留 MANAGER（限店，不砍功能）。

### 长期（放第二批，需迁移）

`MtOrder` 增加 `tenantId` 列并按 storeId → Store.tenantId 回填，未映射历史数据按业务决定归属或清理。当前生产同步按 `MEITUAN_ORG_ID` 单组织运行，短期方案已封闭越权面。

### 测试计划

新增 `tests/routes/meituanData.test.ts`：

1. MANAGER 只见本店订单；请求其他门店订单详情 → 404。
2. FINANCE 见租户全部门店，不见未映射订单。
3. SUPER_ADMIN 可见未映射订单。
4. 其他角色（KITCHEN_LEAD/SUPPLY_CHAIN 等）→ 403。

## 修复 C：手工销量与日报互斥（双扣闸门）

### 现状

- 手工销量 `POST /dishes/sales`（dishes.ts:686）写 `DishSale(source='manual' 等)` + `StockConsumption(sourceType='dish_sale')`。
- 日报确认（dailyBusinessImports.ts:1236）只删 `source='daily_pos_upload'` 的销量后重建。
- 两侧互不感知 → 同日同菜：手工 10 份 + 日报 10 份 = **BOM 消耗扣 20 份、销量榜双计**。DishSale 唯一约束含 source，拦不住跨来源双记。

### 方案：双向闸门（推荐，需确认业务口径）

**不静默删手工数据**（手工录入可能是有意补数），改为阻断 + 明确提示：

1. **手工录入侧**（dishes.ts POST /sales 事务前检查）：同日同店存在 `status='CONFIRMED'` 的日报 → 409："该营业日期已有确认的日报，请通过日报更正调整销量"。
2. **日报确认侧**（confirm 事务内、CAS 抢占后检查）：同日同店存在 `source != 'daily_pos_upload'` 的 DishSale → 409，返回冲突清单（source、菜品名、数量），提示先删除手工销量再确认。
3. **补删除入口**：新增 `DELETE /dishes/sales/:id`——仅允许删除手工来源（`source` 非 `daily_pos_upload`），同事务删除对应 `dish_sale` 消耗并触发成本重估；当前**不存在任何删除端点**，这是解除冲突的必要配套。

### 备选方案（不推荐）

日报确认时自动将手工销量并入/作废。缺点：静默处置用户手工录入的数据，违背"已生效数据不静默改写"的项目原则。

### 测试计划

新增/扩展 `tests/routes/dishes.test.ts` 与 `dailyBusinessImports` 用例：

1. 先手工录销量、再确认同日日报 → 409 且列出冲突菜品。
2. 删除手工销量后确认日报 → 成功，消耗只扣一次。
3. 先确认日报、再手工录同日销量 → 409。
4. 不同日期互不影响；`EXCLUDE` 菜品照旧跳过。

## 交付与部署

- 分支：`fix/p0-timezone-meituan-manual-sale`，自 `e0e3b83` 拉出。
- 三个修复独立成 3 个 commit，便于单独 revert。
- 全量验证：`pnpm --filter @dianjie/api test`（含 TZ 矩阵）、`api build`、`web test` + `tsc --noEmit`。
- 部署：独立发布工作树走 `scripts/deploy-worktree.sh`，部署后角色化冒烟（店长录手工销量、财务看美团面板、生成一张测试单据核对单号月份）。
- 回滚：纯代码修复、无 schema 迁移 → revert + 重新发布即可，零数据风险。
- 需要生产写操作的部分：无（三件套均为代码行为修复）。

## 第二批建议（不在本次审批范围，另行立项）

按严重度排序：

1. **单据详情兜底放行**：`documents.ts:240` 尾部无条件 return 改默认 403；`/preview` 同理（M1）。
2. **v2Dashboard 集团营收泄漏**：`v2Dashboard.ts:117-147` 非门店角色不应共享全租户营收 hero（M3）。
3. **门店 fail-open 三处**：`orders.ts:676`、`orders.ts:1794`、`dashboard.ts:29/151` 改 `storeId || '__NONE__'` fail-closed 模式（M4）。
4. **付款单银行账号裁剪**：`payments.ts:71-93` 对 SUPPLY_CHAIN 隐藏 `bankAccount`（M5）。
5. **SUPPLY_CHAIN 改价纳入审批**：与供应商同口径走 PRICE_ADJUSTMENT（M2，改动较大）。
6. **在途单超时提醒**：DELIVERING/SHIPPED 超 48h 进待办/通知（对应生产 1 张卡单疑点）。
7. `MtOrder` 加 tenantId 迁移回填。
8. `supplyChainDailyPackage.ts:382` 审核时间 max 的字符串比较缺陷（改 Date 比较）。
9. `dailyBusinessImport.ts:19-31` dead code 清理。

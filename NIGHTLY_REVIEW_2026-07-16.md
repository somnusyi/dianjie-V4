# 滇界 V4 夜间架构开发与回归记录

执行分支：`release/20260715-p0`
边界：仅本地隔离库；不合并 `main`，不修改生产环境

## 2026-07-15 18:50 第一轮

### 检查范围

- 分批配送后的门店收货重试行为。
- 配送单、入库单与采购单状态之间的幂等关系。
- 网络丢失首次成功响应后的客户端重复提交。

### 修改

- 收货成功后再次提交同一订单，不再返回“订单状态不可收货”。
- 后端按租户和门店权限查回原入库单，返回 `duplicated: true`。
- 补充端到端断言：重复收货返回同一入库单，同一配送单始终只有一张入库单。

### 验证

- 分两次配送、两次收货：通过。
- 第一次部分收货后的网络重试：通过。
- 重试后入库单数量保持 1：通过。

### 后续高风险审计

- 当前收货流程的入库单创建、报损单创建、配送状态更新分布在多个事务中；需继续收敛为原子事务，避免中途失败留下部分成功数据。
- 需增加并发双收货测试，验证唯一约束冲突能返回原入库单而不是 500。

### 待用户确认

- 本轮没有新增业务规则问题；补送、报损收货和已确认入库单更正仍按此前约定暂不擅自实现。

## 2026-07-15 19:10 第二轮（提前执行）

### 客户问题对齐

- 供应商端将“门店订货单”和“配送单”作为两张独立单据展示，不混为一个状态列表。
- 两类单据均支持按日期范围、商品名称、商品规格、商品编码和单号检索。
- 查询在服务端分页执行，并继续按租户、门店和供应商角色隔离数据。
- 日期范围增加前后顺序校验，避免无效查询静默返回空结果。

### 页面修改

- 供应商订单页增加单据类型切换、商品/编码/单号输入框、开始与结束日期、查询和清空操作。
- 新增配送单列表，显示配送单号、来源订货单、门店、发货时间、金额、商品摘要和关联入库单。
- 390 × 844 手机尺寸下检查通过；列表切换、关键词查询和清空操作正常，浏览器无运行错误。

### 自动化验证

- API 测试：42/42 通过。
- API TypeScript build：通过。
- Web TypeScript 检查：通过。
- 配送端到端：两次配送、两次收货、重复提交幂等、商品名称/编码/日期检索、反向日期校验全部通过。
- 订货单不可变端到端：通过。
- 供应商商品管理端到端：通过（分类筛选、图片、批量分类、批量停售审批、操作记录）。
- 供应商分类管理端到端：通过。
- `git diff --check`：通过。

### 下一轮

- 将收货流程的入库单、报损单、库存、配送状态和采购单状态收敛为原子事务。
- 增加并发双收货测试；竞争请求应返回同一入库单，不应出现 500 或部分成功。

## 2026-07-15 19:25 第三轮（提前执行）

### 收货事务修复

- 入库单、入库明细、短量报损单、配送明细实收数、配送状态、订货单累计实收数、订货单状态及操作日志已合并到同一数据库事务。
- 事务开始时以配送单状态和 `rowVersion` 抢占收货权；并发请求只有一条能写入，其余请求在首条提交后返回同一入库单及 `duplicated: true`。
- 入库单和报损单编号改用数据库业务序列，不再使用 `count + 1`；首次启用序列时会读取历史最大编号作为下限，避免与旧单号碰撞。
- 财务凭证和自动账期仍作为主事务后的派生流程；凭证已有来源单唯一幂等，自动账期的失败补偿和并发幂等继续列为后续审计项。

### 验证

- 两个并发收货请求同时发出：均返回 200，且仅生成 1 张入库单、1 条配送已收货事件。
- 两次分批配送、累计实收和供应商库存扣减：通过。
- 历史单号下限兼容已加入单元测试，首次启用业务序列时从历史最大编号继续递增。
- 端到端脚本补强失败清理：即使断言中途失败，也会按本次运行时间和测试供应商清理入库测试数据。
- API 单元测试 43/43、API 构建、Web 类型检查、配送收货端到端和订货单不可变性回归均通过。

## 2026-07-15 19:45 第四轮（提前执行）

### 对账与账期幂等

- 修复收货后对账单、账期记录和入库单状态分步提交的问题，三者改为同一数据库事务。
- 以入库单 ID 获取事务级锁；同一入库单的并发重试串行执行，后续请求直接返回首个请求创建的对账单和账期。
- 对账单编号改用数据库业务序列，并兼容历史最大单号，不再使用 `count + 1`。
- 收货主事务成功后，即使财务派生流程临时失败也不再向门店返回“收货失败”；重复提交会安全补偿缺失的对账单和账期。
- 配送端到端增加断言：并发收货后每张入库单仅有 1 张账期记录和 1 条对账明细；测试清理同步删除对账单表头，避免本地遗留孤立测试数据。
- 外部主营供应商的两次分批配送、并发收货、对账、账期及供应商库存扣减端到端通过；API 单测 43/43、API 构建、Web 类型检查和订货单不可变性回归通过。

## 2026-07-15 20:05 第五轮（提前执行）

### 24 小时自动收货

- 旧自动收货仅写 `autoConfirmed` 标志，不能真正阻止门店同时手工收货；现改为以配送单状态和 `rowVersion` 原子抢占。
- 自动收货的入库单、明细、配送实收、累计实收、配送状态、订货单状态、事件和操作日志合并到同一事务。
- 自动入库单编号改用数据库业务序列并兼容历史最大单号，移除 `count + 1`。
- 自动收货补齐收货凭证、对账和账期派生；财务派生失败不回滚已经完成的真实收货，并保留明确错误日志。
- 配送端到端的第二次分批配送同时发起自动收货和门店手工收货，验证两者竞争后仅生成 1 张入库单、1 条收货事件，并共同完成累计实收。
- 自动/手工竞争端到端、API 单测 43/43、API 构建、Web 类型检查和订货单不可变性回归全部通过。

## 2026-07-16 05:46 第六轮

### 检查范围

- 供应商手工入库、报损、盘点调整和库存快照导入的并发更新一致性。
- 供应商库存流水统计与明细的租户、供应商隔离。
- 批量入库以相反商品顺序并发执行时的死锁风险。

### 修改

- 所有供应商库存“读现存量后写新存量”路径改为在事务内锁定商品行，再计算库存和 `balanceAfter`，避免并发请求互相覆盖。
- 批量入库按商品 ID 稳定排序后一次锁定全部商品，避免两个逆序批次形成循环等待。
- 盘点调整、报损和既有 SKU 的库存快照导入复用相同锁定规则；配送发货的原子扣减也会与该行锁自然串行。
- 库存近 7/30 天统计、到期日统计及流水明细查询显式补齐 `tenantId + supplierId` 双重隔离。
- 新增本地隔离库端到端脚本，覆盖跨租户读写、同 SKU 双入库、入库/报损竞争、盘点/入库竞争、快照/入库竞争及逆序批量。

### 自动化验证

- 新增供应商库存并发端到端：全部通过；测试临时商品、分类、流水和跨租户数据均在 `finally` 中清理。
- 配送收货端到端：通过；2 张配送单、2 张入库单、累计发货/实收 5，继续覆盖并发双收货和自动/手工竞争。
- API 单元/集成测试：43/43 通过；API TypeScript build：通过；Web TypeScript 检查：通过。
- 390 × 844 浏览器复核：库存 KPI、分类筛选、商品编码搜索、商品卡片和底部导航正常，控制台无 warning/error。
- `git diff --check`、新增文件空白检查和高置信敏感信息扫描：通过。
- 配送脚本默认端口为 4445；本轮显式指定当前本地 API `127.0.0.1:4444` 后通过，未连接生产。

### 风险与待确认

- 库存快照/盘点属于“设置目标值”，与增量入库并发时以获得行锁的先后顺序为准；库存与流水保持一致，但业务上是否需要阻止盘点期间入库仍需客户确认，本轮未擅自增加流程限制。
- 补送、报损收货、已确认入库单更正和完整盘点审批流程继续保持只记录、不代替业务方决策。

## 2026-07-16 05:51 第七轮

### 检查范围

- `dianjie_v4_local` 既有隔离库的 migration 应用状态与 schema 漂移。
- 全新临时空库从首个 migration 到当前 schema 的完整部署链。
- 迁移后订货单首次提交快照、改单确认和事件记录回归。

### 验证

- 既有库：`prisma migrate deploy` 无待执行项，`migrate status` 显示 34 个 migration 全部应用，schema diff 为零。
- 临时空库：从零依次应用全部 34 个 migration，随后 `migrate status` 最新、schema diff 为零；验证结束后终止临时连接并删除临时库。
- 全程只连接本机 `127.0.0.1:5434`，未使用 `db reset`、`prisma db push`，未连接生产。
- 订货单不可变端到端：通过；首次提交快照金额保持 16.65，门店确认改单后当前金额为 26.82，事件链 5 条。
- API 单元/集成测试：43/43 通过；API TypeScript build：通过；Web TypeScript 检查：通过。
- `git diff --check` 与敏感信息检查：通过。

### 风险与待确认

- 本轮未发现迁移断链或 schema 漂移；Prisma CLI 提示存在 7.x 大版本升级，但发布候选继续固定当前 5.22.0，不在夜间擅自升级依赖。
- 生产 migration 仍须按发布流程由人工在受控窗口执行，本轮没有连接或修改生产数据库。

## 2026-07-16 06:03 第八轮

### 检查范围

- 供应商新品、涨价、单项/批量停售审批的并发编号与重复提交。
- 通用审批单和付款申请共用 DOC 单号时的唯一约束竞争。
- 商品审批终态回调的租户隔离，以及商品管理移动端分类、编码、状态和操作记录入口。

### 修改

- 新增租户级 DOC 业务序列，首次启用从历史最大单号继续递增，移除商品、通用审批和付款申请中的 `count + 1` 编号。
- 供应商单条建品改为商品、审批单和操作日志同一事务提交，避免留下无审批单的 `PENDING_APPROVAL` 商品。
- 单项/批量停售先原子抢占 `ENABLED → PENDING_DISABLE`，再在同一事务生成审批单和日志；重复请求返回 4xx，不再创建双审批单。
- 涨价申请按商品获取事务级锁，并在锁内复查待审批单；审批单、其他允许字段和操作日志原子提交。
- 批量上传、新品、停售、调价、通用审批和付款申请统一使用并发安全 DOC 编号。
- 商品审批通过/驳回回调补齐 `tenantId` 条件，避免仅凭 payload 商品 ID 修改其他租户数据。

### 自动化验证

- 商品管理端到端：分类、图片、批量分类、停售审批、审计记录和禁用物理清空通过；两个独立停售请求并发生成不同 DOC 单号，同一 SKU 双提交仅 1 个成功且仅 1 张待审单。
- 分类管理端到端：新增、改名联动商品/库存、停用阻断、恢复、排序和操作记录全部通过。
- DOC 编号单元测试覆盖历史最大值续号和新月份从 1 起号；API 单元/集成测试 45/45 通过。
- API TypeScript build、Web TypeScript 检查、`git diff --check`、新增文件空白检查和敏感信息扫描：通过。
- 390 × 844 浏览器复核：分类/状态筛选、商品编码搜索、图片标记、停售入口和操作记录入口正常，控制台无 warning/error。

### 风险与待确认

- 批量上传仍保留逐行部分成功语义；商品行创建完成后才生成整批审批单。DOC 并发冲突已消除，但若最终审批单创建遇到非并发型基础设施故障，仍需后续补偿审计。
- 本轮没有改变停售审批人、涨价审批规则或批量导入的部分成功业务语义。

## 2026-07-16 06:08 第九轮

### 检查范围

- 受版本控制的交接文档、本地迁移脚本和部署校验脚本中的高置信敏感信息。
- 私钥、云访问键、GitHub token、嵌入式数据库口令和明文密码赋值模式。

### 修改

- 从 `HANDOFF.md`、`scripts/README.md`、`scripts/deploy-verify.sh` 和本地测试租户迁移脚本中移除同一组明文生产风格凭证，统一替换为不可用占位符。
- 未修改本地 `.env`，未输出或提交新凭证，未连接生产，也未执行凭证轮换。

### 自动化验证

- 全部受版本控制文件复扫：未发现私钥、云访问键或 GitHub token；高置信明文值已从当前分支移除。
- 剩余扫描命中均为环境变量拼接、占位 URL 或本地测试账号口令，未按生产凭证处理。
- 供应商库存并发端到端：跨租户、双入库、入库/报损、逆序批量、盘点和快照竞争全部通过。
- API 单元/集成测试 45/45、API TypeScript build、Web TypeScript 检查和 `git diff --check`：通过。

### 风险与待确认

- 已移除的值仍可能存在于 Git 历史和外部副本；按夜间边界本轮不擅自轮换，发布负责人应在受控流程中确认是否需要轮换并清理历史。

## 2026-07-16 06:16 第十轮

### 检查范围

- Web `dev` / `build` 共用 `.next` 的缓存污染风险，以及根构建和既有部署脚本调用的 Web package 入口。
- 3200 端口现有本仓库 Next dev 进程、生产 standalone 产物和恢复开发服务后的移动端页面。

### 修改

- 新增 `scripts/web-next-safe.sh`，让 Web build/dev 共用进程锁；并发启动或 dev 未停时明确失败，不再清理正在使用的 `.next`。
- 生产构建开始前强制清理旧 `.next`，完成后校验 `BUILD_ID` 和 standalone server；dev 启动时若识别到生产产物，先清理再启动。
- `apps/web/package.json` 的 `build`、`dev` 统一接入安全入口，因此根构建和现有部署脚本无需各自维护不同清理规则。
- 按要求停止原 Web dev，完成生产构建后通过新入口干净恢复 3200 开发服务；未部署或写入生产。

### 自动化验证

- 负向流程：dev 运行时 build 被进程锁拒绝，开发缓存与服务保持正常；构建结束锁自动清理，生产 `BUILD_ID` 不会被带入恢复后的 dev。
- Web 全量生产构建两次通过，134 个页面生成完成，standalone server 产物校验通过；Web TypeScript 检查通过。
- 配送流端到端通过：两张独立配送单、两张入库单，发货与收货数量均为 5。
- API 单元/集成测试 45/45、API TypeScript build、shell 语法检查、`git diff --check` 和高置信敏感信息扫描：通过。
- 390 × 844 浏览器复核：首页与订单页正常，订货单/配送单切换、商品名称/编码/单号和日期范围查询入口可用，控制台无 warning/error。

### 风险与待确认

- Next/Sentry 依赖仍输出既有 OpenTelemetry 动态依赖构建 warning，但构建和页面生成成功；本轮未升级依赖。
- 安全入口覆盖仓库 package scripts；绕过 package scripts 直接执行 `next dev/build` 仍属于非标准操作，需在发布手册中保持禁止。

## 2026-07-16 06:30 第十一轮

### 检查范围

- 供应商商品批量上传的逐行部分成功、自动编码、分类创建、审批单、批次终态和操作日志一致性。
- 单条/批量商品创建的租户与供应商隔离，以及未绑定供应商账号的商品查询边界。

### 修改

- 将批次、有效商品、自动分类、批次统计、`NEW_DISH` 审批单和操作日志收敛到同一数据库事务；审批单或其他基础设施写入失败时整批回滚，不再留下无法审批的 `PENDING_APPROVAL` 商品。
- 保留业务层逐行部分成功：格式错误、停用分类、重复编码和越权供应商按行返回失败；有效行仍在同一事务内提交。
- 批量商品改用预生成 ID + `createMany(skipDuplicates)`，并将缺省编码改为随机短 ID，避免同毫秒多行导入互撞；显式重复编码不再被错误改写成新编码。
- 总部单条/批量建品均校验供应商属于当前租户；供应商角色无绑定时商品列表返回 403，不再退化为租户全量查询；异常跨租户绑定也明确拒绝。
- 商品状态输入收紧为数据库已定义枚举，非法状态在写库前按行或请求返回校验错误。

### 自动化验证

- 商品管理端到端通过：分类/图片/批量分类/停售审批、并发审批单号、重复停售、批量部分成功和操作记录均正常。
- 批量事务端到端通过：两个缺省编码批内唯一；显式重复编码仅该行失败；批次商品与审批单 payload 完全一致。
- 本地故障注入通过：审批单 INSERT 被验证触发器拒绝后，商品、批次和自动分类均为 0；触发器/函数已在 finally 清理。
- 租户隔离端到端通过：总部账号单条/批量引用外租户供应商均被阻断，未绑定供应商账号查询商品返回 403。
- API 单元/集成测试 45/45、API TypeScript build、Web TypeScript 检查、`git diff --check` 和高置信敏感信息扫描：通过。
- 390 × 844 浏览器复核：批量上传商品页的模板下载、文件选择、必填说明和支持格式正常，控制台无 warning/error。
- 首次扩展 E2E 命中未热重载的旧 API 后，已显式重启 4444；精确清除该次本地验证批次并复扫，验证商品、批次、临时账号/租户和触发器残留均为 0。

### 风险与待确认

- 单次最多 500 行现在共用一个事务；正确性已验证，但大文件峰值锁时长和延迟仍建议在发布前做 500 行本地压测。
- 本轮未改变审批人、逐行校验/重复数据的部分成功语义，也未自动重试基础设施故障；此类故障返回 500 并保证无部分落库。

## 2026-07-16 06:34 第十二轮

### 检查范围

- 单条供应商新建商品时，首次自动分类与商品、审批单、操作日志之间的事务边界。
- 批量导入上限 500 行的本地响应时间、审批 payload 完整性和清理可靠性。

### 修改

- 单条建品不再于事务外创建新分类；分类创建/并发复查、商品、`NEW_DISH` 审批单和操作日志统一在同一事务提交。
- 事务开始前仍快速拒绝已停用分类，事务内再次复查分类状态；并发停用/改名时返回 409，避免使用过期分类状态创建商品。
- 分类并发创建改用 `createMany(skipDuplicates)`，避免唯一约束竞争导致事务进入失败状态。
- 商品管理端到端新增单条审批单故障回滚，以及 500 行上限批量导入覆盖。

### 自动化验证

- 单条故障注入通过：审批单 INSERT 失败后，商品和首次自动分类均为 0；批量故障回滚用例继续通过。
- 500 行本地批量导入成功 500、失败 0，只生成一个批次和一张含 500 个商品 ID 的审批单；本机 API 往返耗时约 74ms。
- 商品管理完整端到端通过：批量部分成功、500 行上限、单条/批量回滚、跨租户/未绑定供应商隔离、审批并发和操作记录均通过。
- API 单元/集成测试 45/45、API TypeScript build、Web TypeScript 检查、`git diff --check` 和高置信敏感信息扫描：通过。
- 验证后本地商品、批次、回滚分类和临时触发器残留均为 0；390 × 844 浏览器商品页仍为基线 1 SKU / 1 分类，筛选、批量上传和新建入口正常，控制台无 warning/error。

### 风险与待确认

- 74ms 为本机隔离库单次结果，不代表生产容量基线；发布后仍应通过只读指标观察真实导入延迟和事务锁等待。
- 本轮未改变新分类的审批规则或商品上架审批人。

## 2026-07-16 06:44 第十三轮

### 检查范围

- 商品新增/批量上架/停售审批的重复提交、终态回调原子性，以及通用单据撤回竞争。
- 商品批次撤回与待审批单、审批回调并发时的状态一致性。

### 修改

- 审批决策按文档获取 PostgreSQL 事务级 advisory lock；文档步骤、决策记录、单据终态、商品/调价回调和操作日志在同一事务提交。
- 商品审批回调失败不再被吞掉并留下“单据已批准、商品未变化”的半提交状态；失败时整笔审批回滚并返回 500。
- 新品上架/停售回调增加预期状态条件；批量上架额外检查批次未撤回，避免旧审批重新启用已停售商品。
- 通用单据撤回使用同一文档锁，并在事务内同步回滚商品待审状态；撤回与审批不能互相覆盖终态。
- 商品批次撤回在同一事务停售商品、原子抢占批次、取消对应待审批单并记录被取消单据 ID；并发重复撤回返回 409。

### 自动化验证

- 同一张批量上架单并发批准两次：仅 1 个请求成功、仅 1 条决策记录，商品全部启用。
- 先撤回再批准：审批单为 `CANCELED`、商品为 `DISABLED`，后续批准返回 404/409 且不能翻转商品。
- 撤回与批准并发：撤回请求成功，批次必有 `revokedAt`，商品最终必为 `DISABLED`；单据只会落在 `APPROVED` 或 `CANCELED` 合法终态。
- 商品管理端到端、500 行批量、单条/批量故障回滚、跨租户/未绑定供应商隔离全部通过；500 行本轮本机往返约 83ms。
- API 单元/集成测试 45/45、API TypeScript build、Web TypeScript 检查、`git diff --check` 和高置信敏感信息扫描：通过。
- 390 × 844 浏览器商品页保持 1 SKU / 1 分类基线，控制台无 warning/error；验证商品、批次、临时账号和触发器残留均为 0。
- 额外清除一张 06:00 遗留的本地验证孤立审批单：payload 商品均已不存在且无操作日志；按精确 ID 删除，未触碰业务单据。

### 风险与待确认

- advisory lock 使用 PostgreSQL `hashtext`，与当前数据库架构一致；若未来更换数据库引擎需替换锁实现。
- 本轮未改变审批角色、步骤路由或撤回权限；并发场景允许“批准先提交、随后撤回批次”，此时单据保留 `APPROVED` 审计终态，但商品最终保持停售且批次标记撤回。

## 2026-07-16 06:51 第十四轮

### 检查范围

- 审批详情/预览对供应商、门店和租户的可见性校验。
- 审批 payload 中商品、批次和供应商引用的租户边界，以及历史缺少 `supplierId` 的兼容路径。

### 修改

- 新建、批量上架、单项/批量停售和调价单 payload 统一写入 `supplierId`；供应商名称查询同时限定 `tenantId`。
- 供应商查看历史单据时，优先用 payload `batchId` / `productId(s)` 反查当前租户实际供应商；仅当供应商名称在当前租户内唯一且属于当前账号时才兼容放行。
- 审批预览补齐与详情一致的集团角色、发起人、已审批人、门店和供应商可见性检查；店长不再默认查看无门店归属的集团单据。
- 预览中的单项/批量商品、分类统计、价格统计及 28 天采购使用量全部增加租户条件，异常历史 payload 不再带出外租户商品。

### 自动化验证

- 当前租户验证单据的 payload 指向外租户商品时，预览返回 `product: null`，未泄露商品名称、价格或供应商。
- 仅带其他供应商名称、没有 `supplierId` 的历史兼容单据，当前供应商访问详情和预览均返回 403。
- 新批量审批单 payload 的 `supplierId` 与登录供应商一致；既有批量/单条审批、撤回并发和 500 行导入端到端继续通过（本轮约 83ms）。
- API 单元/集成测试 45/45、API TypeScript build、Web TypeScript 检查、商品管理端到端、`git diff --check` 和高置信敏感信息扫描：通过。
- 390 × 844 浏览器商品页保持基线且搜索入口正常，控制台无 warning/error；验证商品、批次、临时租户/账号/单据残留均为 0。

### 风险与待确认

- 历史单据若既无 `supplierId`、也无可反查的商品/批次，并且供应商名称不唯一，将按安全优先返回 403；需由管理员查看或后续受控补齐归属。
- 本轮未修改审批内容、审批角色或商品业务数据，仅收紧读取隔离和新单归属快照。

## 2026-07-16 06:56 第十五轮（发布候选总复验）

### 检查范围

- 当前分支与远端同步、API/Web 编译、关键商品/分类/库存/订货/配送端到端、本地迁移状态和移动端核心页面。
- 多步骤审批重复请求是否应强制携带 `stepId` 的现状审计。

### 修改

- 本轮无业务代码修改；仅记录发布候选复验证据。
- 前端 inbox 已持有 `stepId`，但各审批 POST 当前未携带；强制同一用户不可连续审批会改变财务/老板职责规则，按夜间边界未擅自实施。

### 自动化验证

- `release/20260715-p0` 与远端 `FETCH_HEAD` ahead/behind 均为 0；工作树仅保留未跟踪的用户业务 Excel。
- API 单元/集成测试 45/45、API TypeScript build、Web TypeScript 检查、`git diff --check` 和全仓高置信敏感信息扫描：通过。
- 商品管理端到端通过：500 行约 75ms，部分成功、审批重复提交/撤回竞争、故障原子回滚、跨租户预览和供应商隔离全部通过。
- 分类管理、供应商库存并发、店长库存、订货单首次快照/改单事件和配送收货全链路端到端全部通过。
- 配送流首次串行执行在登录阶段命中 10 次/分钟安全限流 429；重启本地 API 清空内存测试计数后单独重跑通过：2 张配送单、2 张入库单，发货/收货数量均为 5。
- 本地 `dianjie_v4_local`：34 个 migration 全部应用，`migrate status` 最新；使用 Prisma 5.22 支持的 `--from-schema-datasource` 复核 schema diff 为零。
- 390 × 844 浏览器复核供应商订单、商品、库存三页：核心查询/基线内容和底部导航正常，控制台无 warning/error。
- 验证商品、批次、临时账号/租户/单据和故障触发器残留均为 0。

### 风险与待确认

- 多步骤审批若要用 `stepId` 防止极端重复请求推进下一步，需要产品负责人确认“同一账号能否兼任连续审批角色”，再同步升级全部 Web 调用方；当前文档锁已保证同一步不会并发双写。
- 全仓仍有既有框架 deprecation/build warning（Vite CJS、Fastify routerPath、OpenTelemetry 动态依赖）；本轮测试与构建均成功，未在发布候选夜间升级依赖。

## 2026-07-18 05:54 第十六轮（供应链长循环收口）

### 检查范围

- 接管并复核 2026-07-17 供应链长循环留下的未提交代码、9 个迁移、API/Web 测试和供应商 PC/移动端页面；业务 xlsx 始终保持未跟踪，不纳入任何暂存或提交。
- `dianjie_v4_local` 既有隔离库与临时空库的 `migrate deploy`、`migrate status` 和 schema diff。
- 供应商订单、商品、库存、到货差异和月度对账在桌面端及 390 × 844 移动端的真实浏览器渲染。

### 修改

- 修复 `20260717223000_supplier_stock_batches` 在历史悬空 `supplierId` 上的开账批次回填：只为同租户且供应商真实存在的商品创建批次，不伪造供应商、不删除或改写历史商品。
- 既有库最后一个迁移首次触发外键 23503 后确认 PostgreSQL 已完整回滚；将该本地失败记录标记为 rolled back，再通过 `migrate deploy` 正常重放，未使用 `db reset` 或 `prisma db push`。
- 到货差异与月度对账两个新增供应商页面补齐移动端底部导航，分别归入订单与账单入口；与既有供应商移动端交互保持一致。
- 清理 4 组陈旧 API 开发进程，恢复为单一 4444 开发进程；Web 按安全入口停 dev、生产构建并干净恢复 3200。

### 自动化与浏览器验证

- API 单元测试 98/98、独立 PostgreSQL 集成测试 33/33、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `dianjie_v4_local`：56 个迁移全部应用，`migrate status` 最新，schema diff 为零；历史悬空供应商商品共 722 条，仅跳过批次开账，原数据未改动。
- 临时空库：56 个迁移从零完整部署，`migrate status` 最新，schema diff 为零；临时库验证后已删除，残留为 0。
- Web 生产构建通过：138 个页面生成，standalone server 产物校验通过；仅保留既有 OpenTelemetry 动态依赖 warning。
- 桌面浏览器：订单、商品、库存、到货差异和月度对账均无运行时或控制台错误；商品分类/编码、可用/已占库存、两类独立单据及服务端查询入口正常。
- 390 × 844：订单、商品、库存、到货差异和月度对账无横向溢出；新增页面底部导航可见，桌面侧栏在移动端隐藏；临时供应商浏览器账号已精确删除。
- `git diff --check` 通过；业务 xlsx 保持未跟踪并排除在提交之外。

### 风险与待确认

- 722 条历史商品引用已不存在的供应商，属于存量数据治理问题；本轮按安全优先仅阻止迁移失败，不擅自猜测或重绑供应商。发布负责人应另行确认这些商品应归档、补绑还是继续隔离。
- 到货短缺/破损是否自动回补供应商物理库存、补送流程、已确认入库单更正和完整盘点审批仍按既定边界保持不变，等待业务确认。

## 2026-07-18 05:56 第十七轮（并发发货门禁）

### 检查范围与修改

- 审计同一已确认订货单使用两个不同业务幂等键并发发货时，是否可能生成双配送单、重复扣减供应商库存或重复核销预占。
- 发货主事务已有 `status + rowVersion` 原子认领，本轮不改业务代码；将真实 API 集成用例从“同键串行重试”补强为“异键并发竞争 + 胜出键网络重试”。
- 并发断言要求恰好一个请求成功，另一个稳定返回 4xx；赢家同键重试必须返回原配送单及 `duplicated: true`，最终配送单、出库流水和库存扣减均只有一份。

### 自动化验证

- 下单 → 接单预占 → 异键并发发货 → 同键重试 → 送达 → 实收/差异 → 对账/仲裁的真实 API 端到端连续运行 5 次通过。
- 全量 API 单元测试 98/98、独立 PostgreSQL 集成测试 33/33、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 通过；本轮仅修改自动化测试和夜间报告，没有改变分批配送、补送或库存业务规则。

### 风险与待确认

- 当前策略仍是同一时刻只允许一张活动配送单；未来若开放真正分批并行配送，需要先定义活动配送上限、预占拆分和门店收货顺序，本轮未擅自放宽。

## 2026-07-18 06:05 第十八轮（本地预览租户别名）

### 检查范围与修改

- 复核 Web 两个登录入口及演示文案使用的稳定租户别名 `test`，发现 API 在设置 `PREVIEW_TENANT_SLUG=yaohai-test` 时只接受实际租户 slug，导致本地预览登录在查账号前固定返回 403。
- 新增纯函数收敛预览租户解析：未启用预览隔离时保持原租户 slug；启用时仅允许配置的实际 slug 与稳定别名 `test`，两者都解析到配置租户；其余租户继续拒绝。
- 该映射只在显式设置 `PREVIEW_TENANT_SLUG` 时生效，未改变生产租户解析、认证口令、角色权限或会话签发规则。

### 自动化与浏览器验证

- 新增 3 个单元用例，覆盖非预览保持原值、实际 slug/稳定别名通过以及其他租户拒绝；全量 API 单元测试 101/101 通过。
- `dianjie_v4_local_test` 已应用全部 56 个迁移；独立 PostgreSQL 集成测试 33/33、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- 本地 API 复核：`test` 与配置的实际 slug 均进入同一租户账号校验，非预览租户仍返回 403。
- 真实浏览器从 `/v2/login?tenant=test` 登录后进入 `/v2/supplier/home`，页面显示预期测试用户、供应商工作台及完整导航；精确创建的临时浏览器账号已删除，残留为 0。
- `git diff --check` 与高置信敏感信息扫描通过；业务 xlsx 继续保持未跟踪且不进入提交。

### 风险与待确认

- `test` 是本地预览入口的固定兼容别名，不会在未配置预览隔离的环境中自动指向其他租户；若将来需要多套并行预览租户，应改为显式别名配置，而不是继续增加硬编码别名。
- 本轮未改变任何供应链单据、库存或审批业务规则。

## 2026-07-18 06:08 第十九轮（配送记录快照搜索）

### 检查范围与修改

- 复核供应商订货/配送查询契约时确认：配送列表展示的是发货时冻结的商品名称、编码和规格，但关键词条件只查询商品主数据当前值；商品改名或换码后，用户无法按列表中实际看到的旧值找回历史配送单。
- 配送记录关键词查询增加发货快照名称、编码和规格，同时保留当前商品主数据匹配，兼容新旧单据；分页、租户、供应商和门店隔离条件保持在同一个数据库查询中。
- 洞察审计、客户、SKU 排行三个路由在单一 API 进程上均返回认证态 401 而非 404，确认先前 404 是陈旧开发进程造成，本轮未制造无意义路由改动。

### 自动化与端到端验证

- 扩展真实供应链端到端：下单、接单预占、并发发货后修改商品名称和编码；分别用发货快照中的旧名称、旧编码搜索，均只返回原配送单，响应商品字段仍为不可变快照。
- 该端到端继续完成送达、实收短量、入库单、差异单、对账与仲裁，证明快照搜索未影响收货和结算链路。
- 全量 API 单元测试 101/101、独立 PostgreSQL 集成测试 33/33、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；集成测试数据已由用例清理，业务 xlsx 继续保持未跟踪。

### 风险与待确认

- 订货单行当前没有独立商品文字快照列，首次提交快照保存在单据级 JSON 中；本轮未为列表搜索引入 JSON 模糊扫描或迁移，以免在缺乏索引与性能基线时扩大查询成本。
- 日期范围目前仍按单据创建时间过滤；若业务希望配送记录按发货时间而非创建时间筛选，需要产品负责人明确口径后再调整。

## 2026-07-18 06:10 第二十轮（日期与分页输入护栏）

### 检查范围与修改

- 审计订货单、配送单日期范围与分页参数，确认原先日期只校验 `YYYY-MM-DD` 外形，`2026-02-29`、`2026-04-31` 等不存在日期仍可进入数据库查询；页码也没有与全局分页工具一致的上限。
- 抽取共享日历日期 schema，并复用于订货、配送与供应商库存批次日期；订货/配送页码统一限制为 1 至 100000，避免超大 `skip` 触发 Prisma 错误或无意义查询。
- 日期范围的时区和“开始不晚于结束”规则保持不变；供应商库存原有日期语义仅做等价复用。

### 自动化与端到端验证

- 新增 7 个日期单元断言：真实闰日通过，不存在日期、非 ISO 日期和空值拒绝。
- 真实 API 端到端分别请求订货与配送列表，伪日期和第 100001 页均稳定返回 400，未进入 Prisma 查询；原供应链全链路继续通过。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 34/34、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；业务 xlsx 保持未跟踪。

### 风险与待确认

- 页码上限与现有共享分页工具保持一致，属于资源保护而非业务数量限制；若未来改用游标分页可移除该深页限制。
- 本轮未调整日期筛选字段口径，也未改变库存数量、单据状态或审批规则。

## 2026-07-18 06:12 第二十一轮（并发双收货门禁）

### 检查范围与修改

- 回到发布 P0 收货原子性要求，复核同一已送达配送单被两个门店请求同时确认时，配送状态 CAS、入库单唯一约束、差异单和库存/财务派生是否可能双写或返回 500。
- 现有实现已在同一事务内以 `status + rowVersion` 原子认领配送单，并由 `receipt.deliveryOrderId` 唯一约束兜底；竞争失败分支会查询原入库单并返回幂等成功。本轮不改业务代码，仅将串行收货端到端升级为真实双请求并发。
- 并发断言要求两个请求均返回 200，恰有一个标记 `duplicated: true`，且两者返回同一入库单；数据库只能存在 1 张入库单和 1 张到货差异单。

### 自动化与端到端验证

- 下单、接单预占、异键并发发货、送达、双请求并发收货、差异结算与仲裁全链路连续运行 5 次通过；没有 500、重复入库单或重复差异单。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 34/34、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试租户、单据和库存数据由用例清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 收货后的凭证、对账和账期派生仍采用主事务成功后幂等补偿，不与库存/入库主事务强耦合；本轮并发用例覆盖两请求同时触发补偿的唯一约束竞争。
- 补送、已确认入库单更正及报损后是否回补供应商物理库存仍属待确认业务规则，本轮未改变。

## 2026-07-18 06:19 第二十二轮（门店下单商品图片）

### 检查范围与修改

- 回归供应商商品图片链路，确认上传、租户目录校验、商品 API 重新签名及供应商商品页展示已存在；确定性缺口位于门店下单端：厨师长和总厨选品类型未接收 `imageUrl`，页面也未渲染图片。
- 厨师长发起采购与总厨代店下单两个页面均在选品抽屉和已选商品行展示商品缩略图；无图商品保持原布局，不增加占位噪音，图片采用懒加载并带商品名替代文本。
- 本轮仅消费 API 已有签名 URL，没有改上传权限、对象目录、商品审批或下单业务规则。

### 自动化与浏览器验证

- 390 × 844 真实浏览器：门店账号选择供应商后按名称筛选，选品抽屉出现带 HTTPS 签名源的商品图；加入商品后已选行仍显示同一图片，数量、金额和提交按钮正常。
- 本地浏览器验证只临时注入虚拟 OSS 签名参数，验证后已恢复标准 API 开发环境；临时用户和商品均精确删除，残留为 0。
- Web dev 先停止后执行生产构建，138 个页面生成及 standalone 产物校验通过，随后清理生产缓存并恢复单一 3200 dev；仅保留既有 OpenTelemetry 动态依赖 warning。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 34/34、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；业务 xlsx 保持未跟踪。

### 风险与待确认

- 图片源仍依赖 OSS 可访问性和一小时签名有效期；商品列表每次读取都会重新签名，页面没有持久化过期 URL。
- 本轮未真实上传文件到 OSS，也未写入生产或业务数据；上传大小、MIME 和租户目录安全边界保持原实现。

## 2026-07-18 06:22 第二十三轮（商品批量操作历史）

### 检查范围与修改

- 复核供应商商品批量分类、批量停售审批、恢复供应及操作人/时间记录；主体能力已存在，未重复造轮子。
- 确认操作历史 `limit` 原先直接经过 `Number/Math`，`NaN`、小数或超过 200 的值会形成 Prisma 非法 `take` 并可能返回 500；改用共享有界整数解析，非法值稳定返回 400。
- 历史查询的租户、供应商商品 ID 和 `metadata.supplierId` 双重隔离保持不变。

### 自动化与端到端验证

- 扩展供应商隔离端到端：创建启用分类、批量归类 1 个 SKU、将停用 SKU 批量恢复供应；商品最终分类和状态正确。
- 历史列表包含“批量修改商品分类”和“批量恢复供应”，操作人为当前供应商账号且时间可解析；注入的另一供应商私有商品操作不可见。
- `limit=NaN`、`limit=1.5`、`limit=201` 均返回 400，未进入数据库分页。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 35/35、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；集成测试数据已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 供应商发起停售仍进入总厨审批，恢复已停用商品仍为直接操作；这是现有既定规则，本轮仅验证而未改变审批语义。
- 操作历史当前返回最近最多 200 条、未提供翻页；现有移动端入口只展示最近 50 条。若需长期审计检索，应另行定义分页和筛选需求。

## 2026-07-18 06:25 第二十四轮（供应链稳定分页）

### 检查范围与修改

- 审计商品、订货单和配送单的服务端 offset 分页，确认仅按 `createdAt` 排序；批量导入或同事务内多单据时间戳相同时，数据库没有确定性次序，跨页加载可能出现重复或漏项。
- 商品列表统一按 `createdAt ASC, id ASC`，订货与配送列表统一按 `createdAt DESC, id DESC`；筛选、总数、租户/供应商/门店隔离和每页上限不变。
- 该修改只确定同时间戳记录的顺序，不改变正常时间不同的业务排序。

### 自动化与端到端验证

- 在隔离库为同一供应商分别创建 2 个完全相同 `createdAt` 的商品、订货单和配送单；每类连续 3 轮并行请求第 1/2 页，返回 ID 顺序固定、总数均为 2 且无重复漏项。
- 商品使用名称筛选，订货和配送使用单号关键词筛选，证明次排序与查询条件、服务器分页共同生效。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 36/36、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试商品和单据已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 仍为 offset 分页；极深页在高频新增数据下不能提供游标级快照一致性，但 100000 页资源上限和确定性次排序已避免常见重复/漏项。
- 本轮未改变单据状态、金额、库存或审批规则。

## 2026-07-18 06:27 第二十五轮（订货日期写入校验）

### 检查范围与修改

- 审计订货单创建和改单申请的期望到货日期，确认创建仅校验非空、改单仅校验 `YYYY-MM-DD` 外形；不存在日期可能在 `new Date()` 或 Prisma 写入阶段转成 500。
- 两个写入 schema 统一复用真实日历日期校验，合法闰日继续接受，不存在日期在任何数据库查询或事务前返回 400。
- 列表日期范围和库存批次日期已在上一轮复用相同 schema，本轮完成供应链日期入口收敛。

### 自动化与端到端验证

- 真实 API 端到端验证创建订货单传 `2026-02-29` 返回 400，不生成单据；对已创建订货单申请改为 `2026-04-31` 同样返回 400，不生成修订。
- 随后合法订单继续完成接单预占、并发发货、送达、并发收货、差异结算与仲裁，证明日期护栏未影响正常流程。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 36/36、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试单据已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 当前只验证有效日历日期，没有强制“不得早于今天”；历史补录、测试与业务改期是否允许过去日期需要产品口径，本轮未擅自限制。
- 本轮未改变改单确认角色、首次提交快照、金额或库存规则。

## 2026-07-18 06:29 第二十六轮（商品筛选输入隔离）

### 检查范围与修改

- 审计供应商商品列表的分类、状态和关键词筛选，确认原先直接将 query 写入 Prisma；非法枚举状态会触发 Prisma 校验错误并可能返回 500，关键词和分类也没有长度上限。
- 新增商品列表筛选 schema：分类最多 40 字符、关键词最多 80 字符、状态仅允许四个商品状态；空状态继续等价为“全部”，分页及兼容参数保持原行为。
- 供应商、门店与集团角色的目录隔离条件仍在筛选后强制叠加，调用方无法用 query 覆盖。

### 自动化与端到端验证

- 供应商 A 按商品编码搜索只返回自家目标 SKU，按“其他”分类筛选同样只返回供应商 A 商品，不泄露供应商 B 同租户目录。
- `status=UNKNOWN`、81 字符关键词和 41 字符分类均返回 400，未进入 Prisma 查询。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 37/37、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试目录数据已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 商品列表为兼容选品下拉仍支持不传 `page` 时返回全量；门店角色只返回已启用商品，供应商可查看自家全部状态。
- 本轮未改变商品分类名称长度、状态审批或价格规则，仅收紧读取 query。

## 2026-07-18 06:32 第二十七轮（入库单列表隔离与分页）

### 检查范围与修改

- 审计入库单列表发现 `status`、供应商 ID 和门店 ID 直接进入 Prisma，非法状态可能返回 500；分页排序也仅使用 `createdAt`。
- 新增入库单列表筛选 schema：状态只允许 7 个入库状态，供应商/门店 ID 为 1 至 100 字符；空状态保持“全部”。分页增加 `id DESC` 次排序。
- 供应商和门店角色仍强制使用令牌中的绑定范围，query 不能覆盖；集团角色的显式筛选保持兼容。

### 自动化与端到端验证

- 供应商 A 查询入库单列表得到 0 条，直接打开供应商 B 入库单返回 404；`status=UNKNOWN` 返回 400。
- 为供应商 A 创建 2 张相同 `createdAt` 的确认入库单，连续 3 轮并行请求第 1/2 页，ID 顺序固定、总数为 2 且无重复漏项。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 37/37、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试入库单已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 入库单列表目前没有日期范围和商品关键词筛选；客户本轮明确优先的是订货/配送查询，本轮未扩展入库单产品需求。
- 本轮未改变手工入库、确认、报损、作废或三方核对规则。

## 2026-07-18 06:34 第二十八轮（供应商库存读取稳定性）

### 检查范围与修改

- 审计供应商库存列表、临期批次、活动预占、批次余额和库存流水的截断排序；这些查询已强制租户/供应商条件且 query 使用 Zod，但同一排序值下缺少最终 ID 次序。
- 库存列表、临期批次、预占、批次和流水分别补 `id` 的升/降序兜底，确保相同库存、名称、日期或创建时间下返回稳定；既有 FEFO、预期到货日和时间主排序不变。
- `productId` 继续只作为供应商范围内的附加条件，不能扩大查询权限。

### 自动化与端到端验证

- 供应商 A 使用供应商 B 商品 ID 查询活动预占、批次和流水，三个接口均返回空数组。
- 为供应商 A 创建 2 条相同未来 `createdAt` 的调整流水，连续 3 轮查询前 2 条均按 ID 降序稳定返回。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 37/37、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试流水已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 预占、批次和流水当前为有上限的最近/优先记录列表而非完整分页；移动端已有商品筛选。若要长期库存审计检索，需要另行定义游标或页码产品需求。
- 本轮未改变库存余额、批次消耗、报损或盘点业务规则。

## 2026-07-18 06:38 第二十九轮（门店领用记录隔离与稳定性）

### 检查范围与修改

- 审计店长/厨师长门店库存读取和人工领用记录；角色权限、租户条件及绑定门店优先级已存在，调用方传入其他门店 ID 不能覆盖账号绑定。
- 领用记录原按日期、创建时间降序且截断最近 100 条，同一时间写入时缺少确定性次序；补充 `id DESC` 最终排序。
- 保持实物盘点、预计库存、人工消耗和报损口径不变；未对尚无业务确认的盘点流程作推断。

### 自动化与端到端验证

- 将门店库存路由纳入真实 PostgreSQL 隔离端到端；同租户创建两个门店后，厨师长显式请求另一门店仍只得到绑定门店的领用记录，另一门店数据不可见。
- `days=0`、`days=366`、`days=NaN` 均返回 400；绑定门店 2 条同日期、同创建时间记录连续 3 轮均按 ID 降序稳定返回。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 38/38、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试门店和领用记录已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 领用记录当前固定返回最近 100 条而非分页；现有页面用于近期查询。若要长期门店库存审计，需要单独定义分页、日期区间和导出需求。
- 本轮未改变领用写入、库存估算、报损或盘点业务规则。

## 2026-07-18 06:40 第三十轮（盘点与预计库存稳定读取）

### 检查范围与修改

- 审计最新实物盘点和基于盘点滚动的预计库存；快照查询已按租户和门店隔离，门店角色仍强制使用账号绑定门店。
- 盘点明细原只按人工 `sortOrder` 排序，同序号时结果不确定；补 `id ASC`。预计库存商品原只按名称排序，同名 SKU 时结果不确定；补 `id ASC`。
- 最新快照已由 `(storeId, snapshotDate)` 唯一约束保证同门店同日期唯一，本轮不改变盘点日期、单位换算、低库存或库存估算语义。

### 自动化与端到端验证

- 真实 PostgreSQL 集成测试为绑定门店和另一门店分别创建盘点快照；厨师长即使显式传入另一门店 ID，仍只读取绑定门店的快照与预计库存。
- 绑定门店 2 条同 `sortOrder` 盘点明细及 2 个同名商品连续 3 轮并行读取，分别按明细 ID、商品 ID 升序稳定返回。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 39/39、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试快照与商品已级联清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 盘点数据目前由受控脚本导入和复核，没有在线盘点流程；是否开放移动端盘点、复盘或更正仍需业务确认，本轮未扩展。
- 本轮仅确定同键记录的展示顺序，不改变库存数量、金额或状态。

## 2026-07-18 06:46 第三十一轮（人工领用并发幂等）

### 检查范围与修改

- 审计店长/厨师长人工领用重复提交：原接口使用随机批次 ID，页面也未发送幂等键；全局 Redis 幂等在缓存不可用时会降级，并发双击可能生成两批领用。
- 领用请求新增可选 8 至 80 字符幂等键；服务端将租户、用户、方法、路径和客户端 key 哈希成操作 ID，在同一事务内获取 PostgreSQL advisory lock、核对既有领用内容并创建领用明细与操作日志。
- 同 key、同内容重放返回原操作 ID 并标记 `duplicated=true`；同 key 改日期、备注、商品或数量返回 409。未传 key 的旧调用保持随机批次兼容行为。
- 库存页面每次打开领用弹窗生成一个客户端 key，失败重试沿用，成功后刷新；提交期间按钮禁用并显示“录入中”，阻止常见快速双击。

### 自动化与端到端验证

- 真实 PostgreSQL 并发发送 2 个完全相同领用请求，两者均 200，一次正常、一次幂等重放；数据库只有 1 条领用和 1 条操作日志，操作 ID 相同。随后相同 key 改数量返回 409。
- Web 安全生产构建先停止 dev 并清理 `.next`，138 个静态页面生成成功；之后干净恢复开发服务。
- 390×844 移动端浏览器复核库存页和领用弹窗：入口、日期、商品、数量、备注与确认按钮可见，弹窗无横向溢出；浏览器视口已恢复并完成清理。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 40/40、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试领用和日志已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 数据库幂等只对携带 key 的新页面和调用方生效；不带 key 的旧客户端仍按兼容路径创建新领用批次。
- 本轮不改变领用数量、盘点估算或库存扣减口径，仅保证同一显式请求不重复记账。

## 2026-07-18 06:49 第三十二轮（商品导入批次权限）

### 检查范围与修改

- 审计供应商商品批量上传、历史和撤回；批量创建本身已限制商品写角色并强制供应商绑定，但上传历史和撤回接口遗漏了相同角色白名单。
- 修复任意门店登录角色可查看上传历史、并可撤回整批商品导致停售的越权路径；历史与撤回现仅允许管理员、采购、总厨和供应商商品角色，供应商仍强制绑定自身范围。
- 上传原始文件名新增 255 字符上限，非法输入在事务前返回 400；历史最近 50 条增加 `id DESC` 次排序，保持相同创建时间下稳定。
- 批量 500 行上限、逐行 Zod 校验、重复编码处理、分类状态复核、商品/审批单/操作日志原子事务均已存在，本轮未重复实现。

### 自动化与端到端验证

- 真实 PostgreSQL 为供应商 A 创建 2 个同时间上传批次、供应商 B 创建 1 个批次；供应商 A 连续 3 轮只见自家两批且顺序稳定，无法撤回供应商 B 批次。
- 门店厨师长读取上传历史和撤回供应商 A 批次均返回 403，批次 `revokedAt` 保持为空；256 字符文件名返回 400 且未写入批次。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 41/41、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试上传批次已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 商品上传当前允许部分行成功、错误行跳过，这是页面明确提示并返回逐行结果的既有产品语义，本轮未改成整表全成全败。
- 批次撤回仍会停售该批当前商品并取消对应待审批单，但不物理删除历史；权限收紧不改变该业务语义。

## 2026-07-18 06:53 第三十三轮（商品编辑校验与审批防绕过）

### 检查范围与修改

- 审计供应商商品编辑发现字段虽有 allowlist，但没有写入 schema；API 可接受负价格、非正起订量/步长、超限保质期和超出页面约束的实发上限。
- 新增严格商品编辑 schema：名称/编码/规格/分类/单位/图片键长度与格式受限；价格非负且符合数据库量级，起订量和步长必须为正，保质期 0 至 3650 天，实发百分比 1 至 10、绝对加量 0 至 10000。
- 修复供应商将 `PENDING_APPROVAL` 商品直接改为 `ENABLED` 的审批绕过；单条和批量“恢复供应”现在都只接受原状态为 `DISABLED` 的商品，待审批商品保持待审批。
- 单条恢复和批量恢复均在写事务中带原状态条件；并发状态变化返回 409，避免检查后状态竞争。

### 自动化与端到端验证

- 真实 API 逐项提交负价格、零起订量、负步长、超长规格、超限保质期及四种越界实发上限，全部返回 400；原商品价格保持 10。
- 创建供应商 A 待审批商品后，单条启用和批量启用均返回 400，数据库状态仍为 `PENDING_APPROVAL`；既有已停用商品批量恢复回归继续通过。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 42/42、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试待审批商品已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 供应商恢复已停用商品仍为直接操作，这是既有规则；若恢复也需总厨审批，应由产品明确后另行调整。
- 本轮未改变涨价审批、降价直生效、停售审批或批量导入的部分成功语义。

## 2026-07-18 06:56 第三十四轮（分类并发排序与历史稳定性）

### 检查范围与修改

- 审计供应商分类读取、新增、改名、停用、恢复、排序与合并；跨供应商合并隔离、系统“其他”分类保护和合并原子事务已有测试覆盖。
- 分类新增原在事务外读取最大 `sortOrder`；同一供应商并发新增不同分类时可能分配相同序号。新增现复用供应商级 PostgreSQL advisory lock，在同一事务内检查重名、计算排序号、创建分类并写操作日志。
- 商品关键操作历史原只按创建时间倒序且截断最多 200 条；增加 `id DESC` 最终排序，同时间操作稳定返回。

### 自动化与端到端验证

- 真实 API 并发新增 2 个不同分类，两者均返回 201，数据库获得两个不同排序号；分类与操作日志在测试结束后清理。
- 注入 2 条完全相同 `createdAt` 的供应商 A 商品分类日志，连续 3 轮查询均按 ID 降序稳定返回，供应商隔离条件保持生效。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 43/43、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；业务 xlsx 保持未跟踪。

### 风险与待确认

- 分类改名和手动排序目前各自为事务，但尚未统一获取同一供应商级锁；低概率并发管理操作仍可能返回唯一约束错误或需要刷新重试，后续可继续收敛。
- 本轮不改变分类停用、恢复、合并或商品归类业务规则。

## 2026-07-18 06:59 第三十五轮（分类改名与归类并发收敛）

### 检查范围与修改

- 继续审计分类并发写入：分类合并和新增已使用供应商级锁，但改名仍在锁外读取旧名称，批量归类也只在事务前确认目标分类；两者并发可能在改名完成后把商品写回旧分类名。
- 分类改名/停用/恢复现在进入事务后先获取同一供应商级 advisory lock，再重新读取分类、校验系统保护和重名、同步商品名称并写审计日志。
- 供应商批量归类在写事务内获取同一把锁并重新确认目标分类仍存在且启用；若改名已先完成，旧名称请求返回 400，不生成孤立分类。

### 自动化与端到端验证

- 连续 3 轮为供应商 A 创建分类，并并发执行“分类改名”和“将商品归入旧名称”；改名均成功，归类按锁顺序成功或返回 400。
- 若归类先执行，后续改名原子搬迁商品到新名称；若改名先执行，商品保持原分类。三轮最终均不存在引用已消失旧分类名的商品。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 44/44、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试分类与商品变更已清理，业务 xlsx 保持未跟踪。

### 风险与待确认

- 手动分类顺序保存尚未纳入同一供应商级锁；并发新增分类时会因“必须包含全部分类”校验返回 400，管理员刷新重试即可，不会造成商品孤立。
- 本轮不改变分类改名、停用、恢复或合并语义，仅收敛并发顺序。

## 2026-07-18 07:01 第三十六轮（库存快照导入边界回归）

### 检查范围与修改

- 审计供应商库存入库、盘点调整、报损与全量快照导入；商品行锁、供应商/租户条件、预占下限、余额/流水/批次原子更新和日期/数量边界均已存在。
- 库存快照采用逐行事务并返回 `adjusted/skipped/failed`，允许部分成功；未知 SKU 会在任何写入前整批返回 409。这是接口和页面已有显式语义，本轮不擅自改成整表事务。
- 本轮未修改业务代码，新增真实 API 回归覆盖跨供应商名称、trim 后重复品名、重复目标值跳过及库存批次余额一致性。

### 自动化与端到端验证

- 供应商 A 用供应商 B 商品名导入返回 `UNMATCHED_STOCK_SKU` 409，供应商 B 库存保持 22；同一清单中两条 trim 后同名商品返回 400。
- 将供应商 A 商品目标库存从 10 调为 9 后，同一请求重放不新增调整而计入 `skipped=1`；随后恢复为 10，商品库存与所有批次剩余量合计均为 10。
- 全量 API 单元测试 108/108、独立 PostgreSQL 集成测试 45/45、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `git diff --check` 与高置信敏感信息扫描通过；测试库存变化已恢复并在清理阶段删除，业务 xlsx 保持未跟踪。

### 风险与待确认

- 快照导入按商品名称精确匹配；同名 SKU 不唯一时该行失败并要求先整理主数据。是否改为编码优先匹配需要产品和模板字段口径，本轮未推断。
- 本轮只补自动化证据，不改变盘点、报损、预占或导入部分成功规则。

## 2026-07-18 07:03 第三十七轮（发布候选最终复核）

### 检查范围与结果

- 再次确认当前分支为 `release/20260715-p0`，工作区仅保留用户业务文件 `库存主数据映射审计_2026-07-15.xlsx` 未跟踪；未合并 main、未创建 PR、未连接或写入生产。
- 本地既有库 `dianjie_v4_local` 共有 56 个迁移，`prisma migrate status` 为最新；数据库到当前 Prisma datamodel 的 schema diff 为零。
- 刚 fetch 的远端发布分支与本地 `HEAD` 哈希一致，领先/落后为 `0/0`。
- 标准开发服务已干净恢复：API 监听 4444、Web 监听 3200，`/health` 与 `/inventory` 均返回 HTTP 200。

### 最终自动化基线

- API 单元测试 108/108、独立 PostgreSQL 集成测试 45/45、API TypeScript build 全部通过。
- Web 测试 13/13、Web TypeScript 检查及安全生产构建通过；生产构建生成 138 个静态页面，之后已清除生产 `.next` 并恢复 dev。
- 供应商订货/配送日期与商品筛选、分页与快照搜索，首次提交/改单确认/实际发货，收货原子事务与并发幂等，商品分类/批量操作/图片、库存及店长库存均有真实 API 或浏览器回归证据。

### 停止条件与待确认

- 当前客户优先项内未发现新的安全、确定性小批次；剩余事项涉及业务决策或存量数据治理，按夜间边界停止继续修改。
- 发布前最重要的人工决策仍是 722 条历史商品引用已不存在供应商：本次迁移仅跳过其开账批次并保留原数据，需决定归档、补绑或继续隔离。
- 补送、已确认入库单更正、报损后是否回补供应商物理库存及在线盘点流程仍需业务确认；本轮没有擅自定义。

## 2026-07-19 05:43 第三十八轮（商品创建数值边界）

### 检查范围与修改

- 回归供应商商品单条新建和批量导入，确认商品编辑已限制 `Decimal(10,2)` 存储上限，但创建 schema 仍允许价格、库存、安全库存、起订量和步长超过 `99,999,999.99`；此类输入会进入 Prisma/PostgreSQL 后返回 500。
- 单条和批量创建现统一在 Zod 层应用数据库数值上限：单条请求稳定返回 400；批量导入按既有逐行部分成功语义记录失败行，不进入商品、审批单或操作日志写入。
- 真实 API 端到端补充单条价格和批量起订量超限断言，并精确清理全行失败时保留的空上传批次。
- 端到端原有“未绑定供应商账号读取商品返回 403”断言更新为 401；当前全局认证会在进入商品路由前逐请求核验供应商绑定及启用状态，因此未绑定账号按设计直接失效，而不是落到路由能力层。

### 自动化与端到端验证

- 独立本地 `dianjie_v4_ci` 空库应用全部 56 个 migration 后，供应商隔离目标文件 22/22、全量 PostgreSQL 集成测试 48/48 通过；新增断言覆盖单条五类超限输入、批量全行失败和零商品残留。
- 全量 API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。
- `dianjie_v4_local` + 真实 4444 API 商品管理端到端通过：数值上限、分类/图片、批量分类/停售、并发审批、500 行导入、事务故障回滚、跨租户和未绑定账号拦截全部为真；500 行批次本轮为 81ms。
- 首次端到端命中未加载当前代码的旧 4444 进程后，已只重启本仓库 API dev 并完整重跑通过；API `/health`、Web `/inventory` 均为 HTTP 200，3200 Web dev 未停止或改动。
- `git diff --check`、全仓高置信私钥/云密钥/GitHub token 扫描和改动内嵌数据库凭证扫描通过；数值边界测试商品与空批次残留均为 0，业务 xlsx 保持未跟踪。

### 风险与待确认

- 本轮只将数据库必然拒绝的超限输入提前校验，没有新增小数位规则，也未改变商品审批、批量部分成功、库存或分类业务语义。
- 供应商账号解除绑定后即使登录接口曾颁发 access token，后续请求也会由全局认证以 401 拒绝；这是现有 fail-closed 安全设计，本轮仅对齐端到端证据，没有修改认证行为。

## 2026-07-19 05:59 第三十九轮（订货与发货数值边界）

### 检查范围与修改

- 审计订货创建、改单申请和实际发货写入，确认订货数量/实发数量此前没有对齐 PostgreSQL `Decimal(10,2)` 物理上限，订货行金额、订货总额和配送金额也未在写入前对齐 `Decimal(12,2)`；极端输入可能在 Prisma/PostgreSQL 层返回 500。
- 创建、改单和发货 schema 现统一限制单行数量不超过 `99,999,999.99`，创建订货单同时限制最多 500 行；该限制只反映数据库安全边界，不新增起订量、步长、实发上限等业务规则。
- 订货创建、改单快照和发货均在事务前检查单行及总金额上限；发货累计数量、单次/累计金额改用 `Prisma.Decimal` 计算并按两位小数落库，避免 JS 浮点累加进入金额字段。
- 真实 API 脚本改为每轮创建唯一商品及开账批次，结束后清理预占、批次分配、流水、批次和商品；解决固定商品缺少可用批次、历史同名商品污染搜索断言及清理顺序触发外键失败的问题。

### 自动化与端到端验证

- 独立 `dianjie_v4_ci` 测试库全量 PostgreSQL 集成测试 48/48 通过；覆盖超限订货数量、501 行、单行/总金额溢出、超限改单和超限发货，并断言被拒绝请求不生成订货单。
- `dianjie_v4_local` + 真实 4444 API 配送全流程通过：超限输入返回 400，分两次配送/收货、手工与自动收货并发幂等、商品/日期搜索、快照不变及库存流水均通过；结果为 2 张配送单、2 张入库单、实发/实收 5。首次提交不可变与改单确认链路也通过，原金额 16.65、改单后 26.82、时间线 5 条。
- 首次脚本运行发现前两次失败运行残留的专用验证数据；已按专用供应商编号和 `delivery-order-` 幂等键前缀精确删除本地 2 张测试订货单、4 张配送单、4 张入库单及其测试专用关联记录。重跑后配送订货、配送商品、改单订货和改单商品四类残留均为 0。
- 全量 API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。集成测试第一次使用既有库 URL 时被 `_test/_ci` 安全护栏在收集阶段拒绝、未产生写入；显式切换到 `dianjie_v4_ci` 后完整通过。

### 风险与待确认

- 本轮只提前拒绝数据库必然无法存储的输入，并保持订货单与配送单为两张独立单据；未改变补送、报损收货、已确认入库单更正或盘点规则。
- 金额上限校验与现有商品价格上限共同保证写入安全；未来若数据库字段精度调整，应同步修改此处常量和集成用例。

## 2026-07-19 06:05 第四十轮（收货输入安全）

### 检查范围与修改

- 审计手工收货与自动收货竞争路径；配送单状态抢占、入库单/入库明细、到货差异单、累计实收及订货单状态已在同一事务，配送单唯一入库约束和并发双收货幂等返回已有覆盖。
- 手工收货此前直接解构未经校验的 JSON body；`items: [null]`、错误数量类型或错误证据图片类型可能触发运行时异常并返回 500。现增加严格 Zod schema，校验明细结构、数量物理上限、最多 500 行、最多 9 张非空字符串证据图片、差异类型及多余字段。
- 同一商品重复提交多行实收数量现返回 400，不再由 `Map` 静默采用最后一行；缺省明细仍按本次配送实发数量全收，原因仍按既有逻辑 trim 后保留 30 字，未改变报损或应付语义。

### 自动化与端到端验证

- 供应链目标集成文件 2/2、独立 `dianjie_v4_ci` 全量集成测试 48/48 通过；新增 7 类畸形 body 断言，并确认每次均返回 400、入库单为 0、配送单保持 `DELIVERED`，之后正常并发双收货仍只生成一张入库单和一张到货差异单。
- `dianjie_v4_local` + 真实 4444 API 配送端到端通过；`null` 明细、字符串数量、重复商品、非字符串图片和多余字段均被拒绝且零写入，随后分批配送、并发手工收货、手工/自动收货竞争及查询链路全部通过。
- 全量 API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过；真实脚本结束后专用订货和唯一商品残留均为 0。

### 风险与待确认

- 本轮不改变到货短量与到货破损的财务口径，也不处理已确认入库单更正；这些仍按既定业务确认边界保留。
- 证据图片接口现在明确拒绝超过 9 张或非字符串项，不再静默截断；当前移动端本就按最多 9 张提交。

## 2026-07-19 06:12 第四十一轮（供应商入库批次并发）

### 检查范围与修改

- 审计供应商手工/Excel 入库、盘点调整、报损与库存快照路径；商品行锁、租户/供应商隔离、库存/流水/批次原子写入和库存数值边界已存在。
- 自定义入库批次号受数据库 `(tenantId, productId, batchNo)` 唯一约束，但同请求重复、历史重放或并发请求此前会在创建批次时触发 Prisma 唯一约束并返回 500。
- 入库 schema 现在按 trim 后的“商品 + 批次号”拒绝同请求重复；事务获取全部商品行锁后重新查询自定义批次号，因此同一商品并发入库由首个请求创建，后续请求稳定返回 409，库存、流水和批次保持整批原子。

### 自动化与端到端验证

- 供应商隔离目标集成文件 23/23、独立 `dianjie_v4_ci` 全量集成测试 49/49 通过；新增同请求重复零写入、并发 `[200,409]`、历史重放 409、最终库存只增加 1 且仅一条流水/一个批次的断言。
- 新增本地真实 API 验证脚本；`dianjie_v4_local` + 4444 API 实测同请求重复返回 400，并发同批次返回 `[200,409]`，再次重放返回 409，最终 `stock=1 / movements=1 / batches=1`。
- 全量 API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过；真实脚本专用商品和批次残留均为 0。

### 风险与待确认

- 同一批次号仍允许用于不同商品，符合当前数据库唯一键和按商品管理批次的既有规则；本轮未扩大为供应商全局唯一。
- 本轮不改变快照导入的部分成功、未知 SKU 整批拒绝、盘点或报损规则。
## 2026-07-19 06:16 第四十二轮（补录入库金额边界）

### 检查范围与修改

- 审计非采购流程的补录入库单；请求已限制数量、单价、两位小数、最多 500 行、租户/门店/供应商/商品归属及重复商品，但“数量 × 单价”的单行金额和多行总额未对齐 `Receipt/ReceiptItem Decimal(12,2)`。
- 合法的数量与单价组合仍可生成超过 `9,999,999,999.99` 的金额，并在入库单事务内由 Prisma/PostgreSQL 返回 500。现使用 `Prisma.Decimal` 预计算并复用每行金额，单行或总额超限均在任何单号、入库单、明细和日志写入前返回 400。
- 本轮只增加数据库物理上限，不修改补录入库单的 DRAFT 状态、确认流程、商品快照或财务派生规则。

### 自动化与端到端验证

- 供应链目标集成文件 2/2、独立 `dianjie_v4_ci` 全量集成测试 49/49 通过；新增单行 `1,000 × 10,000,000` 溢出和两行各 50 亿、总额 100 亿溢出，均返回 400 且入库单计数不变。
- 初次目标用例因测试 Fastify 实例未注册 `/api/receipts` 返回 404；补齐真实入库路由装配后重跑通过，不是业务实现失败。
- `dianjie_v4_local` + 4444 真实 API 确认补录入库金额溢出返回 400，随后配送/收货全链、收货输入校验与并发幂等继续通过；专用订货和商品残留均为 0。
- 全量 API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。

### 风险与待确认

- 本轮不改变补录入库的授权角色，也不触及报损入库和已确认入库单更正语义。
- 若未来提升数据库金额字段精度，应同步更新入库、订货/配送共用的物理上限与集成用例。

## 2026-07-19 06:20 第四十三轮（送达与验收单输入安全）

### 检查范围与修改

- 审计订货取消、拒单、发货、送达和厨师验收单状态操作；取消的非字符串原因已返回 400，拒单会显式转字符串，不存在 Prisma 类型异常。本轮聚焦仍会返回 500 的送达与验收单路径。
- 送达接口此前将对象型 `note` 直接交给 Prisma 字符串字段；厨师验收单只校验 `images` 是数组，数字/对象项会进入 PostgreSQL `String[]`。两者现使用严格 Zod schema，拒绝错误类型和多余字段。
- 送达备注仍为可选，明确最长 500 字；验收单仍要求 1 至 5 张照片、备注可选且最长 500 字，并新增每个图片地址必须是非空字符串的校验。状态机、角色和图片数量规则未改变。

### 自动化与端到端验证

- 供应链目标集成文件 2/2、独立 `dianjie_v4_ci` 全量集成测试 49/49 通过；数字图片数组和对象型送达备注均返回 400，订货单图片保持空数组、配送单保持 `SHIPPED`，随后正常送达/收货继续通过。
- `dianjie_v4_local` + 4444 真实 API 同样确认两类畸形请求返回 400 且零状态写入；随后配送/收货全链、补录入库金额边界、收货输入校验及并发幂等全部通过，专用订货和商品残留为 0。
- 全量 API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过。

### 风险与待确认

- 本轮未修改撤回和拒单原因语义，也未改变送达后 24 小时自动收货逻辑。
- 图片地址只校验非空字符串，不在 API 层推断 OSS 域名或媒体类型；现有上传与展示链路保持不变。

## 2026-07-19 06:24 第四十四轮（店内报损金额边界）

### 检查范围与修改

- 审计店内报损数量与成本金额写入：单行数量已限制为 1,000,000，但门店库存移动平均成本由既有库存快照和确认入库形成；高成本商品仍可能使 `LossClaim/LossClaimItem Decimal(12,2)` 溢出并由 Prisma/PostgreSQL 返回 500。
- 现使用 `Prisma.Decimal` 在报损编号、操作日志和报损单事务之前检查单行及总金额，超过 `9,999,999,999.99` 时返回 400 且不产生写入。
- 本轮只对齐数据库物理上限，不改变 ¥500 审批阈值、¥3,000 通知规则、成本来源、供应商实物库存或报损业务口径。

### 自动化与端到端验证

- 供应商隔离目标集成文件 24/24、独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 50/50 通过；新增移动平均成本 20,000、数量 1,000,000 的溢出场景，确认返回 400 且报损单数量不变。
- `dianjie_v4_local` + 4444 真实 API 报损完整性脚本通过：金额边界返回 400，四张合法报损单号保持唯一，权威成本为 10.83，并发审批只成功一次且店内预计库存为 116；脚本中的历史到货补报断言同步为当前 `409 / ARRIVAL_CLAIM_WINDOW_CLOSED` 明确关闭契约。
- 全量 API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过；专用报损商品、库存快照和跨租户测试数据残留均为 0。

### 风险与待确认

- 本轮未改变供应商责任报损、审批后库存处理或验收后补报关闭策略；后者只修正验证脚本的过期预期。
- 若未来调整数据库金额字段精度，应同步更新报损金额常量和集成用例。

## 2026-07-19 06:32 第四十五轮（报损审核输入安全）

### 检查范围与修改

- 审计供应商处理到货差异和总厨审核店内报损接口；前者在权限校验前直接解构 body，空 body 会触发运行时异常，对象型备注会在 `trim()` 处返回 500；后者会将对象型备注直接写入 Prisma 字符串字段并返回 500。
- 两个审核接口现共用严格 Zod schema：操作只允许 `approve/reject`，备注必须是 trim 后不超过 500 字的字符串，拒绝时必须填写非空原因，并拒绝多余字段。角色权限仍先于 schema 校验执行，未授权请求不会因校验顺序泄露接口细节。
- 本轮不修改审核角色、待审状态抢占、账期调整、供应商库存或通知规则；合法备注只做既有语义下的空白清理。

### 自动化与端到端验证

- 供应链目标集成文件 2/2、独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 50/50 通过；空 body、对象备注、501 字备注和多余字段均返回 400，报损保持 `PENDING`、账期金额保持 50，随后正常拒绝和仲裁继续通过。
- `dianjie_v4_local` + 4444 真实 API 店内报损完整性脚本通过：四类畸形总厨审核均返回 400 且状态不变，之后并发批准/驳回仍只产生一个终态；供应商报损并发脚本也通过相同畸形输入、跨供应商隔离、批准/拒绝幂等和自动审核竞争。
- 供应商并发脚本同时对齐两条已落地但未更新的安全断言：未绑定供应商账号由全局认证以 401 fail-closed；到货差异批准保持供应商库存不变且不生成报损库存流水。这里只修正验证预期，未修改对应实现。
- 全量 API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过；近 20 分钟专用商品、订单、报损单、账号、供应商和快照残留均为 0。首次残留审计命令因 CJS 不支持 top-level await 在查询前退出，改用异步 IIFE 后成功完成零残留核验。

### 风险与待确认

- 本轮未恢复已关闭的验收后补报入口，也未改变到货差异批准后的库存口径；两项继续遵循 7 月 18 日发布候选的既定实现。
- 备注上限为技术输入边界；若前端未来需要更长审核说明，应在同步评估数据库字段、移动端展示和审计日志后统一调整。

## 2026-07-19 06:40 第四十六轮（商品导入分类并发）

### 检查范围与修改

- 审计供应商商品分类、单条建档和 Excel/CSV 批量导入；分类新增、改名/停用和批量归类已共用供应商级事务锁，但单条及批量商品建档自动创建缺失分类时未进入该锁。
- 并发导入不同新分类可能同时读取相同最大排序号并形成重复 `sortOrder`，也可能与分类停用、改名或归类竞争。两条自动建类路径现均在查询分类状态、分配排序号和创建商品前获取同一事务级 advisory lock。
- 商品、分类、批次、审批单和操作日志仍在原事务中提交；未修改导入的逐行校验、部分成功、未知供应商隔离、审批或撤回语义。

### 自动化与端到端验证

- 供应商隔离目标集成文件 24/24、独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 50/50 通过；四个不同新分类并发批量导入均成功创建商品和分类，四个排序号唯一，用例内清理后上传历史隔离断言继续通过。
- 目标用例首次运行暴露两项测试夹具问题：生成的商品编码超过 40 字，按既有部分成功契约返回 201/零创建；新增批次未就地清理而影响后续历史断言。缩短专用编码并补齐用例内商品、批次、审批单、日志和分类清理后重跑通过，实现 build 始终通过。
- `dianjie_v4_local` + 4444 真实供应商商品管理端到端通过：新增四路并发导入分类排序唯一；分类/编码查询、图片 key、批量分类、批量停售审批、500 行导入（93ms）、审批/撤回竞争、事务回滚、跨租户和解绑账号边界全部继续通过。
- 全量 API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过；近 15 分钟专用商品、上传批次、并发分类、临时账号和临时租户残留均为 0。

### 风险与待确认

- advisory lock 只按租户和供应商串行化分类元数据变更，不会串行化不同供应商的导入；500 行真实导入性能证据未见退化。
- 本轮没有将分类排序号提升为数据库唯一约束，避免对历史重复排序数据直接施加迁移风险；新写入路径由事务锁保证稳定分配，列表仍以名称作为同排序号兜底。

## 2026-07-19 06:44 第四十七轮（商品分类并发排序）

### 检查范围与修改

- 继续审计供应商分类写路径；分类排序接口此前在事务外查询“是否包含全部分类”，随后按客户端顺序逐行更新，但没有进入分类 advisory lock。两个相反顺序的并发保存可能以相反顺序获取行锁并触发 PostgreSQL 死锁，其中一个请求返回 500。
- 分类排序现先获取与新增、改名/停用、合并、批量归类和自动建类相同的供应商级事务锁，再在锁内重新查询当前全部分类、校验提交集合、按租户/供应商范围更新每个排序号并写操作日志。
- 若等待锁期间分类集合发生变化，请求稳定返回 400；若分类状态在锁内异常变化则返回 409。未修改分类排序含义、最大 200 类限制或列表展示规则。

### 自动化与端到端验证

- 供应商隔离目标集成文件 24/24、独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 50/50 通过；两个完全相反的完整分类顺序并发提交均返回 200，最终每个分类排序号唯一。
- `dianjie_v4_local` + 4444 真实分类管理端到端通过：并发反向排序稳定串行成功；分类新增、改名同步商品与库存、停用阻断归类/建档、恢复、顺序保存和操作历史全部继续通过。
- 全量 API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查全部通过；专用分类联动商品和三类临时分类残留均为 0。

### 风险与待确认

- 锁粒度仍限定为当前租户的当前供应商，不影响其他供应商同时维护分类。
- 本轮继续使用应用事务锁保护新写入，不新增数据库排序唯一约束；历史相同排序号仍由名称稳定兜底，不在夜间擅自重排客户数据。
- 共享 `dianjie_v4_local` 不能作为 release 的“零 schema diff”证据，因为它包含 main 分支更晚的盘点功能；release 自身的干净部署证据应以本轮临时空库结果为准。
- 部署前应确认目标环境的迁移账本属于哪条分支；若目标库已应用 main 专属盘点迁移，不应由 release 反向删除。完整在线盘点流程仍是既定业务确认边界，本轮未擅自引入。

## 2026-07-20 05:44 第四十九轮（单商品归类并发一致性）

### 检查范围与修改

- 开始时仓库位于 `feature/chef-bom-lifecycle`；确认该分支无跟踪修改、`release/20260715-p0` 未被其他 worktree 占用后安全切回 release。用户业务 xlsx 全程保持未跟踪，未读取、修改或暂存。
- 继续审计供应商分类写路径：批量归类已纳入供应商级分类事务锁，但单商品编辑仍在事务外校验分类后直接写入。它与分类改名并发时，可在改名提交后把商品写回已不存在的旧分类。
- 单商品普通编辑和“改分类 + 涨价审批”两条事务路径现均先获取同一把供应商分类 advisory lock，并在锁内重新确认目标分类存在且启用。编辑在等锁期间遇到先完成的改名时返回 409，编辑先完成时后续改名会原子搬迁商品，不再产生孤立分类名。
- 本轮未修改分类、涨价、停售或审批业务语义，也未新增数据库约束或迁移。

### 自动化与端到端验证

- 新增可控 PostgreSQL 并发回归：用临时触发器稳定放大赛窗，修复前 25 个目标用例中稳定复现 1 个失败，商品最终落在已删除旧分类；修复后普通编辑和涨价审批两种模式均收敛到改名后新分类，目标文件 25/25 通过。临时触发器、函数、序列和业务数据均在 `finally` 中清理。
- 独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 51/51、API 单元测试 106/106、API TypeScript build、Web 测试 13/13 及 Web TypeScript 检查全部通过。
- `dianjie_v4_local` + 4444 真实供应商商品管理端到端通过：分类/编码/图片、批量归类和停售审批、四路并发自动建类、500 行导入（80ms）、审批/撤回竞争、故障回滚、跨租户与解绑账号边界均通过，验证数据已清理。
- 390×844 真实浏览器复核商品报价表和分类管理：分类入口、操作记录、批量上传、新建 SKU、分类概况/筛选/排序和底部导航布局正常，无横向溢出，控制台无 warning/error。

### 运行环境污染与风险

- 首次集成测试在业务用例前被共享 `node_modules` 中跨分支 Prisma Client 拦住，它错误期待 main 的 `inventoryMode` 字段；按 release schema 重新 `prisma generate` 后恢复。首次 4444 端到端也证实旧直启 API 进程未跟随分支切换；精确停止本仓库旧进程、用标准 dev 入口恢复后全量通过。
- API 4444 与 Web 3200 已均以当前 release 代码干净恢复，健康检查均为 HTTP 200。切换共享 worktree 分支后仍必须重新生成 Prisma Client 并重启长驻 dev，否则会得到与分支不一致的假回归结果。

## 2026-07-20 05:51 第五十轮（API Prisma 分支一致性门禁）

### 检查范围与修改

- 针对上轮实际命中的共享 `node_modules` Prisma Client 污染继续审计 API 标准命令；原 `dev/build/start/test/test:integration` 均可直接复用其他分支生成的 Client，切换 worktree 分支后可在业务代码运行前因字段不一致失败，或让长驻进程继续服务旧分支模型。
- API package 新增统一 `prisma:generate` 命令，并为 `dev`、`build`、`start`、`test`、`test:integration`、`test:watch`、`test:coverage` 和 `test:remote` 八个标准入口补齐 pre-hook。每次入口启动前都会按当前 checkout 的 `packages/db/prisma/schema.prisma` 重新生成 Client。
- 生成步骤只更新本地依赖产物，不连接或修改数据库；本轮未执行 `db reset`、`db push` 或生产部署。

### 自动化与运行验证

- 标准 API `test`、`build` 和 `test:integration` 均在日志中先执行 release Prisma generate；生成后 DMMF 明确不包含 main 专属 `Supplier.inventoryMode`，证明 Client 已回到 release 模型。
- API 单元测试 106/106、独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 51/51、API TypeScript build、Web 测试 13/13 及 Web TypeScript 检查全部通过。
- `prestart` 独立校验通过；标准 `pnpm --filter @dianjie/api dev` 在日志中先执行 `predev` 并生成 Client，随后 4444 成功启动。
- 新进程上的 `dianjie_v4_local` 真实供应商商品管理端到端再次全项通过：四路分类并发、500 行导入（77ms）、批量操作、审批/撤回竞争、故障回滚和租户边界正常，测试数据已清理。API 4444 和 Web 3200 健康检查均为 HTTP 200。

### 风险与使用边界

- 根工作区 `pnpm build` 原已在递归构建前生成 Prisma Client；API `prebuild` 会额外重复一次，代价约半秒，但保证直接运行 API package 构建也安全。
- 绕过 package scripts 直接执行 `tsx`、`vitest` 或 `node dist/index.js` 仍会绕过门禁；发布和开发回归应继续使用标准 pnpm 入口。
## 2026-07-20 06:02 第五十一轮（入库单核对输入安全）

### 检查范围与修改

- 先复核上轮 Prisma 门禁的发布路径：当前部署脚本使用完整依赖安装并显式生成 Client，PM2 生产入口直接运行 `dist/index.js`，新 `prestart` 不会改变现有生产启动链。
- 审计入库单三方核对：核对接口允许对象型备注进入 Prisma 字符串字段并返回 500；撤销核对在缺失或非法 `actor` 时会落入默认分支，误清空财务核对。
- 核对现只接受 `supplier|finance`、最长 500 字的字符串备注且拒绝多余字段；撤销必须明确提交合法 `actor`。未改变供应商/财务角色、三方齐备前置条件、撤销级联或已确认入库单业务语义。

### 自动化与端到端验证

- 新回归修复前稳定复现对象备注返回 500（全量集成 50/51，唯一失败）；修复后供应链目标文件 2/2、独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 51/51 通过。对象/501 字/多余字段备注及缺失/非法/多余字段撤销均返回 400，原六个核对字段保持不变。
- `dianjie_v4_local` + 4444 真实 API 入库完整性脚本通过：新增非法核对/撤销零写入、合法财务核对/撤销，并继续通过租户/门店/供应商隔离、并发双确认、报损原子收货和财务派生故障补偿。
- API 单元测试 106/106、API TypeScript build、Web 测试 13/13、Web TypeScript 检查、`git diff --check` 全部通过；近 20 分钟专用门店、供应商、账号、商品、入库单、报损单、触发器和函数残留均为 0。

### 运行风险与待确认

- 首次真实 API 复核仍由未重载源码的长驻 4444 进程返回旧 500；脚本 `finally` 完整清理后，精确停止本仓库进程并用标准 `pnpm --filter @dianjie/api dev` 恢复，重跑全部通过。切分支或变更路由后仍不应仅依赖 `tsx watch` 自动重载作为端到端证据。
- 补送、已确认入库单更正、报损后供应商物理库存口径和在线盘点流程仍为待确认项，本轮未擅自推进。

## 2026-07-20 06:11 第五十二轮（入库单终态并发原子性）

### 检查范围与修改

- 继续审计入库单状态转换：拒收和作废都先查可操作状态，随后按裸 `id` 更新；并发确认先完成时，旧请求仍可把 `CONFIRMED/ACCOUNTED` 覆盖为 `REJECTED/VOID`，造成入库终态与账期/对账派生矛盾。
- 送达、拒收和作废现都带原状态条件抢占；等锁期间状态已变时返回 409，不再盲写。
- 拒收的状态与操作日志、作废的状态/未付账期取消/操作日志分别收敛到同一事务。原允许状态集、角色、门店/供应商隔离和确认后财务派生规则未改变。

### 自动化与端到端验证

- 新增 PostgreSQL 受控赛窗：修复前稳定复现确认已抢占 `CONFIRMED`、并发拒收随后覆盖，确认派生读到矛盾状态并返回 500；修复后拒收/作废竞争均为确认 200、竞争请求 409、最终 `ACCOUNTED`，重复送达为一个 200 和一个 409。
- 供应链目标文件 3/3、独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 52/52 通过；API 单元测试 106/106、API TypeScript build、Web 测试 13/13 和 Web TypeScript 检查全部通过。
- 标准入口重启 4444 后，`dianjie_v4_local` 真实 HTTP 入库完整性脚本使用同类受控赛窗通过三类并发转换，同时继续通过输入安全、跨供应商隔离、报损原子收货和财务故障补偿。
- 本地专用入库单和 `local_receipt_*` 触发器/函数/序列残留为 0；CI 库 `test_receipt_state_*` 触发器/函数/序列残留为 0。

### 风险与待确认

- 竞争败方改为 409 只表达“状态已被其他请求处理”；不会自动撤销已完成的确认，也不引入已确认入库单更正语义。
- 补送、已确认入库单业务更正、报损后供应商物理库存口径和在线盘点仍保持待确认。

## 2026-07-20 06:24 第五十三轮（配送与厨师验收并发一致性）

### 检查范围与修改

- 审计订货单配送状态与厨师验收单：验收接口虽先限定 `DELIVERING`，但随后按裸 `id` 写入。供应商送达事务占锁期间，验收请求可读到旧状态，并在送达提交为 `PENDING_CONFIRM` 后继续写入照片、时间和日志，越过接口既定状态规则。
- 厨师验收现以原租户、门店和 `DELIVERING` 条件执行原子 `updateMany`；等待锁期间状态已变化时返回 409。验收字段与操作日志收敛到同一事务，响应复用实际入库的同一个 `ackedAt`。
- 本轮不把验收单改为供应商送达的强制前置条件，不改变允许角色、图片数量、备注边界或通知规则。

### 自动化与端到端验证

- 新增 PostgreSQL 受控赛窗：修复前稳定得到送达 200、过期验收仍 200，并观察到 `PENDING_CONFIRM` 订单被补写验收照片；修复后送达 200、验收 409，最终状态保持 `PENDING_CONFIRM`，验收字段和对应操作日志均为零写入。
- 独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 53/53、API 单元测试 106/106、API TypeScript build、Web 测试 13/13 和 Web `tsc --noEmit` 全部通过。
- 标准入口重启 4444 后，`dianjie_v4_local` 真实 HTTP 配送脚本通过相同受控竞态，并继续通过订货/配送查询、快照不可变、分批实发、并发双收货、自动/人工收货竞争和金额/输入边界；结果明确包含 `chefAckDeliveryRace: true`。
- 本地 `local_chef_ack_*` 与 CI `test_chef_ack_*` 触发器、函数、序列均为 0；本地专用商品和订货单、CI 专用租户残留均为 0。

### 运行说明与待续审计

- 首次目标命令使用了缺少本地账号的临时数据库 URL，在连接门禁处被拒绝且未执行测试逻辑；随后从本地 API 环境仅在进程内派生 CI 库地址，不输出凭证，修复前后证据和全量测试均成功。
- 送达事务中的订货单状态更新仍需在下一轮单独审计并发竞争返回值；本轮保持小批次边界，不连带改变其他状态转换。
- 补送、已确认入库单更正、报损后供应商物理库存口径和在线盘点流程仍保持待确认。

## 2026-07-20 06:29 第五十四轮（配送送达审计原子性）

### 检查范围与修改

- 继续审计供应商送达：配送单、订货单和配送事件原在一个事务中提交，但“供应商标记送达”操作日志在事务提交后单独写入。日志写入失败时接口返回 500，实际状态却已变为待收货，重试又会因状态不符失败，形成无审计且不可重试的半成功。
- 送达操作日志现与配送单 `SHIPPED → DELIVERED`、订货单 `DELIVERING → PENDING_CONFIRM` 和配送事件处于同一事务；日志故障会回滚全部事实，清除故障后原请求可安全重试。
- 同时检查订货单条件更新计数，异常状态竞争返回 409 并回滚已抢占的配送单；24 小时自动收货时间以本次统一的 `deliveredAt` 计算。通知仍只在事务成功后发出。

### 自动化与端到端验证

- 新增 `op_logs` 故障注入：修复前稳定得到接口 500，但订货单已为 `PENDING_CONFIRM`、配送单已为 `DELIVERED` 且已有配送事件；修复后同一 500 会保持 `DELIVERING/SHIPPED`，配送事件和送达日志均为 0，移除故障后重试 200 且各事实恰好一份。
- 独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 54/54、API 单元测试 106/106、API TypeScript build、Web 测试 13/13 和 Web `tsc --noEmit` 全部通过。
- 标准入口重启 4444 后，`dianjie_v4_local` 真实 HTTP 配送脚本通过送达日志失败回滚与清障重试，并继续通过厨师验收并发、订货/配送查询、快照不可变、分批实发和并发收货；结果明确包含 `deliveryAuditRollback: true` 与 `chefAckDeliveryRace: true`。
- 本地 `local_delivery_log_*`/`local_chef_ack_*` 与 CI `test_delivery_log_*`/`test_chef_ack_*` 触发器、函数均为 0；本地专用商品/订货单和 CI 专用租户残留均为 0。

### 风险与待确认

- 本轮只收敛既有送达事实和技术审计，不改变厨师验收是否强制、24 小时自动收货、通知对象或配送单业务语义。
- 补送、已确认入库单更正、报损后供应商物理库存口径和在线盘点流程仍保持待确认。

## 2026-07-20 06:36 第五十五轮（确认发货库存与审计原子性）

### 检查范围与修改

- 继续审计供应商确认发货：订货单状态、独立配送单、实发数量、供应商商品/批次库存、库存流水和预占释放原已在一个事务中提交，但“供应商确认发货”操作日志在事务提交后单独写入。日志写入失败时接口返回 500，库存和配送事实却已落库；重试会命中已有配送单，无法补回缺失审计。
- 确认发货操作日志现与订货单状态、配送单及明细、商品/批次库存扣减、批次分配、库存流水和预占释放处于同一事务；日志故障会回滚全部事实，清除故障后原请求可正常发货。
- 调整数量说明和金额审计内容保持原样；通知仍只在事务成功后发出。本轮不合并订货单与配送单，也不修改实发数量、库存分配或价格口径。

### 自动化与端到端验证

- 新增 `op_logs` 故障注入：修复前稳定得到接口 500，但订货单已为 `DELIVERING`、配送单已生成、商品与批次库存已扣减且预占已释放；修复后同一 500 会保持 `CONFIRMED`，配送单、出库流水和发货日志均为 0，商品及批次库存保持原值，随后正常发货成功。
- 独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 54/54、API 单元测试 106/106、API TypeScript build、Web 测试 13/13 和 Web `tsc --noEmit` 全部通过。
- 标准入口重启 4444 后，`dianjie_v4_local` 真实 HTTP 配送脚本通过确认发货日志失败回滚，并继续通过送达日志回滚、厨师验收并发、订货/配送查询、快照不可变、分批实发和并发收货；结果明确包含 `shipmentAuditRollback: true`、`deliveryAuditRollback: true` 与 `chefAckDeliveryRace: true`。
- 本地与 CI 库的 `local/test_shipment_log_*`、`local/test_delivery_log_*`、`local/test_chef_ack_*` 触发器和函数均为 0；本地专用商品及 CI 专用租户残留均为 0。一次只读残留审计因 `tsx -e` 的 CJS top-level await 限制在查询前退出，改用异步 IIFE 后完成零残留核验。

### 风险与待确认

- 本轮只收敛既有确认发货事实和技术审计，不改变配送单独立性、供应商直送门店、分批发货或库存批次顺序。
- 补送、已确认入库单更正、报损后供应商物理库存口径和在线盘点流程仍保持待确认。

## 2026-07-20 06:42 第五十六轮（确认发货幂等内容一致性）

### 检查范围与修改

- 先复核供应商接单和拒单，确认状态、库存预占/释放、事件和操作日志已经位于同一事务，没有重复修改。随后按重复提交优先项审计确认发货：原接口只凭 `purchaseOrderId + idempotencyKey` 返回已有配送单，同一个键换用不同备注或实发数量也会静默返回 200，客户端会把未执行的新请求误认为成功。
- 现将规范化后的发货请求指纹随配送单 `CREATED` 事件在原发货事务内保存；命中已有配送单时先比对指纹，内容不一致返回 409，完全一致的网络重放仍返回原配送单及 `duplicated: true`。
- 指纹将备注、是否显式提交明细以及按商品行稳定排序后的实发数量纳入比较；历史配送事件没有指纹时按配送备注和已有明细执行兼容校验，不新增数据库字段或迁移。

### 自动化与端到端验证

- 新回归修复前稳定复现同一幂等键改备注并把实发 6 改为 5 仍返回 200；修复后相同请求重放为 200/同一配送单，不同内容为 409，配送单保持 1 张、商品及批次库存只扣一次、出库流水保持 1 条。
- 独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 54/54、API 单元测试 106/106、API TypeScript build、Web 测试 13/13 和 Web `tsc --noEmit` 全部通过。
- 标准入口重启 4444 后，`dianjie_v4_local` 真实 HTTP 配送脚本确认同键同内容返回原配送单、同键不同数量/备注返回 409，库存仍为 98 且配送单仍为 1；随后送达、收货和第二次配送继续通过，结果明确包含 `shipmentReplayConflict: true`。
- 本地和 CI 库的临时触发器/函数、本地专用配送商品及 CI 专用租户残留均为 0；`git diff --check` 通过。

### 风险与待确认

- 本轮只收紧幂等键的技术契约，不改变合法实发上限、分批配送、补送语义或订货单与配送单的独立性。
- 补送的业务审批/通知口径、已确认入库单更正、报损后供应商物理库存口径和在线盘点流程仍保持待确认。

## 2026-07-20 06:48 第五十七轮（并发确认发货幂等收敛）

### 检查范围与修改

- 继续验证重复提交的并发边界：两个完全相同的确认发货请求同时到达时，原实现可能都在配送单生成前通过首次幂等查询；首个请求完成状态抢占后，第二个请求返回 409，而不是返回首个请求生成的配送单。
- 发货主事务现按租户、订货单和幂等键获取 PostgreSQL 事务级 advisory lock，并在状态抢占、配送单和库存写入前二次查询已有配送单。相同键的并发请求串行后复用同一配送单；不同键仍由订货单状态和 `rowVersion` 竞争保护。
- 二次命中继续复用上一轮请求指纹校验：同键同内容返回 200/`duplicated: true`，同键不同内容返回 409。重放分支在通知之前返回，不会重复发送发货通知。

### 自动化与端到端验证

- 新增独立商品、批次和订货单的并发回归；修复前稳定得到 `[200, 409]`，修复后两个请求均为 200、配送单 ID 相同且恰有一个标记重放，配送单/出库流水各 1，商品库存只从 3 扣到 1。
- 独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试由 54 增至 55 项并 55/55 通过；API 单元测试 106/106、API TypeScript build、Web 测试 13/13 和 Web `tsc --noEmit` 全部通过。新增夹具后将旧的租户级流水断言收紧为主商品范围，避免两个独立用例互相计数。
- 首次 API build 发现历史配送明细的 `purchaseOrderItemId` 在模型中可为空；兼容匹配逻辑本就将空关联视为不匹配，类型声明对齐数据库可空性后 build 重跑通过。
- 标准入口重启 4444 后，`dianjie_v4_local` 真实 HTTP 配送脚本同时发出两个首批发货请求，均返回 200/同一配送单，随后同键冲突、送达、收货和第二次配送继续通过；结果明确包含 `shipmentConcurrentReplay: true` 与 `shipmentReplayConflict: true`。
- 本地和 CI 的专用商品、订货单、测试租户及临时触发器/函数残留均为 0；`git diff --check` 通过。

### 风险与待确认

- advisory lock 仅串行化同一订货单的同一幂等键，不阻塞其他订单或不同幂等键；未改变配送状态机、库存批次顺序或补送业务语义。
- 补送的业务审批/通知口径、已确认入库单更正、报损后供应商物理库存口径和在线盘点流程仍保持待确认。

## 2026-07-20 06:52 第五十八轮（订货创建幂等内容一致性）

### 检查范围与修改

- 审计订货单首次创建的重复提交：原内存缓存命中、数据库已存在查询和唯一约束竞争补偿都只按租户、创建人和幂等键返回原订货单；同一个键改门店、供应商、日期、备注、商品或数量仍会静默得到 200，客户端会误认为新请求已执行。
- 现将规范化创建请求指纹随订货单 `CREATED` 事件在原创建事务内保存；三条重放路径统一先校验指纹，内容一致返回原订货单，内容不同返回 409。
- 指纹包含鉴权后实际门店、供应商、到货日期、备注以及按商品稳定排序后的数量。客户端单价不参与比较，因为该字段原本就被服务端忽略并以商品主数据价格为权威；商品行顺序不同但语义相同仍视为同一请求。
- 历史订货事件没有指纹时优先使用首次提交快照进行兼容比较，避免后来获批改单影响原请求判断；没有快照的更早数据再回退到原始行字段。

### 自动化与端到端验证

- 新回归修复前稳定复现同一幂等键将订货数量 1 改为 2 仍返回 200；修复后相同请求返回原订货单 ID，不同数量返回 409，数据库始终只有一张该键订货单。
- 独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试由 55 增至 56 项并 56/56 通过；API 单元测试 106/106、API TypeScript build、Web 测试 13/13 和 Web `tsc --noEmit` 全部通过。
- 标准入口重启 4444 后，`dianjie_v4_local` 真实 HTTP 配送脚本通过订货创建同键同内容/冲突内容校验，并继续通过发货审计回滚、并发同键发货、发货冲突、送达、收货和第二次配送；结果明确包含 `orderCreateReplayConflict: true`。
- 本地和 CI 的专用商品、订货单、测试租户及临时触发器/函数残留均为 0；`git diff --check` 通过。

### 风险与待确认

- 本轮只收紧首次创建的幂等契约，不改变服务端权威价格、首次快照、改单流程或订货单与配送单独立性。
- 补送的业务审批/通知口径、已确认入库单更正、报损后供应商物理库存口径和在线盘点流程仍保持待确认。

## 2026-07-20 06:58 第五十九轮（订货创建与审计原子性）

### 检查范围与修改

- 继续审计订货单首次创建：订货单、明细、首次提交快照及 `CREATED`/`SUBMITTED` 事件原已位于同一可串行化事务，但“创建采购订单”操作日志在事务提交后单独写入。日志失败时接口返回 500，前述业务事实却已落库；客户端以原幂等键重试只会得到已存在订货单，缺失审计无法补回。
- 将原有操作日志按原文案移入创建主事务。日志故障现在会连同流水号分配、订货单、明细、首次快照和事件一起回滚；清除故障后原请求可安全重试。
- 缓存失效和通知仍在事务成功后执行，避免扩大数据库事务时间。本轮不改变门店/供应商范围、服务端权威价格、首次快照或改单业务规则。

### 自动化与端到端验证

- 新增 `op_logs` 故障注入。修复前稳定得到接口 500 但订货单已存在；修复后同一故障返回 500 且该幂等键订货单和创建日志均为 0，真实 4444 HTTP 流程结果明确包含 `orderCreateAuditRollback: true`，清障后的正常创建、同键重放和冲突请求继续通过。
- 独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 56/56、API 单元测试 106/106、API TypeScript build、Web 测试 13/13 和 Web `tsc --noEmit` 全部通过。
- 真实流程同时继续通过发货日志回滚、并发同键发货、发货冲突、送达日志回滚、厨师验收并发、并发双收货、订货/配送查询和首次快照不可变。
- 本地与 CI 库的 `local/test_*` 临时触发器和函数均为 0；本地专用商品、订货单以及 CI `supply-flow-*` 租户残留均为 0。

### 风险与待确认

- 本轮只收敛创建业务事实和技术审计的一致性，不改变订货单与配送单独立性、供应商直送门店或通知口径。
- 补送的业务审批/通知口径、已确认入库单更正、报损后供应商物理库存口径和在线盘点流程仍保持待确认。

## 2026-07-20 07:03 第六十轮（并发订货创建幂等收敛）

### 检查范围与修改

- 继续验证首次创建的并发重复提交：两个完全相同且带同一幂等键的请求同时到达时，原实现由数据库保证最终只有一张订货单，但可串行化事务的竞争失败 `P2034` 未进入幂等补偿，调用方会收到 `[500, 200]`。
- 创建事务的异常补偿现同时处理唯一约束竞争 `P2002` 和可串行化写冲突 `P2034`：按租户、创建人和幂等键回查已提交订货单，并复用上一轮请求指纹校验。同键同内容返回原单；同键不同内容仍返回 409；若没有可回放订货单则保留原异常，不掩盖其他事务故障。
- 补偿分支在通知和缓存之前返回，不会重复生成创建事件、操作日志或下单通知。本轮不降低事务隔离级别，也不增加跨订单锁。

### 自动化与端到端验证

- 新回归修复前稳定得到 `[500, 200]`；修复后两个并发请求均为 200 且订货单 ID 相同，数据库只有一张该键订货单、两个首次状态事件和一条创建操作日志。
- 独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 56/56、API 单元测试 106/106、API TypeScript build、Web 测试 13/13 和 Web `tsc --noEmit` 全部通过。
- 重启标准 4444 入口后，`dianjie_v4_local` 真实 HTTP 流程明确包含 `orderCreateConcurrentReplay: true`，并继续通过创建审计回滚、冲突重放、发货/送达日志回滚、并发发货、厨师验收并发和并发双收货。
- 本地与 CI 库的临时触发器/函数、本地专用商品和订货单、CI `supply-flow-*` 租户残留均为 0。

### 风险与待确认

- 本轮只修复并发幂等的技术返回，不改变订货内容、首次快照、改单确认或通知对象。
- 补送的业务审批/通知口径、已确认入库单更正、报损后供应商物理库存口径和在线盘点流程仍保持待确认。

## 2026-07-20 07:08 第六十一轮（改单请求幂等与审计身份）

### 检查范围与修改

- 对齐“改单必须门店确认、记录申请人/处理人/时间”继续审计改单申请：原 `requestKey` 命中后不校验申请人和请求内容，同一键改数量或由另一申请人复用会直接返回原改单；两个完全相同的并发申请则返回 `[201, 409]`，网络重放被表现为业务冲突。
- 现以申请人、原因、基础行版本和规范化后的目标快照共同校验重放内容；同键同申请人同内容返回原改单，同键内容或申请人不一致返回 409。商品行以稳定排序后的目标快照比较，顺序差异不会制造伪冲突。
- 可串行化事务的唯一约束 `P2002` 与写冲突 `P2034` 均会在失败后回查该键改单并执行相同校验；找不到可回放记录时保留原错误。改单、`REVISION_REQUESTED` 事件和申请操作日志仍在原事务中只写一次，通知只由真正创建的一方发送。

### 自动化与端到端验证

- 新回归修复前稳定得到并发 `[201, 409]`；修复后为 `[201, 200]` 且改单 ID 相同，改单、申请事件和操作日志各一条，同键改数量返回 409。随后由门店驳回，并断言 `requestedById/requestedAt` 与 `reviewedById/reviewedAt` 均完整。
- 独立 `dianjie_v4_ci` 全量 PostgreSQL 集成测试 56/56、API 单元测试 106/106、API TypeScript build、Web 测试 13/13 和 Web `tsc --noEmit` 全部通过。
- 重启标准 4444 入口后，真实 HTTP 流程明确包含 `revisionConcurrentReplay: true`、`revisionReplayConflict: true` 与 `revisionActorsRecorded: true`，并继续通过创建、发货、送达和收货全链路回归。
- 本地和 CI 的临时触发器/函数、本地专用商品、订货单、改单以及 CI `supply-flow-*` 租户残留均为 0。

### 风险与待确认

- 本轮不改变改单需门店确认、供应商只能调整商品/数量、批准后订单行替换或驳回后沿用原单的既定规则。
- 补送的业务审批/通知口径、已确认入库单更正、报损后供应商物理库存口径和在线盘点流程仍保持待确认。

## 2026-07-24 13:28 生产安全收敛批次（瑶海 7 月 22 日盘点导入门禁）

### 检查范围与修改

- 在隔离分支 `stabilize/20260724-production-safety` 审计 7 月 22 日闭店盘点、生产只读导出的 7 月 13 日基线和当前商品主数据；未上传盘点、未部署、未连接或写入生产。
- 新增库存快照导入计划器：按当前 `inventoryUnit` 合同归一化，只允许当前租户的存续商品；同模板、同商品且库存单位未变化时才沿用上一基线冻结系数。
- 导入提交增加零缺口硬门禁、精确租户/门店定位、显式确认口令和绑定源文件哈希的人工复核清单；未匹配、歧义、待换算、错误或未命中的复核配置均阻止写入，替换删除仍在完整门禁之后和同一事务内执行。
- 修复真实工作簿“合计金额位于标签上一行”和单位富文本解析；离线模式不再依赖数据库环境变量。
- 增加计划器单元测试和工作簿到 PostgreSQL 的集成测试，覆盖当前库存单位、上一基线冻结系数、库存单位变化、停用商品、跨租户同名商品、富文本单元格、人工复核依据和失败替换不删除原快照。

### 7 月 22 日 dry-run

- 原文件 SHA-256 为 `acb0b065b91b33a8d01fba8c71d3444bd39a01ae143fbe349d2d52e1444939e5`；167 行、127 行非零、40 行为零、金额 `¥27,605.50`，明细与表尾勾稽。
- 生产只读导出离线计划得到 137 行匹配，其中 122 行可安全沿用；30 行需重新绑定存续商品，15 行需确认换算，共 45 行阻止提交，`canCommit=false`。
- 12 个非零阻塞行中，8 行需确认存续商品映射，4 行需确认换算：米布丁、腐乳酱、李锦记生抽、牛肉酱。其余 33 行虽为零库存仍不允许静默跳过。
- `dianjie_v4_local` 缺少生产的 7 月 13 日基线，直接匹配结果不具生产代表性；本轮仅在 `dianjie_v4_ci` 写入并清理测试夹具。

### 自动化验证

- API 单元测试 152/152、PostgreSQL 集成测试 93/93、API TypeScript build 通过。
- Web 测试 22/22、Web `tsc --noEmit` 通过；本轮无 Web 修改，无需启动或构建 Web。
- 真实 7 月 22 日工作簿在无 `DATABASE_URL` 环境完成离线解析；导入脚本集成测试确认失败替换不会删除既有快照。
- `git diff --check` 通过；未提交业务工作簿、生产只读导出或临时分析文件。

### 风险与待确认

- 当前导入门禁是“安全阻止”状态，不代表 7 月 22 日基线可写入；业务和采购确认 45 行后还需重新 dry-run 到 `canCommit=true`。
- 胡萝卜汁、清远鸡、BOM 用量、赠品/员工餐和零库存模板行的业务口径仍未擅自决定。

## 2026-07-25 12:54 统一发布候选第一轮（基线对账与上传对象后缀）

### 检查范围与修改

- 从生产安全收敛 `b525807` 创建隔离分支 `integration/20260725-unified-rc`；未修改原
  release worktree，未连接或写入生产。
- 对账 production `a6d64c9`、main `f5a4192`、安全线 `b525807` 与旧 release
  `8467043`。旧 release 有 56 条迁移，其他三个基线有 63 条，因此统一候选以安全线为底。
- Kimi 完成生产变更台账，Qwen 完成 77 个旧 release 提交的独立分类；Codex 以本地 Git
  复核为 9 个 patch-equivalent、8 个代码补丁可干净应用、7 个仅文档、53 个需手工对照。
- 选择 release `0058082` 作为首个独立低风险批次：上传对象扩展名改由白名单 MIME 决定，
  不再信任用户文件名；保留安全线的库存盘点上传类别和租户对象路径隔离。
- 新增 11 个上传后缀单元用例。

### 自动化与端到端验证

- API 全量单元测试 163/163、API TypeScript 编译、Web `tsc --noEmit` 通过。
- 独立本地 PostgreSQL `dianjie_v4_ci` 的 63 条迁移状态为 up to date。
- 供应商租户隔离、查询分页与并发边界集成测试 26/26 通过。
- 完整基线、分类、生产 UNKNOWN 和脚本门禁记录见 `UNIFIED_RC_REVIEW_2026-07-25.md`。

### 风险与待确认

- Kimi 标记的生产脚本执行情况只来自 Git/部署记录，无法替代生产只读事实核验；
  `notify-chef-confirm-0724.ts` 明确保留为 UNKNOWN，禁止自动运行。
- Qwen 的分类是第二意见；只有 Codex 本地 Git 复核并通过本批测试后才允许移植。
- 补送、报损收货、已确认入库单更正和盘点流程仍保持业务阻塞，不擅自决定。
- 生产门禁保持 LOCKED；不推 main、不部署生产。

### 独立交叉复核

- Qwen 在禁用扩展/MCP 的安全模式下读取完整提交 diff，结论为 PASS，确认 MIME 映射、
  租户对象路径和生产门禁无阻塞问题。
- 接受其报告措辞意见：现明确 11 个测试实际覆盖 10 个允许 MIME 与 1 个未知 MIME
  回退，不再声称测试直接覆盖文件名大小写。
- Qwen 把 10 个映射行误数为 11 个并据此声称共有 12 个用例；Codex 逐项复核后驳回，
  实际 Vitest 结果和源文件均为 11 个。
- 上传路由最终对象 key 的 OSS mock 集成断言作为非阻塞后续增强；本轮代码和现有
  PostgreSQL 隔离测试结论不变。

## 2026-07-25 13:50 环境基线对齐（生产只读、本地写入）

### 检查范围与修改

- 用户授权连接生产后，仅通过 SSH/数据库执行只读事实采集；未部署、未重启服务、未修改
  生产文件、配置、迁移账本或业务数据。
- 确认生产实际 deployed commit 为 `a6d64c9`，Node 20.20.1、pnpm 10.32.1、
  Prisma 5.22.0、PostgreSQL 15.16；API、Web、CMB 在线且健康检查通过。
- 生产63条迁移全部完成、0失败、schema diff 为零。发现一条历史 migration checksum
  与当前文件不同，但生产字段和 datamodel 一致；保留审计，不修改或重跑旧 migration。
- 生产只读数据库证据确认 7月21/24 三个手工通知 eventKey 均各 sent 1，BOM单位纠错
  操作日志 1 条，7月22消耗作废/补记各30行；相关一次性脚本全部标为已执行、不得重跑。
- 本地既有 `dianjie_v4_local` 原缺最后一条迁移；先生成并验证权限600的 custom-format
  备份，再通过 `migrate deploy` 补齐。对齐后本地、CI和临时空库均为63/63和零 diff。
- 固化 Node 20.x、pnpm 10.32.1、Prisma 5.22.0；新增运行时契约检查，补环境变量模板和
  README，移除模板注释中的测试银行标识。

### 自动化验证

- Node 20.20.2 / pnpm 10.32.1 下 `pnpm env:check`、冻结 lockfile 离线安装通过。
- API 单元 163/163、API build、Web 22/22、Web tsc、Web production build 142页通过。
- PostgreSQL 全量集成 93/93；本地既有库、CI库、临时空库 migrate/status/diff 全部通过，
  临时迁移库残留为0。
- 完整事实、配置开关、脚本执行证据、备份和剩余风险见
  `ENVIRONMENT_BASELINE_2026-07-25.md`。

### 风险与后续

- 生产仍运行 `a6d64c9`，统一 RC 不因此自动获准部署；生产 gate 继续 LOCKED。
- 下一次生产变更前必须生成新备份；7月23备份只证明最近一次部署备份可读取。
- 迁移 checksum 差异不得通过改旧 migration、resolve 或 db push 掩盖。
- Web 日志存在旧客户端请求旧 Server Action 的记录，但当前 HTML/CSS 和服务健康；继续监控。

## 2026-07-25 14:12 三 AI 第一条功能集成列车

### 检查范围与修改

- Kimi 审计供应商订货单与配送记录查询，确认日期范围、商品名称/编码、服务端分页、
  tenant/supplier 隔离和移动端双视图均已存在；未重复改运行时代码，新增 8 个
  PostgreSQL API 回归，覆盖上述契约及商品改名后的配送快照搜索。
- Qwen 回归商品、分类、批量操作、图片和审计能力，发现门店厨师/总厨下单页虽可搜索
  商品编码但商品卡片与已选行未显示编码；两页补充 `#商品编码`，并新增 10 个商品筛选
  schema 用例。Codex 集成时仅修正一个测试标题措辞，不改变运行逻辑。
- Codex 复核收货主链路，确认入库单、短量报损、配送/采购状态、实收数量和库存单位快照
  已处于同一事务；补齐 `P2002/P2034` 唯一约束竞争的幂等回查，避免已成功的重复收货
  被返回为 500。
- 新增故障注入集成回归：报损单插入失败时，入库单、报损单、配送状态、采购状态、
  实收数量、事件和操作日志全部回滚；清除故障后同请求可成功重试。

### 自动化与端到端验证

- Node 20.20.2 / pnpm 10.32.1 运行时契约和冻结 lockfile 离线安装通过。
- API 单元 29 文件、173/173；PostgreSQL 集成 17 文件、102/102；API TypeScript build
  通过。
- Web `tsc --noEmit`、6 文件 22/22 测试、正式生产构建 142 页通过；构建后以独立
  3230 端口恢复开发态，`.next/BUILD_ID` 已清除。
- 收货任务分支在独立 3220 dev 完成浏览器烟测：登录页正常渲染，390×844 下页面宽度
  与滚动宽度均为 390，浏览器 warning/error 为 0；原用户 3200 dev 的旧缺文件错误未动。
- `git diff --check`、新增行高置信敏感信息检查和三条远端任务分支 SHA 核对通过。

### 风险与后续

- 本列车没有改变补送、报损后的供应商物理库存、已确认入库单更正或盘点状态机口径。
- 商品编码页面修改已通过编译和构建；登录后真实下单数据的浏览器交互留给统一 RC 的
  认证 E2E 列车，不为此读取或伪造用户会话。
- 生产仍为 `a6d64c9`；本列车只进入 `integration/20260725-unified-rc`，不合并 main、
  不部署或写生产，发布门禁继续 LOCKED。

## 2026-07-25 14:48 三 AI 第二条功能集成列车

### 检查范围与修改

- Codex 把订货首次快照、改单门店批准、申请/处理人和时间、按实际发货数量形成配送单
  收进同一条 PostgreSQL 集成场景；断言批准改单后 `submittedSnapshot`、hash、
  `originalTotalAmount` 和原始行数量不变，供应商不能自批，配送金额和商品快照按实发冻结。
- Kimi 审计分类、批量导入、批量分类、停售审批、操作记录和移动端后，发现供应商可用
  “撤回上传批次”直接把已上架商品改为停用，绕过停售审批。现仅允许撤回待审批创建态；
  列表返回 `canRevoke`，前端隐藏不可撤回按钮。
- Codex 复核 Kimi 首版时发现状态检查位于事务外的 TOCTOU 竞态；最终在撤回事务内按
  商品 id 稳定排序执行 `FOR UPDATE`，再检查 `ENABLED` 状态和更新批次/商品，并用并发
  行锁回归证明审批先完成时撤回必须拒绝。
- Qwen 首版只给库存列表加 `limit`，会静默截断；经两次退回后改为真正的
  `page/pageSize/total/totalPages`，保留旧数组响应兼容，并补供应商/租户隔离、稳定顺序、
  越界参数和跨页不重叠测试。Codex 再补服务端名称/编码/规格多词搜索与分类筛选，避免
  前端分页后只搜索已加载 50 条；移动端提供“加载更多”并丢弃过期查询响应。
- Codex 另修旧采购订单页：原按钮以空 body 调用已要求撤回原因的新 API 且吞掉错误；
  现在仅在 `SUBMITTED/CONFIRMED` 对店长/管理员显示，提交必填且不超过 200 字的原因，
  并展示成功或失败反馈。

### 自动化与端到端验证

- Node 20.20.2、pnpm 10.32.1、Prisma 5.22.0、63 条迁移运行时契约通过；冻结 lockfile
  离线安装通过。
- API 单元 29 文件、173/173；独立 `dianjie_v4_ci` PostgreSQL 集成 19 文件、
  129/129；API TypeScript build 通过。
- Web `tsc --noEmit`、7 文件 24/24 测试、production build 142 页通过；构建后在独立
  3298 端口启动开发态并停止，`.next/BUILD_ID` 与临时监听均已清除。
- 专项回归包括订货快照/实际发货、库存分页/筛选、批次撤回审批行锁竞态和旧撤回表单；
  `git diff --check`、本列车高置信敏感信息检查和远端任务分支核对通过。

### 风险与后续

- 供应商库存兼容模式仍返回旧数组并以 2000 条为安全上限；当前 Web 主列表和单品详情均
  已使用分页接口。外部旧客户端应迁移到分页契约后再考虑移除兼容模式。
- 库存“低于警戒/断货”按钮仍对已加载页做即时筛选；列表按可用库存紧急度排序并可继续
  加载。本列车未改变库存口径或供应商库存启用规则。
- 补送、报损后的供应商物理库存、已确认入库单更正和盘点状态机继续等待业务确认。
- 生产仍为 `a6d64c9`；统一候选未合并 main、未部署或写生产，发布门禁继续 LOCKED。

## 2026-07-25 22:15 V5 内部供应链第三条功能集成列车

### 已集成

- `546591f`：商品目录按当前筛选导出 CSV；统一多词搜索语义、分类过滤、10,000 行上限、
  CSV 公式注入防护与 SUPPLIER_OWNER 精确权限。
- `116b6b3`：新增内部 `SUPPLY_CHAIN` 角色、租户内跨店订单/配送/收货/库存/纯消耗只读
  工作台；拒绝营业额、成本率、收货财务对象、供应商写页面和订单/收货/库存写操作。
- `4f7e893`：收货关键字、业务到货日期和后端分页；订单关键字同时匹配首次提交快照与当前
  商品主数据。`orders.ts` / `receipts.ts` 的重叠改动经人工逐行合并。
- `44ab080`：商品变更逐人系统消息与 exact `toUsers` 企微基础服务；稳定 dedupeKey、
  NotificationLog 失败证据和 fire-and-forget 边界。

### 门禁结果

- 导出 API 41/41、Web 14/14；权限 API 15 项、Web 9 项、PostgreSQL 36 项；查询组合
  API 33 项、PostgreSQL 45 项；通知单元 17 项、PostgreSQL 5 项。
- API build、Web tsc、两次 1440×900 PC 浏览器复核通过，控制台零错误。
- 新角色 schema 在全新空库完成 64 条 migrate deploy，status up to date，
  migrations-to-schema diff 为零；隔离临时数据库和 Web 缓存均已清理。
- 精准通知首个 runner 因指定测试文件缺失失败，证据保留；补齐无网络测试、tenant 操作者
  限定和禁止角色广播退化后才集成。

### 继续执行

- Codex `SC-PARTIAL-CLOSE-CODEX-003`：部分发货关闭未发余量并释放预占。
- Kimi `SC-PRODUCT-DIRECT-KIMI-005`：内部供应链五类商品维护直接生效并接精准通知。
- Qwen `SC-PRODUCT-PC-QWEN-002`：内部供应链专用商品 PC 管理页。
- 三项均从统一 RC `44ab080` 独立分支启动，尚未审查或集成；生产 gate 继续 LOCKED，
  不合并 main、不创建 PR、不部署或写生产。

## 2026-07-26 01:27 V5 内部供应链第四条功能集成列车

### 已集成

- `2dcf401`、`3c79335`：内部供应链商品新增、编辑、调价、停售、恢复全部直接生效，
  租户/供应商/分类边界收紧，外部供应商审批流程不变；事务提交后精准通知本租户总厨。
- `a4af97e`：内部供应链专用商品 PC 台，支持服务端筛选分页、新增编辑调价状态和商品
  图片上传/预览，仅 `SUPPLY_CHAIN` 可进入。
- `6272d99`：首次部分发货即关闭未发余量、释放对应预占、只按实发扣仓；并发与幂等均
  禁止第二张有效配送单，原下单人获知“不会补送”。
- `8d6c036`：旧发货系统消息由租户级 `MANAGER` 广播改为原下单人 exact message，
  带关联订单和稳定去重键，门店企微触达仍走精准解析通道。

### 门禁结果

- API 单元 281/281、全新空库 64 条迁移后的 PostgreSQL 集成 138/138、API build 通过。
- Web 102/102、Web tsc、隔离缓存 production build 146 页通过；未终止或污染用户
  3200 dev。
- 1440×900 浏览器验证筛选、组合条件、两页分页和真实调价直接生效；页面无横向溢出，
  控制台零 warning/error。
- 三条 feature 精确 SHA、干净工作区、完整 diff、`git diff --check` 和高置信敏感信息
  检查通过；runner 原退出 2 的失败证据保留，不把 runner 状态伪装成成功。

### 继续执行

- Codex：内部商品批量分类/状态直接生效与精准通知 API。
- Kimi：内部商品 PC 当前筛选导出和批量操作。
- Qwen：部分发货页面对齐“余量关闭、不补送”状态机。
- 生产 gate 保持 LOCKED；不合并 main、不创建 PR、不部署或写生产。

## 2026-07-26 02:18 V5 内部供应链第五条功能集成列车

### 已集成

- `9510cbb`：内部商品批量分类/停售/恢复直接生效，逐商品乐观更新、审计与精准总厨通知；
  无实质变化不通知，外部供应商审批语义保持不变。
- `5dc5908`、`393065d`：PC 当前筛选 CSV、跨页选择、批量分类/状态；分类候选限定所选
  tenant 内供应商的启用分类。
- `4a5f2da`：供应商发货页零数量禁用，确认前展示将永久关闭的当前剩余量，成功后明确
  未发余量不会补送。

### 门禁与浏览器结果

- API 单元 291/291、全新空库 64 条迁移后的 PostgreSQL 集成 138/138、API build 通过。
- Web 152/152、Web tsc、隔离缓存 production build 146 页通过。
- 真实 PC 浏览器选中供应商 A 跨两页 25 个商品并批量改为“叶菜”；数据库确认 A 的
  25 行全部更新、B 的 3 行未变、总厨精确通知 25 条。
- 真实部分发货把 10/6 实发为 7/6，确认关闭 3 斤；订单/配送状态与 65 元实发金额正确，
  原下单人收到普通发货与余量关闭两条系统消息。
- 本地服务首次被执行环境终止只造成 502，数据库仍为发货前状态；隔离服务重启后成功。
  临时数据库、Next 缓存、seed、测试服务和浏览器标签全部清理。
- runner 失败/不可信完成证据均保留；总管修正剩余量计算、成功/错误状态和供应商分类
  范围后，才以精确 SHA 集成。生产 gate 保持 LOCKED，不触碰 main/PR/生产。

## 2026-07-26 04:35 V5 第六至第八条功能集成列车

### 已集成

- `7d00c25`、`67a09dc`：内部供应链收货、订单与配送只读 PC 工作台，服务端筛选分页、
  详情和角色只读边界完整；`ae8535e` 补门店下单商品图片及失败回退。
- `d7ec5c`、`ad0751e`、`d4fc50f`、`7584918`：商品采购/库存/订货/成本四单位主数据，
  API 与 PC 因子校验一致；`93a3b6e` 保留旧代码直写兼容。
- `bed1b9a`、`72610a0`：只读 PC 页面回归，覆盖筛选参数、权限和快速请求陈旧响应。
- `54fe120`、`0797457`：数据库约束和 15 个真实 PostgreSQL 合同用例；缺因子的旧库存
  单位映射保持待核验，不默认成 1:1。
- `584c8ef`、`1dba11b`：订单、配送、收货逐行冻结四单位、换算因子及核验状态并提供
  手工 rollback；商品后续改变不会污染历史换算。

### 最终门禁

- API 单元 309/309、PostgreSQL 集成 153/153、API build 通过。
- Web 322/322、Web tsc 通过。
- 全新空库应用 66 条迁移、status up to date、migrations-to-schema diff 为零；三类
  旧商品回填样本精确符合“无映射才 1:1、明确因子沿用、缺因子保持待核验”。
- 三个 runner 阻塞批次经总管修复和逐项验收后转为 DONE；精确 feature SHA、远端分支、
  工作树、diff 与敏感信息检查通过。

### 风险与后续

- `costUnit` 已定义为采购成本计价单位，但现有 `Product.price`/订单 `unitPrice` 仍按
  订货单位数量直接计价；非 1:1 单位金额换算需独立修复和数据库/E2E 回归。
- 默认仓、多仓接口预留和入库/收货运行时更正仍待实施。生产 gate 保持 LOCKED，不合并
  main、不创建 PR、不部署、不写生产、不执行一次性生产脚本。

## 2026-07-26 05:16 V5 第九条功能集成列车

### 已集成

- `29047cd0`：供应商库存接口建立固定默认仓合同；省略或传 `default` 等价，未知仓在
  DB 前拒绝，响应带稳定默认仓元数据。
- `4ec73c94`：订单按成本单位单价和四单位因子折成订货单位单价；新订单/新增改单行使用
  Decimal 权威换算，历史行继续使用冻结金额。
- `fc49792a`、`cf4e36fd`：内部商品 PC 的列表、表单、调价确认统一使用成本单位口径，
  只读展示订货单位折算价，持久化 PENDING 合同显示待核验。
- `111c0060`：供应商库存汇总和批量商品影响预览以订货单位估值；待核验 SKU 排除并
  返回明确计数，不再把成本单位单价直接乘订货单位库存。
- `d4e56ad4`：库存列表、详情、流水、入库、调整、报损和导入 PC 显式传
  `warehouseId=default` 并展示“默认仓”。
- `c40002f0`：修复新增 Decimal helper 后 4 个商品路由 mock 丢失 Prisma 导出的测试
  收集问题。

### 门禁与环境证据

- API 单元 341/341、PostgreSQL 集成 153/153、API build 通过；Web 367/367、Web tsc
  通过。
- 1440×900 浏览器确认动态成本单位价格标签、默认仓标题、无横向溢出和控制台零错误；
  dev 日志确认库存列表/summary 请求携带 `warehouseId=default`。
- 浏览器时隔离数据库服务已经停止，数据请求返回 502，因此浏览器只计为 UI/URL smoke；
  数据正确性与并发隔离证据来自已通过的全量 PostgreSQL 集成。
- production Web build 因磁盘 `ENOSPC` 中止；只清理统一 RC 自身失败产物，保留用户
  3200 服务和其他工作区不动。

### 继续执行

- Codex `SC-ORDER-PRICE-PC-CODEX-016`：门店下单预览按成本单位折成订货单位金额。
- Qwen `SC-COST-EXPORT-QWEN-017`：商品 CSV 输出成本单位价格与完整四单位合同。
- Kimi `SC-STOCK-VALUE-PC-KIMI-018`：库存 PC 明示待核验 SKU 未计入货值。
- 生产 gate 保持 LOCKED，不合并 main、不建 PR、不部署或写生产。

## 2026-07-26 05:38 V5 第十条功能集成列车

### 已集成

- `c76f02db`（feature `f15a3f7e`）：库存列表使用后端订货单位折算价，PENDING 行和汇总
  明示暂未计入货值，分类可用货值不再回退到成本单位 `price`。
- `85c05a09`（feature `440d8bec`）：商品 CSV 增加四单位、三因子、成本单位价、订货
  折算价和换算状态；待核验/非法合同留空，legacy 1:1 明示兼容。
- `2b1e7444`（feature `b654d208`）：旧 PC 下单页按成本单位价格折成订货单位只读价，
  非 1:1 PENDING 禁止提交，API 继续权威重算。

### 验收

- 三个 runner 均受 ENOSPC 或完成上报失败影响；没有直接接入 runner 草稿。总管剥离
  Qwen 的近四千行行尾噪声和 451 MiB 未跟踪 store、清理 Kimi 的安装噪声，重跑专项、
  build/tsc、核对父提交和远端精确 SHA 后才逐项集成。
- 组合门禁：API 346/346、API build、Web 396/396、Web tsc、`git diff --check`。
  本列无 schema/数据库写路径变化；PostgreSQL 153/153 沿用第九列同一候选证据。
- 当前明确未完成：两个 V2 门店采购新建页和供应商订单追加商品预览仍把成本单位价当
  订货单位价；库存详情/analytics 的部分价格标签仍含混；真实 Warehouse/按仓账本及
  已确认入库/收货更正单仍待开发。
- production Web build 继续受工作盘 ENOSPC 阻断；生产 gate 保持 LOCKED，不合并
  main、不建 PR、不部署、不写生产、不运行真实数据修正脚本。

## 2026-07-26 06:37 V5 第十一条功能集成列车

### 已集成

- `7708689c`（feature `39f9f254`）：两个 V2 采购新建页按四单位把成本单位价格折为
  订货单位价格与行金额，非 1:1 PENDING 和非法数量不能提交。
- `44fad266`（feature `f04ead54`）：供应商改单新增行使用当前折算价格；既有订单行继续
  使用冻结单价与冻结单位，待核验新增行不显示虚假合计。
- `4e03f24a`（feature `6b197c6b`）：库存详情与滞销 SKU 明确订货单位价格/货值待核验；
  热销排行继续按历史收货快照金额，不被当前商品调价污染。

### 门禁与 PC 复核

- API 单元 346/346、API build；Web 426/426、Web tsc、独立 production build
  149 路由、`git diff --check` 和高置信敏感信息检查通过。
- 新增洞察 PostgreSQL 集成用例因本机没有 `_test/_ci` 数据库且 Docker/PostgreSQL
  未运行，被数据库安全门禁拒绝；未绕过门禁，留作下一次隔离库补跑。
- 1440×900 浏览器覆盖登录、V2 下单、供应商分析、改单与库存详情；页面加载且控制台
  无前端错误。因本地 API 未启动只记为 UI/路由 smoke，不宣称真实数据 E2E。
- 三个 runner 原本都因 Node 25 与仓库 Node 20 约束不兼容而 FAILED；源码由总管修复、
  提交并验收。控制面 `20b2d52` 已把 AI 与可信检查统一固定到 Node 20，20/20 测试通过。
- 生产 gate 保持 LOCKED；不合并 main、不建 PR、不部署、不写生产、不运行生产脚本。

### 下一步

- 在隔离 `_test/_ci` PostgreSQL 补跑洞察真实数据库用例。
- 继续真实 Warehouse/按仓账本，以及已确认入库/收货更正单的反向库存、差异与应付状态机。

## 2026-07-26 06:57 V5 第十二条功能集成列车

### 已集成

- `be2e7884`（feature `b01bf583`）：12 个供应商洞察无数据库 Fastify 路由测试，覆盖
  四单位折算/PENDING、历史收货快照金额、状态/日期、管理员显式 supplier、认证
  tenant/supplier scope 和 query 越权阻断。
- runner 未形成提交而 FAILED；总管补真实 Prisma where 与不读取当前 `price` 的断言，
  12/12、API 全量 358/358、API build 通过后才集成。

### 未集成

- Kimi `1d7c548` 把供应链仓入库更正与门店收货更正错误耦合，并包含未确认角色权限及
  禁用的 `db push` 回滚建议；任务合同也因错误路径失败，提交保留但不接入。
- Codex 仓库草稿按 supplier 创建多个默认仓，违背 tenant 当前单默认仓规则，且未解决
  `Product.stock` 与新仓余额兼容期漂移；总管取消 run，脏草稿保留、不提交、不集成。

### 下一步

- 仓库改为 tenant 级唯一默认仓，先设计兼容期双写/核对，再做 schema 与隔离空库复验。
- 手工入库更正与门店收货更正拆成两条状态机；门店实收修正不能自动恢复供应链仓库存。
- 生产 gate 继续 LOCKED，不合并 main、不建 PR、不部署、不写生产。

## 2026-07-26 07:23 V5 第十三条审计列车

### 已集成

- `b637b186`、`c75edb64`（feature 最终 `426ba0fc`）：把供应链手工入库更正与门店
  收货更正拆成独立状态机。门店实收修正只影响门店库存、配送差异和后续应付，不恢复
  供应链仓库存；SUPPLY_CHAIN 管仓，总厨不获得更正写权限。
- `54950bbb`、`bad38970`（feature 最终 `95680d5a`）：逐文件冻结 Product.stock、
  批次、预占、流水、发货与读取路径的默认仓兼容门禁和测试矩阵；真实仓按 tenant 建模，
  API `default` 仅为别名，不是跨 tenant 共用主键。
- 两个 runner 的原始错误报告与 FAILED 状态保留；总管纠正语义、通过报告合同、
  `git diff --check` 和敏感信息检查后才集成。

### 已推送但未集成

- `1f606d9d`、`bff1503a`：tenant 级 `Warehouse` / `WarehouseStock` schema 与单一
  migration 候选，后者把物理余额精度与现有库存事实统一为 `Decimal(12,3)`；
  包含所有 Product 的确定性余额初始化、五类事实 warehouseId、同 tenant 复合外键、
  单默认仓约束、Product → WarehouseStock 单向兼容桥和完整 rollback。
- 静态合同 4/4、Prisma validate/generate、API build、测试 TypeScript 编译、diff 和
  敏感信息检查通过；feature 远端 SHA 已核对，工作树干净。
- 本机无可用隔离 PostgreSQL，Docker 守护进程不可用，所以数据库 integration、
  空库/历史样本 migrate deploy/status/diff、并发约束及 rollback 均未执行。schema
  任务保持 BLOCKED，提交不得进入统一 RC。

### 后续门禁

- 恢复隔离 `_test/_ci` PostgreSQL 后，先验收 `bff1503a` 的迁移链、触发器、跨 tenant
  外键、历史回填、回滚与 schema diff，再决定是否逐提交集成。
- 仓库 schema 通过后，按审计顺序改造 writer；供应链入库更正与门店收货更正继续独立
  实现，不运行真实数据修正脚本。
- 生产 gate 保持 LOCKED；不合并 main、不建 PR、不部署、不写生产、不触碰凭证或业务
  xlsx。

## 2026-07-26 07:51 V5 第十四条仓库集成列车

### 已集成

- `1c23b0f5`（feature `1f606d9d`）：tenant 级 `Warehouse`、`WarehouseStock`、单默认仓
  唯一约束、五类事实 warehouseId、确定性历史回填和 Product.stock 单向兼容桥。
- `e40266db`（feature `bff1503a`）：仓库物理余额统一为 `Decimal(12,3)`。
- `8bde07c7`（feature `563ad704`）：补跨 tenant、并发、约束验证和未来多仓精度合同。
- `a55bba0f`（feature `50f5d962`）：修复首轮全量数据库套件发现的商品/租户硬删除
  兼容；tenant 所有的仓及派生余额使用级联，五类审计事实对仓继续限制删除。

### 门禁

- 全新空库 67 条迁移、重复 deploy、status 与 schema diff 通过；旧 66 条迁移后的历史
  tenant/Product/流水/批次/分配样本增量迁移正确；独立 rollback 演练通过。
- 首轮 PostgreSQL 全量因新余额 RESTRICT 外键造成 19 个文件连锁失败，失败证据保留；
  级联修复后在全新 `_ci` 库重跑 23 文件、165/165。
- 仓库专项 8/8；API 单元 362/362、API build、Web tsc、`git diff --check` 和敏感信息
  检查通过。Web 运行时代码未变，上一轮全量 426/426 继续有效。
- 生产 gate 保持 LOCKED；不合并 main、不建 PR、不部署、不写生产、不运行真实数据
  修正脚本。

### 下一步

- 逐 writer 把余额、批次、预占、流水、发货与查询切到真实 warehouseId，完成核对后再
  移除 Product.stock 单向桥和旧写补仓触发器。
- 分别实现供应链手工入库更正与门店实收更正；门店更正不得恢复供应链仓库存。

## 2026-07-26 08:50 V5 第十五条仓库解析与数量精度列车

### 已集成

- `44dd622c`（feature `cbdab05a`）：新增认证 tenant 范围的真实仓解析器，启用默认仓、
  显式仓、未知/禁用仓和跨 tenant 边界均有确定性测试；旧默认仓 helper 保持不变，
  caller 将逐项切换。
- `8869bc27`、`b79ad98c`、`71ac289d`（feature 最终 `a9f0db39`）：商品库存、安全库存、
  最小订货量和步长升级为 `Decimal(12,3)`；迁移安全重建依赖 stock 的同步触发器并
  删除并发流程留下的重复 migration。
- `739436d4`（feature `56470d2c`）：API 以 Decimal 精确校验三位小数和
  `999999999.999` 上界，拒绝四位、负数、非有限、科学计数与越界输入；编辑只开放
  最小订货量和步长。
- `619f8836`、`d131e44e`（feature `507efa98`）：商品 PC 显示/输入三位小数，保留库存
  与安全库存只读边界，并修复 1440×900 下长弹窗保存按钮不可达。
- `5404d44f`：把旧 PostgreSQL 越界测试对齐新数量精度，仍保留价格一亿元非法上界。

### 验收

- 空库 68 条迁移、历史样本升级、三位小数同步触发器、回滚预检/安全回滚、最终
  `migrate status` 与 migrations-to-schema diff 均通过。
- 首轮 PostgreSQL 164/165 失败证据保留；修复陈旧测试后在新的全新 `_ci` 库重跑
  23 文件、165/165。API 单元 394/394、API build、Web 450/450、Web tsc、
  `git diff --check` 和敏感信息检查通过。
- 1440×900 浏览器复核确认表格不溢出、三位小数保真、四位小数阻止请求，弹窗改为内部
  滚动后保存操作可达；临时 Next 缓存已移入废纸篓，可恢复。
- Qwen/Codex/Kimi runner 的未提交、行尾噪声、缺 migration 和 sandbox 失败均保留；
  控制服务缺管理端 token，未把手工修复伪报成控制面 DONE。

### 下一步

- 按仓库兼容审计 G2 顺序让商品期初、供应链入库/调整/报损/快照、订单预占和实际发货
  显式携带真实 tenant warehouseId；运行时完全切换并连续核对前不移除兼容桥。
- 供应链手工入库更正与门店实收更正继续独立实现，任何真实数据修正都不在本自动化执行。
- 生产 gate 保持 LOCKED；不合并 main、不建 PR、不部署、不写生产。

## 2026-07-26 09:19 V5 十二小时持续开发最终收口（竞态纠正）

### 最终候选与第十六列

- 统一 RC 运行时代码最终为 `65fea51a9ef640303011f78923765eba4ac40f71`。09:06 报告
  在另一个总管仍提交第十六列时读取了陈旧状态，误记为“无提交、未集成”；现已按远端
  exact SHA、patch-id、完整 diff 和独立门禁重新验收。
- `5184b1803ceda28aa19af5a18c421d667421604a`（feature
  `fb9c34e82194bb1cd99f9854516155d33b8870aa`）让内部商品正期初库存的流水、批次及显式带仓批次分配
  绑定认证 tenant 的同一真实默认仓；仓解析在商品写入前完成，失败时整笔事务无事实。
- `0c21eb781a48fd4ecb188c2bd946a011f9bf73ba`（feature
  `e2a1f053811cce97dbc27c0fa96fd8e0b864bbb0`）让预占、批次和流水 GET 解析 default/显式真实仓，
  Prisma 查询同时限定 tenant、supplier 和 warehouse；未切换的列表、summary 与写端点
  继续 fail closed。
- `0579253cfab135b18b7cb6326d5626d92bf1e38f`、
  `65fea51a9ef640303011f78923765eba4ac40f71`（feature
  `e90f969bbf790f49b6bcdd4ef3373940ff3ede77`、
  `286439a5879436e210bba80e19d2eb3051729256`）新增 Product.stock 与
  WarehouseStock 双源只读核对，并把默认仓解析提前到预占、流水和批次查询之前，避免
  STRICT 审计混入其他仓事实。缺行、停用和超过 0.001 的差异只形成 ERROR，不自动修复。
- Codex/Kimi 原 runner 均以 SIGTERM 退出，Qwen supervisor 曾停在 STOPPING 并由可信
  reaper 回收；保留原状态，只把 exact SHA、远端、diff、测试和安全边界均通过的已有
  feature 提交纳入统一 RC。

### 最终验收

- Node `20.20.2`、pnpm `10.32.1`、Prisma `5.22.0`；第十六列专项 4 文件 80/80、
  API 全量 47 文件 437/437、API build、Web 全量 24 文件 451/451、Web tsc、
  `git diff --check` 和高置信敏感信息检查通过。
- 两套全新临时 PostgreSQL 均从零应用 68 条 migration，deploy/status 通过且
  migrations-to-schema diff 为零；最终完整数据库集成 23 文件 165/165。临时容器
  已删除，未连接现有本地库或生产。
- 第十六列没有 Web 运行时代码变更；商品 PC 的 1440×900 三位小数、长表单和旧负库存
  编辑证据来自同一组合中的 `0b94cf77`，未宣称真实数据写入 E2E。
- 本窗口整体已覆盖内部供应链角色与跨店只读、收货/订单/配送查询分页、安全导出、商品
  五类直接生效与精准双通知、PC 商品台与门店图片、部分发货余量关闭、四单位及快照、
  成本单位计价、tenant 默认仓模型、仓解析、商品三位小数及上述首批真实仓 caller。

### 停止点与风险

- 第十六列之外的供应链入库、调整、报损、快照、订单预占、实际发货等 writer 仍未逐项
  切到真实 warehouseId；全部守恒核对完成前不得移除 Product.stock 单向兼容桥。
- Kimi feature worktree 在已推送提交后保留两处未提交的空 warehouseId 别名兼容草稿；
  未审查、未集成、未清理，不属于本候选。
- 供应链手工入库更正与门店实收更正仍只有冻结边界，运行时状态机未完成；门店实收更正
  不得恢复供应链仓库存。瑶海 7.22 盘点继续等待 31 项业务确认。
- 最终 active runner 为 0；隔离 Web/API/PostgreSQL 与缓存已收口，用户 3200 服务和
  其他脏工作区未触碰。生产 gate 保持 LOCKED；未合并/直推 main、未建 PR、未部署或
  写生产、未运行一次性生产脚本。

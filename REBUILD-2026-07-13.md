# 滇界 V4 重建说明（2026-07-13）

## 重建基线

- GitHub `main`: `f07884e94145de58992e6ed6a47d4cab41ae64ec`
- 线上部署标记: `1feb0f942a15e687bd8524806b793899c65be218`（该对象已不在 GitHub、ECS 或本机 Git 对象库中）
- 线上 Web BUILD_ID: `2aBUZT7p5WowNqYMkf1EB`
- 线上 API `dist` 与 GitHub 基线逐文件一致；仅额外保留本次资金通道停用开关。
- Web 以 GitHub 较新源码为主，并从线上构建还原历史订单/报损分页增量。

## 本分支补回的线上增量

- 厨师长采购历史分页。
- 总厨门店订单、集团报损分页。
- 厨师长店内报损/验收短量分类和分页。
- 供应商订单、报损、历史订单分页。
- `/api/loss-claims` 增加可选分页、`isManual` 和 `createdAfter` 条件；不传分页参数时仍返回旧版数组，兼容存量页面。

## 安全和部署修正

- `CMB_AUTOPAY_ENABLED` 默认关闭；只有显式设为 `true` 才运行自动付款。
- `CMB_INTERNAL_TRANSFER_ENABLED` 默认关闭；只有显式设为 `true` 才允许内部转账。
- `CMB_SYNC_ENABLED` 默认关闭；只有生产环境显式设为 `true` 才允许后台读取并入账招行流水。
- 修复邀请列表的 Prisma 类型错误，使 API 可完整编译。
- Nginx 兼容站点对齐线上端口：Web `3204`、API `4004`、上传上限 `60M`、旧 APK 入口 `8080`。
- 固定包管理器为 `pnpm@10.32.1`。

## 尚未执行

- 未轮换任何生产凭证。
- 未把本重建分支部署到生产，也未推送到远端。
- 生产数据库尚未执行 GitHub 中的 `20260617100000_add_store_consignee` 迁移；部署前必须先备份并审核迁移。
- 审计发现的支付幂等、现金账原子性、租户隔离等问题仍需按修复计划逐项处理，不能仅靠本次重建视为已解决。

## 工作区说明

本地采用 Git sparse checkout，仅展开阿里云运行所需的 API、Web 源码、招行服务、数据库包和部署脚本。移动端原生工程及二进制资源仍保留在 Git 历史和远端仓库中，需要时可用 `git sparse-checkout add <path>` 展开。

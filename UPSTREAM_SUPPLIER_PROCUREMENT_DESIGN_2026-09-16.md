# 上游供应商采购、收货、入库与对账闭环设计

> 版本：2026-09-16（技术总负责人方案草案）
> 范围：供应链总仓向上游供应商采购，不替代现有“门店向内部供应链订货”的履约链路。

## 0. 已确认的业务决策

2026-09-16 已由业务负责人确认：

1. 所有上游供应商货物统一送到总仓，本期不支持上游供应商直送门店。
2. 供应商从第一阶段开始使用自己的账号登录、接单、改单和发货，不采用供应链人员长期代录模式。
3. 上游采购默认按月对账；结算周期可按供应商配置。
4. 普通收货允许单人确认；大额、超收、临时改价和敏感品类触发第二人复核。
5. 供应商管理不是后补模块，而是第一阶段正式范围，包括档案、账号、合同/供货商品、接单、发货、差异处理和对账状态。

本方案后续章节如存在“代理录入”或“第二阶段再开放供应商门户”等旧假设，以本节已确认决策为准。

## 1. 结论

当前风险的根源不是“手工入库页面不够好用”，而是手工入库同时承担了采购、收货、定价和库存记账四种职责。操作员可以跳过订单、修改数量或金额后直接增加库存，系统缺少前后单据之间的约束。

本次应建立独立的上游采购闭环：

```text
供应商与采购合同
        ↓
采购订单 → 供应商接单/改单 → 发货单
        ↓
总仓收货验收 → 到货差异/报损 → 自动入库
        ↓
供应商对账单 → 发票 → 付款/核销
```

核心控制原则：

1. 正常采购必须“先有采购单，后有收货入库”。
2. 只有已确认的收货单可以写入总仓库存台账，且一张收货单只能入账一次。
3. 数量、价格、单位、供应商在采购单确认时冻结；后续只能通过改单、差异单或冲销单调整，不能覆盖历史。
4. 到货差异与库存事实分开：实际合格数量先入库，供应商责任金额进入差异/索赔和对账。
5. 手工入库保留，但仅用于期初、盘盈、非采购入库和紧急例外，必须审批并记录原因。

## 2. 现有系统可复用能力

项目已经具备以下基础，不应重复开发：

- `Supplier`：供应商档案、状态、账期、收款信息及业务范围。
- `ProductUpstreamSource`：商品与上游供应商关系，包含供应商 SKU、采购单位、换算、报价、起订量和交期。
- `WarehouseLedgerBalance / Movement / Lot`：总仓余额、不可变流水和批次。
- `WarehouseDoc`：现有手工入库/出库及审核、反审核、改单记录。
- 门店履约链路：`PurchaseOrder / DeliveryOrder / Receipt / LossClaim` 已验证了接单、配送、收货和差异处理的基本交互。
- `Reconciliation / Invoice / Payment`：已有对账、发票和付款骨架。
- 供应商账号与门户：已有供应商负责人/员工账号和供应商端页面框架。

现有 `PurchaseOrder` 是“门店向门店供货主体订货”，不能直接改造成“总仓向上游供应商采购”。两者参与方、库存方向、结算口径不同，强行复用会产生状态和权限混乱。界面组件和通用服务可以复用，业务实体必须分开。

## 3. 业务边界

### 3.1 两条链路明确分离

| 链路 | 买方 | 卖方/履约方 | 库存影响 |
|---|---|---|---|
| 上游采购 | 供应链总仓 | 外部上游供应商 | 收货后增加总仓库存 |
| 门店订货 | 门店 | 内部供应链/直供商 | 发货减少总仓或供应商库存，收货增加门店库存 |

一个合作方可以同时具有 `WAREHOUSE_UPSTREAM` 和 `DIRECT_STORE_VENDOR` 范围，但订单必须明确属于哪条链路，不能仅凭供应商身份推断。

### 3.2 手工入库的新定位

允许的原因限定为：

- `OPENING_BALANCE`：启用新仓或新 SKU 的期初。
- `STOCK_GAIN`：盘点确认后的盘盈。
- `NON_PURCHASE_RECEIPT`：赠品、退回、借入等非采购入库。
- `EMERGENCY_RECEIPT`：系统故障时的紧急入库，事后必须补采购依据并复核。
- `CORRECTION`：经授权的历史纠错，以冲销+重记实现。

正常“供应商送货”不再允许走手工入库。

## 4. 核心单据和数据模型

建议采用独立前缀 `Upstream*`，避免与门店采购订单混淆。

### 4.1 供应商合同与供货关系

在 `ProductUpstreamSource` 基础上补充可生效的价格合同：

#### `UpstreamSupplierContract`

- 供应商、合同号、生效/失效日期。
- 结算周期：现结、周结、月结、固定账期。
- 含税/未税口径、默认税率、币种。
- 付款方式、退换货与差异处理规则。
- 状态：草稿、有效、到期、终止。
- 合同附件及版本。

#### `UpstreamSupplierContractLine`

- 商品、供应商 SKU。
- 采购单位及采购到库存单位换算。
- 协议价、税率、起订量、最小包装、交期。
- 允许短溢装比例。
- 生效时间段。

`ProductUpstreamSource.quotedUnitPrice` 可保留为最新参考报价；采购订单正式价格必须来自合同或经授权的临时报价，并冻结到订单行。

### 4.2 上游采购订单

#### `UpstreamPurchaseOrder`

- 单号、供应商、收货仓库、期望到货时间。
- 订单来源：人工采购、补货建议、紧急采购、历史补单。
- 价格口径、税额、价税合计。
- 当前版本号、状态、并发版本号、幂等键。
- 制单人、审核人、提交/确认/关闭时间。

#### `UpstreamPurchaseOrderLine`

- 商品及商品名称/编码/规格快照。
- 供应商 SKU 快照。
- 采购单位、库存单位和换算快照。
- 订购数量、确认数量、已发数量、累计收货数量。
- 冻结单价、税率、未税金额、税额、价税合计。
- 容许短溢装比例。

#### `UpstreamPurchaseOrderRevision`

保存每次供应商或采购员提出的改单：改数量、价格、到货日、增删商品。必须包含修改前快照、修改后快照、申请人、确认人和原因。供应商不能直接覆盖已确认采购单。

### 4.3 供应商发货单

#### `UpstreamShipment`

- 关联采购单，可一单多次发货。
- 供应商发货单号、物流信息、预计到仓时间。
- 发货人、车辆/司机、附件。
- 状态：草稿、已发货、部分收货、已收货、取消。

#### `UpstreamShipmentLine`

- 关联采购订单行。
- 本次发货数量、生产日期、批次号、保质期/到期日。
- 供应商包装信息。

正常业务必须由已绑定该供应商的负责人或员工账号创建发货单。仅在故障应急模式下允许管理员代理录入，并必须填写原因、记录真实代理人，且进入异常审计列表。

### 4.4 总仓收货验收单

#### `UpstreamReceipt`

- 采购单、发货单、供应商、仓库。
- 到仓时间、验收人、复核人。
- 状态：草稿、验收中、待复核、已入账、已冲销。
- 入库流水批次号、幂等键。
- 照片、视频、签字和备注。

#### `UpstreamReceiptLine`

- 订单数量、本次发货数量、实到数量。
- 合格数量、破损数量、拒收数量、短缺数量、超收数量。
- 入库批次、生产日期、到期日。
- 采购单位数量与换算后的库存单位数量。
- 订单价格快照、合格应付金额。

收货确认时一个数据库事务完成：

1. 锁定采购单行与收货单，防止重复提交。
2. 校验单位换算、容差和累计收货上限。
3. 只对“合格数量”生成 `WarehouseLedgerMovement` 和批次。
4. 更新采购单累计收货数量与状态。
5. 对短缺、破损、错货、质量问题自动生成到货差异。
6. 写入不可变操作事件。

### 4.5 到货差异、报损和补报

#### `UpstreamArrivalClaim`

差异类型：

- `SHORTAGE`：短量。
- `DAMAGE`：破损。
- `QUALITY`：品质不合格。
- `WRONG_ITEM`：错货。
- `OVERAGE`：超收。
- `POST_RECEIPT_DAMAGE`：收货后拆包发现的到货异常。

保存责任方、争议金额、证据、供应商处理意见、供应链仲裁结果和财务调整方式。

允许已收货后补报，但必须满足：

- 在可配置时限内，例如收货后 24 或 48 小时。
- 关联原采购单、收货单和具体批次。
- 上传证据并填写原因。
- 不直接修改原收货数量；若库存已入账，确认后生成 `LOSS` 或退货出库流水。
- 财务按索赔结果生成借项/贷项调整，不重复扣款。

补发不是“免费货物不记账”。补发单必须关联原差异单，照常收货和入库；财务侧用贷项抵扣补发货值，使库存价值和供应商应付同时可解释。

### 4.6 对账、发票与付款

#### `UpstreamSettlementStatement`

建议不要直接复用当前只关联 `Receipt` 的 `ReconciliationItem`，而是建立可容纳多种业务行的对账明细：

- 供应商、结算周期、币种、税务口径。
- 期初未结、期间采购入库、退货、索赔扣款、补价/折让、期末应付。
- 我方确认、供应商确认、差异中、已锁定、已付款状态。
- 对账版本及双方确认记录。

#### `UpstreamSettlementLine`

- 来源类型：收货单、索赔单、退货单、价格调整、其他调整。
- 来源单号与业务日期。
- 原金额、调整金额、应付金额。

计算原则：

```text
期间应付 = 合格收货金额
         - 供应商承担的短缺/破损/质量扣款
         - 退货金额
         + 经批准的价格补差或其他应付调整
```

对账单锁定后才允许关联发票和生成付款申请。任何跨期修改以新调整行进入下一期，不修改已锁定对账单。

## 5. 状态机

### 5.1 采购单

```text
DRAFT
  → PENDING_APPROVAL
  → SUBMITTED_TO_SUPPLIER
  → SUPPLIER_ACCEPTED
  ↔ CHANGE_PROPOSED（双方确认后生成新版本）
  → PARTIALLY_SHIPPED
  → SHIPPED
  → PARTIALLY_RECEIVED
  → RECEIVED
  → SETTLEMENT_PENDING
  → CLOSED
```

任意未履约阶段可以按权限取消；已收货后不能取消，只能关单或走退货/冲销。

### 5.2 收货单

```text
DRAFT → INSPECTING → PENDING_REVIEW → POSTED → REVERSED
```

`POSTED` 是唯一允许产生入库流水的状态。不得从 `POSTED` 回到草稿修改；纠错必须生成冲销单和更正收货单。

### 5.3 差异单

```text
PENDING_SUPPLIER
  → SUPPLIER_ACCEPTED → RESOLVED
  → SUPPLIER_REJECTED → ARBITRATION → RESOLVED
  → AUTO_ACCEPTED → RESOLVED
```

解决方式包括扣款、补货、退货、双方分摊、我方承担和不处理。

### 5.4 对账单

```text
DRAFT → SENT_TO_SUPPLIER → DISPUTED/CONFIRMED → LOCKED → INVOICED → PAID
```

## 6. 权限和职责分离

| 角色 | 主要权限 |
|---|---|
| 采购/供应链制单员 | 建采购单、选择合同价格、提交订单、跟催 |
| 供应链审核人 | 审批采购单、确认供应商改单、关单、仲裁差异 |
| 上游供应商负责人/员工 | 接单、提出改单、录发货、确认差异、确认对账 |
| 仓库收货员 | 录实收、批次、效期和证据；不能改采购价格 |
| 仓库复核员 | 确认收货并触发入库；不能修改已确认采购价 |
| 财务 | 锁定对账、审核发票、付款与核销；不能修改实收数量 |
| 管理员 | 配置权限和例外规则，不代替业务审批 |

小团队可以让同一账号兼任多个角色，但系统仍要把“制单、审核、收货、财务锁定”记录成不同动作，并支持金额阈值下的双人复核。

所有写操作均需：租户隔离、角色权限、业务对象归属校验、幂等键、行版本校验和审计事件。不能只在前端隐藏按钮。

## 7. 页面与交互

### 7.1 供应链端

建议导航调整为：

1. **采购工作台**：待审批、待供应商接单、待发货、待收货、异常、超期。
2. **采购订单**：新建、复制、改单、关闭和完整时间线。
3. **到货验收**：按供应商/采购单快速收货，适配平板和手机。
4. **到货差异**：短缺、破损、品质、错货与补报。
5. **总仓库存**：只查看余额、批次、流水；正常采购不再提供直接入库按钮。
6. **供应商管理**：档案、账号、合同、供货商品、价格、交期和绩效。
7. **供应商对账**：待生成、待双方确认、争议、待票、待付、已结清。

### 7.2 供应商端

复用现有 `/v2/supplier` 门户框架，增加“总仓采购”业务模式：

- 待接订单。
- 接单或提出改单。
- 创建发货单。
- 查看验收与差异，确认或申诉。
- 确认月度对账、上传发票、查看付款状态。

供应商若同时承接门店直供和总仓采购，首页必须按业务类型分栏，不把两类订单混在同一个列表。

## 8. 风控规则

### P0 必须实现

- 无有效供应商、供货关系或已核验单位换算，不允许下采购单。
- 无采购单不得走正常采购入库。
- 已确认订单价格不可直接修改。
- 累计收货数量超过容差必须额外审批。
- 重复点击、网络重试不得重复入库或重复生成应付。
- 收货入库与库存流水必须同事务提交。
- 已入账收货只能冲销，不能删除或覆盖。
- 收货、索赔、对账的金额口径必须可从原始单据重算。

### P1 应实现

- 采购金额/临时报价/超收按阈值审批。
- 交期逾期、未接单、未对账自动待办提醒。
- 供应商价格波动、准时率、差异率和索赔率分析。
- 合同到期、证照到期、银行账户变更需要复核。

## 9. 与现有库存系统的集成

扩展库存流水类型：

- `UPSTREAM_RECEIPT`
- `UPSTREAM_RECEIPT_REVERSAL`
- `PURCHASE_RETURN`
- `PURCHASE_CLAIM_LOSS`

每条流水写明：采购单、发货单、收货单、收货行、供应商、批次、原始采购数量和单位、换算率、库存数量、单位成本和幂等键。

供应商不是库存余额维度。总仓同一 SKU 的库存仍按仓库聚合，供应商信息作为批次和流水来源保留，用于追溯和成本核算。

## 10. 实施路线

### 阶段 0：规则确认与数据清理（2～3 天）

- 确认本文末尾的业务决策。
- 清理上游供应商、供货关系和单位换算。
- 盘点现有未结手工入库和未对账记录。
- 定义采购单、收货单、差异单和对账单编号规则。

### 阶段 1：供应商管理＋采购—收货—自动入库闭环（预计 2～3 周）

- 上游供应商档案、账号邀请/绑定、合同与供货商品。
- 上游采购单、审批、供应商账号接单；管理员代理仅用于有审计记录的故障应急。
- 供应商端待接单、改单申请和发货操作。
- 分批发货、总仓验收、部分收货。
- 合格数量自动写入总仓库存与批次。
- 短缺/破损自动生成差异单。
- 手工入库改为例外入口并加强权限。

上线门槛：核心端到端场景全部通过，库存重复入账测试、并发测试和权限测试通过。

### 阶段 2：完整差异协商与月度对账（预计 1～2 周）

- 差异协商、收货后补报、补发/扣款。
- 周期对账、双方确认、锁定。
- 对接已有发票和付款能力。

### 阶段 3：补货与经营分析

- 基于库存、安全线、在途量和销售消耗生成补货建议。
- 供应商准时率、缺货率、质量差异率、价格趋势和结算效率。
- 按规则半自动生成采购草稿，但仍需人确认。

## 11. 历史数据处理

- 不把所有历史手工入库强行伪造成采购单。
- 历史单据保留来源标记 `LEGACY_MANUAL_INBOUND`。
- 只对当前未结算期间的手工入库，按供应商和日期补建“历史采购汇总单”用于对账。
- 新功能启用日之后，正常采购手工入库入口关闭。
- 上线前建立一次经确认的总仓期初，并确保之后只用业务流水连续记账。

## 12. 验收场景

至少覆盖：

1. 一张采购单一次全部收货。
2. 一张采购单分三次发货、分三次收货。
3. 短量、破损、品质不合格、错货和超收。
4. 收货后拆包发现问题并补报。
5. 供应商拒绝差异，供应链仲裁后部分扣款。
6. 补发货物再次收货，库存增加但应付净额正确。
7. 供应商提出改数量/改到货日，经我方确认后生效。
8. 订单价与当前商品报价后来变化，历史订单金额不变。
9. 重复点击、断网重试和并发收货不重复入库。
10. 已入账收货冲销后，库存、批次、应付和对账均可追溯。
11. 越权账号无法确认收货、仲裁差异或锁定对账。
12. 已锁定对账跨期调整，不改历史账单。

## 13. 已确认项的系统落地规则

### 13.1 物流边界

- 上游采购单必须指定总仓，不能选择门店地址。
- 发货、收货和库存流水必须携带同一 `warehouseId`。
- 门店补货继续走既有总仓到门店履约链，与本模块分离。

### 13.2 供应商账号

- 供应商档案创建后必须邀请至少一个 `SUPPLIER_OWNER` 才能启用采购接单。
- `SUPPLIER_OWNER` 可邀请本公司的 `SUPPLIER_STAFF`，不能访问其他供应商数据。
- 采购单只允许绑定状态正常、存在有效账号、具有上游供货范围的供应商。
- 接单、改单、发货、差异处理和对账确认均记录供应商操作人。

### 13.3 结算周期

- 上游供应商默认 `MONTHLY`。
- 保留 `WEEKLY`、`FIXED_DAYS`、`ON_DELIVERY` 作为单个供应商配置。
- 月结周期默认自然月；如需“每月 26 日至次月 25 日”等非自然月，增加结算日配置。

### 13.4 收货复核

普通收货单人确认；满足任一条件时进入 `PENDING_REVIEW`：

- 订单/收货金额超过租户配置阈值。
- 存在超收。
- 使用临时价格、无合同价或确认后改价。
- 命中敏感品类。
- 单位换算或批次/效期信息异常。

触发复核时，录入人不能复核自己的单据。

### 13.5 采用默认值、后续可配置的规则

- 收货后补报：普通品 24 小时；冻品、整箱拆包商品 48 小时。
- 采购订单按含税价录入，同时保存税率和未税金额。
- 供应商可以提出改价，但不能自行生效；供应链审核，超过阈值再由负责人审批。
- 差异争议由供应链负责人裁决，财务只按已裁决结果入账。

# 滇界 V4 当前项目交接文档

> 更新时间：2026-07-18 15:20（Asia/Shanghai）
> 工作区：`/Users/somnusyi/Documents/Codex/dianjie-V4-rebuilt`
> 生产基线：GitHub `main` / 生产均为 `f33d091`
> 当前开发分支：`feature/ops-control-stocktake`，尚未合并和部署
> 业务规则：与本文冲突时，以 `SUPPLY_CHAIN_DECISIONS_2026-07-18.md` 为准。

## 1. 系统定位

滇界 V4 是餐饮集团真实使用的多租户经营与供应链系统，当前以合肥瑶海店为试点。覆盖老板、财务、总厨、店长、厨师长、供应商等角色，主要闭环包括：

- 门店订货、供应商接单、库存预占、实发调整、配送和收货；
- 供应商库存与门店预计库存两本独立账；
- 每日营业双表导入、菜品销量、BOM 食材消耗和历史回补；
- 门店报损、盘点、应付、对账、付款、发票、资金和凭证；
- 老板、门店、总厨、财务和供应商工作台。

系统继续采用模块化单体，不在当前阶段拆微服务：

```text
Next.js 14 Web / PWA
        │ REST
Fastify 4 TypeScript API
        │
Prisma 5 + PostgreSQL

外部：Redis、阿里云 OSS、招行辅助服务、美团、企业微信
```

## 2. 当前代码与生产状态

### 2.1 已上线生产基线

- GitHub `main`：`f33d091`；
- 生产应用提交：`f33d091`，2026-07-18 11:58 发布完成；
- 生产迁移：56 条已应用、0 条失败；
- 生产进程：API、Web、CMB 在线；
- 发布前备份、迁移和健康检查已完成；
- 招行自动付款保持关闭，美团生产接口尚未启用，真实营业数据仍以每日双表为准。

### 2.2 当前未发布开发分支

- 分支：`feature/ops-control-stocktake`；
- 基线：从 `f33d091` 创建；
- 本地数据库和测试数据库：57 条迁移；
- 新增第 57 条迁移：`20260718130000_store_inventory_counts`；
- 本分支不得在未完成发布审批、生产备份和操作员 UAT 前直接部署。

### 2.3 工作区保护

- 未跟踪的 `库存主数据映射审计_2026-07-15.xlsx` 是用户业务文件，不修改、不删除、不提交；
- 不在 Git、文档或聊天中保存生产密码、数据库连接、云密钥或员工口令；
- 不在生产目录直接改代码，不用 `prisma db push` 代替迁移。

## 3. 本轮新增：每日运营控制中心

### 3.1 目标

把原来分散在日报上传、BOM 待办和库存页面的信息汇总成每天可执行的异常清单，避免按角色继续堆孤立页面。

### 3.2 已实现

- 后端：`GET /api/daily-business-imports/control-center`；
- 前端：`/v2/operations-control`；
- 支持近 7、14、30 天；
- 汇总每个门店、每个营业日的上传、预览、确认、更正和逾期状态；
- 展示营业额、营业收入、订单量、销售菜品数、库存扣减 SKU；
- 汇总 BOM 暂缓、历史回补、阻断和警告；
- 汇总门店盘点基准、低库存、负库存、待匹配和临期情况；
- 门店角色只看绑定门店，集团角色可看全部启用门店；
- 供应商等无权角色在前端得到明确说明，后端继续返回 403。

### 3.3 运营口径

- 每天上午 11:00 前应上传前一天两份营业 Excel；
- 只有确认版本进入可信营业和库存消耗；
- 更正版保持版本链，不能覆盖历史事实；
- BOM 暂缓不阻塞营业入账，但必须进入总厨待办并支持历史回补；
- 控制中心是监控和导航入口，不重复实现日报、BOM、库存模块的写操作。

## 4. 本轮新增：门店在线月度盘点

### 4.1 状态机

```text
DRAFT → COUNTING → REVIEWING → CONFIRMED
  │         │
  └─────────┴→ CANCELLED

CONFIRMED → REVERSED → 重新建立盘点单
```

### 4.2 已实现接口

- `GET/POST /api/inventory-counts`；
- `GET /api/inventory-counts/:id`；
- `POST /:id/start`；
- `PUT /:id/items`；
- `POST /:id/submit`；
- `POST /:id/confirm`；
- `POST /:id/cancel`；
- `POST /:id/reverse`。

前端页面：

- `/v2/inventory-counts`：记录、新建和续盘；
- `/v2/inventory-counts/:id`：录入、保存、复核、确认、取消和冲销；
- 厨师长和店长库存页已增加入口。

### 4.3 盘点规则

- 每月闭店执行全品项盘点；
- 创建时冻结启用食材、商品名称、编码、单位、账面数量和移动平均成本；
- 所有项目必须明确录入，零库存填 0；
- 实盘数量不能为负；
- 非零差异必须填写原因；
- 差异率超过 5% 或差异金额超过 200 元必须有图片证据；
- 提交后进入复核，确认后原子生成新的 `InventorySnapshot` 可信基准；
- 盘盈盘亏只属于门店内部库存和成本，不改变供应商物理库存、预占、应付或索赔；
- 已确认盘点不可直接编辑；仅允许冲销最新盘点基准后重盘；
- 并发创建、并发确认通过数据库约束、行版本和串行化事务保护；
- 当前按项目决策由统一授权角色提交并确认，模型预留未来录入/审批分权。

### 4.4 数据模型

- `InventoryCount`：单号、门店、盘点日、版本、状态、汇总金额、操作人与审计时间；
- `InventoryCountItem`：冻结商品快照、账面/实盘/差异数量和金额、原因、证据；
- `InventorySnapshot` 继续作为预计库存计算的可信基准；
- 冲销盘点会删除其衍生的最新基准，但保留盘点单审计事实为 `REVERSED`。

## 5. 已确认的关键供应链规则

### 5.1 订货、实发和收货

- 所有货物目前由供应商直接送门店，没有集团总仓；
- 原始订货量、冻结单价和原始金额必须保留；
- 供应商发货前可调整本次实发量，实发决定配送金额和供应商库存扣减；
- 页面必须区分订购量、实发量、实收量，以及订货金额、实发金额、应付金额；
- 门店收货时一次性确认短缺、破损、品质和证据；
- 收货确认是供应商责任截止点，之后不得补报；
- 不建立免费补送；差异应付为合格实收数量乘冻结单价；
- 到货差异只影响责任和应付，不自动回补供应商库存；
- 已确认入库单的商品、数量、价格和应付不因后续盘点倒改。

### 5.2 两本库存账

- 供应商库存：物理库存、活动预占、可用库存分开；接单预占，取消释放，发货核销预占并扣物理库存；
- 门店预计库存：最近可信盘点 + 后续确认实收 - BOM/人工消耗 - 门店内部报损；
- 门店盘点、报损和后续发现的差异与供应商库存、应付完全隔离；
- 本地两个旧租户的 722 个悬空供应商商品保留并隔离，不补绑、不删除；生产处理前必须重新做只读审计。

### 5.3 营业和 BOM

- 美团 API 上线前，店长每天上传综合营业统计和菜品销售明细；
- 必须预览后确认，确认动作原子写入营业、菜品销量和可执行的 BOM 消耗；
- 退菜不补回库存，BOM 使用毛重；
- 百家蘸料不扣库存，赠品按对应商品扣库存；
- 鲜鸡和冻品只按已确认映射替代，不做模糊自动串货；
- 菜品未建档或无可执行 BOM 时明确暂缓，由总厨工作台处理并回补历史消耗。

## 6. 验证基线

当前分支完成以下验证：

- Prisma schema 校验通过；
- 本地业务库、独立测试库均为 57/57 迁移；
- API 单元测试：106/106；
- PostgreSQL 集成测试：50/50；
- Web 测试：13/13；
- API 和 Web TypeScript：通过；
- Web 正式生产构建：140/140 页面；
- `git diff --check`：通过；
- 本地实际账号验收：运营中心返回真实汇总；
- 本地完整盘点验收：创建、开始、344 项保存、提交、确认生成基准、冲销恢复旧基准均成功，验收数据已清理；
- 供应商库存未因盘点确认变化；并发确认只允许一次成功。

构建仅保留既有 OpenTelemetry/Sentry 动态依赖警告，不是本轮新增错误。

运行注意：新增 Prisma 模型后必须执行 `prisma generate` 并重启 API；仅热更新代码不足以加载新版 Prisma Client。

## 7. 下一阶段：供应链重点改进

建议不再按“给每个角色各做一套孤立页面”推进，而按共享领域闭环拆工作流；角色只决定权限和工作台视图。

### P0：供应链数据可信度

1. 商品主数据治理：集团食材主档、供应商报价/供货关系、规格单位换算分层；
2. 处理商品别名、重复 SKU、供应商商品与集团食材映射，建立可审计合并；
3. 对生产负库存、待匹配、单位换算和盘点基准做只读审计及修复清单；
4. 对订单、配送、收货、应付金额建立跨单据对账视图。

### P1：供应商工作台重构

1. 首页变成待接单、待发货、在途异常、低库存、临期和对账异常任务中心；
2. 商品与分类从简单标签升级为可搜索、批量维护、启停、图片、规格、供货价和门店可售范围；
3. 库存页统一展示物理、预占、可用、批次、到期日和流水；
4. 发货页强化实发数量、金额变动、缺货原因和门店确认；
5. 所有列表增加分页、筛选、导出和稳定的移动端反馈。

### P2：收货与对账闭环

1. 真实小额订单跑通订货→接单→实发→发货→送达→收货→库存→应付；
2. 报损单补齐带图片打印详情；
3. 建立订单金额、配送金额、实收应付三段差异解释；
4. 对账单和发票读取冻结事实，不随商品主档修改而变化。

### P3：工程和运营保障

1. 把 57 条迁移、API/Web 测试和正式构建固化到 CI；
2. 增加 5xx、导入失败、负库存和日报逾期告警；
3. 建 staging 或脱敏生产副本，避免在真实租户做破坏性 E2E；
4. 建立发布、回滚、迁移和业务验收的单一清单。

## 8. 多人并行边界

建议按领域分工而非按角色分工：

| 工作流 | 主要目录 | 交付 |
|---|---|---|
| 主数据与供应商商品 | `products.ts`、供应商分类/商品页、Prisma 主数据模型 | 主档、报价、分类、映射和批量维护 |
| 订货配送收货 | `orders.ts`、`deliveries.ts`、`receipts.ts` 及对应页面 | 单据状态机、金额解释、真实 E2E |
| 库存与盘点 | `storeInventory.ts`、`inventoryCounts.ts`、供应商库存服务 | 两本账、批次、盘点和异常治理 |
| 营业与 BOM | `dailyBusinessImports.ts`、菜品/BOM 页面 | 每日导入、暂缓、回补、成本 |
| 财务与对账 | 应付、对账、付款、发票和凭证模块 | 冻结事实、幂等和审计 |
| 平台质量 | 测试、CI、监控、发布脚本 | 回归矩阵、告警、发布证据 |

以下文件是高冲突区，只允许指定集成人修改：

- `packages/db/prisma/schema.prisma` 与迁移；
- `apps/api/src/index.ts`；
- 公共认证、库存和业务编号服务；
- 生产部署脚本与环境配置模板。

每个分支必须包含：业务边界、迁移影响、自动化测试、手工验收证据和回滚说明。发布负责人只从已审查的 `main` 部署生产。

## 9. 当前明确未做

- 本轮在线盘点与运营中心尚未部署生产；
- 盘点差异阈值目前为代码常量，后续可配置；
- 美团生产同步、完整企微 SSO、招行自动付款未启用；
- 不建设总仓、调拨、生产领料、寄售和多仓位等复杂能力；
- 不自动处理生产负库存或 722 条历史悬空商品；
- 不因门店盘点反向更改供应商库存、入库单或应付。

## 10. 接手执行顺序

1. 阅读本文和 `SUPPLY_CHAIN_DECISIONS_2026-07-18.md`；
2. 检查当前分支、工作树和用户未跟踪文件；
3. 运行 Prisma 校验、API/Web 测试、类型检查和正式构建；
4. 对 `feature/ops-control-stocktake` 做代码审查和门店操作员 UAT；
5. 获得发布确认后，按受控脚本备份、迁移、部署并验收；
6. 发布稳定后，从“供应链数据可信度”P0 开始下一阶段开发。

# 滇界 V4 12 小时无人值守推进任务

开始时间：2026-07-16 19:10（北京时间）  
执行分支：`release/20260715-p0`  
执行边界：仅本地代码与 `dianjie_v4_local`；不推送、不部署、不修改生产数据或凭证。

## 目标

在不等待人工干预的情况下，以可审计的小批次持续推进当前发布候选：优先消除数据一致性、权限隔离和核心业务闭环风险，再补齐自动化测试、运行手册与生产发布证据。

## 循环协议

每轮必须依次完成：

1. 从代码、数据库约束、页面和现有测试中选择一个最高风险且业务口径明确的问题。
2. 先定义失败场景和验收条件，再做最小范围修改。
3. 运行相关单测、数据库端到端、TypeScript/Next 构建与空白检查。
4. 失败时修复或撤回本轮，不把红色基线带入下一轮。
5. 通过后记录证据并建立本地 Git 检查点。
6. 重新排序风险队列，进入下一轮。

## 不自动决策的事项

- 不猜测食材单份克重、补送、报损收货、已确认入库更正等业务规则。
- 不升级框架大版本，不变更审批角色和财务口径。
- 不删除用户文件，不提交本地业务 Excel 或环境文件。
- 不连接或写入生产数据库，不执行远端发布、推送和凭证轮换。

缺少口径的事项进入阻塞清单，但不阻塞其他工作。当前业务阻塞仅有：`虎掌菌` 每份扣减克重待确认。

## 12 小时工作队列

### A. 架构与数据入口

- 日报双文件的日期、门店、金额、赠品和多门店边界。
- 预览版本分配、重复上传和并发确认的幂等与原子性。
- BOM 变更指纹、历史更正、营业记录与库存消耗的审计闭环。
- 迁移从零部署、既有库 schema diff 和必要索引。

### B. 核心模块

- 订货单不可变、供应商改单确认和单号并发。
- 分批配送、手工/自动收货竞争、短量报损和财务派生。
- 供应商商品/分类审批、库存入库/报损/快照并发。
- 店长与厨师长预计库存、采购收货和日报扣减之间的口径一致性。

### C. 权限与安全

- 店长、厨师长、供应商、总部角色的租户/门店/供应商隔离。
- 文件上传大小、格式、错误信息和异常数据拒绝策略。
- 高置信敏感信息扫描与生产操作护栏。

### D. 测试与发布证据

- API 单测、关键数据库 E2E、Web 类型检查和正式构建。
- 迁移链、幂等重试、并发竞争、故障回滚与测试残留检查。
- 生产发布前检查、备份、回滚和 UAT 清单。

## 时间分配

| 时段 | 重点 | 出口条件 |
|---|---|---|
| 0-2 小时 | 架构基线、日报/BOM P0 | 数据入口负向用例与并发 E2E 通过 |
| 2-5 小时 | 订货/配送/收货/库存 | 核心脚本全部通过且无测试残留 |
| 5-8 小时 | 权限、财务派生、异常补偿 | 跨租户与故障注入通过 |
| 8-10 小时 | Web 操作体验和可观测性 | 页面构建、错误反馈和状态提示通过 |
| 10-12 小时 | 全量回归、文档和发布清单 | 绿灯基线、本地提交和风险清单完成 |

## 执行记录

### 第 1 轮：日报数据入口与并发预览

- 修复显式收入为 0 的赠品被错误替换为“销售额减优惠”的问题。
- 综合营业文件只允许单店单日，拒绝多行门店/日期数据和负数关键指标。
- 菜品销售文件采集门店名，阻断两份文件门店不一致、销售文件多门店和退菜跨门店。
- 增加报表门店与登录账号目标门店的匹配校验，防止错店导入。
- 严格校验日历日期；当天和未来营业日不能提前导入。
- 日报预览版本分配改为门店/营业日事务锁，解决并发重复预览的版本冲突。
- 新增并发重复预览端到端断言；同一文件对只生成一个审计版本。

验证：日报解析 7/7、API TypeScript 构建、日报数据库端到端均通过；端到端最终营业收入 120、销量 3、BOM 消耗 0.75，历史状态为两版 `SUPERSEDED` 加一版 `CONFIRMED`，测试数据已清理。

### 第 2 轮：手工销量与 BOM 消耗一致性

- 门店级角色的销量列表、销量榜和预计消耗强制限定绑定门店，不能通过查询参数越权。
- 手工销量只能写入 `manual` 数据源，不能覆盖日报/POS 等系统来源记录。
- 写入前校验门店和菜品均属于当前租户，并严格校验营业日期。
- 销量 upsert、旧消耗删除和新 BOM 消耗创建合并为同一事务，并按门店/菜品/日期加事务锁。
- `EXCLUDE` 菜品的手工销量不生成库存消耗；BOM 数量按 6 位精度写入。
- 配方新增/修改强制食材属于当前租户，且配方单位必须等于库存 SKU 单位。
- 新增本地数据库端到端，覆盖跨店读写、系统来源伪造、跨租户配方、单位错配、并发双更新和库存写入故障回滚。

验证：两个并发销量更新均成功，最终销量 4 与消耗 1kg 一致；故障注入时接口返回 500，销量和原消耗完整回滚；临时触发器、账号、门店、租户和业务数据全部清理。

### 第 3 轮：日报审计表数据库完整性与迁移链

- 将日报状态从自由字符串收紧为数据库枚举：`PREVIEWED / CONFIRMING / CONFIRMED / SUPERSEDED`。
- 为日报审计记录补齐租户、门店、创建人和确认人的数据库外键，防止孤儿记录。
- 确认人删除时保留业务审计并将引用置空；租户、门店和创建人仍受限制保护。
- 增加可重复执行的本地空库迁移验证脚本，带本地库护栏、自动建库、全链部署、schema diff 和自动清理。
- 首次空库演练发现复合唯一索引的 Prisma/数据库命名漂移；补齐 `map` 后重新演练为零差异。

验证：既有本地库 40/40 迁移已应用，schema diff 为零；全新临时空库从首条迁移部署 40/40 成功，`migrate status` 最新、schema diff 为零，临时数据库自动删除。

### 第 4 轮：独立入库单权限、报损原子性与金额一致性

- 补录、确认、报损确认、拒收和作废统一收紧为门店操作角色或品牌管理员；店长和厨师长只能操作绑定门店。
- 供应商标记送达和供应商核对均校验入库单归属，阻断同租户内供应商之间横向越权。
- 补录前校验门店、供应商、商品的租户和供应商归属，拒绝重复商品、非法日期以及超过数据库精度的数量/单价。
- 手工入库单号改用事务安全的业务序列；入库单、明细和操作日志在同一事务创建。
- 报损确认要求完整且不重复提交全部明细，拒绝超收；实收金额和损耗金额改用 Decimal 逐行计算并按两位小数入账。
- 入库单抢占、明细实收、报损单、日志和采购单完结判断合并为单个事务，并发重复确认只能有一个请求落账。
- 修复手工入库报损时把空采购单 ID 写成空字符串导致外键失败的问题，未关联采购单时保持 `null`。
- 新增本地数据库端到端脚本，临时测试门店、账号、供应商和商品按精确 ID 回收，不触碰既有测试业务数据。

验证：入库单端到端覆盖跨店、跨供应商、角色越权、商品归属、明细遗漏、超收、并发双确认、账期和对账单唯一性；最终实收 ¥14.43、损耗 ¥4.44，全部断言通过且临时数据为零。订货单不可变、两批配送/手工自动收货竞争、供应商库存并发与 Web TypeScript 回归同步通过；API 60/60 测试及构建通过。

### 第 5 轮：确认入库后的财务派生故障补偿

- 普通确认入库在主状态已提交、账期或对账单派生短暂失败后，允许同门店授权角色安全重试。
- 重试继续遵守租户和门店边界，并复用 `receiptId` 事务锁与唯一约束补齐账期、对账单，不重复写确认日志和通知。
- 已正常完成的重复请求返回 `duplicated=true`，客户端可把网络重试视为幂等成功。
- 端到端测试增加 PostgreSQL 触发器故障注入：首次确认强制让账期写入失败，验证入库单保留 `CONFIRMED`、无半条账期/对账单；移除故障后原请求重试自动恢复为 `ACCOUNTED`，且账期和对账单各一条。

验证：故障注入首次返回 500、派生数据为零；重试返回 200 和 `duplicated=true`，最终财务派生唯一且完整，临时触发器和业务数据全部清理。

### 第 6 轮：供应商报损处理与自动同意并发一致性

- 报损同意的状态迁移、供应商库存回补、库存流水和操作日志合并为同一事务。
- 以 `loss-handle:<claimId>` 事务锁串行化供应商同意、供应商拒绝和 24 小时自动同意；三条路径不再各自抢写产生分裂状态。
- 库存回补按 `claimId + productId` 检测既有流水并幂等执行，所有明细要么整体提交、要么整体回滚；商品必须属于同租户责任供应商。
- 供应商并发拒绝时，账期金额加回、`ON_HOLD` 冻结、报损状态和日志同事务提交，重复请求不会二次加款。
- 自动同意调度器改用同一原子服务，避免先写 `AUTO_APPROVED`、后回补库存失败造成终态与库存不一致。
- 报损列表、创建和处理对缺失 `storeId` / `supplierId` 的门店或供应商账号采用拒绝式隔离，不能退化为全租户访问。
- 新增本地数据库端到端，覆盖跨供应商、未绑定供应商、并发同意、并发拒绝、自动同意与人工拒绝竞争，以及全部临时数据回收。

验证：并发同意一主一幂等，供应商库存只从 95 回补至 97 且仅一条流水；并发拒绝账期只从 15 加回至 25 并冻结；自动/人工竞争最终状态与库存、账期完全匹配。API 60/60 测试和构建通过，测试商品、订单、入库、报损、凭证和账号残留均为零。

### 第 7 轮：门店库存查询隔离与人工消耗原子性

- 门店库存、盘点快照和消耗记录统一增加角色白名单；供应商账号不能读取门店库存或全租户消耗。
- 门店级角色缺失绑定门店时明确拒绝，不再因过滤条件缺失退化为全租户查询；集团角色跨店查询会校验门店属于当前租户。
- 人工消耗补齐厨师长角色，采用严格 Zod 校验：正数、最多 6 位小数、单请求商品唯一、合法非未来日期、备注和批量上限。
- 写入前校验门店和所有食材均属于当前租户，混入跨租户食材时整单拒绝。
- 一批人工消耗使用统一 `operationId`，全部消耗明细和审计日志在同一事务提交；来源标记为 `manual` 便于追溯。
- 门店预计库存继续严格使用盘点基准、实收入库和消耗事件，不修改供应商库存 `Product.stock`。
- 扩展本地数据库端到端，加入供应商越权、未绑定门店、负数/重复/未来日期/超精度、跨租户商品和触发器故障回滚。

验证：故障注入第二条消耗失败时第一条同步回滚且无审计半记录；合法消耗后期初 100 + 入库 20 - 消耗 15 = 预计库存 105，移动平均成本 10.833333，供应商库存仍为 50。临时触发器、租户、账号、商品和业务记录残留均为零。

### 第 8 轮：店长日报上传状态机与审计查看体验

- 日报状态每 60 秒静默刷新，并提供显式刷新入口；后台状态变化不会覆盖店长当前正在查看的历史版本。
- 历史记录把 `CONFIRMING` 明确显示为“确认处理中”，不再与“待确认”混淆；`SUPERSEDED` 明确显示为“已被新版替代”。
- 旧版和确认中的预览改为只读状态，彻底移除确认操作入口，避免店长点开历史版本后提交必然失败的请求。
- 预览补充两份源文件名和文件大小，增加可展开的预计食材扣减明细，便于确认前核对 SKU、数量和单位。
- 修复问题提示卡标题重复渲染；保留阻断项时确认按钮禁用、确认完成提示和事务原子性说明。
- 顺手确认测试数据中 7 月 14 日待确认版本因“虎掌菌”缺少可执行 BOM 被正确阻断，未猜测克重、未写入营业或库存。

验证：新增 Web 状态机单测 3/3 通过，Web TypeScript 严格检查通过；在本地瑶海店长页面完成浏览器验收：7 月 15 日旧版只显示“仅供审计查看”，无确认按钮；7 月 14 日待确认版本显示 139 个食材 SKU、阻断项和禁用确认按钮；当前确认版与四条历史状态文案正确。浏览器测试未执行任何确认或数据写入。

### 第 9 轮：店内报损成本可信性与并发审核

- 店内报损新增严格输入校验：数量必须为正且最多两位小数，同一食材不能重复，限制单次数量、明细数、原因、备注和证据数量。
- 服务端彻底忽略客户端单价，按“最近实物盘点 + 后续确认收货”形成的门店移动平均成本计价；无盘点基准或食材未进入门店库存基准时阻断提交。
- 厨师长报损页面改为显示并预估移动均价，不再把供应商当前目录价误当门店损耗成本。
- 店内报损单、明细、审计日志与并发安全业务单号合并为同一事务；审计日志写入失败时整张报损单回滚。
- 总厨审核使用报损级事务锁，状态迁移与审核日志同事务提交；并发通过/驳回只能有一个终态生效。
- 供应商责任报损同样改用业务序列表分配单号，并将报损单、明细和日志原子提交；补充重复订单行、数量精度、文本和证据边界校验，订单数量和单价仍以服务端快照为准。
- 未擅自处理“验收后多轮补报短量的累计上限”和“争议仲裁部分金额对应多少库存回补”两项尚未确认的业务口径。

验证：新增本地数据库端到端覆盖伪造单价、跨租户、未映射食材、负数/重复/超精度、日志故障回滚、并发单号和并发审核；客户端传入 ¥99,999 时落库移动均价为 ¥10.83，2kg 报损金额为 ¥21.66；并发四张报损单号唯一，审核一成功一冲突，预计库存与唯一终态一致且供应商库存不变。既有供应商报损并发端到端继续通过；API 60/60、Web 3/3、API 构建、Web TypeScript 均通过，临时触发器、通知、账号、租户和业务数据已清理。

### 第 10 轮：生产密钥与演示数据安全护栏

- API 在生产模式下强制要求 `JWT_SECRET` 至少 32 位且不得使用示例占位值；弱密钥或缺失密钥时拒绝启动，开发环境仍可使用仅限本地的明确回退值。
- 生产环境的 5xx 响应统一返回通用错误文案，详细异常仍由服务端日志和 Sentry 留存，避免把数据库结构、文件路径或内部调用栈直接暴露给客户端。
- 更新 Fastify 路由标签读取方式，消除已废弃的 `routerPath` 依赖并保留 Sentry 的租户、角色和路由定位信息。
- 两套历史种子脚本移除所有硬编码弱密码；仅当非生产环境显式设置 `ALLOW_DEMO_SEED=true` 且提供至少 12 位 `DEMO_SEED_PASSWORD` 时才允许运行。
- 环境变量模板补充演示种子开关说明，默认关闭；不写入任何真实凭证。
- 保留现有 HTTP/CORS 兼容配置，证书和 HTTPS 按用户要求暂不纳入本轮变更。

验证：API TypeScript 构建通过，单元/集成测试 60/60 通过；`NODE_ENV=production` 配合短 JWT 密钥时进程在监听端口前以明确错误退出；未授权种子脚本和生产模式种子脚本均在数据库访问前安全拒绝。工作区敏感文件扫描未发现被 Git 跟踪的私钥或真实环境文件。

### 第 11 轮：真实生产构建与 standalone 产物冒烟

- 平滑停止本地 Web 开发进程，清空开发缓存后执行完整 Next.js 生产构建，避免开发模式 `.next` 与生产产物相互污染。
- 生产构建完成全部 134 个页面的静态生成，并产出 `BUILD_ID` 与 standalone server；构建脚本的完整性门禁通过。
- 按实际部署目录结构组装 standalone、`.next/static` 和 `public`，直接启动生产 server 做 HTTP 冒烟。
- 验证登录页、构建后的 CSS 静态资源和 `/api/health` 前端代理均真实可用；随后停止临时生产进程并恢复 `localhost:3200` 开发服务。
- 构建仅出现 Sentry/OpenTelemetry 动态依赖的已知 Webpack 警告，不影响编译、页面生成或运行；记录为后续依赖升级观察项。

验证：`/v2/login` 返回 200（8699 bytes），关键 CSS 返回 200（39734 bytes），API 代理返回 200 且数据库健康；本地开发服务恢复并监听 3200。

### 第 12 轮：六角色发布门禁与测试账号安全

- 修复本地烟雾套件仍硬编码 `test` 租户的问题：本地地址现在自动读取 API 环境中的 `PREVIEW_TENANT_SLUG`，并同步给 API 与浏览器测试。
- 非本地 smoke 强制显式提供 `E2E_PASSWORD`，避免发布脚本隐式依赖仓库内弱默认口令；缺失时在联网前拒绝执行。
- 本地仍兼容既有隔离测试账号，生产/远程目标不再接受默认密码；文档补充明确调用方式。
- 加固历史测试账号初始化脚本：仅允许非生产、`PREVIEW_MODE=true`、显式开关开启且数据库名为 `dianjie_v4_local` 时运行，并要求至少 12 位本地测试口令。
- 测试账号种子脚本移除硬编码密码及明文输出；环境模板增加默认关闭的授权开关。

验证：首次旧配置运行被 `yaohai-test` 保护层正确拒绝并定位为测试配置漂移；修复后无需额外参数即可自动识别本地预览租户。六角色 API 回归 28/28、浏览器回归 21/21、控制台与 5xx 错误 0；远程缺失密码和未授权测试账号初始化均安全拒绝，Shell/Node/TypeScript 静态检查通过。

### 第 13 轮：供应商库存精度、报损和盘点并发完整性

- 将供应商库存入库、盘点调整、报损和全量快照统一约束到 `Product.stock Decimal(10,2)` 的真实精度与容量，阻止流水保留 3 位而余额被数据库四舍五入形成账实分裂。
- 库存加减改用 `Prisma.Decimal` 运算，避免 JavaScript 浮点累加误差；入库后超过字段容量时整批事务回滚。
- 手工报损超过当前库存时返回 409，不再静默截断为“把现有库存全部报损”，因此用户提交数量与审计流水始终一致。
- 生产日期和到期日期从格式校验升级为真实日历校验，并禁止到期日早于生产日。
- 流水查询对类型和分页参数做严格校验，非法参数返回 400，不再把 `NaN` 或未知枚举传给 Prisma 形成 500。
- 全量库存清单拒绝同一文件内重复品名；按租户、供应商和品名加事务锁，两个并发快照不能创建同名重复 SKU。
- 同名历史 SKU 若已经不唯一则逐行明确失败，要求先整理主数据，避免随意命中其中一条改错库存。
- 分类创建纳入单条快照事务并串行化，商品或流水失败时不会残留孤立分类。

验证：供应商库存数据库端到端覆盖跨租户、非法精度、无效日期、倒置保质期、非法流水筛选、超库存报损、重复清单、并发同名建 SKU、并发入库/报损、逆序批量死锁、盘点/入库和快照/入库竞争，全部断言通过；并发同名导入最终只有 1 个 SKU、库存 6、初始流水 1 条，测试数据已清理。

### 第 14 轮：发票付款并发、现金余额与审计原子性

- 发票付款发起改为在事务内锁定发票，并重新计算 `PENDING + SUCCESS` 已预占金额；并发请求不能用同一份旧余额创建超额付款。
- 付款金额、方式和备注增加严格校验，金额最多两位小数且不得超出数据库容量，未知付款方式不再直接落库。
- 付款成功、失败和取消均锁定付款记录并执行原子状态迁移；同一银行回调重复送达返回幂等成功，不重复累计发票已付、现金流水或审计日志。
- 付款成功时同步锁定发票，使用 Decimal 累计并阻断任何超票金额；付款状态、发票累计、现金账户、现金流水和操作日志在同一事务提交。
- 现金账户余额写入增加数据库行锁，解决不同发票同时付款时两笔流水读取同一旧余额造成余额丢失的问题。
- 付款成功后凭证仍使用来源键幂等生成；核心资金事务不再依赖异步凭证是否成功。
- 未绑定供应商账号读取发票、待付款和付款历史时明确返回 403；店长等非财务角色不能查看全租户发票付款历史。
- 账期付款路径被发票付款锁定时，账期取消、付款创建和互斥审计必须整体成功，审计失败不再被静默吞掉。
- 新增仅限本地隔离库的发票付款数据库端到端脚本，覆盖越权、精度、超额预占、重复回调、跨发票现金余额竞争和故障恢复。

验证：100 元发票并发发起两笔 60 元付款，结果严格为一笔 201、一笔 409；补发 40 元后预占合计正好 100。60 元重复成功确认只产生一次累计和一次现金流水；40 元与另一张发票 30 元并发到账后，现金余额从 940 准确降至 870。强制 `OpLog` 插入失败时，20 元付款保持 PENDING、发票已付仍为 0、现金仍为 870、流水为 0；解除故障后重试成功，最终余额 850，测试凭证与业务数据全部清理。

### 第 15 轮：非订单付款申请的资金台账与并发一致性

- 付款申请金额、收款信息、附件、会计科目和付款账户增加严格输入边界；非法状态、分页和多余字段返回 400，不再把未知值传进数据库。
- 店长、厨师长和总厨仍可发起正常付款申请，但自定义会计科目只允许财务角色设置，且编码与名称必须同时填写。
- 创建付款申请与操作日志合并为同一事务；审计日志失败不会留下无审计来源的业务单据。
- 标记已付与通用审批引擎复用同一单据事务锁，并锁定单据行；重复点击或网络重试返回幂等成功，不会重复扣款。
- 付款申请、资金账户余额、现金流水和操作日志改为单个原子事务；任一步失败时付款标记、余额和流水整体回滚。
- 资金账户按用户选择精确路由：中国银行 1674、建设银行 3618 和库存现金分别匹配对应活动账户；未配置目标账户时阻断付款，不再静默漏账或误扣租户第一张银行卡。
- 资金余额运算复用带数据库行锁的现金台账服务，多个付款事件并发时不会丢失余额更新；付款 payload 保存资金账户和流水 ID 便于追溯。
- 已发生付款的凭证按来源键幂等补建；凭证短暂失败不重复扣款，重试“标记已付”会再次尝试补建并回写凭证 ID。
- 付款申请撤回与审批使用同一事务锁，状态迁移和操作日志原子提交；并发撤回一主一幂等，只有一条撤回审计。
- 新增仅限本地隔离库的付款申请完整性端到端脚本，覆盖权限、输入、账户路由、重复付款、凭证幂等、审计故障回滚恢复和并发撤回。

验证：100 元付款申请并发执行两次均得到可重试成功，其中一次明确 `duplicated=true`；中国银行余额只从 1000 降至 900，现金流水和凭证各一条。选择未配置的建设银行返回 409 且没有付款标记或流水。强制 `OpLog` 写入失败时 20 元付款、余额和流水全部回滚，解除故障后重试从 900 正确降至 880；30 元库存现金付款只把现金账户从 100 降至 70，银行余额保持 880。并发撤回只有一条审计。专项数据库端到端、API 60/60 测试、API 构建、Web TypeScript 和 134 页生产构建全部通过；预览服务已恢复。

### 第 16 轮：工资付款、资金台账与凭证恢复一致性

- 工资付款要求财务人员明确选择活动资金账户，现金付款只能选择现金账户，转账只能选择银行账户；招商银行付款还会校验账户绑定，不再隐式扣减租户第一张账户。
- 工资月份、金额、日期、流水号和明细文本增加严格边界校验；工资汇总改用 `Prisma.Decimal` 计算，避免浮点误差和超精度金额进入数据库。
- 付款前校验“应发 = 实发 + 社保 + 个税”，不平衡工资单拒绝付款；“其他扣项”因会计科目尚未确认而明确阻断并记录待定口径，避免生成借贷不平的凭证。
- 标记已付在工资单级事务锁内完成状态检查、资金账户锁定、余额扣减、现金流水和操作日志，任一步失败均整体回滚；并发点击和相同参数重试为幂等成功，不重复扣款。
- 已付款重试会核验原付款方式、日期、银行流水和资金账户；参数不同返回冲突，防止把历史付款悄然改写。即使原账户后来停用，仍可从已落账现金流水恢复并完成凭证补建。
- 工资付款凭证根据实际账户精确映射库存现金、中国银行 1674、建设银行 3618、招商银行 0001 等科目，不再统一记入笼统银行科目。
- 凭证生成短暂失败不会回滚已完成的真实付款，也不会在重试时再次扣款；相同参数重试会幂等补建凭证，并将对应的凭证失败告警标记为已解决。
- 财务端付款弹窗补充付款账户、余额和加载错误展示，账户与付款方式联动过滤，并在未选账户时阻止提交。
- 新增仅限本地隔离库的工资付款完整性端到端脚本，覆盖输入和账户约束、并发重复付款、借贷平衡保护、审计故障回滚、现金/银行精确路由以及凭证故障恢复。

验证：100 元应发、80 元实发、10 元社保和 10 元个税的工资单并发付款，银行余额只从 1000 降至 920，现金流水和凭证各一条，凭证贷方精确落入中国银行 1674 科目。强制操作日志失败时 20 元付款保持 APPROVED、余额和流水不变，解除后重试成功。30 元现金工资只把现金账户从 100 降至 70；15 元工资在强制凭证失败后保持已付款且银行只扣一次，重试成功补建凭证并关闭失败告警。专项端到端重复执行通过，API 60/60、API 构建、Web TypeScript 和差异格式检查全部通过。

### 第 17 轮：备用金申请、开支、退余和关账资金闭环

- 修复店长端把“备用金申请”写入通用审批单、财务端却读取专用备用金表的架构断链；店长申请现在直接进入同一套 `PettyCash` 状态机，并明确展示“待财务批准”，不再承诺后端并未实现的 3,000 元自动通过。
- 新增店长移动端备用金管理页，覆盖申请入口、状态跟踪、开支明细、录入/删除开支、自动计算应退余款和提交报账；店长抽屉同步改为“申请、录开支、月末报账”的真实入口。
- 财务批准与取消增加备用金级事务锁、幂等状态迁移和同事务操作日志；申请单、审批和审计任一步失败均不会留下半笔业务。
- 备用金发放要求明确选择活动资金账户，并校验现金、银行转账和招行账户类型；发放状态、账户余额、现金流水和操作日志同事务提交，并发点击只扣款一次。
- 发放凭证按实际账户映射库存现金、中国银行、建设银行或招商银行明细科目；凭证短暂失败不重复扣款，相同参数重试可幂等补建。
- 开支金额、日期、类别、附件和关联对象增加严格校验；开支必须属于备用金月份，同一备用金串行录入且累计不得超过批准额，提交报账后禁止继续增删。
- 店长提交的花销金额必须与已录开支逐笔汇总完全一致，退余款由“批准额 - 开支明细”确定；不再允许仅靠手填两个合计数完成报账。
- 财务关账若有退余款必须选择实际收款账户，系统在关账事务内增加资金余额并写收入流水；退余、状态和审计原子提交，并发关账只入账一次。
- 关账凭证使用实际收款账户，开支借记销售费用、退余借记现金/银行、全额贷记备用金；关账后的相同参数重试仅补凭证，不重复收款。
- 列表和详情补齐角色权限，供应商等无关角色不能读取或创建备用金；查询状态、月份和请求体多余字段均严格校验。
- 六角色浏览器发布门禁加入店长备用金管理/申请页和财务备用金页，后续发布会自动捕获白屏、500 和权限回归。

验证：100 元备用金并发发放两次均返回可重试成功，其中一次标记 `duplicated=true`，银行只从 1000 降至 900，资金流水和凭证各一条。强制操作日志失败时 20 元发放保持 APPROVED、余额和流水不变，解除后重试成功。100 元备用金并发录入两笔 60 元开支时严格一笔成功、一笔 409；50 元手填花销不能覆盖实际 60 元明细，正确报账后任何增删均被阻断。40 元退余并发关账只入账一次，银行从 780 回升至 820，关账凭证精确借记中国银行 40、费用 60；强制关账审计失败时状态、10 元退余和流水全部回滚，解除后重试成功。专项数据库端到端通过；API 60/60、Web 3/3、API 构建、Web TypeScript、135 页生产构建、六角色 API 28/28 和浏览器 24/24 全部通过，浏览器控制台及 5xx 为 0，临时账号、门店、资金和业务数据均已清理。

### 第 18 轮：资本支出、总部代付还款与资金台账闭环

- 资本支出付款不再隐式使用招行或租户第一张账户，要求财务明确选择银行、招行或库存现金及对应活动账户；现金和银行账户类型不匹配时阻断。
- 付款日期、方式、账户和流水号增加严格输入边界；相同参数重试为幂等成功，不同参数不能覆盖既有付款，历史已付款但缺失资金流水时拒绝伪造恢复。
- 支出付款在单据级事务锁内完成状态迁移、合同已付、项目已支出、账户余额、现金流水和操作日志，任一步失败整体回滚；并发点击只扣款一次。
- 合同支出申请按合同加事务锁并锁定项目、合同行，重新汇总待审批、已批准和已付款占用；两个并发申请不能共同穿透合同剩余额度。
- 支出申请增加严格类别、金额、文本和多余字段校验；已取消/已还清项目及非活动合同不能继续新增支出，申请和操作日志同事务提交。
- 审批、驳回和撤回共用支出级事务锁；审批/撤回同时发生时只能形成一个终态和一条终态审计，网络重试不会重复写日志。
- 总部代付付款凭证统一形成“借：其他应收款—门店代付，贷：实际现金/银行账户”，精确反映总部对门店的应收，而非误记供应商应付。
- 门店还款必须绑定项目关联门店、实际收款账户和唯一到账流水号；同一流水同参数重试幂等，不同参数冲突，且任何还款不得超过项目剩余应还。
- 还款记录、项目累计/还清状态、收款账户余额、现金流水和操作日志原子提交；收款凭证按实际账户借记并贷记其他应收款，与代付款形成闭环。
- 付款或还款的凭证短暂失败不逆转已发生的真实资金，但相同参数重试可补建凭证且不二次扣收款。
- 财务移动端和 PC 端付款入口新增方式、活动账户、余额、付款日和流水号选择；老板项目详情的还款入口新增实际收款账户和唯一到账流水号。
- 六角色浏览器门禁新增财务移动端及 PC 资本支出页面，持续捕获白屏、500 和权限回归。

验证：300 元合同已有 130 元占用时并发提交两笔 100 元申请，严格只有一笔 201、另一笔 409；审批和撤回并发严格一笔成功、一笔冲突，最终只有一个终态和一条终态日志。100 元支出并发付款只让银行从 1000 降至 900，项目和合同各只累计 100，现金流水和凭证各一条；强制操作日志失败时支出、项目、合同、银行和流水全部回滚，解除后重试成功。强制凭证失败时 10 元真实付款只扣一次，相同参数重试补建凭证。50 元还款并发提交只入账一次，银行精确增加 50，项目累计、现金流水和凭证各一份；错门店、重复流水改参数和超额还款均被阻断。专项数据库端到端、API 60/60、Web 3/3、API/Web TypeScript、135 页生产构建、六角色 API 28/28 和浏览器 26/26 全部通过，浏览器控制台及 5xx 为 0，临时触发器、账号、门店、资金和业务数据均已清理。

### 第 19 轮：资金账户、手工流水与跨界面登录完整性

- 资金账户创建和修改增加严格账户类型、名称、银行资料、备注、招行绑定和多余字段校验；非银行账户不能绑定招行实时账号。
- 同租户活动账户的名称、银行账号和招行实时账号按标识符事务锁串行化并阻止重复，两个并发同名创建严格只成功一笔。
- 账户创建、修改和停用均与操作日志同事务提交；强制审计写入失败时不留下无审计来源的账户。
- 非零余额账户禁止停用，避免账户从汇总消失后集团总资金被静默减少；零余额停用支持幂等重试且只写一条停用日志。
- 手工资金流水要求客户端持久 UUID 操作号，服务端按租户和操作号加事务锁；相同参数重试返回同一流水，不同参数不能覆盖。
- 手工流水不再接受客户端自定义 `refType/refId`，统一标记为 `MANUAL_CASHBOOK`，防止人工记录伪装成资本支出、发票付款等系统事件。
- 金额、方向、分类、日期、备注和查询分页/月份增加严格校验；金额最多两位小数且不得录入未来流水，非法筛选返回 400 而非 Prisma 500。
- 账户余额更新复用数据库行锁和 Decimal 资金台账服务；多个不同手工流水并发时逐笔形成准确余额快照，不再因读取同一旧余额而丢账。
- 手工流水、账户余额和操作日志原子提交；审计日志失败时流水与余额整体回滚，解除故障后原操作号可以安全重试。
- 真实招行内部转账和银行流水同步增加生产环境硬护栏：预览/开发环境即使误配功能开关也只返回 503，绝不调用银行；外部异常不再向客户端泄露内部错误。
- 内部转账凭证改为可等待、可捕获的创建流程，未来启用时能返回凭证告警；在实现持久银行请求日志和查询补偿前仍保持功能关闭。
- 修复财务资金页查询不存在的账期状态 `FAILED` 导致 500，改为真实状态 `OVERDUE`；账期列表严格校验状态与天数，并阻断总厨等无关集团角色读取全租户付款计划。
- 修复 v2 登录进入旧桌面资金台账时两套 localStorage 键不兼容造成的 401 和跳登录；访问令牌、刷新令牌和用户信息现兼容新旧页面，退出只清认证键而不清空整个站点存储。
- 浏览器门禁新增移动资金页和旧桌面资金台账页，并升级为任何 `console.error`、页面异常或 5xx 都直接阻断，不再把“页面能渲染但后台接口报错”误判为通过。

验证：同一手工操作号并发提交两次严格一笔 201、一笔幂等 200，账户仅从 100 增至 120且只有一条流水；随后 30 元收入和 40 元支出并发，最终余额精确为 110且最后余额快照一致。强制操作日志失败时 10 元收入、流水和余额全部回滚，解除故障后同操作号重试成功。账户创建审计失败整笔回滚；并发同名账户仅一个创建；非零余额停用返回 409，零余额停用重试仅一条审计。预览环境真实转账和银行同步均返回 503；非法 `FAILED` 账期状态返回 400、`OVERDUE` 返回 200。专项数据库端到端重复执行通过；API/Web TypeScript、135 页生产构建、六角色 API 29/29 和浏览器 28/28 全部通过，浏览器控制台及 5xx 为 0，测试触发器、账号、账户、流水和日志全部清理。

### 第 20 轮：付款计划审批并发、审计与界面口径一致性

- 付款计划审批改为显式动作输入，空请求、未知动作、多余字段、超长备注及无原因驳回均返回 400；资金审批不再把任意非 `approve` 值默认为驳回，也不允许靠空请求隐式通过。
- 审批和驳回共用账期级事务锁；审批状态、审批人/时间、驳回原因、结构化元数据和操作日志在同一事务提交，并发审批与驳回只能形成一个终态。
- 相同操作人、相同动作和相同原因的网络重试返回幂等成功，不重复写操作日志；与既有终态冲突的请求明确返回 409。
- 旧版独立驳回接口保留兼容，但改用相同事务状态机、强制填写原因并补齐操作日志，不再形成无审计的驳回记录。
- 删除未被调用且绕过租户、权限、事务与日志的旧审批服务函数，避免未来代码误接入不安全旁路。
- 审批通过通知仅在首次事务成功后发送，重复请求不重复通知；通知补充付款计划 `refId`，便于从消息追溯原单。
- 财务审批页面将驳回原因改为必填并前置禁用提交；移除“审批后自动转账”的错误承诺，统一显示“进入待付款流程，由财务核对并执行”，与当前自动银行付款停用状态一致。
- 浏览器发布门禁新增旧版 `/approval` 审批台，并继续将任何控制台异常、页面错误或 5xx 作为失败；首次开发服务器冷编译出现一次 Next RSC 降级提示，预热后完整重跑为 0 错误。
- 本地 `tsx watch` 对补丁工具写入未稳定收到文件事件，专项测试曾两次命中上一版进程；每次临时数据均被脚本清理，最终采用显式重启 API 后验证当前源码。后续本地验收不依赖热更新是否生效。

验证：空动作、非法动作、无原因驳回和门店角色审批全部被拒绝；同一账期审批/驳回并发严格一笔 200、一笔 409，幂等重试只保留一条终态日志。强制 `OpLog` 插入失败时账期保持 `PENDING_APPROVAL` 且审批人为空，解除故障后可安全重试；兼容驳回接口重复提交只写一次日志，审批通知数量与首次通过数量完全一致。专项数据库端到端重复执行并清理全部门店、供应商、账号、入库、账期、通知、日志和触发器；API 60/60、Web 3/3、API/Web TypeScript、135 页生产构建、七账号 API 29/29 和浏览器 29/29 全部通过，最终浏览器控制台与 5xx 为 0。

### 第 21 轮：供应商对账生成、审核与入库单唯一归属

- 数据库为 `ReconciliationItem.receiptId` 增加唯一约束，硬性保证一张入库单只能进入一张对账单；迁移会在发现历史重复时主动失败，要求财务先审计，不会猜测保留哪条或自动删账。
- 迁移前对本地库执行重复检查，结果为 0；迁移已在本地隔离库成功执行，Prisma schema、客户端和 41 条迁移状态一致。
- 手工生成对账单增加供应商级事务锁和数据库唯一约束双保险；同一批入库单的重复点击、并发请求，以及自动/手工流程交叉竞争都不能重复挂单。
- 供应商、期间和请求体改为严格校验，拒绝无效日历日期、倒置期间、超过 366 天和多余字段；金额使用 `Prisma.Decimal` 汇总，不再经 JavaScript 浮点计算。
- 对账业务单号切换到并发安全的 `BusinessSequence`，并在事务内以既有最大单号作为下限，避免计数式编号在删除历史或并发生成时撞号。
- 对账单、明细、入库单 `CONFIRMED → ACCOUNTED` 和操作日志在同一事务提交；明细或审计失败时对账单、入库状态和业务序列全部回滚。
- 审核通过/驳回增加对账单级事务锁、显式动作、驳回必填原因和严格备注边界；状态、审核时间、原因、结构化元数据与日志原子提交。
- 同一操作人的相同审核重试返回幂等成功且不重复日志；审核与驳回竞争只能形成一个终态，冲突请求返回 409。
- 对账列表权限收紧为集团财务/老板和已绑定供应商；店长、厨师长、总厨等角色不能读取全租户对账，未绑定供应商采用拒绝式隔离，供应商仅能查看自家单据。
- 对账页面补齐读取失败提示、生成和审核防重复点击、期间前置校验；驳回改为强制录入原因，不再让页面提交必然被后端拒绝的空原因。
- API 角色门禁新增店长读取对账 403，浏览器门禁新增旧版 `/reconciliations` 财务对账页。

验证：两笔合计 30 元的已确认入库并发生成两次，结果严格一笔 201、一笔 409，最终仅一张对账单、两条唯一明细、两张入库均为 `ACCOUNTED` 且只有一条生成日志；直接尝试把已对账入库挂入另一张单据被数据库 `P2002` 阻断。强制生成日志失败时 40 元入库保持 `CONFIRMED`、无对账明细，解除后重试成功；审核/驳回并发严格一笔 200、一笔 409，幂等重试仅一条审核日志；强制审核日志失败时状态和审核时间均回滚，解除后恢复。专项脚本恢复原业务序列并清理全部临时数据；API 60/60、Web 3/3、API/Web TypeScript、Prisma validate/migrate status、135 页生产构建、七账号 API 30/30、浏览器 30/30 全部通过，最终控制台与 5xx 为 0。

### 第 22 轮：供应商付款单、资金扣减与凭证恢复闭环

- 数据库为付款单的 `reconciliationId` 增加唯一约束，落实当前“一张对账单一次全额付款”的业务模型；同租户非空支付流水号同样唯一，阻断跨付款单重复引用真实流水。
- 迁移先把历史空白流水规范为 `NULL`，再检查重复对账单和重复有效流水；发现真实冲突会明确失败，不会自动删除或猜测保留账目。本地历史重复检查均为 0。
- 付款单创建改为对账单级事务锁和数据库唯一约束双保险；并发双击以相同参数返回同一付款单和幂等成功，不同金额、方式或备注明确冲突。
- 付款金额严格等于已审核对账单总额，使用 `Prisma.Decimal` 比较；付款方式只接受网银、支付宝、微信和现金，移除会触发数据库枚举错误的伪 `OFFLINE/CMB_AUTOPAY` 输入。
- 付款单编号切换到并发安全的 `BusinessSequence`，并以既有当月最大号为下限；付款单、对账单 `APPROVED → PAYMENT_GENERATED` 和操作日志同事务提交。
- 实际付款必须选择活动资金账户和真实付款日期；网银、支付宝、微信分别只能使用对应类型账户，现金只能使用现金账户；非现金付款必须填写真实支付流水号，未来日期被拒绝。
- 付款单、对账单 `PAYMENT_GENERATED → DONE`、账户余额扣减、资金流水和结构化操作日志在同一事务提交；任一步失败全部回滚，不再出现“业务显示已付但资金台账未扣”的断链。
- 付款执行增加付款单级和支付流水级事务锁；相同参数并发或网络重试只扣款一次，不同账户、日期、流水或备注不能覆盖历史付款。
- 付款凭证按实际出款账户精确贷记库存现金、中国银行、建设银行、招商银行或对应银行存款科目；凭证短暂失败不逆转真实付款，相同参数重试只补凭证、不再扣款。
- 付款列表权限收紧为财务/老板和已绑定供应商；店长等角色不能读取全租户付款，未绑定供应商明确拒绝，供应商只看自家数据。
- 财务付款页增加账户余额、付款日期、真实流水和付款备注；账户按付款方式联动筛选，未配置对应账户时阻止提交，提交按钮带防重复锁；对账金额只读，不能被页面改写。
- 浏览器发布门禁新增 `/payments` 页面，并新增仅限本地隔离库的付款完整性脚本，覆盖数据库唯一约束、角色隔离、严格输入、并发、两类审计故障和凭证故障恢复。

验证：同一 100 元对账单并发创建付款单严格一笔 201、一笔幂等 200，数据库只有一单、一条日志；并发执行实际付款两次均可安全返回，其中一次 `duplicated=true`，银行余额只减少 100，现金流水、操作日志和凭证各一条。重复流水号在另一付款单被 409 拒绝。强制创建日志失败时不留付款单且对账单保持 `APPROVED`；强制付款日志失败时付款单保持 `UNPAID`、对账单保持 `PAYMENT_GENERATED`、余额和流水不变；解除后均可安全重试。强制凭证失败时真实付款只扣一次，移除故障后同参数重试补建凭证。专项脚本清理全部账号、供应商、账户、付款、对账、流水、凭证、失败队列和触发器；API 60/60、Web 3/3、API/Web TypeScript、42 条空库迁移链、135 页生产构建、七账号 API 30/30、浏览器 31/31 全部通过，最终控制台与 5xx 为 0。

### 第 23 轮：供应商发票上传、入库单归属与审核闭环

- 发票列表和详情权限收紧为财务/老板与已绑定供应商；店长等无关角色不能读取全租户发票，未绑定供应商明确返回 403，供应商只能读取本供应商单据。
- 上传元数据改为严格校验：发票号码、金额精度与上限、税率/税额勾稽、非未来开票日期、备注长度、入库单数量和重复项全部有边界，多余字段和非法 JSON 不再落入 Prisma 500。
- 上传文件只接受一份非空 JPEG、PNG、WebP 或 PDF，大小上限 10MB；对象使用租户/供应商隔离键和私有缓存策略，数据库只保存稳定地址，读取时生成短期签名地址。
- 先完整校验元数据和文件再上传对象；数据库建单冲突或失败会删除本次新对象，相同发票的幂等重试也会清理冗余上传，避免对象存储孤儿文件。
- 发票创建增加发票号码与全部入库单的事务级 advisory lock；供应商、入库状态、归属和金额在锁内重新校验，只允许 `CONFIRMED/ACCOUNTED` 入库单且合计必须与发票金额精确一致。
- 发票、入库单关联和操作日志同事务提交；两个发票并发争抢同一入库单时只能成功一个，审计失败时建单及关联整体回滚，解除故障后可安全重试。
- 同租户发票号码的完全相同请求返回幂等成功，不同供应商、金额或入库集合明确冲突；数据库现有发票号码唯一约束继续作为最终防线。
- 审核改为显式通过/驳回动作、驳回原因必填和严格备注边界；审核在发票锁内重验入库集合、供应商、状态和金额，历史孤立或金额漂移发票不能被放行。
- 审核状态、驳回时解绑入库单和结构化操作日志原子提交；审核/驳回并发只能形成一个终态，相同操作人和参数重试幂等且不重复日志。
- 待开票/待付款入库范围统一收紧为 `CONFIRMED/ACCOUNTED`，草稿入库不再进入应付口径；供应商上传页自动带入勾选入库单精确合计与本地日期，并直接展示接口失败。
- 财务发票页清理加载错误恢复逻辑；浏览器发布门禁新增财务移动端、财务 PC 端及供应商发票页，API 门禁新增店长读取发票 403。

验证：同一 100 元入库单被两个发票并发争抢时严格只有一张成功；相同发票重试返回同一记录，直接重复号码被数据库唯一约束阻断。强制创建审计失败时不留发票且入库单不被占用，解除后重试成功；无关联入库单的历史异常发票审核被阻断。审核/驳回并发只形成一个终态，重复审核不增加日志；强制驳回审计失败时状态和入库关联全部回滚，解除后恢复。专项脚本两次通过并清理全部临时账号、供应商、发票、入库和触发器；未调用真实外部对象存储，仅对对象生命周期代码完成类型、构建和静态门禁。API 60/60、Web 3/3、API/Web TypeScript、135 页生产构建、七账号 API 31/31、浏览器 34/34 全部通过，最终控制台与 5xx 为 0，数据库残留检查为 0。

### 第 24 轮：发票分次付款、真实资金账户与凭证恢复闭环

- 发票付款增加客户端 UUID 操作号及租户内唯一约束；同一操作号的双击或网络并发重试返回同一付款，不同参数不能覆盖既有请求。
- 数据库为发票付款的租户内非空支付流水号增加唯一约束，并在迁移前规范空白值、审计历史重复；发现真实冲突会阻断迁移而非猜测删除。
- 付款确认新增实际资金账户关联；成功确认必须选择活动账户和真实业务日期，银行/招行、微信、支付宝、现金分别只能匹配对应账户类型。
- 非现金付款强制填写真实支付流水号，未来日期、重复流水和错误账户类型均被阻断；已成功付款的相同参数重试幂等，不同账户、日期、流水或备注明确冲突。
- 付款单、发票累计/付清时间、账户余额、资金流水和结构化操作日志在同一事务提交；找不到账户或写流水失败不再允许把发票标成已付。
- 付款成功使用付款单、发票、账户行锁及流水 advisory lock；同一付款并发确认只扣一次，不同发票同时从同一账户付款也能形成准确余额快照。
- 发票付款凭证按实际账户贷记库存现金、银行存款、支付宝或微信明细科目；原先微信/支付宝误落银行存款的共用科目映射已修正。
- 凭证生成改为等待式幂等补建；真实付款提交后即使凭证短暂失败，相同参数重试也只补凭证并关闭失败告警，不重复扣款。
- 历史付款兼容检查现有 `Payment` 来源凭证，避免升级后对旧记录重复建证；新发票付款使用独立 `InvoicePayment` 来源保持语义清晰。
- 失败确认要求原因且终态重试参数一致；取消仅允许待确认付款。失败或取消后释放发票可付款额度，成功终态不可被反向覆盖。
- 待付款接口用 Decimal 计算已付、待确认预占和可发起金额，返回私有发票的短期签名地址，并补齐严格查询参数与角色隔离。
- 财务移动端和 PC 端新增付款方式、防重复操作号及完整“确认成功/标记失败/取消”界面；成功确认必须选择真实账户、日期和非现金流水。
- 页面移除尚未启用证书的招行实时余额自动调用，改用系统资金台账活动银行账户汇总，避免只浏览页面就触发外部银行 502，也与实际出款数据源保持一致。
- API 门禁新增店长读取发票付款 403，浏览器门禁新增移动端和 PC 应付页；迁移在本地历史库和一次性空库均完整验证。

验证：相同操作号并发发起 60 元付款严格一笔 201、一笔幂等 200且返回同一记录，修改金额或用新操作号穿透剩余额度均被拒绝。60 元付款并发确认两次只扣款一次；随后 40 元与另一张发票 30 元并发确认，账户余额从 940 精确降至 870且发票累计准确。缺失流水、微信账户执行银行付款、修改付款日重试及跨付款复用流水均被阻断。强制操作日志失败时 20 元付款、发票累计、账户余额和资金流水全部回滚，解除后重试成功。强制 15 元凭证失败时真实资金只扣一次，相同参数重试补建唯一凭证且余额保持 835。专项脚本通过并清理全部临时数据与触发器；API 60/60、Web 3/3、API 构建、API/Web TypeScript、43 条空库迁移链、135 页生产构建、七账号 API 32/32、浏览器 36/36 全部通过，最终控制台、5xx 与数据库残留均为 0。

### 第 25 轮：招行后台任务默认拒绝与同步入账完整性

- 招行流水后台同步新增独立 `CMB_SYNC_ENABLED` 开关；仅生产环境且显式为 `true` 时才注册启动后首跑和 30 分钟定时任务，开发/预览环境即使误配也不访问银行。
- 账期自动付款同样升级为“生产环境 + 非预览 + 显式开关”三重条件；未开启时调度扫描不再枚举并调用到期付款，避免持续错误和误触外部服务。
- `.env.example` 与重建说明补齐三个真实银行动作的默认关闭口径：自动付款、内部转账、流水同步彼此独立，不能因配置其中一个而隐式打开其他能力。
- 资金流水新增租户、账户、来源类型、来源 ID 四字段唯一约束；同一外部/业务引用在同一账户最多生成一条，同时允许内部转账在两个不同账户各写一条合法借贷流水。
- 迁移在创建唯一索引前审计历史重复；发现冲突会明确阻断，不会自动删除或猜测保留账目，本地历史数据重复为 0。
- 单条招行流水入账提取为不访问外部服务的原子函数，按租户、账户和银行同步键加 advisory lock，并由数据库唯一约束作最终防线。
- 同步入账复用资金台账账户行锁和 Decimal 余额更新；多条不同银行流水并发写同一账户时逐笔形成准确余额和余额快照，不再用并发不安全的读后写 Number 算法。
- 资金流水、账户余额和结构化操作日志同事务提交；审计失败时流水与余额整体回滚，解除故障后同一银行序列可以安全重试。
- 同步循环现在区分“首次写入”和“并发时已由另一工作进程写入”，不会把并发重复误报为新入账；数据库唯一冲突会回读既有流水并返回幂等结果。
- 每条自动同步流水记录账户、银行日期、序列、方向、金额、参考号和资金流水 ID，解决原实现只有一条流水但没有独立操作审计的问题。
- 新增只使用本地伪银行交易对象的专项脚本，明确验证关闭时不会访问外部银行；整个长时任务仍未调用真实招行接口。

验证：同一 25 元伪银行流水并发入账两次只成功一笔，账户从 100 精确变为 125且两个调用返回同一资金流水；随后 5 元和 10 元出账并发，余额及最后快照均为 110。相同业务引用可在内部转账两端各写一笔，但同一账户第三次写入被数据库 P2002 阻断。强制同步审计失败时 20 元入账、资金流水和余额整体回滚，解除后重试成功并将余额准确更新至 130；每一条最终同步流水均有对应审计。专项脚本通过且未访问银行，临时账号、账户、流水、日志和触发器全部清理；API 60/60、API 构建、API TypeScript、44 条空库迁移链、七账号 API 32/32、浏览器 36/36 全部通过，最终控制台、5xx 与数据库残留均为 0。

### 第 26 轮：入库确认后的凭证、应付与对账自动补偿

- 抽取统一的入库财务派生服务，正常收货、重复收货、独立入库确认和 24 小时自动收货不再各自维护一套不完整逻辑；凭证与应付/对账两个分支独立执行，任一短暂失败不会阻断另一分支或逆转已经发生的真实收货。
- 收货凭证从不可等待的 fire-and-forget 改为可等待、可捕获的幂等创建；使用 `Receipt` 来源唯一键，重复请求和并发恢复最多生成一张凭证，并在恢复成功后关闭对应凭证失败告警。
- 应付计划、自动对账、入库 `ACCOUNTED` 状态和结构化操作日志现在同一事务提交；强制日志写入失败时全部财务派生回滚，解除故障后可安全重试，不留下无审计的半笔账。
- 已完成订单过去不会再进入 `PENDING_CONFIRM` 扫描，导致派生失败永久依赖人工重试；每日任务新增最近 30 天的缺口修复，手工与自动确认入库均覆盖。
- 修复扫描在数据库层直接选择缺少账期、对账、入账状态或凭证的记录；凭证缺口使用参数化 anti-join，避免先取固定 200 张完整记录后再过滤造成后续缺口长期饥饿。
- 修复任务单次有界且逐单隔离；某张入库修复失败只记录明细，不阻断后续入库、报损自动审批和周期凭证任务，多实例并发运行仍由业务锁与唯一约束保证幂等。
- 防御性限制只有 `CONFIRMED/ACCOUNTED` 入库可以生成派生，作废或拒收入库不会被补建凭证；总仓 `HEADQ_WAREHOUSE` 继续按内部调拨口径跳过外部应付、对账和自动凭证。
- 入库完整性回归脚本同步补齐凭证与失败队列清理，避免测试通过后留下孤立凭证；本轮发现的两条本地测试孤立凭证已清理，复跑确认残留为 0。
- 新增仅限本地隔离库的派生恢复专项脚本，覆盖凭证/财务分支互不阻断、审计故障原子回滚、每日修复、失败队列关闭、并发幂等和总仓内部调拨短路。

验证：强制入库财务审计失败时凭证仍独立成功，但账期、对账和 `ACCOUNTED` 状态全部不落库；解除故障后每日修复一次补齐且只生成一条派生日志。强制凭证插入失败时账期与对账正常完成并写入失败队列，解除后每日修复补建唯一凭证并关闭告警。两个并发恢复调用后凭证、账期、对账和派生日志各一份；总仓入库不生成外部应付派生。专项脚本、配送自动/手工竞争、入库确认故障恢复均通过；API 60/60、Web 3/3、API/Web TypeScript、135 页生产构建、七账号 API 32/32、浏览器 36/36 全部通过，最终浏览器控制台与 5xx 为 0。

### 第 27 轮：账期提醒持久幂等与日任务故障隔离

- 系统通知新增租户内可选业务幂等键；未指定键的历史通知保持原有可重复语义，账期提醒使用稳定的“账期 ID + 提醒类型”键并由数据库唯一索引兜底。
- 同一账期的 T-3 与 T-1 提醒拥有独立幂等键；两个应用实例并发扫描时最多一方写入通知并触达外部通道，另一方读取唯一冲突为幂等成功，不重复发送。
- 提醒流程改为先持久化系统内通知，再推进 `notified3Days/notified1Day` 标记；若通知写库失败，错误明确上抛且标记保持未发送，不会出现“无通知却永远不再扫描”。
- 通知已经落库但标记更新失败时，下一次扫描命中同一幂等键，不再重复触达，只补齐账期标记，关闭原先插入通知与更新标记之间的崩溃窗口。
- 带幂等键的通知改为 fail closed：数据库异常时不再继续发送企微后又把任务视作成功；普通业务通知继续保留既有 best-effort 行为，避免无关模块发生语义突变。
- T-3、T-1 扫描改为逐单 try/catch，并分别统计成功与失败；单张提醒异常不再中断整轮日任务，后续自动付款、逾期、自动收货、派生修复、报损和周期凭证仍会继续。
- 日任务首轮只查询账期 ID，由幂等处理函数在执行前重新读取状态、供应商和门店，减少无用关联查询，并避免使用扫描开始时的过期状态生成提醒。
- 新增第 45 条迁移和仅限本地隔离库的提醒完整性脚本，覆盖并发去重、通知失败不推进标记、标记失败后的无重复恢复，以及 T-3/T-1 相互独立。

验证：同一账期并发调用两次 T-3 提醒只落一条通知并最终标记完成。强制账期标记更新失败时通知保留一条而标记为 false，解除故障后重试命中 `duplicated=true` 并只补标记；强制通知插入失败时通知为 0、T-1 标记为 false，解除后恢复成功。专项脚本通过并清理全部临时门店、账号、入库、账期、通知、函数和触发器；45 条迁移在历史库与空库均一致。API 60/60、Web 3/3、API/Web TypeScript、135 页生产构建、七账号 API 32/32、浏览器 36/36 全部通过，最终浏览器控制台与 5xx 为 0。

### 第 28 轮：企微触达预占、并发去重与失败恢复

- 修复第二套触达层的经典“先查 sent、再发企微、最后写日志”并发窗口；原实现中两个应用实例会同时查不到已发送记录，并对同一用户重复触达。
- 外部通道调用前新增数据库事务级投递预占：按租户、用户、事件键和通道取得 advisory lock，在锁内检查最近 5 分钟的 `processing/sent`，唯一胜者先写 `processing` 日志后才允许调用企微。
- 并发失败方看到 `processing` 即按频控抑制，不需要等外部网络请求结束；多进程、多线程和网络重试共用同一数据库仲裁，不依赖单机内存锁。
- 企微成功后把预占更新为 `sent`；企微失败则更新为 `failed`，失败记录不阻断下一次重试，避免一次临时网络错误永久吞掉业务通知。
- 若企微已经成功但完成日志更新失败，系统保留 `processing` 而不会错误改成 `failed`；当前 5 分钟窗口继续抑制重发，降低“外部已收、内部却认为失败”造成的二次触达风险。
- 进程在预占后崩溃时，`processing` 只冻结当前 5 分钟频控窗口；超过窗口可重新预占恢复，不会形成永久死信。
- `bypassFrequency` 的紧急通知语义保持不变，显式绕过时允许重复预留；用户偏好、静默时段和无企微账号的既有路由不变。
- 抽取可独立验证的预占和完成函数，并新增仅限本地隔离库的 20 路并发专项脚本，覆盖单胜者、sent 抑制、failed 重试、stale processing 恢复、紧急绕过和预占写库失败。

验证：20 个并发调用严格只有一个获得投递预占，数据库只有一条事件日志；完成为 sent 后窗口内再次预留返回 null。标记 failed 后立即可取得新预留；把 processing 人工回拨到 6 分钟前后可恢复；两个显式 bypass 调用均成功预留。强制投递日志插入失败时不留下占位且错误明确上抛。专项脚本清理账号、日志、函数和触发器后通过，API 60/60 与 API TypeScript/构建通过。

### 第 29 轮：公开申请与匿名错误上报边界收敛

- 公开账号申请改为严格白名单输入，租户标识、关联实体 ID、姓名、密码和申请说明均设置格式或长度上限，未知字段直接拒绝，避免匿名请求携带任意载荷进入业务层。
- 匿名申请增加每来源每小时 5 次限流；供应商和门店公开候选列表增加每分钟 60 次限流及严格查询参数，门店列表不再暴露已停用门店。
- 同租户、同手机号申请使用数据库事务级 advisory lock 串行化；锁内重新检查正式账号和待审批申请，多实例并发不再产生重复待审批记录。
- 供应商员工和门店角色关联对象在同一事务内按租户及启用状态校验，避免跨租户 ID、已停用对象或校验后状态竞争进入申请记录。
- 匿名更新器错误上报由任意 JSON 改为固定字段白名单，消息、堆栈、版本、平台、错误码和 User-Agent 都有上限，并增加每分钟 20 次限流，降低日志注入和磁盘放大风险。
- 新增仅限本地预览隔离库的公开接口专项脚本，同时验证并发单记录、严格字段、匿名限流、公开查询收敛和更新器日志白名单。

验证：5 路并发匿名申请中 4 个合法请求和 1 个携带伪造字段的请求，最终严格只有一条 201 和一条待审批记录，其余全部被 400 拒绝；第 6 次匿名申请返回 429。非法租户标识、多余查询字段和任意对象错误上报均被拒绝，合法错误上报返回 204。专项脚本清理临时申请后通过；API 60/60、API TypeScript/构建及 `git diff --check` 通过。

### 第 30 轮：账号邀请单次消费与全链路原子审计

- 修复邀请接受流程的并发重复消费窗口：同一邀请先按邀请 ID 取得数据库事务级 advisory lock，再在锁内重新读取撤销、消费和过期状态；两个不同手机号同时提交时只有一个能创建账号。
- 账号创建、`consumedAt/consumedByUserId` 写回和结构化操作日志改为同一事务；移除原先先消费、事务后补消费人、再事务外写日志的三段式半完成风险。
- 接受邀请时在事务内重新验证租户活动状态，以及关联门店、供应商的租户归属和启用状态；门店角色同时初始化兼容字段 `storeId` 与正式多店字段 `storeIds`。
- 邀请撤销与邀请接受使用同一把业务锁；二者并发时严格只能形成“已撤销”或“已消费”一个终态，不再可能撤销后仍创建账号或消费后再撤销。
- 邀请创建和撤销都补齐同事务结构化操作日志；若日志写入失败，业务状态整体回滚，不留下无审计邀请或账号。
- 创建和接受请求改为严格白名单，邀请、门店和供应商标识设置格式/长度约束；公开邀请详情增加每分钟限流，创建邀请不允许绑定停用实体。
- 新增仅限本地隔离库的邀请完整性专项脚本，覆盖不同手机号并发消费、强制审计失败原子回滚与恢复、撤销/接受竞争、创建审计和严格输入。

验证：同一邀请两个手机号并发接受严格一条 201、一条 400，数据库只有一个账号、一个消费人和一条激活日志。强制激活审计触发器报错时接口返回 500，账号为 0、邀请仍未消费；移除故障后原请求成功且只形成一套记录。撤销与接受并发严格一方成功、一方 400，邀请最终状态与账号存在性一致。专项脚本清理全部临时邀请、账号、日志、函数和触发器后通过；API 60/60、API TypeScript/构建及 `git diff --check` 通过。

### 第 31 轮：账号申请决策、供应商开户与编号原子化

- 账号申请审批与拒绝按申请 ID 使用同一数据库事务级 advisory lock；重复审批及审批/拒绝并发只能有一个胜者，后到请求明确返回已处理。
- 审批中的关联门店检查、手机号占用检查、供应商员工归属校验、供应商老板开户、账号创建、申请终态和结构化操作日志统一到一个事务。
- 修复供应商老板审批原先在事务外创建供应商的问题；后续账号唯一冲突或审计失败不再留下无负责人、无已批准申请的孤立供应商。
- 供应商编号新增租户级事务锁，锁内按所有规范 `SUP` 编号计算真实数字最大值；两个不同申请并发审批不会争用同一编号，也避免字典序在 `SUP999/SUP1000` 处倒退。
- 店长/厨师长审批只允许绑定启用门店，并同步初始化 `storeId/storeIds`；供应商员工只允许绑定本租户启用供应商。
- 申请列表查询、审批/拒绝请求及路径标识补齐严格 schema；非法状态值和多余写入字段返回 400，而不是传入 ORM 或被静默忽略。
- 新增仅限本地隔离库的申请决策完整性脚本，覆盖同申请重复审批、审批/拒绝竞争、审计故障全回滚、故障恢复、不同申请供应商编号并发及严格查询。

验证：同一供应商老板申请并发审批严格一条 200、一条 400，只生成一个账号、一个供应商和一条决策日志。审批与拒绝同时执行只形成一个终态，若拒绝胜出则账号和供应商均为 0。强制操作日志失败时接口 500，申请保持待审批且账号、供应商均为 0；解除后重试成功。两个不同申请并发审批均返回 200且供应商编号互不相同。专项脚本清理所有临时申请、账号、供应商、日志、函数和触发器后通过；API 60/60、API TypeScript/构建及 `git diff --check` 通过。

### 第 32 轮：用户管理权限提升阻断与原子审计

- 修复普通管理员可在用户创建或编辑接口直接授予 `SUPER_ADMIN` 的权限提升漏洞；只有现有超级管理员可以创建超级管理员，普通管理员也不能修改、禁用或重置超级管理员。
- 用户列表、创建、编辑和重置密码全部改为严格 Zod 白名单；角色与状态只接受数据库已知枚举，姓名、邮箱、手机号、密码和实体 ID 均设置格式及长度边界，未知字段明确返回 400。
- 严格化后逐一核对 Web 调用方，兼容旧用户编辑页随请求回传只读邮箱的行为；邮箱现在执行格式与租户内唯一性校验后可原子更新，避免安全收敛造成合法编辑回归。
- 店长、厨师长、主管角色强制绑定本租户启用门店；供应商负责人和员工强制绑定本租户启用供应商，并同步维护 `storeId/storeIds`，避免创建可登录但没有业务边界的悬空账号。
- 用户创建按租户和手机号/邮箱取得事务级身份锁；锁内复核唯一性，并以数据库唯一约束兜底，重复并发创建不会生成两套账号。
- 用户创建、资料/角色修改、启停和密码重置均把业务变更与结构化操作日志放在同一事务；用户级锁保证修改、启停和重置竞争使用最新角色与状态执行权限判断。
- 创建或更新时若操作日志失败，账号创建或资料变更整体回滚；不再出现用户已经生效但审计缺失的状态。
- 新增仅限本地隔离库的用户管理完整性脚本，覆盖管理员提权攻击、超级管理员保护、严格输入、角色绑定、创建/更新审计故障回滚与恢复，以及超级管理员合法授权路径。

验证：普通管理员创建或晋升超级管理员、重置或禁用超级管理员均返回 403，数据库没有非法高权账号；非法筛选、伪造字段和缺少供应商绑定均返回 400。强制用户操作日志失败时新账号为 0且目标用户名保持原值，解除后创建和更新分别成功且有唯一日志；超级管理员创建测试超级管理员成功。专项脚本清理全部临时账号、日志、函数和触发器后通过；API 60/60、API TypeScript/构建及 `git diff --check` 通过。

### 第 33 轮：认证输入、租户绑定与密码变更原子化

- 登录请求改为严格白名单，账号、密码和租户标识设置长度/格式边界；未知字段不再被静默接受，避免认证日志与实际参与校验的字段不一致。
- 登录成功后的最后登录时间与结构化登录日志放入同一事务；审计写入失败时不会单独推进活动时间，也不会向客户端签发一个被系统记为失败的登录响应。
- 刷新令牌请求增加每分钟 30 次频控、严格单字段 schema 和 4096 字节上限；账号查询同时绑定令牌内用户 ID 与租户 ID，并复核账号及租户活动状态。
- 登出请求收紧为可选 refresh token 单字段白名单，阻止无关载荷进入令牌撤销流程。
- 自助改密增加每小时 10 次频控、严格新旧密码边界和用户级事务锁；锁内重新读取活动账号并校验旧密码，密码更新与操作日志同事务提交。
- 两个使用相同旧密码的并发改密请求串行执行；第一个成功后第二个基于最新密码明确返回 401，不会互相覆盖或产生两条成功但未知最终密码。
- 新增仅限本地隔离库的认证完整性脚本，覆盖严格登录/刷新/登出、登录审计故障回滚、刷新租户绑定、改密审计故障回滚与恢复、并发改密单胜者。

验证：强制登录日志失败时接口 500且 `lastLoginAt` 保持空，解除后登录及正常刷新成功；登录、刷新和登出的多余字段均被 400 拒绝。强制改密日志失败时原密码仍有效，解除后恢复成功；随后两路并发改密严格一条 200、一条 401，最终密码与成功请求一致且只有两条成功改密日志。专项脚本清理临时账号、日志、撤销令牌、函数和触发器后通过；API 60/60、API TypeScript/构建及 `git diff --check` 通过。

### 第 34 轮：发布门禁扩面、限流节奏与残留审计

- 浏览器发布门禁新增旧版 `/users` 和新版 `/v2/me/team` 两个老板用户管理入口，确保本轮严格 schema 与权限调整不会造成页面白屏、加载失败或后台 5xx。
- 扩展后 38 项浏览器流程在 Next 开发模式下因双请求机制会共享同一测试 IP 并超过全局每分钟 200 次保护；门禁改为在最后一个供应商角色前自然冷却 65 秒，而不是放宽服务端限流。
- 冷却行为只改变自动测试节奏，任何真实 429、控制台错误、页面异常或 5xx 仍会令发布门禁失败；可通过环境变量显式调整，生产服务配置不变。
- 完整执行根工作区构建，Prisma Client、API TypeScript 和 Web 生产构建全部通过，135 个页面生成且 standalone 产物存在。
- 45 条迁移在一次性空数据库从零完整部署，随后 `migrate status` 和 schema diff 均为零；临时数据库自动删除，本地主库迁移账本保持最新。
- 数据库残留审计覆盖本轮测试邮箱、申请、供应商、故障触发器、故障函数和迁移临时库；所有计数为 0。工作树仅保留用户原有未跟踪库存审计表，未被加入提交或修改。

验证：七账号 API 门禁 32/32 通过；首次清缓存后的浏览器冷启动出现一次 Next RSC 回退并按规则阻断，预热重跑 36/36、控制台 0。加入用户管理两页及限流冷却后最终浏览器 38/38、控制台与 5xx 为 0。API 60/60、根生产构建、45 条空库迁移链、本地主库迁移状态、残留审计和 `git diff --check` 全部通过。

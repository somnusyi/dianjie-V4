# 滇界 V4 统一发布候选审计

更新时间：2026-07-26 07:32 CST

## 结论

- 新统一候选分支为 `integration/20260725-unified-rc`，从生产安全收敛基线
  `b525807d7f7f75ac1d0325b399e7a05e3ae5f3ff` 创建。
- 生产归档 `a6d64c9`、`main@f5a4192` 和安全基线均包含 63 条迁移；旧
  `release/20260715-p0@8467043` 只有 56 条，不能作为统一候选的直接底座。
- 旧 release 相对安全基线共有 77 个提交：9 个已 patch-equivalent，68 个需分类。
  本轮没有批量 cherry-pick，而是选择一个可独立验证的低风险输入安全批次手工移植。
- 生产门禁继续保持 `LOCKED`。本轮未连接生产、未执行生产脚本、未部署、未写生产数据库。

## 基线与分叉证据

| 用途 | Git 引用 | 提交 |
| --- | --- | --- |
| 已知生产归档 | `a6d64c9` | 登录页有效会话自动进入 |
| GitHub main | `origin/main` | `f5a4192` |
| 生产安全收敛 | `origin/stabilize/20260724-production-safety` | `b525807` |
| 历史夜间 release | `origin/release/20260715-p0` | `8467043` |
| 新统一候选 | `integration/20260725-unified-rc` | 基于 `b525807` |

`b525807...8467043` 的左右提交数为 `80 / 77`；`git cherry` 结果为 9 个等价
补丁和 68 个非等价补丁。历史 release 的夜间报告也属于补丁内容，因此不能用“原提交能
否直接 cherry-pick”代替代码语义判断。

## 多 AI 对账

### Kimi：生产变更台账

Kimi 在只读 worktree 中核对了 `a6d64c9..f5a4192`：

- 仅有 3 个提交：1 个部署记录、1 个重复商品档案合并脚本、1 个总厨通知脚本；
  没有 API/Web 运行时代码、Prisma schema 或迁移变化。
- 生产归档和 main 均有 63 条迁移。
- 已执行或疑似执行的一次性数据脚本必须视为历史审计证据，不得进入自动发布流程。
- `notify-chef-confirm-0724.ts` 是否实际执行、当前生产数据库事实、PM2/ECS 健康状态、
  生产配置开关等无法由 Git 证明，统一记为 `UNKNOWN`。

其中“疑似执行”不能当成生产事实。本报告只据此设置禁止自动重跑的安全门禁；是否执行仍
需要生产只读日志或人工记录确认。

### Qwen：旧 release 独立分类

Qwen 报告覆盖 77 个提交：9 个 patch-equivalent，并把其余提交初分为约 40 个可移植、
约 20 个需基于安全线手工重做、约 8 个业务规则阻塞。由于其执行环境未直接运行 Git，
这些数量仅作第二意见，不作为自动合并依据。

Codex 使用本地 Git 对 68 个非等价补丁重新做了代码级预检，得到：

- 代码补丁可干净应用（仍需语义复核）8 个：
  `bbccc2d 133105c e054618 c33c555 080c927 f4b4287 0058082 86d6041`
- 仅夜间报告文档 7 个：
  `e46a646 e902b61 131c2fc ed87690 279db9f 45f3cfb 8467043`
- 需手工对照安全线 53 个：
  `9e023d4 ff26b2a 589fa63 b660328 67411bc e17eb0e 7be48fb 4d997e3 80d3f3d`
  `26caff7 3d18f1c b40c8b8 1f0c1aa a315914 a684937 4058e84 6c0e4d0 3553ed9`
  `f8373ee 9af381c 314c4a1 c00cfb3 1825966 e265583 f0af96d fc35f6c 916d4e0`
  `cf7621f 1457f3f 23c2df6 d466067 368c88d f589337 fec61d6 db0ba2d dea5f87`
  `3bc0a9d ba801dd 5a46231 59b0724 cf62c1d f16ae0a 56b4316 7e1874c 03e550d`
  `6532acc 410644a 3b2e772 c22ac2a 405192c 959e8a2 9ce08f6 5380e4b`

以上“干净应用”只表示代码上下文不冲突，不表示业务语义已经批准。补送、报损收货、
已确认入库单更正和盘点状态机仍不得由 AI 自行决定。

## 本轮实现：上传对象后缀安全

来源：release 提交 `0058082b8bd75eb68ebdd655a0455647274efab8`。

手工移植而非 cherry-pick，保留安全线已经存在的 `inventory-counts` 上传类别及
`category/tenantId/objectPath` 签名隔离：

- 上传对象扩展名只由已通过白名单的 MIME 决定，不再信任用户文件名。
- 明确映射 JPEG、PNG、WebP、GIF、MP4、MOV、WebM、M4V、3GP、PDF。
- 未知 MIME 防御性回退为 `.bin`。
- 新增 11 个单元用例：覆盖 10 个允许 MIME 的精确映射和 1 个未知 MIME 的 `.bin`
  防御性回退。上传逻辑不再读取文件名扩展名，因此用户提供的多重或伪造扩展名不会进入
  对象 key。

## 验证记录

- API 全量单元测试：28 个文件，163/163 通过。
- 上传后缀专项测试：11/11 通过。
- 本地 PostgreSQL `dianjie_v4_ci`：63 条迁移，`migrate status` 为 up to date。
- 供应商隔离/分页 PostgreSQL 集成测试：26/26 通过。
- API TypeScript 编译：通过。
- Web `tsc --noEmit`：通过。
- API 标准 build（含 Prisma Client generate）：通过。
- `git diff --check`：通过。
- 变更文件名和新增行高置信敏感信息检查：通过。

## Qwen 交叉复核

Qwen 以禁用扩展和 MCP 的安全模式读取完整 `git show` 后给出 `PASS`，确认 MIME
白名单映射、租户路径结构和继续锁定生产门禁的结论。

复核意见处理如下：

- 接受：原报告声称测试覆盖“大小写/多重扩展名欺骗”不够精确，已改为测试实际覆盖范围。
- 非阻塞：可在后续增加 OSS mock 的上传路由集成用例，直接断言最终对象 key。
- 驳回计数误判：映射表为 4 个图片 MIME、5 个视频 MIME、1 个 PDF MIME，共 10 个；
  加未知 MIME 回退用例后确为 11 个测试，不是 12 个。
- `inventory-counts` 为 safety 父提交既有能力，本轮通过当前完整文件和父提交对照确认
  未回退；报告没有声称本提交新增该类别。

## 生产与数据脚本门禁

- 任何 `correct-*`、`merge-duplicate-archives-*`、`notify-chef-*-0724` 等一次性脚本均不
  自动执行。
- `notify-chef-confirm-0724.ts` 的生产执行状态为 `UNKNOWN`；确认前不得运行。
- 旧 release 缺少的 7 条迁移不得通过 `db push` 补齐；统一候选继续继承安全线完整的
  63 条迁移历史。
- 生产门禁只有在统一候选通过交叉复核、明确生产现状且人工授权后才能解锁。

## 下一批建议

1. 对剩余 7 个代码可干净应用的候选逐个做语义 diff，优先纯输入校验、稳定分页和测试。
2. 对 53 个冲突候选按“供应商查询 → 商品管理 → 订单快照/改单 → 收货事务”分批手工重做，
   每批保留独立测试与来源提交。
3. 业务规则阻塞项只记录问题，不改代码。
4. 统一候选未完成前，Kimi/Qwen/Codex 都只向各自任务分支提交，不推 main，不部署生产。

## 第一条并行功能集成列车（2026-07-25 14:12）

三条任务都从环境基线 `466592e` 独立开发，并已无冲突移入统一 RC：

| 任务 | 任务分支提交 | 统一 RC 提交 | 结果 |
| --- | --- | --- | --- |
| 供应商订货/配送查询 | `bcf936c` | `9e50bd7` | 既有能力满足，新增 8 个查询回归 |
| 供应商商品回归 | `3f215f7` | `501ce79` | 门店下单页补商品编码，新增 10 个筛选测试 |
| 收货事务/并发幂等 | `2a59698` | `5d498a2` | 唯一竞争返回原入库单，新增事务回滚测试 |

集成复核结论：

- 订货与配送仍为两类独立单据和 API；查询回归覆盖日期、商品名称/编码、分页、供应商隔离
  及历史配送快照。
- 商品分类、批量停售/恢复、批量分类、图片上传/门店展示、操作人/时间已有能力未重做；
  本轮只修明确缺失的门店下单编码展示。
- 收货主事实仍以同一数据库事务提交；财务派生继续采用既有幂等补偿，不把外部/派生失败
  伪装成收货失败。

列车验证：

- API 单元 29 文件、173/173。
- PostgreSQL 集成 17 文件、102/102。
- API build、Web tsc、Web 6 文件 22/22 测试。
- Web production build 142 页，安全恢复开发态后无 `BUILD_ID`。
- 390×844 浏览器烟测无横向溢出或浏览器 warning/error。
- diff、敏感信息和远端提交核对通过。

生产门禁继续 `LOCKED`；此列车不构成部署授权。

## 第二条并行功能集成列车（2026-07-25 14:48）

| 任务 | 任务分支提交 | 统一 RC 结果 |
| --- | --- | --- |
| 订货快照/改单/实发 | `5bb3685` | 新增完整数据库回归，运行逻辑审计通过 |
| 分类导入/停售审批 | `4988878`、`0aa04c2` | 阻断批次撤回绕过审批，并收敛事务外竞态 |
| 库存分页与隔离 | `7a67248`、`46bf10e`、`4743049` | 首版退回；最终真实分页、稳定元数据和移动端加载更多 |
| 旧采购页撤回契约 | `6b840fa` | 补必填原因、状态/角色限制和错误反馈 |

Codex 集成复核追加了两项修正：

- 库存分页后的搜索/分类必须由服务端覆盖全部供应商商品，不能只过滤已加载 50 条；
  API 新增多词名称/编码/规格搜索和分类条件，Web 对快速查询使用请求序号防止旧响应覆盖。
- 批次撤回并发测试用显式锁定握手替代固定 sleep，确保回归不依赖机器时序。

最终统一门禁：运行时契约、冻结离线安装、API 单元 173/173、PostgreSQL 集成
129/129、API build、Web tsc、Web 24/24、Web production build 142 页、开发态恢复、
diff 和敏感信息检查全部通过。候选 HEAD 为 `f672d66`；生产 gate 继续 `LOCKED`。

## V5 内部供应链第三条集成列车（2026-07-25 22:15）

本列车以内部供应链需求基线 v0.2 为准，只把逐项审查、冲突人工解决并复验的提交移入
`integration/20260725-unified-rc`：

| 能力 | 任务分支精确提交 | 统一 RC 提交 | 结果 |
| --- | --- | --- | --- |
| 商品筛选 CSV 导出 | `b737e4c` | `546591f` | 过滤语义一致、10,000 行硬上限、CSV 注入防护 |
| 内部角色与跨店只读 | `9334aee` | `116b6b3` | `SUPPLY_CHAIN` 租户内跨店只读，财务字段/入口与写操作拒绝 |
| 收货/订单查询分页 | `6c101ba` | `4f7e893` | 收货关键字/业务日期/分页，订单首次快照与当前主数据同时搜索 |
| 商品变更精准通知基础 | `cf5bb98` | `44ab080` | 逐人系统消息、exact `toUsers` 企微、幂等与失败日志 |

### 人工冲突处理

- `orders.ts` 同时保留内部角色的 `supplyDataReadScope` 和 Qwen 的首次提交快照搜索；
  快照 SQL 使用参数化查询且先限定 tenant，最终 Prisma 查询继续叠加门店/外部供应商 scope。
- `receipts.ts` 同时保留服务端关键字、业务日期、稳定分页和商品快照 DTO；内部供应链响应
  继续剥离 paymentSchedule、invoice、临时银行及财务核对字段。
- Kimi 精准通知 runner 因任务包指定的测试文件缺失退出 2；失败证据保留。Codex 补上
  确定性无网络测试、操作者 tenant 限定和禁止角色广播退化后，才推送并集成。

### 验证

- 商品导出：API 41/41、Web 14/14、API build、Web tsc；1440×900 浏览器验证筛选请求、
  单行结果和控制台零错误。
- 角色安全：API 权限/合同 15 项、Web 角色路由 9 项、PostgreSQL 供应链回归 36 项；
  全新空库 64 条 `migrate deploy`、`status`、migrations-to-schema diff 均通过。
- 查询组合：API 33 项、PostgreSQL 隔离/查询/并发 45 项、API build、Web tsc；
  1440×900 浏览器验证关键字 trim、日期范围和跨页保留筛选，控制台零错误。
- 精准通知：单元 17 项、隔离 PostgreSQL 集成 5 项、API build、Web tsc；企微通道在测试
  中固定模拟失败，不访问外部网络，NotificationLog 失败证据可重试。
- 所有集成提交均通过 `git diff --check`、高置信敏感信息检查、远端精确 SHA 和干净工作区
  核对。最新运行时代码提交为 `44ab080b61c5e6db109f08d6cae0d6b34bd2f5c2`；
  本节后续报告提交不改变运行时代码。

### 当前执行队列与风险

- 三个新批次已从 `44ab080` 启动：Codex 部分发货关闭余量、Kimi 商品五类直接生效 API、
  Qwen 内部供应链商品 PC 台；三者文件边界独立，尚未集成。
- 精准通知目前只是基础服务，必须等商品路由事务成功后接线；在接线批次通过前不能声称
  五类商品操作已经完成。
- 订单快照 JSON 搜索当前没有专用索引；正确性和隔离已通过，较大租户的数据量性能需后续
  基准测试，不在本列车盲目引入 schema。
- 生产仍运行既有归档，生产 gate 保持 `LOCKED`；未合并 main、未部署、未写生产，也未
  执行任何一次性生产脚本。

## V5 内部供应链第四条集成列车（2026-07-26 01:27）

三条 runner 虽均因“进程结束时尚未满足完成不变量”退出 2，但随后各自工作区形成了干净、
已推送的 feature 提交。总管逐项核对精确 SHA、完整 diff、分支祖先关系和安全边界后，
才按 API、Web、履约事务的顺序手工移入统一 RC：

| 能力 | 任务分支精确提交 | 统一 RC 提交 | 结果 |
| --- | --- | --- | --- |
| 商品维护五类直接生效 | `eb63fab`、`fcb6db0` | `2dcf401`、`3c79335` | 内部新增/编辑/调价/停售/恢复直接生效；外部供应商审批不变；事务成功后精准通知总厨 |
| 内部商品 PC 管理页 | `767dbbc` | `a4af97e` | 服务端筛选/分页、新增编辑调价状态、图片上传预览和内部角色路由 |
| 部分发货关闭余量 | `f9096e7` | `6272d99` | 首张有效配送单关闭未发余量，按实发扣仓并释放余量预占；并发/幂等禁止第二张有效配送单 |
| 发货系统消息收件人收敛 | 总管复核修复 | `8d6c036` | `ORDER_SHIPPED` 仅写给原下单人，稳定去重且不再租户级广播 |

### 验收结论

- 商品 API 对内部 `SUPPLY_CHAIN` 强制最终态并校验 tenant 内供应商/分类；创建和修改均在
  事务成功后按稳定事件键调用精准通知，立即重放无实质变化时不重复通知。
- PC 页面只向内部供应链角色开放。1440×900 真实浏览器通过关键字、供应商+状态组合、
  27 条两页分页和一次实际调价；调价后从 `¥5.00` 更新为 `¥6.25`，页面提示“直接生效
  并已通知总厨”。页面和表格均无横向溢出，浏览器 warning/error 为 0。
- 部分发货保留原订货数量与首次快照，配送和扣仓按实发数量；未发余量在同一事务关闭并
  释放对应预占，订单不得二次发货。原下单人同时收到“不会补送”的系统消息和 exact-user
  企微事件。
- 复核额外发现旧 `ORDER_SHIPPED` 系统消息按 `MANAGER` 角色广播整个租户。现改为带
  `recipientId/refId/dedupeKey` 的原下单人消息，外部触达继续由按门店解析的
  `PO_DELIVERING` 精准通道承担。

### 完整门禁

- API 全量单元：36 文件、281/281；全新 PostgreSQL 空库应用 64 条迁移后，全量集成：
  21 文件、138/138；API build 通过。
- Web 全量测试：11 文件、102/102；`tsc --noEmit` 通过；隔离
  `NEXT_DIST_DIR` production build 146 页通过，临时产物已清理，未触碰用户 3200 dev。
- `git diff --check`、高置信敏感信息检查、三条远端 feature SHA 和所有工作区干净状态
  通过；临时数据库、浏览器服务、测试商品和隔离 Next 目录均已销毁。

### 下一列确定性任务

- 商品批量分类/停售/恢复补齐内部直接生效与逐商品精准通知，外部供应商审批语义保持不变。
- 内部商品 PC 台补当前筛选导出、批量选择/分类/状态操作。
- 供应商发货页面删除“本次不发可继续补送”的旧提示，明确首次部分发货即关闭未发余量，
  并用前端合同测试防止与 API 状态机再次漂移。

生产 gate 继续 `LOCKED`；未合并或直推 main、未创建 PR、未部署或写生产、未执行一次性
生产脚本。

## V5 内部供应链第五条集成列车（2026-07-26 02:18）

三项确定性任务均从第四列车统一候选独立实施。Codex 与 Qwen runner 在完成不变量前
退出 2；Kimi runner 形成提交但可信完成标记为 false。总管保留原证据，逐文件审查并
修复后，才以精确 feature SHA 顺序集成：

| 能力 | 任务分支精确提交 | 统一 RC 提交 | 结果 |
| --- | --- | --- | --- |
| 商品批量分类/状态直接生效 | `9a0bef2` | `9510cbb` | 内部逐商品乐观更新、审计、精准通知；无变化不通知；外部审批不变 |
| PC 当前筛选导出与批量操作 | `166c298`、`7c67f12` | `5dc5908`、`393065d` | 当前筛选 CSV、跨页选择、供应商范围分类候选和批量确认 |
| 部分发货页面闭环 | `d71736e` | `4a5f2da` | 零发货禁用、关闭余量预览/确认、成功后明确不会补送 |

### 总管复核修正

- 批量商品 API 对内部 `SUPPLY_CHAIN` 强制 tenant 内供应商与分类边界；每个商品都带
  乐观条件写入、独立审计和稳定通知事件键。分类候选接口只返回所选 tenant 内供应商的
  启用分类，不能跨供应商借用分类。
- Kimi 首版的跨页选择与批量操作已保留，同时补供应商范围分类候选，避免 UI 提供无效
  或越权分类。
- Qwen 首版把“关闭余量”按原订货数量计算；总管改为按当前剩余量计算，并分离成功提示
  与错误状态，避免已经部分履约的行重复夸大关闭量。

### 完整门禁与真实浏览器

- API 全量单元 37 文件、291/291；全新 PostgreSQL 空库应用 64 条迁移后，全量集成
  21 文件、138/138；API build 通过。
- Web 全量测试 13 文件、152/152；`tsc --noEmit` 通过；隔离
  `NEXT_DIST_DIR` production build 146 页通过，临时产物已清理。
- 1440×900 真实 PC 浏览器中，供应商 A 的 25 个商品跨两页全部被选中，分类候选只出现
  A 的启用分类；批量改为“叶菜”后数据库 25 行全部更新，供应商 B 的 3 行保持原分类，
  生成 25 条精确总厨通知。
- 同一浏览器完成部分发货：10/6 的两行实发 7/6，零数量提交被禁用，确认框明确 3 斤
  余量永久关闭；成功后订单为 `DELIVERING`、配送单为 `SHIPPED`、金额由 80 按实发变为
  65，并只向原下单人写入 `ORDER_SHIPPED` 与 `ORDER_PARTIAL_CLOSED`。
- 首次本地 API/Web 进程被执行环境终止时，提交返回失败且订单仍为 `CONFIRMED`；
  重启隔离服务后同一流程成功，证明失败未造成半完成业务事实。页面最终无应用
  warning/error；既有 OpenTelemetry 动态依赖构建 warning 不影响生成结果。
- 精确 feature SHA、远端分支、完整 diff、工作区干净、`git diff --check` 与高置信
  敏感信息检查全部通过。临时数据库、浏览器标签、测试服务、seed 和 Next 缓存均已清理。

生产 gate 继续 `LOCKED`；未合并或直推 main、未创建 PR、未部署或写生产、未执行生产
一次性脚本。

## V5 内部供应链第六至第八条集成列车（2026-07-26 04:35）

本阶段接管并验收五个已完成批次和三个 runner 阻塞批次。所有 feature 分支均先核对精确
远端 SHA、完整 diff 与干净工作树；runner 的错误检查、脏草稿和退出状态保留为原始证据，
没有用控制面状态替代代码验收。

| 能力 | 统一 RC 提交 | 结果 |
| --- | --- | --- |
| 收货只读 PC 工作台 | `7d00c25` | 关键字、状态、业务日期、稳定分页与只读详情 |
| 商品四单位主数据 | `d7ec5c`、`ad0751e`、`d4fc50f`、`7584918` | 采购/库存/订货/成本单位及统一库存因子，API/PC 校验一致 |
| 门店下单商品图片 | `ae8535e` | 图片加载失败安全回退，既有下单主流程不重做 |
| 订单/配送只读 PC | `67a09dc` | 服务端筛选分页、详情与内部角色只读边界 |
| 旧代码商品直写兼容 | `93a3b6e` | V5 新字段可空兼容，读取确定性回退，不从规格文本猜测 |
| 只读 PC 竞态回归 | `bed1b9a`、`72610a0` | 10 个 jsdom 页面用例，快速筛选时丢弃陈旧响应 |
| 四单位数据库约束 | `54fe120`、`0797457` | 因子范围/同名一致性/部分字段约束与真实 PostgreSQL 合同 |
| 订单—配送—收货四单位快照 | `584c8ef`、`1dba11b` | 冻结单位、因子及核验状态；后续商品变更不污染历史换算 |

### 数据库与历史兼容结论

- 四单位商品迁移只对无库存映射的旧商品确定性补为同单位 1:1；已有明确换算的旧商品沿用
  原因子；仅有库存单位但缺因子的记录保持 `PENDING` 且 V5 字段为空，不臆造关系。
- 新采购单在行级冻结四单位、三个库存换算因子和当时的核验状态；配送和收货只复制上游
  冻结值。商品之后改名、改单位、改因子或改核验状态，都不会反向改变已冻结单据。
- 历史单据没有完整快照时继续走既有安全回退；待核验商品仍阻止库存入账。迁移不回填
  历史单据，不从规格、金额或数量推断单位。
- 66 条迁移在全新空库完成 `migrate deploy` 与 `status`，migrations-to-schema diff
  为零；另以简单 1:1、明确 12:1、缺因子待核验三类旧商品复验回填结果。

### 最终组合门禁

- API 单元：39 文件、309/309；PostgreSQL 集成：22 文件、153/153；API build 通过。
- Web：18 文件、322/322；`tsc --noEmit` 通过。
- `git diff --check`、本阶段高置信敏感信息检查、feature 远端 SHA 和所有已验收工作树
  干净状态通过。
- 三个原阻塞任务 `SC-READONLY-PC-TEST-KIMI-011`、`SC-UNITS-DB-QWEN-010`、
  `SC-UNITS-SNAPSHOT-CODEX-009` 已在上述证据通过后转为 DONE。

### 保留风险与下一步

- 当前 `Product.price`、订单 `unitPrice` 和金额仍沿用既有“订货数量 × 商品单价”口径，
  尚未把已确认的 `costUnit`（采购成本计价单位）语义接入价格换算。该问题会影响非 1:1
  单位的金额/库存单位成本，必须作为独立 API、数据库合同和 PC 标签批次解决，不能在
  快照迁移中静默改变历史金额。
- 默认仓、多仓接口预留、入库/收货运行时更正仍未完成；涉及真实历史单据修正的操作继续
  只记录，不运行一次性生产脚本。
- 生产 gate 保持 `LOCKED`；本阶段未合并 main、未创建 PR、未部署、未写生产。

## V5 内部供应链第九条集成列车（2026-07-26 05:16）

本列从 `ee68c17` 独立接入默认仓、成本单位计价与对应 PC 合同。Qwen/Codex runner 都在
提交前结束，Kimi runner 的降级克隆还因伪基线不满足祖先校验被拒绝完成上报；总管保留
这些证据，逐项测试、补缺、重建真实 feature 祖先后才集成。

| 能力 | feature 精确提交 | 统一 RC 提交 | 结果 |
| --- | --- | --- | --- |
| 默认仓 API 合同 | `32d53bbf` | `29047cd0` | 省略/default 等价；未知仓在 DB 前 400；列表、summary、流水与写入响应带稳定默认仓元数据 |
| 成本单位订单计价 | `dffefda9` | `4ec73c94` | `costUnitPrice × orderFactor ÷ costFactor` 生成订货单位单价；Decimal、上限、PENDING 与改单快照边界 |
| 商品成本单位 PC | `f005a282`、`2acd60ff` | `fc49792a`、`cf4e36fd` | 列表、表单、调价确认明确元/成本单位；只读订货折算价；持久化 PENDING 不误报已核验 |
| 库存订货单位估值 | 总管复核修复 | `111c0060` | Product.stock 按 orderUnit 乘折算价；待核验 SKU 不静默错算并返回计数 |
| 默认仓 PC | `774393bd` | `d4e56ad4` | 库存列表/summary/详情/流水与入库、调整、报损、导入显式绑定 `warehouseId=default` |
| 商品路由测试隔离 | 总管门禁修复 | `c40002f0` | 完整保留 Prisma Decimal 导出，4 个全 mock 路由套件恢复收集 |

### 计价与兼容结论

- 新订单和新增改单行都只信数据库成本单位单价；历史/已有改单行继续沿用冻结单价，避免
  主数据调价反向污染历史。无结构 legacy 与同单位 1:1 兼容合同保持原金额。
- 非 1:1 显式待核验、缺字段、零/负因子和金额越界在创建订单前拒绝；库存汇总遇到这类
  SKU 不让整个接口失败，而是排除其货值并返回 `valuationPendingSku`。
- 默认仓是固定 `{ id: "default", name: "默认仓" }`，不是调用方可伪造的租户或供应商
  scope；query/body 的未知 warehouseId 统一在数据库访问前拒绝，未来仍可扩展多仓。

### 组合门禁与 PC 复核

- API 单元 41 文件、341/341；隔离 PostgreSQL 全量集成 22 文件、153/153；API build
  通过。供应商库存专项 22/22，验证 0.02 元/g 折成 10 元/斤后 5 斤货值为 50 元。
- Web 全量 19 文件、367/367；`tsc --noEmit` 通过。1440×900 浏览器验证商品新增表单
  动态显示“单价（元 / g）”、库存/入库/导入标题显示默认仓，请求实际带
  `warehouseId=default`；四页无横向溢出，浏览器控制台错误为 0。
- 浏览器复核时本地 PostgreSQL/Docker 服务已停止，所以 API 数据请求按预期返回 502；
  本轮浏览器证据只覆盖渲染、交互和 URL 合同，真实数据与并发由此前 153 项数据库集成
  覆盖，不把 502 包装成数据 E2E 成功。
- production Web build 已编译到 webpack 阶段，但因工作盘只剩约 117 MiB 触发
  `ENOSPC`；只清理本统一 RC 本次生成的不完整 `.next`，未停止另一个用户工作区的
  3200 服务，也未删除其他工作区缓存。
- `git diff --check`、高置信敏感信息检查、远端 feature SHA 与已验收工作树核对通过。

### 继续执行

- 第十批已从 `c40002f0` 启动：Codex 修门店下单金额折算，Qwen 修商品 CSV 成本单位
  口径，Kimi 补库存货值待核验 PC 提示；三者文件边界独立，尚未验收或集成。
- 已确认但仍未实现的是已确认收货/入库的运行时更正；该状态机会改变库存、差异与应付
  事实，继续作为业务待确认项，不对真实历史数据运行修正脚本。
- 生产 gate 继续 `LOCKED`；未合并 main、未创建 PR、未部署或写生产。

## V5 内部供应链第十条集成列车（2026-07-26 05:38）

第十列从 `c40002f0` 同时派发三个互不冲突的小批。三个 runner 都因工作盘耗尽或完成上报
前停住，没有可信提交可直接接入；总管保留 run 失败证据，终止仍在运行的进程，仅删除
Qwen 工作区自己生成的 451 MiB 未跟踪 pnpm store 和 Kimi 工作区本批生成的
`node_modules`，未触碰用户服务、其他工作区或业务数据。

| 能力 | 验收后 feature 精确提交 | 统一 RC 提交 | 结果 |
| --- | --- | --- | --- |
| 库存货值 PC | `f15a3f7e6df1605aad5a76fd3bd4cbd0a92cac11` | `c76f02db` | 汇总提示待核验 SKU 未计价；行级展示订货单位价格；分类可用货值排除 PENDING |
| 商品四单位 CSV | `440d8bec657c16cecc48e1db64ab4b9c3af91b03` | `85c05a09` | 稳定导出四单位、三因子、成本单位价、订货折算价和状态；非法/PENDING 折算价留空 |
| 旧 PC 下单计价 | `b654d208a176bb80652a17cb1dc9357622b4d2e4` | `2b1e7444` | 只读展示订货单位价格与行金额；显式非 1:1 PENDING 禁止提交；API 仍权威重算 |

### 验收和组合门禁

- Kimi 草稿清理安装噪声后，专项 15/15 与 Web tsc 通过；Qwen 草稿剥离整文件行尾改写、
  workspace 配置和本地 store 后，商品导出专项 47/47、API build、Web tsc 通过。
- Codex 草稿在 runner 停止后由总管重跑 14/14 精确 Decimal/legacy/PENDING 金额合同和
  Web tsc，形成并推送干净 feature；三个远端 SHA、父提交 `c40002f0` 和 diff 均核对。
- 集成后 API 单元 41 文件、346/346，API build 通过；Web 21 文件、396/396，Web tsc
  通过；`git diff --check` 通过。第十列没有 schema 或数据库写路径变化，数据库证据继续
  使用第九列同一 RC 上已通过的 PostgreSQL 22 文件、153/153。
- 新增 CSV 列仍逐单元格执行公式注入中和与 RFC4180 转义，不输出租户/供应商内部 ID；
  原有角色、筛选、租户/供应商 scope、行数上限和 Content-Disposition 保持。

### 未完成与明确风险

- `apps/web/src/app/v2/chef/purchase/new/page.tsx` 与
  `apps/web/src/app/v2/chef-director/purchase/new/page.tsx` 仍直接把
  `Product.price` 当订货单位价；供应商订单详情的追加商品预览也仍直接使用主数据价格。
  API 会权威重算，但这些 V2 预览在非 1:1 商品上会误导，必须作为下一批确定性修复。
- 供应商库存详情与 analytics 的部分价格/货值标签仍沿用含混 `price / unit`；列表和
  summary 已正确，但不可把本列描述为所有库存 PC 页面已完成。
- 当前“默认仓”是稳定 API/UI 合同，真实 `Warehouse` 模型、按仓账本和已确认入库/收货
  更正单仍未实现；真实历史单据更正继续只记录问题，不执行脚本。
- production Web build 仍受工作盘空间不足影响；本轮不再生成 `.next`。生产 gate 保持
  `LOCKED`，未合并 main、未建 PR、未部署或写生产。

## V5 内部供应链第十一条集成列车（2026-07-26 06:37）

本列从 `4c432f33` 拆出三个互不冲突的价格展示批次。三个 runner 都形成了可审查源码，
但自动化 shell 使用 Node 25，而仓库强制 Node 20，导致 runner 未能提交和执行可信检查；
原 FAILED run、退出 2、脏工作树和未运行检查均保留为失败证据。总管逐项审查并修复后，
以独立 feature 提交、远端 exact SHA、干净工作树和人工门禁接管验收。

| 能力 | feature 精确提交 | 统一 RC 提交 | 结果 |
| --- | --- | --- | --- |
| V2 门店下单订货单位计价 | `39f9f2542c701a959ccb78f279474a2148c030c5` | `7708689c` | 厨师长/总厨新建采购单按成本单位折成订货单位价格和行金额；PENDING 或非法数量禁止提交 |
| 供应商改单新增行计价 | `f04ead545c5389dc38200d88ea96161d1cafc4a5` | `44fad266` | 新增行使用当前四单位折算；既有行继续显示冻结价格与冻结单位；PENDING 不伪造合计 |
| 库存详情与洞察价格口径 | `6b197c6b8de0ee14867cd20dff08af9dd19281ba` | `4e03f24a` | 库存详情只展示后端订货单位估值；滞销 SKU 四单位折算或待核验；热销金额继续使用收货快照 |

### 验收、浏览器与环境

- API 单元 41 文件、346/346；API build 通过。Web 24 文件、426/426，Web
  `tsc --noEmit` 通过；`git diff --check` 与高置信敏感信息检查通过。
- 新增供应商洞察 PostgreSQL 用例覆盖 g→斤折算、PENDING、跨租户/供应商隔离和历史金额
  不被当前商品价格重算；当前机器没有 `_test/_ci` 数据库，Docker/PostgreSQL 未运行，
  安全门禁因此拒绝执行集成套件。本列不绕过门禁、不连接其他数据库，这组新增用例保留
  为下一次隔离数据库验收的明确缺口。
- 独立 `NEXT_DIST_DIR=.next-v5-train11` production build 成功生成 149 个路由，仅有既有
  OpenTelemetry 动态依赖 warning；592 MiB 临时产物和自动生成的 `tsconfig` 变化均已
  精确清理，默认 `.next` 未被污染。
- 1440×900 PC smoke 覆盖登录、两个 V2 下单页、供应商分析、订单修订和库存详情。
  页面壳与本批文案正常、浏览器控制台无前端错误；本地 API 未启动，数据区按预期显示
  `proxy fetch failed`，所以本证据只计渲染/路由 smoke，不计真实数据 E2E。
- 控制面提交 `20b2d529813a76049f685c6c3da51f0e223e321c` 把 AI 子进程与可信检查的
  `PATH` 固定到 Node 20，同时继续剥离控制面及生产凭证；控制面 build、20/20 测试、
  lint 和敏感信息检查通过。

### 当前风险与后续

- 商品/订单/库存 PC 的成本单位与订货单位展示缺口已收敛；剩余高优先级是实际
  `Warehouse`/按仓账本、已确认入库与收货更正单及反向库存/差异/应付流水。
- 洞察新增的真实 PostgreSQL 用例必须在隔离 `_test/_ci` 库补跑后，才能把该项数据库
  证据标记为完整；不得借用生产或普通本地库绕过名称门禁。
- 生产 gate 继续 `LOCKED`；未合并或直推 main、未建 PR、未部署、未写生产、未运行
  生产一次性脚本，也未改动业务 xlsx 或凭证。

## V5 内部供应链第十二条集成列车（2026-07-26 06:57）

第十二列先补上一列因无隔离 PostgreSQL 而缺少的无数据库路由证据，同时并行预研真实仓库
与更正状态机。只有 Qwen 测试批次通过验收并进入统一 RC；两个高风险设计草稿均因业务
边界错误留在各自工作区，未以“能编译/有文档”替代语义验收。

### 已集成

- `be2e7884`（feature `b01bf583301af27f0317d65220b6378be35f1c40`）新增 12 个
  `supplierInsights` Fastify 无库路由测试，覆盖 g→斤折算、PENDING 不估值、移除
  legacy `price`、热销收货快照金额、确认/入账日期条件、管理员显式 supplier、供应商
  query 不能越权以及 tenant/supplier Prisma scope。
- Qwen runner 留下未提交测试并被控制面正确标记 FAILED/退出 2；总管把“伪造另一租户
  响应”改成对真实 Prisma where 的认证范围断言，并补当前商品查询不选择 `price` 的
  历史金额防回归，才形成上述 feature。

### 未集成并保留的证据

- Kimi `1d7c548d79133e14d5f9d47a3e693ba10b3cd5d2` 因报告路径错误未通过任务合同，
  更重要的是把“供应链仓手工入库更正”和“门店实收更正”耦合成同一库存反向流程，会在
  仅修正门店实收时错误恢复供应链仓库存；还给总厨/外部供应商配置未确认写权限，并把
  禁止的 `db push` 写成回滚手段。该提交保持未集成，不能作为实现依据。
- Codex 仓库草稿被总管主动取消并保持未提交：任务包错误地要求“每个 supplier 一个
  默认仓”，而基线是供应链公司当前只启用一个 tenant 级默认仓；草稿因此会制造多个
  默认仓。其 `WarehouseStock` 也未覆盖兼容期 `Product.stock` 持续写入后的同步切换，
  会立即漂移；另含本机绝对 Prisma generator output。源码与 migration 仅作失败证据。

### 门禁与下一步

- 本列组合 API 单元 42 文件、358/358，API build、`git diff --check` 与高置信敏感信息
  检查通过。Web 未改动，上一列 426/426、tsc、149 路由 production build 与 PC smoke
  证据继续有效。
- 控制面 Node 20 修复得到实际运行实证：Qwen 的执行子进程使用 Node 20.20.2；上一列
  的 Node 25 引擎拒绝未再出现。
- 下一次仓库批次必须从 tenant 级 `Warehouse`、唯一默认仓、`WarehouseStock` 与
  `Product.stock` 兼容期双写/核对开始；供应链手工入库更正与门店收货更正必须拆成两条
  独立状态机。任何 schema 仍需隔离空库 deploy/status/diff 和历史样本复验。
- 生产 gate 保持 `LOCKED`；未合并 main、未建 PR、未部署、未写生产或运行生产脚本。

## V5 内部供应链第十三条审计列车（2026-07-26 07:23）

本列纠正上一列两个高风险草稿，并把仓库 schema 与兼容写路径分成独立门禁。两份设计
报告经语义修正后进入统一 RC；真实 schema 形成远端 feature 候选，但因没有隔离
PostgreSQL，明确不集成。

### 已集成的审计合同

| 合同 | feature 精确提交 | 统一 RC 提交 | 结论 |
| --- | --- | --- | --- |
| 入库/收货更正拆分 | `a9ac63467b73036343744d6c46907a1d3cf62285`、`426ba0fc60f6fb29509067dfb389dfb69e65d22c` | `b637b186`、`c75edb64` | 供应链手工入库更正与门店实收更正独立；门店修正不恢复供应链仓存；SUPPLY_CHAIN 管仓，总厨无更正写权限 |
| 仓库兼容写路径审计 | `0e0d3eb84c55087f376a28c70f539593365e7a68`、`95680d5a10e97d1281947aecb7460a63311fcc5a` | `54950bbb`、`bad38970` | 枚举期初、入库、调整、报损、快照、预占、释放、发货和读取路径，冻结 tenant 默认仓切换门禁与测试矩阵 |

Kimi 原 runner 产物把两个更正流程重新耦合且授予了错误角色；总管修正角色和库存边界，
并把 rollback 限定为 migration deploy/status/diff 与人工 SQL，不允许 `db push/reset`。
Qwen 原报告 494 行并把 API 别名 `default` 错当真实仓主键，还只回填有 supplier 的商品；
最终压缩为 140 行，以真实 tenant 仓 ID、所有 Product、同 tenant 复合外键和逐 writer
门禁为准。两个原 FAILED run 均保留，控制面只在修正提交通过报告合同后人工验收。

### 未集成的 schema 候选

- `1f606d9db5d950b999199f1fd6955b8afe3923eb` 与精度修正
  `bff1503a4b960c24d9a5aac6a2d812c731fa2e66` 在
  `feature/20260726-tenant-warehouse-codex` 新增 tenant 级 `Warehouse`、
  `WarehouseStock`、五类配送/库存事实的真实 warehouse 关系、确定性历史回填、同 tenant
  复合外键、单默认仓与同仓商品余额唯一约束。
- 兼容期只允许 `Product.stock → WarehouseStock.physicalQty` 单向触发器；新 tenant
  自动建立默认仓，旧 writer 只在 warehouseId 为 NULL 时补默认仓，显式跨 tenant 值由
  外键拒绝。rollback 会移除所有兼容触发器、约束、索引和新表。
- 静态合同 4/4、Prisma validate/generate、API build、两个测试文件 TypeScript 编译、
  `git diff --check` 和高置信敏感信息检查通过；物理余额精度已与现有库存事实统一为
  `Decimal(12,3)`，feature 已推送且工作树干净。
- 本机 Docker CLI 存在但守护进程不可用，也没有可用 `_test/_ci` PostgreSQL；因此
  `tenantWarehouseDbContract.integration.test.ts`、空库/历史样本
  `migrate deploy/status/diff` 和 rollback 演练均未执行。按 schema 门禁，该提交保持
  `BLOCKED` 且不进入统一 RC。

### 当前门禁

- 第十二列运行时代码在当前 RC 上继续保持 API 单元 42 文件、358/358 与 API build
  通过；本列集成内容仅为报告，不改变 API/Web/schema。
- 原按 supplier 建仓的脏工作区、错误更正报告提交和两个 runner FAILED 状态均保留为
  证据，没有删除或覆盖。
- 下一步必须先提供隔离 PostgreSQL，执行仓库 migration 的空库、历史样本、约束、
  并发、rollback 和 migrations-to-schema diff；通过后才重新审查
  `bff1503a`，不得以静态编译代替数据库验收。
- 生产 gate 保持 `LOCKED`；未合并或直推 main、未创建 PR、未部署、未写生产、未执行
  一次性数据脚本，也未读取或修改凭证与业务 xlsx。

## V5 内部供应链第十四条仓库集成列车（2026-07-26 07:51）

本列恢复 Docker Desktop 后，只在本机隔离 `_ci` PostgreSQL 中验收第十三列的 schema
候选。npm 可下载缓存清理使工作盘可用空间从约 2.8 GiB 恢复到约 5.1 GiB；旧脏
worktree、Docker 镜像/卷和用户文件均未删除。Docker 僵死后台由官方 CLI 强制停止后
重启，未连接普通本地库或生产库。

### 已集成

| 能力 | feature 精确提交 | 统一 RC 提交 |
| --- | --- | --- |
| tenant 默认仓与按仓余额基础 | `1f606d9db5d950b999199f1fd6955b8afe3923eb` | `1c23b0f5` |
| 仓库余额三位小数 | `bff1503a4b960c24d9a5aac6a2d812c731fa2e66` | `e40266db` |
| 并发、跨租户与多仓数据库合同 | `563ad70443b4f95b4a165b178140e133c10b73b1` | `8bde07c7` |
| 保留商品/租户硬删除兼容 | `50f5d9626a551130d1801046e13a403546f03fbe` | `a55bba0f` |

- `Warehouse` 归 tenant 内部供应链团队所有，不归 supplier；每 tenant 当前只有一个启用
  默认仓，模型和复合键保留未来多仓。
- 所有历史 Product 都从 `Product.stock` 确定性初始化默认仓余额；新 tenant 自动建仓。
  五类配送/库存事实历史行绑定真实 tenant 仓 ID，旧 writer 仅在省略 warehouseId 时由
  触发器补默认仓，显式跨 tenant 值由复合外键拒绝。
- 兼容期保持 `Product.stock → WarehouseStock.physicalQty` 单向同步；不建立反向触发器。
  仓库余额为 `Decimal(12,3)`，并发旧写后余额与 Product 最终值一致。

### 数据库门禁与失败修复

- 全新空库应用 67 条迁移，`migrate deploy`、重复 deploy、`status` 与
  migrations-to-schema diff 全部通过。
- 历史样本先应用旧 66 条迁移，再写入 tenant、无关 supplier 边界的 Product、流水、
  批次和分配，最后只应用仓库迁移；默认仓、4.250 余额与三类历史事实全部绑定同一真实
  tenant 仓 ID，约束均已验证。
- 独立 rollback 库成功移除新表、四个新增列、函数、触发器、索引和约束，并把既有
  DeliveryOrder 占位列恢复为空；未使用 `db push/reset`。
- 首轮全量 PostgreSQL 套件暴露 `WarehouseStock` 的 RESTRICT 外键会阻断现有 Product
  硬删除和测试清理，造成 19 个文件连锁失败。该证据保留；`50f5d962` 把 tenant 拥有的
  Warehouse 及派生 WarehouseStock 对 tenant/warehouse/product 改为 CASCADE，而五类
  审计事实对仓继续 RESTRICT。全新库重跑后 23 文件、165/165 通过。
- 仓库专项数据库合同 8/8，覆盖确定性默认仓、单向桥、单默认仓/余额唯一性、显式跨
  tenant 伪造、五类约束验证、并发旧写一致性、三位小数多仓余额及硬删除兼容。

### 最终组合门禁

- API 单元 43 文件、362/362；API PostgreSQL 集成 23 文件、165/165；API build 通过。
- Web 未改运行时代码；全量 24 文件、426/426 证据继续有效，并在最终 schema 上重跑
  `tsc --noEmit` 通过。
- `git diff --check`、高置信敏感信息检查、feature 远端 exact SHA、干净 feature 与
  integration 工作树均通过。
- 生产 gate 继续 `LOCKED`；未合并 main、未建 PR、未部署、未写生产、未运行真实数据
  修正脚本。

### 下一步

- schema 只是兼容基础；按仓真实运行时仍需按审计顺序逐 writer 切换余额、批次、预占、
  流水、发货和查询，最终移除 Product.stock 单向桥与 NULL 补仓触发器。
- 供应链手工入库更正与门店收货更正边界已冻结但尚未编码；两条状态机必须独立，门店
  实收修正不得恢复供应链仓库存。

## V5 内部供应链第十五条仓库解析与商品数量精度列车（2026-07-26 08:50）

本列在第十四列真实仓 schema 上补 tenant 范围仓库解析，并把商品数量从数据库、API
到 PC 统一为三位小数。所有 feature 都在独立工作区形成精确提交并推送；runner 失败
状态和原始脏工作区保留，总管只把重做后通过门禁的提交逐项接入统一 RC。

### 已集成

| 能力 | feature 精确提交 | 统一 RC 提交 |
| --- | --- | --- |
| tenant 仓库安全解析 | `cbdab05a2333bc0a6e21d14e13d71df2aa72e3e1` | `44dd622c` |
| 商品数量三位小数数据库 | `a9f0db396b483bb411fef2b38bbb31ecf60ccc7c` | `8869bc27`、`b79ad98c`、`71ac289d` |
| 商品数量三位小数 API | `56470d2cd23625311af63232726f32fa8bb3de39` | `739436d4` |
| 商品数量三位小数 PC | `507efa9867865678e0c0ab803002771c0740c47c` | `619f8836`、`d131e44e` |
| 数量上界数据库回归合同 | 总管修复 | `5404d44f` |

- `resolveTenantWarehouseId` 只在认证 tenant 内解析启用的默认仓或显式真实仓 ID；未知、
  禁用与跨 tenant 仓返回稳定 4xx。旧 `requireDefaultWarehouse` 暂不静默切换，等待
  每个 caller 按审计顺序显式接入。
- Product 的 `stock`、`minStock`、`minOrderQty`、`stepQty` 统一为
  `Decimal(12,3)`；迁移在 ALTER 前事务性重建依赖 `stock` 的兼容同步触发器，避免
  PostgreSQL 因 `UPDATE OF stock` 依赖阻断列类型变更。迁移不改业务数据。
- API 使用 Decimal 精确校验，数量上界为 `999999999.999`，拒绝负数、四位小数、
  非有限数、科学计数字符串和越界值；PATCH 只开放最小订货量与步长，没有借精度改造
  新增库存编辑权限。
- PC 保留服务端三位小数字符串，输入步进 `0.001`，编辑时库存和安全库存仍禁用；
  1440×900 mock API 复核确认表格无横向溢出、第三位小数不丢失、四位小数不发请求，
  且长表单改为弹窗内部滚动后保存按钮可达。

### 数据库与组合门禁

- 全新空库应用 68 条迁移；历史 67 迁移库中两位小数样本升级后保持数值，随后可写
  `12345678.901`，Product → WarehouseStock 单向桥继续精确同步。
- 回滚预检在存在三位小数时正确阻断缩回两位；样本归整为两位后，人工回滚和触发器
  重建通过。最终隔离库 `migrate status` 最新，migrations-to-schema diff 为零。
- 首轮全量 PostgreSQL 164/165 暴露旧测试仍把 100,000,000 当作数量非法上界；合同改为
  价格仍以一亿元非法、四个数量字段以十亿元非法后，目标 26/26，新的全新库全量
  23 文件、165/165。
- 最终 API 单元 44 文件、394/394，Web 24 文件、450/450，API build、Web tsc、
  `git diff --check` 与高置信敏感信息检查全部通过。

### 保留的失败证据与边界

- Qwen 首版 API 草稿产生整文件 CRLF 噪声并用浮点乘法判断三位小数；拆出的数据库
  runner 又漏掉 migration。Codex API runner 和两次 Kimi PC runner 均未形成可信提交，
  其中首个 Kimi run 是 sandbox 未包含新 worktree。相关 run 继续为 BLOCKED/FAILED，
  原工作区不删除、不覆盖。
- 控制服务未配置管理端 `CONTROL_PLANE_TOKEN`，所以总管不能伪造 review/DONE；手工修复、
  feature 精确 SHA、远端分支、干净工作树及门禁证据均记录在本报告和执行看板。
- schema 基础与解析器已具备，但按仓运行时 caller 尚未切换。下一列继续按 G2 顺序把
  商品期初、入库/调整/报损/快照、预占与发货显式绑定真实 warehouseId；在全部守恒
  核对完成前不得移除 Product.stock 单向桥。
- 生产 gate 保持 `LOCKED`；未合并或直推 main、未建 PR、未部署、未写生产、未运行
  真实数据修正脚本，也未读取或修改凭证与业务 xlsx。

## 十二小时持续开发最终收口（2026-07-26 09:19，竞态纠正）

- 统一 RC 运行时代码最终为 `65fea51a9ef640303011f78923765eba4ac40f71`。收口期间两个
  总管曾并发读取第十六列：较早报告误记为“无提交、未集成”，但远端 feature 和统一
  RC 随后已经形成提交。现已按 Git patch-id、远端 exact SHA、完整 diff 和独立门禁
  重新验收，以下结果覆盖 09:06 的陈旧描述。
- 商品期初显式仓：feature `fb9c34e82194bb1cd99f9854516155d33b8870aa`，统一
  `5184b1803ceda28aa19af5a18c421d667421604a`。内部供应链/管理员新增有供应商且正期初
  库存的商品时，先在同一事务按认证 tenant 解析启用默认仓，再让期初流水、批次和后续
  显式带仓批次分配使用同一真实 warehouseId；解析失败时商品及库存事实均不落库。
- 库存事实真实仓读取：feature `e2a1f053811cce97dbc27c0fa96fd8e0b864bbb0`，统一
  `0c21eb781a48fd4ecb188c2bd946a011f9bf73ba`。预占、批次和流水 GET 将 default/显式仓
  解析为本 tenant 启用真实仓，并同时保留 tenant、supplier、warehouse 三重 scope；
  列表、summary 和写端点仍保持旧默认仓门禁。
- 默认仓双源审计：feature 初稿 `e90f969bbf790f49b6bcdd4ef3373940ff3ede77`、修正
  `286439a5879436e210bba80e19d2eb3051729256`，统一为 `0579253c`、`65fea51a`。
  修正将默认仓解析提前到库存事实查询之前，避免 STRICT 审计混入其他仓预占、流水和
  批次；WarehouseStock 缺行、停用和超过 0.001 的 Product.stock 差异只报错，不自动
  修复。
- Codex/Kimi 原 runner 均以 SIGTERM 退出，Qwen supervisor 曾停在 STOPPING 并由可信
  reaper 回收；原状态保留，不伪报 runner 成功。只接入 exact SHA、远端、语义 diff、
  相关测试和安全边界均通过的 feature 提交。

### 最终门禁

- Node `20.20.2`、pnpm `10.32.1`、Prisma `5.22.0`；第十六列专项 4 文件 80/80，
  API 全量 47 文件 437/437、API build、Web 全量 24 文件 451/451、Web tsc、
  `git diff --check` 和高置信敏感信息检查通过。三组 feature 与统一提交的 patch-id
  均精确对应，feature 远端 SHA 已核对。
- 两套全新临时 PostgreSQL 均从零应用 68 条 migration，deploy/status 通过且
  migrations-to-schema diff 为零；最终完整数据库集成 23 文件 165/165。临时容器
  已删除，未连接现有本地库或生产。
- 第十六列没有 Web 运行时代码变更；商品 PC 的 1440×900 三位小数、长表单和旧负库存
  编辑复核来自同一最终组合中的 `0b94cf77`，未把浏览器证据扩大为真实数据写入 E2E。

### 停止点与风险

- 第十六列只切了商品期初 writer、三类库存事实读取和只读双源审计。供应链入库、调整、
  报损、快照、订单预占、实际发货等 writer 尚未逐项切到真实 warehouseId；守恒核对
  完成前不得移除 Product.stock 单向兼容桥。
- Kimi feature worktree 在已推送 `e2a1f053811cce97dbc27c0fa96fd8e0b864bbb0` 之后
  另有两处未提交的空 warehouseId 别名兼容草稿；未审查、未集成、未清理，不属于候选。
- 供应链手工入库更正与门店实收更正仍只有冻结边界，运行时状态机未完成；门店实收更正
  不得恢复供应链仓库存。瑶海 7.22 盘点仍等待 31 项业务确认。
- 最终 active runner 为 0；隔离 Web/API/PostgreSQL 与 Next 缓存已收口，用户 3200
  服务和其他脏工作区未触碰。生产 gate 保持 `LOCKED`；未合并/直推 main、未建 PR、
  未部署或写生产、未运行一次性生产脚本。

## 内部供应链模块统一（2026-07-26 10:24）

- 方案：当前内部供应链公司使用统一 PC 后台，原供应商履约与商品能力保留并合入；未来
  外部供应商继续 supplierId 隔离。完整矩阵见 `SUPPLY_CHAIN_MODULE_PLAN_2026-07-26.md`。
- 运行时：新增统一导航、订单履约、分类、默认仓库存、供应商管理、账务只读、经营分析；
  总览保留跨店订单、配送、收货、库存和消耗视角。
- 授权：内部角色可写订单/配送、商品、库存和供应商基础档案；不可写门店收货/消耗、
  财务付款/审核、银行与自动付款字段。账务与经营分析只读且不暴露营业额、成本率。
- 数据边界：默认仓库存必须显式选择供应商；所有查询继续 tenant scope，外部供应商继续
  自身 supplierId scope。
- 验收：API 48 文件 441/441、Web 24 文件 451/451、API build、Web tsc、Web
  production build、PostgreSQL 集成 23 文件 165/165 通过；浏览器逐页确认八个工作台
  页面正常且无 console error。
- 未做：没有连接或写生产，没有真实业务写入，没有定义入库更正/撤销、已确认实收更正，
  没有移除单仓兼容桥。

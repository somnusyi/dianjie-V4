# 滇界 V4 收货差异、冲销与补送 P0 方案

日期：2026-07-15
状态：现状审计完成，关键业务规则待确认
环境：仅本地隔离库；生产环境不修改

## 1. 本轮目标

建立“本次配送 → 本次收货 → 差异确认 → 减款或补送 → 库存与财务闭环”的不可覆盖链路。任何已确认收货数据不直接改写，所有更正通过新单据和事件追溯。

## 2. 已发现的问题

1. `LossClaim` 只关联订货单，没有关联具体 `DeliveryOrder` 和 `Receipt`；分批配送后无法确定差异属于哪一次配送。
2. 验收后的“多轮补报”按订货单累计实发重新计算，没有扣除已报差异，存在重复索赔风险。
3. 供应商拒绝报损时只增加 `PaymentSchedule.amount`，没有同步 Receipt、Reconciliation 和会计凭证，四套金额可能不一致。
4. 收货短量本来就没有进入 Receipt 库存，但供应商同意后仍生成“借损失、贷库存”凭证，会再次冲减不存在的库存。
5. 报损库存回补没有唯一幂等约束；接口重试或并发处理可能重复增加供应商库存。
6. 当前补送仍受订货单累计实发上限限制；已全部发货但实际短收时无法创建不计应付的新补送配送单。
7. 已确认入库单没有正式更正单；直接改数量会破坏库存、应付、对账和审计一致性。

## 3. 推荐业务规则（待确认）

### 3.1 收货差异

- 差异必须绑定本次配送单和本次入库单。
- 差异类型至少区分：`SHORTAGE` 短量、`REJECTED` 拒收、`DAMAGED` 破损、`QUALITY` 质量不合格、`OTHER` 其他。
- 门店按实际可入库数量生成 Receipt；未实际接收的数量不进入门店库存、不形成应付。
- 证据维持“建议上传、非强制”，但拒收、破损、质量问题页面显著提示留证。

### 3.2 供应商处理

- 供应商认可差异时可选择：
  - `CREDIT`：减款结案，不再补货；
  - `REDELIVER`：免费补送，不增加应付。
- 24 小时未响应默认按 `CREDIT` 自动同意。
- 供应商拒绝后冻结关联入库单的待付款流程，由总厨/授权管理员仲裁。
- 仲裁结果必须明确最终认可数量、最终减款金额和是否补送，不能只填自由文本。

### 3.3 免费补送

- 新建独立补送配送单，关联原差异单，单价快照保留原订货价用于库存计价，但 `payableAmount=0`。
- 补送发货正常扣供应商库存；门店收货正常增加实物库存。
- 补送 Receipt 记录库存价值，但不得再次生成对供应商的应付、对账或付款计划。
- 补送数量不得超过该差异单已确认且尚未补送的数量。

### 3.4 入库冲销/更正

- 已确认 Receipt 不允许修改或删除。
- 更正生成 `ReceiptAdjustment` 及正负明细，并关联原 Receipt。
- 尚未支付：同步调整库存价值、对账金额和付款计划；付款流程在审批完成前冻结。
- 已支付或凭证已导出：系统只生成异常任务和更正建议，不自动改银行结果或外部财务系统数据。
- 推荐权限：店长/厨师长发起，财务确认；涉及数量争议时总厨参与审核。

## 4. 推荐数据模型

- `ReceiptVariance`：差异单号、配送单、入库单、类型、状态、处理方式、证据、责任人和时间。
- `ReceiptVarianceItem`：本次实发、实收、差异、已补送、待补送、原价快照。
- `ReceiptAdjustment` / `ReceiptAdjustmentItem`：对已确认入库单的追加式更正记录。
- `DeliveryOrder.kind`：`STANDARD`、`REDELIVERY`、`REVERSAL`。
- `DeliveryOrder.varianceId`：补送单/冲销单来源。
- 所有处理动作写独立事件，并和业务更新放在同一事务。

现有 `LossClaim` 保留用于“店内盘点报损”；采购到货短量逐步迁移为 `ReceiptVariance`，避免把物流差异和门店经营损耗混在一起。

## 5. API 计划

- `POST /api/deliveries/:id/receive`：按配送单收货并自动生成差异单。
- `GET /api/receipt-variances`、`GET /api/receipt-variances/:id`。
- `PATCH /api/receipt-variances/:id/respond`：供应商选择减款、补送或拒绝。
- `PATCH /api/receipt-variances/:id/resolve`：总厨/管理员结构化仲裁。
- `POST /api/receipt-variances/:id/redelivery`：创建免费补送配送单。
- `POST /api/receipts/:id/adjustments`：发起入库更正。
- `PATCH /api/receipt-adjustments/:id/review`：财务审核并应用更正。
- 旧订单收货与报损接口保留兼容，但内部调用同一差异服务。

## 6. P0 验证场景

1. 一张配送单实发 10、实收 8，生成净入库 8 和差异 2。
2. 供应商选择减款：不重复扣应付、不生成虚假库存损失凭证，差异结案。
3. 供应商选择补送：生成免费补送单 2，补送收货后库存增加 2，应付仍不增加。
4. 同一差异重复请求不得重复回补库存、重复创建补送单或重复调整账期。
5. 多次补报累计不得超过本次配送实发减已入库、已处理差异。
6. 供应商拒绝后关联付款冻结；仲裁后金额、对账、凭证状态保持一致。
7. 已确认入库更正采用追加单据；原 Receipt 内容和哈希保持不变。
8. 已付款/已导出凭证场景禁止自动冲销，生成明确人工处理任务。
9. 店长不能处理其他门店差异，供应商不能处理其他供应商差异。
10. 历史 `LossClaim` 不丢失，迁移后仍能查询并标识来源。

## 7. 生产发布门槛

- 在生产数据库副本完成迁移和金额核对。
- 抽查分批配送订单，逐笔核对 Delivery、Receipt、差异、PaymentSchedule、Reconciliation 和 Voucher。
- 店长、厨师长、供应商、财务四角色完成 UAT。
- 备份、回滚脚本和定时任务停启顺序验证完成。
- 未满足上述条件不得部署生产。

# 滇界 V4 当前交接基线（更新至 2026-08-01）

> 本文件随代码版本化，是接手开发时的当前基线。账号、密码、连接串和密钥不写入仓库。

## 1. 项目定位

滇界 V4 是连锁餐饮多租户经营管理系统。核心链路：

`门店订货 → 供应链接单/实发 → 门店验收 → 入库与移动平均成本 → 日报销量 → BOM 消耗 → 盘点/报损 → 财务结算`

角色包括老板/超管、财务、总厨、店长、厨师长、供应链人员等。业务能力按领域实现，角色页面只承担权限与工作台展示。

## 2. 已核实环境

- 主仓库：GitHub `somnusyi/dianjie-V4`
- 本地工作仓：`/Users/somnusyi/Documents/Codex/dianjie-V4-rebuilt`
- 生产：`116.62.32.162`，应用 `/app/dianjie-v4`，自动开发基线 `/app/dianjie-src`
- 生产域名：`https://njdianjie.com`
- 运行时：Node 20、pnpm 10.32.1、Prisma 5.22.0、PostgreSQL 15
- 2026-07-30 P1 发布后：GitHub、本地、生产部署标记和服务器源码按发布流程完成一致性核验
- Prisma migration：仓库与生产均为 73 条，无失败记录
- Gitea 与 GitHub `main` 已完成同步；Gitea 可作为内网代码副本

## 3. 生产业务基线

- 真实租户 DJ001 合肥瑶海店：已运行，5 个关联账号，最近初始盘点日为 2026-07-13
- DJ002 合肥包河万达店：已建店及2个关联账号，尚无初始盘点，不能把预计库存作为可信经营数据
- 自动修复记录（2026-08-01 审计时）：12 次 RESOLVED、11 次 ESCALATED；无任务卡在运行态

## 4. 测试基线

- API 单元测试：789 项通过
- Web 单元测试：541 项通过
- API/Web TypeScript 检查通过
- 集成测试：186/186 项通过
- 集成测试数据库名必须以 `_test` 或 `_ci` 结尾；测试前只允许对该隔离库执行 `prisma migrate deploy`

## 5. 自动开发与部署现状

服务器已安装 Qwen Code 0.15.10。当前生产管线能够：

1. 用户提交反馈并与 Qwen 对话；
2. 管理员批准开发；
3. Qwen Code 在隔离 worktree 修改 Web、非核心 API；核心业务 API 仅在专用开关明确开启时准入；
4. 系统按变更范围独立运行 Web/API 测试、类型检查和构建；核心 API 额外强制运行隔离数据库集成测试；
5. `approved_auto` 模式下，首次批准已构成低风险发布授权，独立测试通过后直接自动部署；`suggest` 模式仍保留上线终审；
6. 自动部署、健康检查和失败回滚。

当前限制：

- 自动修改默认允许 `apps/web` 与非核心 `apps/api`；核心库存、采购、订单、收货、配送、盘点、报损、BOM 消耗、结算和成本域必须设置 `AUTO_FIX_CORE_API_ENABLED=true` 才能准入；
- 核心 API 验证必须设置 `AUTO_FIX_TEST_DATABASE_URL`，且数据库名以 `_test` 或 `_ci` 结尾；禁止继承生产 `DATABASE_URL`；
- 资金、认证、权限、数据库、依赖和部署配置仍为永久红线，不受核心开关影响；
- 自动部署器能够按变更范围分别或组合构建、备份、发布和回滚 Web/API；
- 生产服务器已配置专用 `dianjie_autofix_ci` 隔离库及非特权账号，73 条 migration 和 186 项集成测试已在该库实机通过；
- P0 将发布顺序改为“远程候选源码持久化成功 → 生产部署 → 健康检查 → 晋升 main”，远程不可写时禁止触碰生产。
- 2026-08-01 自动修复可靠性增强：测试进程隔离生产域名变量；明确 `NO_CHANGE` 的任务经独立复验后直接解决；Qwen 临时网络/超时故障自动重试一次；转人工详情支持基于最新代码重新执行；角色业务导航壳不再被过宽红线误伤。

2026-07-29 Qwen 隔离实战结果：

- Qwen Code 在独立 worktree 中新增了全栈变更分类与验证规划模块；
- 首轮实现后，Codex 发现 API typecheck 缺少 Prisma Client 生成前置；
- Qwen 根据审查意见完成第二轮修正，新模块测试 32/32 通过；
- Codex 在另一环境独立复验，API 全量 627 项、类型检查和构建通过；
- P1 第一阶段已把现有 Web-only 策略扩展到 Web + 非核心 API；
- 独立验证按实际改动范围执行：Web 跑单测/类型检查，API 跑单测/Prisma Client 生成/类型检查/构建，混合改动两套都跑；
- API 自动发布只同步编译后的 `apps/api/dist`，发布前备份生产 dist，重启 `dianjie-v4-api` 并执行 API + 页面健康检查；
- 发布失败、人工回滚和回滚失败恢复均使用原始变更路径恢复同一批 Web/API 组件；
- 资金、权限、认证、核心库存写入/成本、数据库、依赖和部署配置仍不在 P1 自动修改范围；
- `approved_auto` 已取消低风险任务的二次上线批准；永久红线仍直接拒绝，`suggest` 模式仍需二次批准。
- 2026-07-30 本地 Gitea 已完成追平；双仓库均已包含 P1 生产发布基线。
- 2026-07-30 核心业务 API 自治闸门完成：开关严格关闭优先、部署前二次检查、隔离数据库集成测试、管理员二次批准及关闸后可回滚均已有测试覆盖。
- 当前生产核心开关继续关闭；专用测试库已经就绪，仍须补齐 GitHub 写权限并完成一次受控演练后，才允许按任务临时开启。

全栈自治目标分阶段实现：

1. Web 全自动；
2. API（不含资金、权限、库存写入）；
3. API 核心域闸门已实现，必须显式开闸并使用隔离数据库运行领域集成测试；生产当前保持关闭；
4. 数据库自治尚未开放；未来只允许新增、向前兼容 migration，必须经过空库迁移、升级验证、生产备份和人工终审，禁止自动修改历史 migration。

## 6. 不可破坏规则

1. 禁止修改历史 migration，禁止用 `db push` 代替 migration。
2. 库存与消耗修正采用冲销+补记，不直接改写已确认历史。
3. 生产写入前必须备份；排查默认只读。
4. `/app/dianjie-src` 必须干净且与生产基线一致，不允许手工编辑。
5. 自动生成代码不能绕过独立测试、diff 审查、远程源码持久化和管理员终审。
6. 业务口径不确定时进入“待外部”，不得由 AI 猜测配方、单位或财务规则。

## 7. 当前任务入口

任务状态以仓库根 [AGENT_BOARD.md](./AGENT_BOARD.md) 为准。历史 worktree 处置依据
[docs/WORKTREE_AUDIT_2026-07-29.md](./docs/WORKTREE_AUDIT_2026-07-29.md)。

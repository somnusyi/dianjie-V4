# 滇界 V4 项目最新进展与开发交接

> 更新时间：2026-07-20（Asia/Shanghai）  
> 适用对象：项目负责人、总架构师、后端、前端、测试、运维及接手开发的 AI  
> 工作区：`/Users/somnusyi/Documents/Codex/dianjie-V4-rebuilt`  
> 生产入口：`http://116.62.32.162:8080/v2/login`  
> 安全说明：本文不包含任何账号密码、数据库连接串、云密钥或员工个人凭证。

## 0. 接手者先读：当前最重要的结论

1. **生产系统稳定在线**：生产应用提交为 `0fabd1f4ecbfdada0fae829be4cd011489604583`，API、数据库、Web 和 CMB 辅助进程健康。
2. **GitHub 与 Gitea 的 `main` 均为 `fedae92`**：该提交只比生产多一份五店上线文档；实际生产应用代码仍对应其父提交 `0fabd1f`。
3. **本地存在一条未部署的供应链加固分支**：`release/20260715-p0` 当前为 `e902b61`，包含 25 个相对共同基线的独有提交；它与 `main` 已分叉，不能直接当作生产最新代码部署。
4. **禁止整分支覆盖或直接部署 release 分支**：`main` 已新增 20 个独有提交和 62 条迁移，覆盖在线盘点、供应商库存模式、版本化 BOM、库存单位/成本、门店月结等；release 分支只有 56 条迁移。应将供应链加固提交逐项移植到最新 `main`，解决冲突后完整回归。
5. **生产已有两家启用门店**：瑶海店已真实运行；包河万达店已建档，但没有店长/厨师长账号、初始盘点基准和门店安全库存策略，尚不满足真实库存和日报扣库上线条件。
6. **4–6 月经营数据已作为财务月结入库**：不会倒灌到日报或实时营业额。现有页面只能较方便地查看 6 月，4–5 月缺少历史月份入口，这是明确的展示待办。

## 1. 项目定位与目标

滇界 V4 是南京云洱之境餐饮集团使用的多角色、可扩展连锁餐饮经营管理系统。当前以合肥瑶海店为真实试点，正在向包河万达店及后续 5 家门店扩展。

系统的核心目标不是单独做一个“采购工具”或“库存表”，而是形成以下可信业务闭环：

1. 门店按实时预估库存提出订货；
2. 供应商接单、调整实发、配送；
3. 门店现场验收并一次性确认差异；
4. 实收形成门店入库和移动平均成本；
5. 每日营业与菜品销量双表导入；
6. 已发布 BOM 将销量换算成食材消耗；
7. 盘点、门店报损和历史流水共同形成实时预估库存；
8. 应付、对账、发票、付款、凭证和月结形成财务事实；
9. 老板、财务、总厨、店长、厨师长、供应商按各自权限使用同一套数据。

## 2. 版本、仓库与环境真相

### 2.1 版本矩阵

| 对象 | 当前版本 | 状态 | 说明 |
|---|---|---|---|
| 生产应用 | `0fabd1f` | 已部署 | 新店上线引导、单位/成本、BOM 生命周期、月结均已上线 |
| GitHub `main` | `fedae92` | 已同步 | 比生产多一份五店上线说明文档，无应用代码差异 |
| Gitea `main` | `fedae92` | 已同步 | 地址：`http://192.168.1.129:1606/weilun/dianjie-V4.git` |
| 主功能分支 | `feature/chef-bom-lifecycle` / `fedae92` | 已同步 | 与远端 main 同点 |
| 本地供应链加固分支 | `release/20260715-p0` / `e902b61` | 未合并、未部署 | 供应链并发、幂等、事务和审计加固候选 |
| GitHub release 分支 | `origin/release/20260715-p0` / `e46a646` | 落后本地 | 本地后续 14 个提交尚未推送 |

共同分叉点为 `f33d091`。相对该点，`main` 有 20 个独有提交，release 分支有 25 个独有提交。当前工作区位于 release 分支，且存在用户业务文件 `库存主数据映射审计_2026-07-15.xlsx`；该文件不得删除、修改或擅自加入 Git。

### 2.2 生产环境

- 服务器：阿里云 ECS，应用目录 `/app/dianjie-v4`。
- 对外地址：`http://116.62.32.162:8080`。
- 生产进程：`dianjie-v4-api`、`dianjie-v4-web`、`dianjie-v4-cmb` 均在线。
- 数据库：PostgreSQL，生产迁移账本 62 条，最后迁移完成时间为 2026-07-19 18:35（UTC+8）。
- 发布脚本：`scripts/deploy-worktree.sh`；包含部署锁、版本祖先检查、生产 DB/构建备份、迁移、构建上传、PM2 重启与健康检查。
- 当前证书和凭证轮换按负责人要求暂缓；未经授权不得自行处理。

### 2.3 本地开发环境

- Web 常用入口：`http://localhost:3200/v2/login`。
- API 常用入口：`http://127.0.0.1:4444`。
- PostgreSQL：本机 Docker，常用端口 `127.0.0.1:5434`。
- 集成测试必须使用名称以 `_test` 或 `_ci` 结尾的隔离数据库；测试保护器会拒绝连接业务库。
- 不要在正在运行的本地 Next.js `.next` 目录直接做生产构建；使用干净 worktree 或项目安全构建脚本。

## 3. 技术架构

```text
浏览器 / PWA / Capacitor
          │
          ▼
 Next.js 14 + React 18 + Tailwind
          │ REST / JWT
          ▼
 Fastify 4 + TypeScript
          │
          ▼
 Prisma 5 + PostgreSQL

辅助能力：Redis / BullMQ、阿里云 OSS、CMB Python 服务、美团接口代码、企业微信代码、Sentry SDK
```

### 3.1 Monorepo

| 路径 | 职责 |
|---|---|
| `apps/web` | 多角色 Web/PWA、移动端和财务 PC 页面 |
| `apps/api` | Fastify REST API、领域服务、导入器、调度与外部集成 |
| `apps/cmb` | 招行国密请求辅助服务；自动付款当前关闭 |
| `packages/db` | Prisma schema、迁移、客户端和 seed |
| `scripts` | 构建、部署、迁移核验、数据回填和专项验收脚本 |

### 3.2 必须保持的工程边界

- 多租户查询必须带 `tenantId`；门店角色再限定 `storeId`，供应商角色再限定 `supplierId`。
- 金额使用 Prisma `Decimal`，禁止用 JavaScript 浮点累计关键财务金额。
- 状态、库存、金额、事件和审计日志应处于同一事务；失败必须全部回滚或可安全重试。
- 幂等键必须校验“同键同内容”，同键不同内容返回 409，不能静默返回旧结果。
- 历史单据读取冻结快照，商品改名、改分类、改价格不能改变历史单据。
- 微信和旧 Android WebView 禁止依赖 `crypto.randomUUID`；使用项目 `clientRequestId()`。
- 不得用 `prisma db push` 代替正式迁移。
- 不拆微服务；除 CMB 辅助服务外，继续保持模块化单体，先保证业务一致性。

## 4. 角色与主要工作台

| 角色 | 主要职责 | 主要入口 |
|---|---|---|
| 老板 | 集团经营、门店、审批、资金和利润总览 | `/v2/boss/home`、`/v2/boss/stores` |
| 财务 | 各店、应付、发票、付款、凭证、月结与报表 | `/v2/finance/home`、`/v2/finance-pc/home` |
| 总厨 | 菜品档案、版本化 BOM、报损/差异仲裁 | `/v2/chef-director/home`、`/v2/chef-director/dishes` |
| 店长 | 营业、日报上传、实时库存、安全库存和门店运营 | `/v2/manager/home`、`/v2/manager/ops` |
| 厨师长 | 实时库存、订货、收货、盘点 | `/v2/chef/home`、`/v2/chef/inventory` |
| 普通厨师 | 门店业务协作；必须绑定门店 | 受角色路由限制 |
| 供应商 | 商品/分类、接单、实发、配送、库存与对账 | `/v2/supplier/home` |
| 管理员/工程 | 账号、门店和系统配置 | `/users`、工程工作台 |

账号密码不写入交接文档。需要生产账号时由项目负责人通过安全渠道提供，或由管理员按最小权限新建。

## 5. 当前模块成熟度

| 模块 | 生产状态 | 当前完成度 | 主要剩余事项 |
|---|---|---|---|
| 登录与权限 | 已上线 | 高 | 企微完整 SSO、生产测试账号治理 |
| 门店建档 | 已上线 | 中高 | 新店资料校验、账号和盘点上线门槛自动化 |
| 订货单 | 已上线 | 高 | 将 release 分支的并发/幂等加固移植到 main |
| 配送与实发 | 已上线 | 高 | 移植发货/送达审计原子性和重放校验 |
| 门店收货 | 已上线 | 高 | 持续真实 UAT；禁止事后补报 |
| 供应商商品/分类 | 已上线 | 中高 | 移植分类并发加固；长期主数据模型评审 |
| 供应商库存 | 已上线，可配置 | 中高 | 当前正式供应商为 `NOT_TRACKED`；做期初盘点后才切严格库存 |
| 门店预计库存 | 已上线 | 中高 | 包河店缺初始基准；各店安全线尚未配置 |
| 在线盘点 | 已上线 | 中高 | 生产尚无通过在线流程确认的盘点单；需真实 UAT |
| 日报双表导入 | 已上线 | 高 | 每日连续使用、历史月份展示、异常提醒 |
| 菜品与 BOM | 已上线 | 中高 | 5 个待处理 BOM 任务；持续补齐规格和新菜 |
| 移动平均成本 | 已上线 | 中高 | 高价值 SKU 单位换算复核、成本月末对账 |
| 月度利润/结账 | 已上线 | 中 | 已导入 4–6 月；缺历史月份查看入口 |
| 财务应付/发票/付款/凭证 | 已上线 | 中高 | 真实财务 UAT、release 分支凭证/事务加固整合 |
| 美团 | 代码存在、生产未启用 | 中低 | 平台审核、配置、门店映射、影子双跑 |
| CMB 自动付款 | 服务在线、自动付款关闭 | 受控 | 必须获得业务授权后才能启用 |
| 监控与 CI | 部分具备 | 中低 | Sentry DSN、正式 CI、staging 和告警 |

## 6. 已确认且不得擅自改变的业务规则

### 6.1 订货、实发与收货

1. 所有货物由供应商直接送门店，目前没有集团总仓。
2. 一张订货单允许多次配送；每次配送生成独立配送单。
3. 必须保留原始订货量、单价和金额；供应商改单必须由门店确认。
4. 供应商发货前可以调整本次实发量；实发量决定配送金额和供应商库存扣减。
5. 页面和接口必须区分订购量、实发量、实收量，以及订货金额、实发金额、应付金额。
6. 门店必须在收货时一次性确认短缺、破损、变质、品质和证据；收货确认是供应商责任截止点。
7. 收货确认后不得再向供应商补报，不建立“免费补送”；之后发现的问题进入门店内部报损或盘点差异。
8. 到货差异应付按“合格实收数量 × 订货冻结单价”计算。
9. 已确认入库单的商品、数量、价格和应付不可直接倒改；非金额资料可做有审计的备注更正。

### 6.2 供应商库存与门店库存

1. 供应商库存和门店库存是两本独立账。
2. 供应商有物理库存、活动预占和可用库存：`可用 = 物理 - 活动预占`。
3. 启用严格库存时，接单预占，取消/拒单释放，发货核销预占并扣物理库存。
4. 未完成供应商期初盘点时使用 `NOT_TRACKED`，不得因为系统库存为 0 阻断真实发货。
5. 门店预计库存：最近可信盘点基准 + 后续确认实收入库 - BOM/人工消耗 - 门店内部报损。
6. 门店盘点与供应商无关，不能生成供应商差异、应付调整或供应商库存变化。
7. 已确认盘点不可编辑；录错时整单冲销后重新盘点。

### 6.3 日报、菜品与 BOM

1. 美团接口正式接入前，店长每天上午 11:00 前上传前一营业日两份 Excel：综合营业统计、菜品销售明细。
2. 必须先预览再确认；确认后原子更新营业、菜品销量和可执行 BOM 库存消耗。
3. 套餐不得作为最终扣库明细，应使用实际出货菜品。
4. 退菜不补回库存；BOM 使用毛重口径。
5. 百家蘸料不扣库存；赠品按对应库存商品直接扣减。
6. 鲜鸡和冻品只能按已确认映射替代，不能模糊串货。
7. 菜品未建档或缺可执行 BOM 时可显式暂缓，进入总厨待办；补齐后幂等回补历史消耗。

## 7. 库存单位、成本与 BOM 模型

### 7.1 P1：采购单位与库存基础单位

- 商品保存采购单位、库存基础单位、每采购单位包含的基础单位数量和验证状态。
- 收货按采购单位录入；库存、BOM、消耗和门店安全线统一使用库存基础单位。
- 收货、消耗和报损保存单位/成本快照，主数据后改不影响历史。
- 已确认示例：生蚝 `1 箱 = 18 个`；乌苏 `1 箱 = 6 罐`。
- 生产共有 456 个商品单位合同：182 个 `VERIFIED`，274 个 `INFERRED`；其中启用商品 452 个。

### 7.2 P2：移动平均成本

- 收货后用剩余正库存与本次采购成本重算基础单位移动平均成本。
- 预估库存为负时，新收货成本成为新的可信成本，避免负库存放大成本。
- 日报 BOM 消耗和门店报损使用当时移动平均成本。
- 中间计算保留 6 位小数，业务单据金额最后统一四舍五入。
- BOM 页面不再把不可靠的采购包装价格直接当作单份菜品成本。

## 8. 生产数据快照（2026-07-20 只读核验）

### 8.1 主数据与业务事实

| 指标 | 当前值 |
|---|---:|
| 启用门店 | 2 |
| 活跃用户 | 18 |
| 启用供应商 | 1 |
| 启用商品 | 452 |
| 菜品 | 162 |
| 已发布 BOM 版本 | 135 |
| 已确认/已记账收货单 | 48 |
| 库存消耗记录 | 682 |
| 已确认日报 | 3 |
| 已确认财务月结 | 3 |
| 待处理 BOM 任务 | 5 |
| 已历史回补 BOM 任务 | 13 |

### 8.2 门店上线状态

| 门店 | 状态 | 账号 | 库存基准 | 安全库存策略 | 判断 |
|---|---|---|---|---|---|
| DJ001 合肥瑶海店 | 运营中 | 店长 1、厨师长 3 | 2026-07-13，167/167 项匹配，价值约 ¥31,649.59 | 尚无门店策略记录 | 已真实运行；需补重点 SKU 安全线 |
| DJ002 合肥包河万达店 | 运营中 | 无已绑定店长/厨师长/厨师 | 无 | 无 | **未满足库存与日报上线门槛** |

### 8.3 已确认日报

| 营业日 | 营业额 | 营业收入 | 订单量 |
|---|---:|---:|---:|
| 2026-07-16 | ¥5,717.40 | ¥4,330.98 | 22 |
| 2026-07-17 | ¥7,468.50 | ¥6,065.28 | 27 |
| 2026-07-18 | ¥11,132.30 | ¥9,315.35 | 39 |

### 8.4 4–6 月财务月结

| 月份 | 财务营业收入 | 净利润 | 状态 |
|---|---:|---:|---|
| 2026-04 | ¥0.00 | -¥550.50 | 已确认 |
| 2026-05 | ¥236,210.05 | ¥83,765.00 | 已确认 |
| 2026-06 | ¥239,639.47 | -¥47,803.63 | 已确认 |

这些数据保存在 `store_monthly_closes`，用于对应月份的财务 P&L，不会倒灌到日报或实时营业额。目前店长“营业 → 上月”可看到 6 月及“财务月结已确认”；4–5 月没有历史月份选择器，属于 P1 展示缺口。

## 9. 自动化验证证据

### 9.1 当前生产/main 证据

- API 单元测试：24 个文件，121 项通过。
- Web 单元测试：5 个文件，16 项通过。
- PostgreSQL 集成测试：13 个文件，56 项通过。
- 新店专项流程：建店、账号门店绑定、安全库存、首次盘点和库存读取通过。
- 供应链专项流程：下单、接单、预占、发货、到货、实收、应付和争议通过。
- 干净 worktree 生产构建：API 成功，Web 142 个页面成功。
- 生产部署后 API、数据库、Web 及关键角色页面健康。

### 9.2 未合并 release 分支证据

- API 单元测试：106/106。
- PostgreSQL 集成测试：56/56。
- Web 测试：13/13；前后端 TypeScript 检查通过。
- 从空库执行 release 的 56 条迁移并与 schema 对比为零差异。
- 真实 HTTP 流程覆盖创建订货、改单、发货、送达、收货的并发重放、审计失败回滚和状态竞争。

这些证据只证明 release 分支本身在其旧基线可用，**不能替代将其移植到当前 main 后的重新测试**。

## 10. 未合并供应链加固分支说明

`release/20260715-p0` 的主要价值是加强供应链写入的一致性：

- 商品、数量、金额和状态输入边界；
- 分类创建、导入、排序和单品编辑的并发串行；
- 供应商入库批次编号并发；
- 收货确认、拒收、作废的状态竞争；
- 厨师验收与配送状态并发；
- 发货和送达业务事实与审计日志原子性；
- 订货创建、发货、改单的同键同内容幂等和并发回放；
- 操作日志失败时业务事实整体回滚。

### 推荐整合方式

1. 从最新 `origin/main` 新建 `integration/supply-hardening-main`。
2. 按领域分组移植，不要把 release 分支整体合并后直接部署。
3. 推荐顺序：输入边界 → 分类并发 → 收货状态机 → 配送审计 → 订货/发货/改单幂等。
4. 每组解决 `orders.ts`、`receipts.ts`、`products.ts`、`lossClaims.ts` 与 main 新单位/成本/BOM/库存逻辑的冲突。
5. 每组运行单元、集成、TypeScript 和专项 HTTP E2E。
6. 最后在生产备份副本上执行全部 62 条 main 迁移和整合后 schema 核验。
7. 只有在 GitHub/Gitea main 同步、干净 worktree 构建和生产备份完成后部署。

## 11. 五店扩展的标准上线流程

每家店必须逐项完成后才能打开真实日报扣库：

1. 财务创建门店主档，确认门店编号、名称、地址和开业状态。
2. 管理员创建并绑定店长、厨师长账号；普通厨师账号也必须绑定门店。
3. 厨师长完成开业初始全品项盘点并确认，建立第一条可信库存基准。
4. 店长设置重点 SKU 的安全库存和补货目标。
5. 用真实小额采购单跑通下单、接单、发货和收货，核对库存增加和移动平均成本。
6. 上传首份日报，确认门店、日期、营业额、营业收入、订单、优惠、菜品映射和 BOM 无阻断问题。
7. 确认日报后抽查至少 5 个高销量菜品的 BOM 扣减与库存余额。
8. 店长、厨师长和财务共同签字确认后，将门店从试运行切为正式运行。

当前包河万达店应立即从第 2 步开始，不应先上传真实日报。

## 12. 下一阶段任务优先级

### P0：生产安全与第二店上线

1. 为 DJ002 创建并绑定店长、厨师长账号。
2. 导入并确认 DJ002 开业初始盘点；设置重点 SKU 安全线和补货目标。
3. 将 release 供应链加固逐组移植到最新 main，完成 62 迁移基线上的全量回归。
4. 给 4–6 月财务月结增加历史月份选择器和来源/差异说明。
5. 对生产 274 个 `INFERRED` 单位换算按销量、金额和混淆风险排序，优先人工确认高风险 SKU。

### P1：业务完整性

1. 总厨处理 5 个待办 BOM，持续补齐新菜、规格菜和实际出货菜。
2. 为瑶海店配置重点商品安全库存，为后续店铺形成可复制模板。
3. 做连续 7 天“日报 → BOM → 库存 → 采购 → 收货 → 成本”真实对账。
4. 完成在线盘点生产 UAT：创建、录入、证据、提交、确认、冲销和历史查看。
5. 建立供应商 `NOT_TRACKED → STRICT` 的期初盘点、启用审批和回退规则。

### P2：规模化与外部集成

1. 美团接口审核、配置、门店映射和影子双跑；正式切换前保留人工日报核对。
2. 配置 Sentry、CI、staging、日志告警和数据质量日报。
3. 企业微信 HTTPS、域名、SSO 和通知 E2E。
4. 经业务授权后再做 CMB 自动付款影子验证；默认继续关闭。

## 13. 标准发布与回滚流程

1. 确认目标提交已推送 GitHub `main`，并同步 Gitea。
2. 使用独立部署 worktree，保证 `git status` 干净。
3. 运行 API/Web 单元测试、隔离 PostgreSQL 集成测试、API build、Web TypeScript 与正式构建。
4. 在生产备份副本上执行 `prisma migrate deploy` 和 schema 核验。
5. 运行 `scripts/deploy-worktree.sh`；不得绕过部署锁、版本祖先检查和自动备份。
6. 部署后核验 `/api/health`、`/v2/login` 和各角色关键页面。
7. 核对 PM2、迁移账本、错误日志增长、生产 `.deployed-commit`。
8. 出现核心链路错误时，使用部署前数据库和构建备份按标准流程回滚；禁止手工改生产数据掩盖问题。

## 14. 关键文件索引

| 文件/目录 | 用途 |
|---|---|
| `packages/db/prisma/schema.prisma` | 数据模型唯一权威入口 |
| `packages/db/prisma/migrations/` | 正式迁移账本 |
| `apps/api/src/routes/orders.ts` | 订货、接单、发货和改单主路由 |
| `apps/api/src/routes/receipts.ts` | 收货、确认、拒收、作废和应付事实 |
| `apps/api/src/routes/inventory.ts` | 门店实时预估库存和策略 |
| `apps/api/src/routes/inventoryCounts.ts` | 在线盘点状态机和可信基准 |
| `apps/api/src/routes/dailyBusinessImports.ts` | 双表日报预览、确认和 BOM 扣减 |
| `apps/api/src/routes/dishes.ts` | 菜品、BOM 版本、发布和历史回补 |
| `apps/api/src/services/inventoryUnits.ts` | 采购单位/库存基础单位换算 |
| `apps/api/src/services/inventoryCosting.ts` | 移动平均成本 |
| `apps/api/src/services/storeInventory.ts` | 门店库存滚动计算 |
| `apps/api/src/routes/profit.ts` | 日报经营与财务月结利润口径 |
| `apps/api/tests/routes/supplyChainFlow.integration.test.ts` | 供应链 PostgreSQL 集成主测试 |
| `apps/api/tests/routes/newStoreOnboarding.integration.test.ts` | 新店上线专项测试 |
| `scripts/deploy-worktree.sh` | 标准生产部署入口 |
| `FIVE_STORE_ROLLOUT_READINESS_2026-07-19.md` | 五店上线门槛与上一轮验收记录（位于 main） |
| `SUPPLY_CHAIN_DECISIONS_2026-07-18.md` | 供应链已确认业务规则 |

## 15. 接手第一天建议清单

1. 阅读本文、`SUPPLY_CHAIN_DECISIONS_2026-07-18.md` 和 main 上的五店上线文档。
2. 不改代码，先确认 GitHub main、Gitea main、生产 `.deployed-commit` 和当前工作区分支。
3. 保护未跟踪的用户 Excel 文件，不做清理或批量提交。
4. 从 `origin/main` 创建新的供应链整合分支，不直接在当前 release 分支继续堆功能。
5. 先完成 DJ002 账号与初始库存资料收集；没有盘点时不进行真实库存扣减。
6. 将 release 加固拆成可审查的小批次移植，每批测试后提交。
7. 所有生产操作先备份、后发布、再验收，并把提交号和验证结果写回交接记录。

## 16. 交接完成标准

接手者只有在以下问题都能明确回答时，才算完成交接：

- 生产当前运行哪个提交，GitHub/Gitea main 又分别是什么？
- 为什么 release 分支不能直接部署？
- 供应商库存和门店库存为什么必须分账？
- 订购量、实发量、实收量和应付金额分别由什么决定？
- 日报确认如何通过 BOM 扣减库存，缺 BOM 时如何暂缓和回补？
- 新门店为什么必须先建账号、做初始盘点和安全库存？
- 4–6 月数据为什么不显示在实时营业额中？
- 如何运行隔离集成测试、标准部署和回滚？

以上任一项不清楚，应先补齐理解和验证证据，再继续生产开发。

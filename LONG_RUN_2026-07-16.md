# 滇界 V4 12 小时无人值守推进任务

开始时间：2026-07-16 19:10（北京时间）  
执行分支：`release/20260715-p0`  
执行边界：仅本地代码与 `dianjie_v4_local`；不推送、不部署、不修改生产数据或凭证。

## 目标

在不等待人工干预的情况下，以可审计的小批次持续推进当前发布候选：优先消除数据一致性、权限隔离和核心业务闭环风险，再补齐自动化测试、运行手册与生产发布证据。

## 循环协议

每轮必须依次完成：

1. 从代码、数据库约束、页面和现有测试中选择一个最高风险且业务口径明确的问题。
2. 先定义失败场景和验收条件，再做最小范围修改。
3. 运行相关单测、数据库端到端、TypeScript/Next 构建与空白检查。
4. 失败时修复或撤回本轮，不把红色基线带入下一轮。
5. 通过后记录证据并建立本地 Git 检查点。
6. 重新排序风险队列，进入下一轮。

## 不自动决策的事项

- 不猜测食材单份克重、补送、报损收货、已确认入库更正等业务规则。
- 不升级框架大版本，不变更审批角色和财务口径。
- 不删除用户文件，不提交本地业务 Excel 或环境文件。
- 不连接或写入生产数据库，不执行远端发布、推送和凭证轮换。

缺少口径的事项进入阻塞清单，但不阻塞其他工作。当前业务阻塞仅有：`虎掌菌` 每份扣减克重待确认。

## 12 小时工作队列

### A. 架构与数据入口

- 日报双文件的日期、门店、金额、赠品和多门店边界。
- 预览版本分配、重复上传和并发确认的幂等与原子性。
- BOM 变更指纹、历史更正、营业记录与库存消耗的审计闭环。
- 迁移从零部署、既有库 schema diff 和必要索引。

### B. 核心模块

- 订货单不可变、供应商改单确认和单号并发。
- 分批配送、手工/自动收货竞争、短量报损和财务派生。
- 供应商商品/分类审批、库存入库/报损/快照并发。
- 店长与厨师长预计库存、采购收货和日报扣减之间的口径一致性。

### C. 权限与安全

- 店长、厨师长、供应商、总部角色的租户/门店/供应商隔离。
- 文件上传大小、格式、错误信息和异常数据拒绝策略。
- 高置信敏感信息扫描与生产操作护栏。

### D. 测试与发布证据

- API 单测、关键数据库 E2E、Web 类型检查和正式构建。
- 迁移链、幂等重试、并发竞争、故障回滚与测试残留检查。
- 生产发布前检查、备份、回滚和 UAT 清单。

## 时间分配

| 时段 | 重点 | 出口条件 |
|---|---|---|
| 0-2 小时 | 架构基线、日报/BOM P0 | 数据入口负向用例与并发 E2E 通过 |
| 2-5 小时 | 订货/配送/收货/库存 | 核心脚本全部通过且无测试残留 |
| 5-8 小时 | 权限、财务派生、异常补偿 | 跨租户与故障注入通过 |
| 8-10 小时 | Web 操作体验和可观测性 | 页面构建、错误反馈和状态提示通过 |
| 10-12 小时 | 全量回归、文档和发布清单 | 绿灯基线、本地提交和风险清单完成 |

## 执行记录

### 第 1 轮：日报数据入口与并发预览

- 修复显式收入为 0 的赠品被错误替换为“销售额减优惠”的问题。
- 综合营业文件只允许单店单日，拒绝多行门店/日期数据和负数关键指标。
- 菜品销售文件采集门店名，阻断两份文件门店不一致、销售文件多门店和退菜跨门店。
- 增加报表门店与登录账号目标门店的匹配校验，防止错店导入。
- 严格校验日历日期；当天和未来营业日不能提前导入。
- 日报预览版本分配改为门店/营业日事务锁，解决并发重复预览的版本冲突。
- 新增并发重复预览端到端断言；同一文件对只生成一个审计版本。

验证：日报解析 7/7、API TypeScript 构建、日报数据库端到端均通过；端到端最终营业收入 120、销量 3、BOM 消耗 0.75，历史状态为两版 `SUPERSEDED` 加一版 `CONFIRMED`，测试数据已清理。

### 第 2 轮：手工销量与 BOM 消耗一致性

- 门店级角色的销量列表、销量榜和预计消耗强制限定绑定门店，不能通过查询参数越权。
- 手工销量只能写入 `manual` 数据源，不能覆盖日报/POS 等系统来源记录。
- 写入前校验门店和菜品均属于当前租户，并严格校验营业日期。
- 销量 upsert、旧消耗删除和新 BOM 消耗创建合并为同一事务，并按门店/菜品/日期加事务锁。
- `EXCLUDE` 菜品的手工销量不生成库存消耗；BOM 数量按 6 位精度写入。
- 配方新增/修改强制食材属于当前租户，且配方单位必须等于库存 SKU 单位。
- 新增本地数据库端到端，覆盖跨店读写、系统来源伪造、跨租户配方、单位错配、并发双更新和库存写入故障回滚。

验证：两个并发销量更新均成功，最终销量 4 与消耗 1kg 一致；故障注入时接口返回 500，销量和原消耗完整回滚；临时触发器、账号、门店、租户和业务数据全部清理。

### 第 3 轮：日报审计表数据库完整性与迁移链

- 将日报状态从自由字符串收紧为数据库枚举：`PREVIEWED / CONFIRMING / CONFIRMED / SUPERSEDED`。
- 为日报审计记录补齐租户、门店、创建人和确认人的数据库外键，防止孤儿记录。
- 确认人删除时保留业务审计并将引用置空；租户、门店和创建人仍受限制保护。
- 增加可重复执行的本地空库迁移验证脚本，带本地库护栏、自动建库、全链部署、schema diff 和自动清理。
- 首次空库演练发现复合唯一索引的 Prisma/数据库命名漂移；补齐 `map` 后重新演练为零差异。

验证：既有本地库 40/40 迁移已应用，schema diff 为零；全新临时空库从首条迁移部署 40/40 成功，`migrate status` 最新、schema diff 为零，临时数据库自动删除。

### 第 4 轮：独立入库单权限、报损原子性与金额一致性

- 补录、确认、报损确认、拒收和作废统一收紧为门店操作角色或品牌管理员；店长和厨师长只能操作绑定门店。
- 供应商标记送达和供应商核对均校验入库单归属，阻断同租户内供应商之间横向越权。
- 补录前校验门店、供应商、商品的租户和供应商归属，拒绝重复商品、非法日期以及超过数据库精度的数量/单价。
- 手工入库单号改用事务安全的业务序列；入库单、明细和操作日志在同一事务创建。
- 报损确认要求完整且不重复提交全部明细，拒绝超收；实收金额和损耗金额改用 Decimal 逐行计算并按两位小数入账。
- 入库单抢占、明细实收、报损单、日志和采购单完结判断合并为单个事务，并发重复确认只能有一个请求落账。
- 修复手工入库报损时把空采购单 ID 写成空字符串导致外键失败的问题，未关联采购单时保持 `null`。
- 新增本地数据库端到端脚本，临时测试门店、账号、供应商和商品按精确 ID 回收，不触碰既有测试业务数据。

验证：入库单端到端覆盖跨店、跨供应商、角色越权、商品归属、明细遗漏、超收、并发双确认、账期和对账单唯一性；最终实收 ¥14.43、损耗 ¥4.44，全部断言通过且临时数据为零。订货单不可变、两批配送/手工自动收货竞争、供应商库存并发与 Web TypeScript 回归同步通过；API 60/60 测试及构建通过。

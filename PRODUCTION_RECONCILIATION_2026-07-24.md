# 生产与发布候选收敛核验（2026-07-24）

## 边界

- 本次仅通过 SSH 读取生产 `.deployed-commit`，并在 PostgreSQL `READ ONLY` 事务中执行聚合查询。
- 未上传文件、未执行迁移、未修改生产数据、未重启服务、未部署应用。
- 代码修复位于隔离分支 `stabilize/20260724-production-safety`，不直接修改或部署 `main`。
- 胡萝卜汁“1 袋 = 1 包”和清远鸡“1 箱约 13 盒”仍等待采购确认，本次不据此继续修改主数据。

## 生产基线

- `.deployed-commit`：`a6d64c9db1a62ebec7c21018af116ea1dfb36430`
- Prisma 已完成迁移：63
- 未完成且未回滚的失败迁移：0
- 最新迁移：`20260722014111_add_consumption_void_correction`
- 远端 `main` 的 `f5a4192` 是部署提交之后的通知脚本提交，当前未反映在 `.deployed-commit`。

## 消耗冲销核验

- 作废消耗：30 行。
- 补记消耗：30 行。
- 30 行补记全部关联到已作废原行。
- 孤儿补记：0。
- 补记与原行的租户、门店或商品不一致：0。
- 当前代码中的直接运行时消耗读取均排除 `voidedAt` 非空行；营业日报使用按营业日期生效的已发布 BOM，不回退旧 `dish_recipes`。

## 供应商库存台账核验

`Product.stock` 是供应商物理库存余额；库存流水为追加式审计事实，批次余额合计应与 `Product.stock` 一致。

| 商品 | Product.stock | 流水累计 / 最新余额 | 批次余额 | 结论 |
|---|---:|---:|---:|---|
| 羽衣甘蓝汁（果汁包） | 4900 | 49 / 49 | 49 | 严重不一致；主数据脚本直接将商品余额乘 100，未同步流水和批次 |
| 胡萝卜香橙（果汁包）[已并入] | 0 | 50 / 50 | 50 | 严重不一致；停用脚本直接清零商品余额 |
| 清远鸡（真空包装）[已并入] | 0 | 183.693 / 183.690 | 183.690 | 严重不一致；停用脚本直接清零商品余额 |
| 冷冻香橙胡萝卜汁 | 6.67 | 6.667 / 6.667 | 6.670 | 商品余额与批次一致；流水存在 0.003 精度差，需纳入统一精度审计 |
| 清远鸡盒装 | -480 | -480 / -480 | 0 | 商品与流水一致，但为负库存且没有批次余额，属于既有历史异常 |

以上差异会影响供应商可用库存、预占、库存金额和库存审计。在完成确认和修复前，不应继续对前三个不一致 SKU 做供应商库存操作。

## 商品状态追查

冷冻香橙胡萝卜汁、清远鸡盒装和竹荪当前为 `PENDING_DISABLE`。只读操作日志与审批单表明，这是 2026-07-23 供应商随后提交的三张停售审批，不是合并脚本遗漏恢复；三张审批仍为 `PENDING`，应由总厨按正常流程处理。

## 本地安全收敛

- 删除旧 `/login` 页面内置的快捷登录身份和口令。
- 上传和重新签名接口使用严格查询 schema。
- OSS 对象重新签名只接受“允许类别 / 当前精确租户 / 非空对象路径”，不再使用租户 ID 子串判断。
- 保留 `main` 已新增的 `inventory-counts` 上传类别。
- 旧登录页补充移动端响应式布局，390×844 不再横向溢出。

## 验证

- API 单元测试：152/152。
- API PostgreSQL 集成测试：93/93。
- 上传租户隔离目标文件：26/26。
- API TypeScript build：通过。
- Web 测试：22/22。
- Web TypeScript 检查：通过。
- Web 正式构建：142 页，通过；仅保留既有 OpenTelemetry 动态依赖 warning。
- 390×844 浏览器：`/login` HTTP 200；快捷角色入口 0；账号和密码输入正常；无横向溢出；控制台错误 0。
- 原工作区 Web dev 已恢复到 3200，`/v2/login` HTTP 200。

## 发布门禁与后续顺序

1. 暂停新的生产部署和生产数据修正，不回滚当前生产。
2. 采购确认胡萝卜汁、清远鸡两个换算关系。
3. 由业务负责人确认三处库存不一致的真实物理余额；再设计带 dry-run、前后对账和审计记录的修复批次。
4. 总厨处理三张待停售审批，避免在库存修复前批准仍有批次余额的重复档案。
5. 安全分支完成复核后，按 `main` 当前语义手工合入；不得整分支合并 `release/20260715-p0`。
6. 安全补丁进入生产前应完成历史凭证轮换；本报告不记录或输出任何凭证值。

## 瑶海 7 月 22 日闭店盘点对账

### 只读事实

- 用户提供的 `瑶海7.22号盘点.xlsx` 有 167 个物料行、127 个非零行，表内金额合计 `¥27,605.50`；行名、规格和单位与生产中的 7 月 13 日盘点模板逐行一致。
- 生产中最新库存基线仍是 2026-07-13：167 行、167 行已绑定商品、金额 `¥31,649.586`。
- 2026-07-22 营业日报于 2026-07-23 02:56:21（Asia/Shanghai）确认，生成 246 条有效 `daily_pos` 消耗，覆盖 152 个商品；这才是“7 月 23 日上传 22 日消耗后”的正确闭店账面时点。
- 从 7 月 13 日基线的次日滚动至 7 月 22 日期间，生产有 9 张已入账收货单、119 个收货明细，采购金额 `¥27,090.26`；没有应计入该时段的已批准门店手工报损。
- 差异工作簿的 136 个可比盘点行可由生产冻结数量、收货和有效消耗独立复算。Kimi 留档口径为账面 `¥33,373.74`、实盘按账面成本计价 `¥27,627.86`、净差 `-¥5,748.65`。以当前生产主数据重算为 `-¥5,750.64`，仅相差 `¥2.01`；差额来自已并入的胡萝卜果汁包档案参考成本从 `4.43` 变为 `4.40`，数量结论未变。
- 不能把当前预计库存总金额直接与盘点表总金额相减：系统滚动结果含 107 个盘点模板未覆盖的商品档案，其中既有重复档案的正余额，也有大量仅消耗形成的负余额；预计库存服务对负余额金额按零显示，而差异分析使用带符号数量。

### 主要差异并非都是真实盘亏

| 物料 | 差异金额 | 判断 |
|---|---:|---|
| 清远鸡盒装 | `+¥1,326.00` | 收货落在“真空包装”重复档案、消耗落在“盒装”；等待确认 `1 箱≈13 盒` |
| 冷冻香橙胡萝卜汁 | `-¥1,198.56` | 同一物料拆成包、袋、克三个档案，不能按单档案认定短缺 |
| 竹荪 | `+¥577.02` | 入库和消耗分属件、克档案，且 BOM 用量仍需复核 |
| 汤底调味粉 | `-¥567.90` | 盘点反推实际消耗约为系统的 4.8 倍，BOM/基线规格待确认 |
| 猪黄喉 | `+¥518.03` | 系统消耗显著过大，已定位到约 10 倍的 BOM 单位错误风险 |
| 白葱 / 紫葱见手青 | `-¥511.71 / -¥331.90` | 盘点反推实际用量约为系统记录的 1.9 倍 |
| 鲜花饼 | `-¥485.60` | 304 枚去向未入库存消耗；业务已说明主要为赠品，不能当普通盘亏 |
| 羽衣甘蓝果汁包 | `-¥452.00` | 消耗落在“羽衣甘蓝叶子”档案；当前主数据又把存续档案从箱改为袋，历史基线单位仍是箱 |
| 大米龙 / 云南腊火腿 | `-¥417.23 / -¥416.89` | BOM 用量偏小或存在未挂 BOM 的领用/损耗 |

### 新基线导入门禁

- 暂停把 7 月 22 日盘点写成生产新基线。审计时的原导入脚本只读取 `Product.unit` 并调用旧 `normalizeInventoryQuantity`，没有按 `inventoryUnit` 合同归一化；而预计库存服务把已存的 `normalizedQuantity` 直接当作 `inventoryUnit`。在采购单位和库存单位不同的商品上会写入错误基线。该缺陷已在隔离分支修复，尚未部署。
- 原导入脚本只报告 `unmatchedCount`、`ambiguousCount` 和 `normalizationPendingCount`，`--commit` 前没有强制它们为零；因此部分映射或待换算状态也能被提交。隔离分支现将所有阻塞行和配置问题收敛为硬门禁。
- 旧口径识别 31 个没有可直接按当前主数据推导换算的盘点行；Kimi 清单列了 30 行，漏列的是数量为零的“鸡架”（分析时隐含按 `1 件=1 箱`）。按新门禁结合存续商品状态重算后，待确认项分成“商品重新绑定”和“单位换算”两类，见下节。
- 新基线必须把合并后的物料绑定到存续档案，不能沿用 7 月 13 日快照中的已停用重复商品 ID；胡萝卜汁、清远鸡和羽衣甘蓝的单位关系必须在采购/业务确认后冻结为本次盘点的换算快照。

### 隔离分支实现与 7 月 22 日 dry-run

- 新增独立导入计划器：只使用当前租户内 `ENABLED` / `PENDING_DISABLE` 商品；同模板且商品、库存单位均未变化时才沿用上一基线的冻结系数；库存单位变化后按当前合同重算。
- 人工复核绑定以源文件 SHA-256、租户、门店编号和盘点日期共同锁定，换算覆盖必须同时提供正数系数、当前库存单位和确认依据；未命中的复核行同样阻止提交。
- `--commit` 现在还需要显式确认口令，并在任何未匹配、名称歧义、待换算或复核配置问题存在时拒绝写入；替换旧快照的删除与新快照创建仍位于同一事务，门禁在事务和删除之前执行。
- 门店从名称模糊匹配改为“租户 slug + 门店编号”精确定位；跨租户同名商品不能参与匹配。离线解析不再要求数据库连接。
- 修复真实工作簿的两个格式边界：表尾合计金额位于“合计金额”标签上一行，以及“保拉纳（白）”盘点单位是 Excel 富文本。原文件离线复核得到 167 行、127 行非零、40 行为零、金额 `¥27,605.50`，SHA-256 为 `acb0b065b91b33a8d01fba8c71d3444bd39a01ae143fbe349d2d52e1444939e5`。
- 使用此前只读导出的生产 7 月 13 日快照 167 行和相关商品主数据做离线计划：137 行匹配到存续商品，其中 122 行可安全沿用冻结换算；30 行因上一基线商品已停用或不存在而未匹配，另有 15 行商品已匹配但单位换算待确认。总计 45 行阻止提交，`canCommit=false`。
- 45 个阻塞行中有 12 行实盘非零。8 行需要业务重新绑定存续商品：美味牛肝菌、奶浆菌、基围虾、吊龙切片、牛筋丸、冷冻酸木瓜汁、香水柠檬、胡萝卜果汁包。4 行需要采购确认换算：米布丁 `1.2 袋 → 件`、腐乳酱 `7 袋 → 箱`、李锦记生抽 `2 桶 → 箱`、牛肉酱 `5 袋 → 箱`。其余 33 行虽然实盘为零，仍保持阻塞，避免把不完整模板写成新基线。
- `dianjie_v4_local` 没有生产的 7 月 13 日基线快照，因此其直接匹配结果不能代表生产；本次只在 `dianjie_v4_ci` 写入测试夹具，生产和本地既有库均未写入盘点。

### 建议执行顺序

1. 先复核隔离分支的导入器修复；在代码进入受控发布前继续禁止生产导入。
2. 业务先确认 8 个非零行应绑定的存续商品，采购确认胡萝卜汁、清远鸡及 4 个非零待换算行；随后一次性处理另外 33 个零库存阻塞行，不能因为本次数量为零而跳过模板治理。
3. 总厨确认黄喉、汤底调味粉、白葱、紫葱、大米龙、腊火腿等 BOM，并为赠品/员工餐建立明确消耗来源。
4. 将确认结果制作成绑定当前文件哈希的复核清单，再运行“旧基线滚动 / 新实盘 / 映射与换算 / 重复档案合并”四柱 dry-run；必须达到 167 行都有确定去向、`canCommit=true` 且差异金额可重复复算。
5. 备份后把 7 月 22 日盘点作为新的不可变实物基线；不改写历史收货、消耗或旧快照。提交后立即复核新基线金额、逐行数量、重复档案零引用以及 7 月 23 日以后滚动结果。

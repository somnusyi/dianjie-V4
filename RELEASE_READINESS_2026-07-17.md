# 滇界 V4 发布候选就绪报告

生成时间：2026-07-17（北京时间）  
候选分支：`release/20260715-p0`  
已验证代码候选：`7c2c2f0`（报告文档提交不改变业务代码）
执行边界：仅本地代码与 `dianjie_v4_local`，未推送、未部署、未访问或修改生产数据与凭证。

## 1. 结论

本分支已从“功能可演示”推进为具备系统化发布门禁的候选版本。核心订货、配送、收货、门店库存、供应商库存、日报导入、BOM 消耗、报损、应付、对账、付款、发票、资金台账、账号申请及认证链路均增加了租户/角色隔离、并发串行化、数据库唯一约束、事务原子性、故障恢复和专项回归。

当前建议是：**可进入受控生产发布评审，但不能跳过备份、迁移基线核对、真实外部配置核对和发布后验收。** 本报告不代表已部署生产。

## 2. 本次长时任务成果

- 共形成 48 个可审计推进轮次和 51 个本地功能/验证提交（相对本地 `origin/main` 跟踪引用；报告文档提交另计）。
- 日报双文件支持门店/日期/金额一致性、预览版本、BOM 指纹、并发确认、历史审计及失败回滚。
- 订货单原始快照、改单确认、业务单号、配送、收货、短量报损、财务派生和库存流水均具备幂等/并发保护。
- 门店预计库存坚持“盘点基准 + 实收入库 - BOM/人工/报损消耗”，与供应商库存严格解耦。
- 应付计划、对账、付款、发票、发票付款、备用金、资本支出和银行同步统一使用真实资金账户、Decimal 金额、唯一业务引用、原子审计与凭证恢复。
- 公开申请、邀请链接、申请审批、用户管理、登录、刷新、企微 OAuth 全面收紧输入边界和权限。
- 修复普通管理员可创建/晋升超级管理员的越权路径。
- JWT 强制区分 `access` 与 `refresh`；企微不再签发 365 天旧式旁路令牌。
- 用户密码变更增加 `authVersion`；旧 refresh token 在改密或管理员重置后立即失效，旧 access 最多按 2 小时自然过期。
- 通知、账期提醒和企微投递增加业务幂等键、发送前预占与失败恢复，避免多实例重复触达。
- 企微公开 OAuth 入口和配置接口增加严格输入、限流、超级管理员兼容及配置/审计原子回滚，专项测试未调用真实企微。
- 付款规则由“无匹配即自动付款”改为“无匹配必须人工审批”，创建/修改/删除均具备严格 schema、租户校验和原子审计。
- 供应商银行资料与门店银行/税号/支付配置按角色做 ORM 层安全投影，管理缓存与低权限请求彻底分域。
- 修复供应商账期枚举与数据库不一致、旧编辑页回传只读字段导致保存失败等兼容问题。
- 正式 standalone 按真实部署目录组装后完成六角色 41/41 浏览器门禁；同时修复旧财务中心和付款审批页的首屏时间 hydration 漂移，生产控制台错误归零。
- 门店收款配置增加店长本店范围、严格白名单、事务锁和审计原子回滚；企微 callback token 与 AES key 改为只写不回显。
- 通知列表、全部已读和单条已读统一收紧收件人/角色边界与查询白名单；同租户其他用户不能查看或代为标记指定通知，非法分页不再触发 500。
- 商品、收货、凭证、日志和菜品榜单统一复用有界整数解析，NaN、Infinity、小数及超限分页在 ORM 前返回 400。
- 删除从未注册、无任何引用但仍含任意请求体批量写入的 259 行初始遗留路由包，防止未来误注册重新引入越权与批量赋值风险。
- 公开招行回单下载增加限流和严格查询白名单，`download=0/1` 语义明确，未知参数不会触发文件系统访问。
- 前端幂等操作号统一兼容 HTTP、旧 WebView 和缺失 Web Crypto 的环境，修复供应商调整实发数量时 `crypto.randomUUID is not a function` 导致请求未提交的问题。

完整逐轮证据见 [LONG_RUN_2026-07-16.md](./LONG_RUN_2026-07-16.md)。

## 3. 数据库变化

当前迁移链共 46 条，可从空 PostgreSQL 数据库完整部署且 schema diff 为零。本轮新增/强化的关键约束包括：

- 日报审计状态、外键、版本和 BOM 指纹完整性；
- 订货、配送、对账、付款、发票付款、资金流水的唯一业务键；
- 通知业务幂等键；
- 用户 `authVersion`（第 46 条迁移，默认 0，向后兼容现有账号和历史 refresh token）。

生产发布前必须先确认生产迁移账本与已知历史 schema 指纹一致。禁止用 `prisma db push` 替代迁移部署。

## 4. 自动化验证基线

最终候选要求同时满足：

- API 单元/集成测试：74/74；
- Web 单元测试：6/6；
- API TypeScript 构建：通过；
- Web TypeScript：通过；
- Next.js 正式构建：135/135 页面并生成 standalone 产物；
- 空库迁移：46/46，`migrate status` 最新，schema diff 为零；
- 七账号 API 门禁：32/32；
- 六角色浏览器门禁：41/41，控制台错误 0、5xx 0；
- 专项数据库脚本：日报、库存、订货、配送、收货、报损、对账、付款、发票、资金、通知、账号与认证全部通过；
- 临时账号、申请、供应商、触发器、函数及迁移临时库残留：0；
- 工作树只允许保留用户原有未跟踪文件 `库存主数据映射审计_2026-07-15.xlsx`，不得加入发布提交。

封版实测证据：API 74/74、七账号 API 32/32、正式 standalone 浏览器 41/41 且控制台错误 0、Next.js 135/135 页面、46/46 空库迁移、schema diff 0、测试残留 0。首次清空 `.next` 后启动开发服务器会出现一次 Next dev 冷编译 RSC 降级并被门禁正确阻断；该开发态现象未发生在按真实部署结构启动的正式产物中。

## 5. 生产发布顺序

1. 冻结候选提交，人工审查 `origin/main...release/20260715-p0`，确认不包含 Excel、`.env`、密钥、数据库导出或临时文件。
2. 读取生产状态但不写入：当前提交、PM2 进程、磁盘、数据库连接、迁移账本和 schema 指纹。
3. 取得发布窗口批准，创建生产数据库全量备份和当前应用构建备份，并现场验证备份可读取。
4. 先执行部署脚本 `--dry-run`，确认目标主机、目录、分支、提交、迁移数量和回滚标签。
5. 使用标准迁移链部署第 46 条及之前尚未登记的迁移；任何漂移、失败或重复历史迁移都立即停止，不自动猜测修复。
6. 部署 API 与 Web 构建，使用 reload/restart 让新 Prisma Client 和 JWT 中间件生效。
7. 执行健康检查、七账号 API 门禁、浏览器门禁，并核对真实门店/供应商只读页面。
8. 观察至少 30 分钟：5xx、登录/refresh 401、数据库连接、任务调度、通知失败、凭证失败队列和资金流水异常。

## 6. 发布后的重点验收

- 密码账号可登录、refresh 可续期；refresh token 不能直接访问普通 API。
- 企微用户需重新走一次 OAuth，得到新的 access + refresh 双令牌；旧 365 天企微 JWT 会被拒绝，这是安全收敛的预期行为。
- 管理员不能创建、晋升、禁用或重置超级管理员；超级管理员授权路径正常。
- 店长上传两份日报时能预览、确认、查看历史；未来日期、错店、金额不平和 BOM 阻断项被拒绝。
- 店长/厨师长库存只显示绑定门店，供应商库存只显示本供应商。
- 订货、接单、发货、收货、报损、对账、付款、发票的状态和金额在各角色页面一致。
- 后台默认不访问招行、美团或真实企微外部接口，只有生产显式开关和配置完整时才启用对应任务。
- 普通订货角色读取供应商/门店候选时不含银行、税号、支付密钥、员工和经营指标；老板/财务管理页面仍正常。
- 付款规则没有命中时返回“需要审批”，不能自动放行。

## 7. 明确未自动执行/仍需人工确认

- 未推送 GitHub，未合并 main，未部署生产。
- 未轮换现有凭证和证书（按当前用户指令暂缓）；仍建议安排独立安全窗口处理。
- 未调用真实招行、美团、企微、OSS 外部动作；正式配置和网络权限必须在发布窗口单独验证。
- 未修改或提交用户的库存主数据审计 Excel。
- 差异审计共 142 个文件、约 21,300 行新增/3,550 行删除；无大于 20MB 的跟踪文件。敏感扩展名检查仅命中 `.env.example`，其中新增内容均为默认关闭的占位开关，无真实值。
- `虎掌菌` 单份扣减克重仍缺业务口径，因此日报确认会继续正确阻断该项，不能由开发自行猜测。
- 已确认入库单更正、补送与报损收货的最终业务规则仍应按客户确认结果另开变更，不混入本次候选。

## 8. 上线约束与剩余架构风险

- **招行自动付款必须继续关闭。** `CMB_AUTOPAY_ENABLED` 只有在生产显式为 `true` 才会触发，但当前付款规则管理页及 `/api/payment-rules/evaluate` 尚未接入账期生成/调度器；实际账期仍使用代码内固定“超过 2,000 元需审批”口径。必须先由业务确认 `block`、优先级、月累计和新供应商规则语义，再统一为一个策略服务并完成影子评估/UAT，不能仅因页面已可配置就打开真实扣款。
- **真实支付和企微密钥仍缺少应用层加密。** Store 中多个 `...Enc` 字段目前仍按 TODO 直接存值，`WeComConfig.appSecret/contactSecret` 也由数据库原文保存。正式录入或轮换真实密钥前，应完成 KMS 或 AES-256-GCM 信封加密、只写不回显和历史密文迁移；本轮遵循指令未轮换现有凭证。
- access token 在账号停用或改密后最多仍可使用既定 2 小时；refresh 已通过 `authVersion` 立即失效。若客户要求即时踢下线，需要为每次请求增加版本/状态缓存校验或短 access + introspection，需评估数据库与缓存成本。
- 企微未获得手机号权限时仍保留“租户内唯一同名未绑定员工”自动匹配逻辑。正式大规模启用前应评估改为管理员确认绑定，避免同名/改名场景误绑。
- 按角色广播的通知目前仍共享单条 `read` 状态；本轮已阻断指定用户通知的越权已读。若业务要求每位同角色员工各自维护已读状态，需要新增 `NotificationRead` 关系与数据迁移，不能复用当前共享布尔值。
- Next.js 开发态清空 `.next` 后首次冷编译会产生一次 RSC fallback；正式 standalone 已按部署脚本相同结构组装 `.next/static` 与 `public` 并完成 41/41、控制台 0 的生产模式门禁。后续 CI 应固化同一门禁，避免退回只测开发服务器。

## 9. 回滚原则

- 首选应用构建回滚，不主动回滚数据库；本轮迁移大多为加列/加约束，旧应用兼容性须在发布前再核对。
- 只有数据确实被错误写入且业务负责人确认可接受数据丢失时，才使用带数据库的回滚。
- 回滚后仍需跑健康检查、登录、订单、库存、财务只读页和迁移状态；不能以 PM2 进程“在线”代替业务验收。

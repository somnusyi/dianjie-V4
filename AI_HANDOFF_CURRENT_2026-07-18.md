# 滇界 V4 当前项目交接文档

> 更新时间：2026-07-18 11:20（Asia/Shanghai）
> 目标读者：接手开发的 AI、架构师、后端、前端、测试与发布负责人
> 当前工作区：`/Users/somnusyi/Documents/Codex/dianjie-V4-rebuilt`
> 当前分支：`release/20260715-p0`
> 最新业务代码提交：`cb45fab`（其后仅有交接文档提交）
> 重要：本文是 2026-07-18 当前状态入口；旧文档中与本文冲突的报损、补送、供应商库存和盘点口径，以本文及 `SUPPLY_CHAIN_DECISIONS_2026-07-18.md` 为准。

## 1. 项目概况

滇界 V4 是南京云洱之境餐饮集团使用的多角色连锁餐饮经营管理系统，当前以合肥瑶海店为真实试点。系统覆盖：

1. 门店订货；
2. 供应商接单、库存预占、调整实发、配送；
3. 门店收货与到货差异；
4. 供应商库存和门店预计库存两本独立账；
5. 每日营业与菜品销量双表导入；
6. 菜品 BOM 换算食材消耗；
7. 报损、对账、付款、发票、资金与凭证；
8. 老板、财务、总厨、店长、厨师长、供应商等工作台。

当前仍采用模块化单体，不拆微服务。除招行辅助服务外，主要业务都在 Next.js + Fastify + PostgreSQL Monorepo 内。

## 2. 当前结论

项目已经不是原型，核心订货、配送、收货、日报、BOM、库存和财务链路均有真实业务代码与自动化测试。当前发布候选分支在权限隔离、并发、幂等、事务、审计和历史快照方面做了大规模加固。

当前代码门禁已经通过，但进入生产前仍需完成受控发布步骤：

- `release/20260715-p0` 尚未合并 GitHub `main`；
- 本轮没有核验生产服务器实际运行的 Git 提交和迁移账本；
- 必须先完成生产备份、迁移 dry-run、合并审查和发布后验收。

## 3. Git 与仓库状态

### 3.1 GitHub

- 远程：`git@github.com:somnusyi/dianjie-V4.git`
- GitHub `main`：`2515e0e`
- GitHub `release/20260715-p0`：已包含业务提交 `cb45fab` 及后续交接文档提交
- 本地业务代码基线：`cb45fab`
- 发布分支相对 `origin/main`：完成本文修订后预计领先 25 个提交，落后 0 个提交。
- 当前没有合并 `main`，也没有在本轮部署生产。

### 3.2 本地 Gitea

- 仓库地址：`http://192.168.1.129:1606/weilun/dianjie-V4.git`
- 当前 clone 的 Git remote 只有 `origin`（GitHub），尚未配置 Gitea remote。
- 2026-07-18 无凭证 `git ls-remote` 可连通但返回 0 个分支；接手者不要假设代码已经同步到 Gitea。
- 不要把 Gitea、GitHub或生产账号密码写入仓库文档；凭证由项目负责人单独提供。

### 3.3 最新供应链收口提交

以下修改已提交为 `cb45fab fix: finalize arrival claims and voucher numbering`：

| 文件 | 作用 |
|---|---|
| `apps/api/src/routes/lossClaims.ts` | 关闭验收后补报；历史旧客户端请求返回 409；差异确认/仲裁不再回补供应商库存 |
| `apps/api/src/services/lossClaimResolution.ts` | 删除按金额比例回补供应商库存数量的废弃逻辑 |
| `apps/api/tests/routes/supplyChainFlow.integration.test.ts` | 更新供应链 E2E，验证补报关闭、供应商库存不回补、应付仍正确 |
| `apps/api/tests/services/lossClaimResolution.test.ts` | 删除废弃库存回补算法测试 |
| `apps/web/src/app/v2/chef/purchase/[id]/receive/page.tsx` | 收货页明确“确认即供应商责任截止点”，移除补报入口 |
| `apps/web/src/app/v2/chef/purchase/[id]/report-loss/page.tsx` | 历史直达页改成规则说明和门店盘损引导 |
| `apps/web/src/app/v2/chef/purchase/po-success/[id]/page.tsx` | 移除订货成功页的“补报短量”入口 |
| `SUPPLY_CHAIN_DECISIONS_2026-07-18.md` | 最新已确认业务决议和本轮落地证据 |

本交接文档将在状态更新后单独作为文档提交。

未跟踪文件 `库存主数据映射审计_2026-07-15.xlsx` 是用户业务文件，禁止擅自删除、修改或加入 Git。

## 4. 技术架构

```text
用户浏览器 / PWA / Capacitor
          │
          ▼
    Next.js 14 Web
          │ REST
          ▼
   Fastify 4 TypeScript API
          │
          ▼
   Prisma 5 + PostgreSQL

外部能力：Redis、阿里云 OSS、招行 CMB Python 服务、美团、企业微信
```

### 4.1 Monorepo

| 路径 | 职责 |
|---|---|
| `apps/web` | Next.js 14、React 18、Tailwind、PWA/Capacitor、多角色页面 |
| `apps/api` | Fastify REST API、领域服务、任务调度、外部集成 |
| `apps/cmb` | 招行国密请求辅助服务 |
| `packages/db` | Prisma schema、56 条迁移、客户端与 seed |
| `scripts` | 构建、部署、迁移核验、数据导入与专项 E2E |

### 4.2 核心工程原则

- 所有正式查询必须带 `tenantId`；门店角色再带 `storeId`，供应商角色再带 `supplierId`。
- 金额必须使用 Prisma `Decimal`，不能使用 JavaScript 浮点累计资金。
- 关键写入必须用数据库唯一约束、事务锁、行版本或业务幂等键兜底，Redis 不能成为唯一保证。
- 状态变化、金额变化和库存变化必须与审计事实处于同一事务，或有明确可重试补偿。
- 历史单据读取冻结快照，商品改名、改分类、停售不能改变历史单据展示。
- 微信/旧 Android WebView 中禁止依赖原生 `window.confirm` 和 `crypto.randomUUID`；分别使用 `ConfirmSheet` 和 `clientRequestId()`。
- 禁止用 `prisma db push` 替代迁移。

## 5. 已确认且不得自行改变的业务规则

### 5.1 订货与配送

- 所有货物由供应商直接送门店，目前没有集团总仓。
- 一张订货单可以多次配送，每次配送生成独立配送单。
- 原始订货量、单价和金额必须保留；供应商修改订单后必须由门店确认。
- 供应商发货前可以调整本次实发量，实发量决定配送金额和供应商库存扣减。
- 页面必须区分订购量、实发量、实收量，以及订货金额、实发金额、应付金额。

### 5.2 收货、到货差异与补送

- 门店必须在收货确认时一次性核对数量、短缺、破损、变质、品质异常和现场证据。
- 收货确认是供应商责任截止点。
- 收货确认后不得再向供应商补报；过后发现的问题不追溯供应商。
- 不建立“免费补送”选项；当前规则是不补送。
- 差异应付按“合格实收数量 × 订货冻结单价”计算。
- 到货差异确认、异议和仲裁只影响责任、应付和争议状态，不自动改变供应商物理库存。
- 供应商库存已在实际发货时按实发量扣减；收货短缺或破损不能再自动加回供应商库存。
- 已确认入库单的商品、数量、价格和应付不能因事后盘点倒改。
- 非金额资料错误可做只留审计的备注更正；实物差异进入门店内部盘损或盘点差异。

### 5.3 供应商库存

- 当前供应商只有一个默认仓。
- 供应商审核通过的商品对所有门店开放。
- 物理库存、活动预占、可用库存分开：`可用 = 物理 - 活动预占`。
- 接单预占，取消/拒单释放，发货核销预占并扣物理库存。
- 供应商报损、调整和快照不能把物理库存降到活动预占以下。
- 供应商库存与门店库存是两本完全独立的账。

### 5.4 门店预计库存与盘点

- 门店预计库存公式：最近可信盘点基准 + 后续确认实收入库 - BOM/人工消耗 - 门店内部报损。
- 门店盘点与供应商无关，不能生成供应商差异、应付调整或供应商库存变化。
- 在线盘点的产品规则已经确定，但功能尚未正式开发完成：每月闭店全品项盘点，启用食材必须全部填写，零库存填 0。
- 未匹配 SKU 或单位换算未确认时禁止过账。
- 盘盈/盘亏只形成门店内部库存差异和成本影响。
- 非零差异必须填原因；建议差异率超过 5% 或金额超过 200 元时要求图片证据，阈值应做成配置。
- 已确认盘点不可直接编辑；录错时整单冲销后重盘。
- 当前权限可由统一授权人员提交并确认，架构预留 `inventory.count` 和 `inventory.approve`。

### 5.5 营业、菜品和 BOM

- 美团接口正式接通前，店长每天上午 11:00 前上传前一天两份 Excel：综合营业统计、菜品销售明细。
- 必须先预览再确认；确认后原子更新营业、菜品销量和可计算库存消耗。
- 退菜不补回库存。
- BOM 使用毛重口径。
- 百家蘸料不扣库存；赠品按对应库存商品直接扣减。
- 鲜鸡和冻品可按已确认映射替代，不能模糊自动串货。
- 菜品未建档或缺可执行 BOM 时可明确暂缓，不阻塞整份日报；待办进入总厨 BOM 工作台并支持后补历史消耗。

### 5.6 外部系统与安全边界

- 招行自动付款必须继续关闭，未经业务授权不得启用。
- 美团生产接入尚未完成，真实营业数据仍以每日双表为准。
- 企业微信完整 SSO/通知仍受 HTTPS、域名和配置约束。
- 证书处理和凭证轮换按项目负责人要求暂缓，接手者不得自行轮换。
- 不得在交接文档、日志或提交中写入生产数据库、云密钥、Git 密码或员工密码。

## 6. 当前模块进度

| 模块 | 当前成熟度 | 已完成 | 主要待办 |
|---|---|---|---|
| 登录/账号/权限 | 高 | access/refresh、角色隔离、供应商绑定 fail-closed、改密撤销 refresh | 生产测试账号治理、企微绑定策略 |
| 订货单 | 高 | 原始快照、改单确认、并发业务编号、幂等 | 真实持续 UAT |
| 配送单 | 高 | 独立配送实体、分批配送、实发调整、旧 WebView 兼容 | 取消/冲销规则和长期导出 |
| 收货/到货差异 | 中高 | 原子收货、并发重试、责任节点、应付基准、证据和打印；确认后不可补报已提交 | 生产 UAT |
| 供应商商品/分类 | 中高 | 商品审批、图片、分类、合并、批量动作、并发锁、审计 | IngredientMaster/SupplierOffer 长期模型评审 |
| 供应商库存 | 中高 | 物理/预占/可用、批次、FEFO 分配、流水、并发保护 | 长期分页/导出、批次 UAT、负库存治理 |
| 门店预计库存 | 中高 | 盘点基准、实收入库、BOM/人工消耗、盘损滚动 | 正式在线月度盘点工作流尚未实现 |
| 日报双表导入 | 高 | 门店/日期/金额校验、预览、原子确认、BOM 指纹、版本和暂缓 | 连续多日真实运营和更正版本 UAT |
| 菜品/BOM | 中 | 菜品档案、规格配方、暂缓任务、历史回补 | 补齐真实销售菜品 BOM、减少待办 |
| 经营报表 | 中高 | 营业额、营业收入、订单、优惠和时间对比 | 多店、渠道和美团自动核对 |
| 财务/凭证 | 中高 | 应付、对账、付款、发票、资金、凭证和恢复；凭证数据库序列与来源幂等锁 | 真实财务 UAT |
| 美团 | 代码存在/生产未启用 | 签名、解析、同步测试 | 平台审核、配置、门店映射和双跑核对 |
| 招行 CMB | 代码就绪/自动付款关闭 | 微服务、失败关闭和幂等 | 业务授权、影子验证、真实配置 |
| 企业微信 | 中 | OAuth/通知代码和安全边界 | HTTPS、域名、SSO 与通知 E2E |
| CI/监控 | 中低 | 本地与发布门禁脚本较完整 | CI 固化、Sentry、staging、告警 |

## 7. 数据与环境现状

### 7.1 本地服务

- Web：`http://127.0.0.1:3200`，2026-07-18 `/v2/login` 返回 200。
- API：`http://127.0.0.1:4444`，2026-07-18 `/health` 返回 200。
- PostgreSQL：Docker `dianjie_v4_local_postgres`，仅绑定 `127.0.0.1:5434`。
- Redis：Docker `dianjie_redis`，健康。
- `dianjie_v4_local`：56 条迁移，状态最新。
- `dianjie_v4_test`：本轮新建的独立集成测试库，56 条迁移，状态最新。

### 7.2 本地 722 条历史商品问题

722 条不是当前正式供应商缺商品，而是两个旧本地租户的历史 `Product.supplierId` 指向已不存在的供应商：

- 旧租户“滇界”：360 条；
- 旧租户“滇界测试”：362 条；
- 628 条正库存、34 条零库存、60 条负库存；
- 被 1,236 条历史入库明细引用；
- 没有被当前订货单、配送、BOM、消耗或盘点快照引用。

已按项目负责人确认，在本地把这两个旧租户设置为 `SUSPENDED`。没有删除商品，没有补绑供应商。此修改是本地数据库状态，不在 Git 中。

生产是否存在同类问题尚未在本轮核验。任何生产处理必须先做只读审计，禁止直接套用本地结论。

### 7.3 最近已记录的生产业务快照

`TEAM_HANDOFF_2026-07-17.md` 记录过 2026-07-17 的生产快照：1 家合肥瑶海正式门店、20 个启用账号、1 家正式供应商、456 个商品 SKU、162 个菜品、326 行 BOM、2026-07-13 盘点基准 167 项。

这些数值可能已经随真实营业和采购发生变化，只能当历史参考，下一位 AI 如需使用必须重新只读核验。

## 8. 当前验证证据

最新未提交供应链规则改动完成后，已验证：

- API 单元测试：106/106 通过；
- PostgreSQL 集成测试：47/47 通过；
- Web 单元测试：13/13 通过；
- API TypeScript/build：通过；
- Web TypeScript：通过；
- `git diff --check`：通过；
- 本地业务库和独立测试库：56 条迁移，均为最新；
- 最新 Web 正式生产构建：138/138 页面生成并验证通过；构建后本地 dev 已安全恢复。

原 `Voucher(tenantId, no)` 并发碰撞已经修复：凭证号改用数据库 `BusinessSequence`，同来源使用事务级锁。真实 PostgreSQL 测试验证 12 个不同来源并发编号连续唯一，同一来源 8 次并发仅生成 1 张凭证，失败队列为 0。

## 9. 生产状态边界

2026-07-18 本轮只做了 HTTP 只读检查：

- `http://116.62.32.162:8080/` 返回 307 跳转；
- `/api/health` 返回 200；
- `/v2/login` 返回 200。

本轮没有 SSH 登录生产机，没有读取生产数据库，没有核验 PM2 实际提交，也没有部署或写生产数据。

旧交接文档记录 2026-07-17 13:50 曾发布提交 `5e0ac5a`，但当前 GitHub `main` 已是 `2515e0e`。接手者必须把生产实际提交作为未知状态重新核验，不能从旧文档推断。

## 10. 下一位 AI 的建议执行顺序

### P0：完成受控合并和发布

1. 阅读本文和 `SUPPLY_CHAIN_DECISIONS_2026-07-18.md`。
2. 推送 `release/20260715-p0`，复核远端提交与本地一致。
3. 只读核验生产实际提交、PM2、磁盘、数据库迁移账本和 schema 指纹。
4. 完成生产数据库和应用备份，并验证备份存在。
5. 审查 `origin/main...release/20260715-p0`，确认不包含 Excel、环境文件或凭证。
6. 合并到 `main`，从标准部署脚本发布，完成健康、登录、供应链、库存和财务只读验收。

### P1：实现门店在线月度盘点

业务规则已确定但功能尚未完成。建议状态机：

```text
DRAFT → COUNTING → REVIEWING → CONFIRMED
                         ↘ CANCELLED
CONFIRMED 发现录错 → REVERSAL → 新盘点单
```

最小闭环应包括：

- 创建盘点单并冻结 SKU 清单；
- 全品项录入，0 库存必须显式填 0；
- 单位换算和未匹配阻断；
- 账面、实盘、差异数量、移动平均成本、差异金额；
- 差异原因和阈值证据；
- 原子确认生成新的 `InventorySnapshot` 与内部差异流水；
- 不生成 `LossClaim` 供应商责任、不改供应商 `Product.stock`、不改应付；
- 并发确认、重复提交、冲销重盘和跨门店权限测试。

### P1：真实供应链 UAT

- 实发调整 → 配送 → 收货差异 → 供应商确认/异议 → 总厨仲裁 → 应付/对账；
- 确认后旧补报入口和 API 必须失败；
- 供应商库存只按实际发货扣减，差异结案不回补；
- 分批配送金额、实收和应付逐单核对；
- PC 供应商端和厨师长移动端都做真实渲染验证。

### P2：主数据和长期模型

- 继续补齐真实销售菜品 BOM；
- 评审 `IngredientMaster` 与 `SupplierOffer` 拆分方案，不要直接创建空壳模型；
- 设计月结正式写模型前，继续使用现有只读月度对账核对真实数据；
- 为供应商库存流水、批次和预占补长期分页/导出。

## 11. 禁止事项

- 禁止删除或提交用户 Excel。
- 禁止把本地历史租户处理直接复制到生产。
- 禁止重新开放“验收后补报”。
- 禁止把门店盘点差异转成供应商报损。
- 禁止自动回补供应商库存。
- 禁止增加免费补送。
- 禁止绕过迁移使用 `prisma db push`。
- 禁止直接在生产目录编辑代码。
- 禁止开启招行自动付款或擅自轮换凭证/证书。
- 禁止把真实账号密码、数据库 URL、AccessKey、Secret 或 token 写入 Git。

## 12. 常用验证命令

以下命令默认在仓库根目录执行。

```bash
# 类型与单元测试
pnpm --filter @dianjie/api exec tsc --noEmit
pnpm --filter @dianjie/api test
pnpm --filter @dianjie/web exec tsc --noEmit
pnpm --filter @dianjie/web test

# API 构建
pnpm --filter @dianjie/api build

# Web 正式构建：必须先按安全脚本处理正在运行的 dev
pnpm --filter @dianjie/web build

# 迁移状态；DATABASE_URL 必须指向本地隔离库
pnpm --filter @dianjie/db exec prisma migrate status --schema prisma/schema.prisma

# 工作区检查
git diff --check
git status --short
git diff --stat
```

集成测试必须使用数据库名以 `_test` 或 `_ci` 结尾的独立数据库；测试守卫会拒绝业务库和生产库。

## 13. 关键文件索引

| 文件 | 用途 |
|---|---|
| `SUPPLY_CHAIN_DECISIONS_2026-07-18.md` | 最新供应商责任、补送、库存、入库更正与盘点规则 |
| `TEAM_HANDOFF_2026-07-17.md` | 7 月 17 日完整架构、角色、生产业务快照和多人协作说明 |
| `NIGHTLY_REVIEW_2026-07-16.md` | 7 月 15–18 日供应链加固逐轮证据 |
| `LONG_RUN_2026-07-16.md` | 早期 48 轮全项目安全与质量加固记录 |
| `RELEASE_READINESS_2026-07-17.md` | 旧候选发布门禁与回滚原则，数据已部分过时 |
| `SUPPLY_CHAIN_LONG_LOOP_2026-07-17.md` | 供应商端长期架构和已完成工作，末尾旧待确认规则已被 7 月 18 日决议覆盖 |
| `packages/db/prisma/schema.prisma` | 当前数据模型 |
| `apps/api/src/routes/orders.ts` | 订货与收货主流程入口之一 |
| `apps/api/src/routes/deliveries.ts` | 独立配送单和实发流程 |
| `apps/api/src/routes/lossClaims.ts` | 到货差异、门店盘损、供应商处理与仲裁 |
| `apps/api/src/services/storeInventory.ts` | 门店预计库存权威计算 |
| `apps/api/src/routes/supplierStock.ts` | 供应商库存、批次、预占和流水 |
| `apps/api/src/routes/dailyBusinessImports.ts` | 每日双表导入与确认 |
| `apps/web/src/app/v2/chef-director/bom` | 总厨 BOM 待办工作台 |
| `scripts/web-next-safe.sh` | 防止 dev/build 相互破坏 `.next` 的安全入口 |

## 14. 交接完成标准

下一位 AI 开始修改前应能明确回答：

1. 当前供应商责任截止点是什么？——收货确认。
2. 确认后能否补报？——不能。
3. 到货差异是否回补供应商库存？——不回补。
4. 门店盘点是否关联供应商？——完全不关联。
5. 当前代码是否已合并 `main`？——没有；完成本文修订后发布分支预计领先 25 个提交。
6. 最新供应链改动是否已提交？——已提交为 `cb45fab`。
7. 用户 Excel 能否加入提交？——不能。
8. 是否可以直接部署生产？——代码门禁已通过，但仍需生产只读核验、备份、合并审查和发布审批。

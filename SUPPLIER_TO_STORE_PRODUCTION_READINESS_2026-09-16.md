# 供应商 → 供应链总仓 → 门店全链路生产就绪评审

> 评审日期：2026-09-16
> 评审角色：总产品负责人 / 技术负责人
> 代码基线：`main @ d9e09ee90cb337de1608c1ec9b1323980d113241` 之上的本地候选改动
> 当前状态：**代码未提交、未推送、未部署生产、生产开关未开启**

## 1. 总结结论

本地候选版已经完成并验证了第一阶段 P0 全链路：

`外部上游供应商 → 供应链采购 → 总仓收货/库存 → 厨师长门店下单 → 总仓拣货出库 → 门店验收/库存`

产品和代码层面可以进入**预发布环境、生产暗部署及受控灰度准备**，不建议现在直接全量生产启用。剩余阻断项不是核心页面或业务状态机缺失，而是生产环境必须完成的历史数据分类、真实账号与对象存储验证、备份恢复演练、真实库存对账和小范围灰度。

放行判断：

| 层级 | 结论 |
| --- | --- |
| 本地功能完整性 | GO |
| 自动化回归 | GO |
| 生产构建 | GO |
| 空库迁移安全 | GO |
| 预发布/UAT | GO |
| 生产暗部署（功能关闭） | 满足本文前置条件后 GO |
| 单供应商真实灰度 | 完成生产数据校验后 GO |
| 全量生产启用 | 当前 NO-GO，需灰度数据证明 |

## 2. 已确认的产品边界

1. 外部供应商第一阶段全部送到总仓，不直接向门店履约。
2. 外部上游供应商范围为 `WAREHOUSE_UPSTREAM`，只能看到本公司的总仓采购业务。
3. 门店看到的供货主体是内部总仓履约主体；内部总仓供应商使用 `sourceType=HEADQ_WAREHOUSE`，并具备 `STORE_FULFILLER` 范围。
4. 外部采购收货生成真实外部应付；总仓向门店履约属于内部调拨，不重复生成外部供应商应付。
5. 月结按月生成，业务模型保留可配置结算周期、账期天数、税率、价格、单位换算和差异规则。
6. 正常采购入库最终必须走采购单、发货单和收货单；普通手工入库只在切换期和审批例外中保留。

## 3. 全链路完成度

| 环节 | 使用角色 | 已完成能力 | 验证结论 |
| --- | --- | --- | --- |
| 供应商准入 | 供应链 | 新建上游供应商、业务范围隔离、负责人邀请、启停 | 完成，页面+数据验证 |
| 供应商账号 | 上游供应商 | 邀请激活、登录、租户/供应商绑定、首次引导 | 完成，手机视口验证 |
| 角色体验 | 上游供应商 | 独立总仓采购首页、角色待办、过滤无关门店直供入口 | 完成，视觉+接口验证 |
| 商品供货关系 | 供应链 | 商品、供应商 SKU、采购单位、库存单位、换算、报价 | 完成，页面+数据库验证 |
| 合同 | 供应链 | 合同版本、有效期、结算、含税、价格、容差和启用 | 完成，端到端验证 |
| 采购下单 | 供应链 | 草稿、审核、发送、冻结商品/单位/价格快照 | 完成，端到端验证 |
| 接单改单 | 上游供应商/供应链 | 接单、供应商提出数量/日期修改、采购接受或拒绝 | 完成，版本不覆盖验证 |
| 发货 | 上游供应商 | 发货单、分批发货、批次、效期、物流字段 | 完成，分批履约验证 |
| 总仓收货 | 供应链 | 实到、合格、破损、拒收、短缺、普通入库 | 完成，数量守恒验证 |
| 高风险复核 | 独立复核人 | 大额/超收/临价/敏感品类第二人复核 | 完成，同人拒绝+异人通过 |
| 总仓库存 | 系统 | 余额、批次、效期、移动流水、数量和价值原子入账 | 完成，数据库不变量验证 |
| 到货差异 | 双方 | 短缺/破损/质量/错货/超收差异、证据、响应、仲裁 | 完成，正常与拒绝分支验证 |
| 收货后补报 | 门店/供应链 | 时限内补报隐蔽到货异常、证据、幂等扣款/损耗 | 完成，重复提交验证 |
| 收货冲销 | 供应链 | 未消耗批次整单冲销、正反流水保留 | 完成，净数量/净价值归零 |
| 月度对账 | 双方/财务 | 收货额、扣款、净应付、供应商确认、财务锁定 | 完成，来源可追溯 |
| 门店目录 | 厨师长 | 只展示可由总仓履约且库存可用的商品 | 完成，页面+数据验证 |
| 门店下单 | 厨师长 | 提交采购单、锁定总仓库存、查看物流状态 | 完成，真实浏览器验证 |
| 总仓履约 | 供应链 | 接单、拣货/出库、送达、扣减内部总仓库存 | 完成，端到端验证 |
| 门店验收 | 厨师长 | 验收、门店库存入账、收货后异常入口 | 完成，真实浏览器验证 |
| 权限隔离 | 所有角色 | 角色、租户、供应商对象级隔离 | 完成，负向用例验证 |
| 待办工作台 | 供应链/供应商 | 按角色统计订单、发货、收货、差异和对账待办 | 完成，接口+页面跳转验证 |

## 4. 角色可用性评审

### 上游供应商负责人

- 首次登录只看到“总仓采购”业务说明，不再误导为门店直供供应商。
- 可查看本供应商采购单、接单、提出改单、分批发货、处理差异和确认月结。
- 看不到其他供应商数据，也看不到无关门店订单、直供库存、商品管理和经营分析入口。

### 供应链采购/总仓

- 可完成供应商准入、商品关系、合同、采购、收货、复核、差异、对账和总仓库存管理。
- 首页展示上游待办总数和快捷入口。
- 供应商管理只展示上游供应商，不混入门店履约主体。

### 财务

- 只处理双方已确认的月度对账单并锁定。
- 供应链角色不能越权锁账。
- 内部总仓向门店的履约不会建立外部付款计划。

### 厨师长/门店

- 从总仓可供目录下单，查看履约进度，并在送达后验收。
- 验收后门店库存增加；总仓库存按内部履约扣减。
- 收货后发现隐藏的到货质量问题仍可在窗口内补报；临期、使用损耗、盘点差异不错误归责给供应商。

## 5. 自动化和构建证据

| 验证 | 结果 |
| --- | --- |
| 浏览器视觉端到端 | 5/5 通过，覆盖 E2E-00～E2E-07 |
| 零到一完整主链 E2E-00 | 新建供应商 → 邀请登录 → 首单入库/月结 → 门店下单 → 总仓履约 → 门店验收，1/1 通过 |
| 可见 Microsoft Edge 真实点击 | 2026-09-17 在最新代码上以四个独立角色会话重跑 E2E-00，1/1 通过（业务步骤约 30 秒，总耗时约 51 秒） |
| API 全量测试 | 109 个文件、1123 项通过 |
| Web 全量测试 | 61 个文件、786 项通过 |
| API 生产构建 | 通过 |
| Web 生产构建 | 通过；仅有已有 OpenTelemetry 动态依赖警告 |
| Web 严格 TypeScript 检查 | 通过（单独执行；生产构建脚本默认跳过此项） |
| 代码变更格式 | `git diff --check` 通过 |
| 数据库迁移 | 90 条从空库顺序执行成功；状态最新；与 Prisma 模型零差异 |

视觉证据：`test-results/upstream/`
HTML 报告：`playwright-report/upstream/index.html`

## 6. 本轮真实操作发现并修复的产品问题

1. 上游供应商曾沿用门店直供供应商首页和引导，角色入口错误；已按业务范围拆分体验。
2. 新建供应商后缓存异步失效，快速刷新可能仍显示旧列表；现改为等待缓存清除完成。
3. 新供应商排在分页末尾，用户会误以为创建失败；现自动按供应商编号筛选并选中。
4. 收货登记后没有自动切换到验收页签；已修复。
5. 同一人第二复核曾返回 500；现为明确的业务冲突，并要求独立账号。
6. 部分收货后无法继续第二批发货；已修复。
7. 最后一批存在确认短缺时订单无法完结；已改为按实到量加已确认短缺判断履约。
8. 收货重试或并发可能重复计数/暴露 500；已实现幂等返回和可恢复冲突。
9. 邀请激活后的登录链接可能丢失租户；已显式保留租户上下文。
10. 供应链和供应商缺少集中待办；已增加按角色的服务端工作台和首页入口。

## 7. 生产前必须完成的非代码工作

以下任一项未完成，都不应全量启用：

1. 对生产数据库做可恢复备份，并在生产副本执行迁移和恢复演练。
2. 将真实外部供货商标记为 `WAREHOUSE_UPSTREAM`；不要把门店履约主体误标成上游供应商。
3. 唯一的内部总仓履约主体核对为 `HEADQ_WAREHOUSE + STORE_FULFILLER`。
4. 核对商品四种单位、上游来源、换算、价格、合同和最小起订量；异常商品不得进入可供目录。
5. 用盘点结果确认总仓期初数量、批次和价值，库存台账与实盘必须对平。
6. 在预发布验证真实邀请渠道、对象存储、图片/视频证据上传和下载权限。
7. 使用真实的供应链、供应商、财务和厨师长账号完成一次 UAT，并抽查双租户隔离。
8. 对订单创建、收货确认、出库和验收做小规模并发/超时演练。
9. 保证生产日志、错误告警、数据库慢查询和库存对账报警可观察。

## 8. 功能开关与推荐推进顺序

| 阶段 | `UPSTREAM_PROCUREMENT_ENABLED` | `UPSTREAM_RECEIPT_POSTING_ENABLED` | `UPSTREAM_MANUAL_INBOUND_RESTRICTED` |
| --- | --- | --- | --- |
| 暗部署 | `false` | `false` | `false` |
| 预发布全流程 | `true` | `true` | `false` |
| 生产影子观察 | `true` | `false` | `false` |
| 单供应商真实过账 | `true` | `true` | `false` |
| 稳定后限制手工入库 | `true` | `true` | 分租户/逐步改为 `true` |

推荐顺序：

1. 代码评审并形成候选提交。
2. 生产备份恢复演练和生产副本迁移。
3. 预发布四角色真实 UAT。
4. 选 1 家供应商影子运行 3 个工作日，与原手工记录逐行核对。
5. 同一供应商真实过账 5 个工作日；库存、重复流水和应付差异必须全部为 0。
6. 扩展到 3～5 家供应商并跑完一个自然月对账。
7. 最后才限制普通手工采购入库，保留审批例外入口。

## 9. 回滚原则

1. 第一时间关闭采购与收货过账开关；手工入库限制保持关闭。
2. 已生成业务单据和库存流水不删除、不改历史，使用冲销或调整单纠错。
3. 不回滚数据库迁移删除表或字段；应用版本回滚必须保持新结构向前兼容。
4. 灰度期每日保存库存余额、移动流水、收货、扣款和应付对账快照。

## 10. 第一阶段之后的明确范围

这些项目尚未作为本轮 P0 完成，不应在销售或培训中宣称已经具备：

- 企业微信、短信、邮件形式的站外主动提醒；站内待办已完成。
- 锁账后的跨期调整单。
- 发票、付款申请、银行付款和回单的专项完整端到端闭环。
- 自动补货预测、多总仓调拨和高级运输调度。
- 外部供应商直接向门店履约；当前产品决定为全部先送总仓。

## 11. 最终负责人意见

当前版本不是“演示页面”，而是已经通过真实角色、真实页面操作和数据库不变量验证的生产候选版。核心功能无已知 P0 阻断，可以进入预发布与受控灰度；但在生产数据和环境门槛完成前，不应跳过暗部署和灰度直接全量开放。

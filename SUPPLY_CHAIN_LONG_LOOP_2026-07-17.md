# 滇界 V4 供应链长时开发 Loop

更新时间：2026-07-17
状态：执行中
适用分支：`release/20260715-p0`

## 1. 已确认的业务边界

- 供应商当前只使用一个默认仓，所有货物从供应商直接送到门店。
- 供应商审核通过的商品对全部门店开放，暂不配置门店可售范围。
- 供应商以电脑端为主要作业入口；手机端保留接单、发货、库存查询等紧急操作。
- `SUPPLIER_OWNER` 与 `SUPPLIER_STAFF` 当前执行同一权限集。
- 权限判断统一使用 capability，未来可拆为订单员、仓管、财务和只读岗位，不重新改造领域接口。
- 保持模块化单体，不在当前阶段拆微服务。

## 2. 目标架构

```text
供应商组织与权限
  └─ capability policy（当前统一，未来按岗位组合）

商品主数据
  ├─ 集团食材主档（BOM 引用，后续引入）
  ├─ 供应商商品/报价（当前 Product，渐进迁移）
  └─ 供应商分类（当前平面分类，支持治理、合并与审计）

采购履约
  订货单 → 供应商确认 → 核对实发 → 配送单 → 门店实收

库存
  默认仓余额 → 预占 → 实发扣减 → 调整/报损 → 不可变流水

质量与结算
  到货差异/报损 → 仲裁 → 应付调整 → 对账 → 发票 → 付款
```

长期原则：

1. 订单、配送、实收和应付金额分别命名，禁止用一个“总金额”混用。
2. 所有数量、金额和状态变化都必须在单个数据库事务内落业务事实和审计记录。
3. Redis 幂等只作加速；关键写入必须由数据库唯一约束、行版本或事务锁兜底。
4. 供应商数据范围默认拒绝；账号缺少供应商绑定时不能退化为租户级访问。
5. 历史单据保留快照；商品改名、改分类或停售不能改变历史打印结果。
6. 新能力先加表/双写/核对，再切读，禁止一次性破坏式迁移。

## 3. 循环执行协议

每轮固定执行：

1. 核对当前代码、数据库迁移、Git 差异和本轮业务边界。
2. 选择一个可独立验收的最小闭环，先补失败/越权/并发用例。
3. 实现 API、事务、审计、Web 交互和兼容层。
4. 运行单元测试、独立 PostgreSQL 集成测试、API 构建、Web 测试、类型检查和生产构建。
5. 对关键页面做电脑端和手机端真实渲染检查。
6. 更新本文件、交接说明和下一轮 P0/P1 优先级。
7. 未通过发布门禁的代码不推生产；不在未确认情况下改生产数据。

## 4. 12 小时建议节奏

| 时间 | 工作块 | 交付物 |
|---|---|---|
| 0–1h | 基线审计 | Git/迁移/测试/生产差异清单 |
| 1–3h | P0 事务与权限 | 越权、重复写、并发写修复及负向测试 |
| 3–5h | 主数据 | 分类、商品、报价、审批和批量操作闭环 |
| 5–7h | 履约 | 订货、实发、配送、实收状态机与金额核对 |
| 7–9h | 库存 | 默认仓余额、预占、流水、批次和成本恒等式 |
| 9–10.5h | 差异与结算 | 报损、仲裁、应付、对账、发票一致性 |
| 10.5–11.5h | E2E 与监控 | 正向链、异常链、跨供应商隔离、业务告警 |
| 11.5–12h | 发布准备 | 迁移 dry-run、构建产物、回滚点、交接文档 |

如果某轮失败，优先修复并重新执行该轮，不以“跳过测试”换取表面进度。

## 5. 当前完成情况

### P0 已完成

- 供应商订单范围 fail-closed；缺少 `supplierId` 直接拒绝。
- 发货必须从 `CONFIRMED` 进入，要求业务幂等键，并用状态 + `rowVersion` 原子认领。
- 库存快照不再自动创建零价可订商品；未知 SKU 整批阻断并进入待建档处理。
- 报损仲裁、库存比例回补、账期调整、结论和日志在一个事务中完成。
- 全局幂等移到认证后，缓存键包含租户和用户，并校验请求体哈希。
- 集成测试只允许 `_test` 或 `_ci` 数据库；清理操作按测试标识精确限定。
- CI 重建全部迁移、检查 schema drift，并运行 API 单元/集成测试和 Web 测试。

### 供应商应用框架已完成

- 新增统一 PC 侧边栏：工作台、订单履约、库存、商品与报价、账单与发票、经营分析、操作记录。
- 手机端继续保留底部快捷导航；宽屏自动隐藏移动导航。
- 导航带 capability 元数据，当前角色共享全集。
- 订单与配送状态、进度和金额名词集中到共享业务词典。
- 修复 `CANCELED/CANCELLED` 状态拼写漂移。

### 分类管理 V2 第一阶段已完成

- 分类概况、搜索、启用/停用筛选。
- 分类改名和停用前展示影响数量并二次确认。
- 排序改为本地调整后一次保存，避免每点一次立即写库。
- 支持分类合并：事务性移动全部 SKU、停用来源、写审计日志。
- 分类合并使用供应商级事务锁，并验证系统分类保护和跨供应商隔离。

### 默认仓可用库存闭环已完成

- 新增独立库存预占事实表；物理库存、已占库存、可用库存不再混用。
- 接单在订单状态 CAS 的同一事务中预占；已接订单撤回/拒单释放；发货核销并扣物理库存。
- 发货只能使用本单预占和未预占余量，不能挤占其他已接订单。
- 盘点、库存快照和供应商报损不能把物理库存降到 ACTIVE 预占以下。
- 生产迁移会回填既有 `CONFIRMED` 订单的剩余预占，历史超卖显示可用 0 而不会继续扩大。
- 库存总览和 SKU 详情显示物理、已占、可用；SKU 可追溯到具体占用订单。
- 两张订单并发抢库存、超额实发、释放和核销已有独立 PostgreSQL 集成测试。

### 履约与金额口径第一阶段已完成

- 订货金额、实发金额、实收/应付金额在订单列表和详情中分开展示。
- 配送、入库、报损明细冻结商品编码、名称、规格、单位和分类；商品主档后续改名不改变历史打印。
- 供应商客户、SKU 排行和月度趋势改为读取确认入库单/入库明细，不再用订货金额冒充收入。
- 新增真实 API 写流程集成测试：下单 → 接单预占 → 幂等发货 → 送达 → 按实收确认 → 报损 → 应付/对账。
- 供应商账号、绑定、`authVersion` 或供应商状态失效后，下一次请求立即拒绝旧 access token。

### 商品主数据与审批语义第二阶段已完成

- 商品页形成 PC 数据表 + 手机卡片双布局；电脑端可快速扫描 SKU、分类、报价、起订量、可用/已占库存、状态，并支持批量选择。
- 批量改类、批量停售/恢复、上传历史和操作日志保留在同一工作台。
- 供应商新品上架与停售新增 `SUPPLIER_OFFER_CREATE` / `SUPPLIER_OFFER_DISABLE` 专用审批类型，不再把供应商 SKU 冒充“新菜品”。
- 历史 `NEW_DISH` 供应商审批单继续兼容处理和预览；新旧单据不会中断。
- 专用审批类型由真实 API 集成测试验证，并保持总厨直审。
- 商品资料与涨价申请改为单次 PATCH 原子命令；规格、起订量等资料保存和涨价审批单创建不会再发生前端两次请求导致的半成功。
- 集成测试验证涨价时旧价格保持不变、其他资料在同一事务保存、总厨调价审批单同时生成。

### 到货差异与可追溯性第一阶段已完成

- 报损新增明确来源：到货短缺、到货破损/品质异常、门店内部报损、历史待核。
- 新到货短缺精确关联订货单、配送单、收货单和配送明细；历史记录不冒险猜测履约链。
- 报损详情/打印单展示来源类型、配送单和收货单，商品仍读取冻结快照。
- 供应商数据健康审计检查到货短缺链路是否缺失或错配。
- 报损拒绝/仲裁优先调整报损绑定的精确收货账期，不再依赖采购单的旧“主收货单”兼容指针。

### 到货差异与应付基准第二阶段已完成

- 供应商订单页的“报损”升级为“到货差异工作台”，展示差异类型、配送单、收货单、责任节点、冻结快照、证据和打印入口。
- 修正“确认差异等于再次扣款”的错误文案：入库时已按实收净额生成的差异只结案，不重复调整；验收后补报才在确认后调整原应付。
- 新增明确应付基准 `NET_AT_RECEIPT`、`GROSS_PENDING_CLAIM`、`NOT_APPLICABLE`、`LEGACY_UNKNOWN`，不再从状态或历史指针猜财务含义。
- 验收产生差异后先冻结对应账期；供应商确认后释放，提出异议则保持冻结并进入总厨仲裁。
- 验收后补报必须绑定具体收货单/配送单；分批配送不允许猜主收货单，且同一收货单同时只允许一笔未结差异。
- 补报明细按配送行累计控制上限，已确认或待处理数量均占用额度，仲裁为零的数量可重新主张，防止重复超额索赔。
- 补报创建时冻结原账期；供应商确认后才扣减应付，拒绝时不重复加回金额。
- 收货页和补报页可明确选择“数量短缺”或“破损/品质异常”。
- 数据健康审计按“实收 + 已结差异”推导应付，不再把合法差异调整误报为金额错误，并检查未结争议是否冻结。
- 差异确认、异议和仲裁在同一事务内同步付款账期、对账明细、对账总额及尚未支付的付款单；已进入付款或已付款时拒绝自动改额。
- API 错误封装优先显示业务原因，不再把“已有未结差异”等错误退化成通用 `Conflict`。

### 月度对账只读模型第一阶段已完成

- 新增供应商月度对账页面，按月逐笔展示订货金额、实发金额、实收金额、应付金额和应付调整。
- 同一订货单分批配送/收货时，月度订货金额按订货单去重；实发、实收和应付仍按独立履约事实汇总。
- 页面明确显示争议冻结、应付派生缺失、到货差异数量及涉及金额，并可回到原订货单追溯。
- 月结视图当前只读，不修改既有“一张入库单一条付款账期”的付款兼容链；验证稳定后再迁移正式月结写模型。
- 月结接口按供应商 fail-closed，双供应商集成测试确认不能读取其他供应商入库单。

### 到货差异独立工作台已完成

- 到货差异从订单页内嵌筛选拆为独立页面，PC 侧边栏、订单页和账单页均可直达。
- 支持月份、责任节点、差异类型以及差异单/订货单/配送单/入库单/门店关键字筛选。
- 供应商可在同页确认差异或提出异议，并可进入原订单、打印带证据详情、导出防公式注入的 CSV。
- 列表 API 增加严格状态、类型、日期和分页参数校验，供应商数据范围继续 fail-closed。
- 修正供应商账单口径：争议冻结只是暂缓付款，仍计入应收总额并单独拆示冻结金额。

### 审计与发布门禁补强已完成

- 数据健康检查覆盖负库存、超预占、预占状态漂移、配送/入库/应付金额、快照、到货差异追溯。
- 新增库存账本恒等式：连续流水的前余额 + 变动 = 后余额，最后流水余额 = 商品当前库存。
- 新增双供应商真实数据库隔离测试，覆盖商品、库存、订单、配送和到货差异入口。
- 当前自动化基线：API 单元测试 96 项、独立 PostgreSQL 集成测试 29 项、Web 测试 12 项全部通过。
- 测试库已应用 55 个迁移，schema drift 为 0；清空旧缓存后的隔离目录生产构建完成 138 个页面。

## 6. 下一轮任务

### 第3轮剩余：商品主数据

1. 在新增表之前完成 `IngredientMaster` 与 `SupplierOffer` 字段、单位换算和双写策略评审；不建立无法持续同步的空壳表。

### 第4轮：履约

1. 明确一次配送与未来分批配送边界；当前先禁止并发活动配送单。
2. 以已上线的月度只读模型核对真实数据后，设计正式月结写模型、供应商确认/异议、财务锁定与付款切换迁移；切换前保留旧兼容链。
3. 为月度对账增加服务端全量导出和金额异常钻取；当前页面导出上限 100 条并明确提示缩小筛选条件。

### 第5轮：默认仓库存

1. 批次库存从“流水上的到期日”升级为可耗尽的批次余额；当前不切 FIFO。
2. 盘点继续按业务要求暂缓，但保留调整入口与审计能力。

## 7. 发布门禁

满足以下条件才可同步生产：

- 全部迁移从空库可重建，`prisma migrate diff --exit-code` 无差异。
- API 单元测试、独立数据库集成测试、API build 全通过。
- Web 测试、TypeScript、生产构建全通过。
- 供应商 A 无法访问供应商 B 的商品、库存、订单、配送、报损、发票。
- 重复点击和并发发货不会重复生成配送单或重复扣库存。
- 分类合并、商品停售、库存导入、报损仲裁均有审计记录。
- staging 完成一笔下单到收货的正向链和一笔报损仲裁异常链。
- 发布前完成数据库备份、迁移 dry-run 和回滚演练。

## 8. 待业务确认（不会阻塞安全开发）

到货短缺/破损在供应商同意后，目前兼容逻辑会自动把损失数量加回供应商仓库。架构上更合理的做法是：

- 到货差异只调整责任、应付和争议状态，不自动改变供应商物理库存；
- “实际少发、货仍在仓内”由供应商发起一笔有原因、有证据的库存更正；
- “补送”必须建立新的配送事实并扣库存，不存在免费或无流水补送。

此处会影响真实库存口径，未获得业务确认前不自动切换。

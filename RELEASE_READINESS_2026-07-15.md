# 滇界 V4 发布候选验收记录

日期：2026-07-15
范围：订货单不可变、分批配送/收货、供应商商品与分类、供应商库存、门店预计库存、经营指标修正
环境：本地隔离库 `dianjie_v4_local`；生产环境未修改

## 当前结论

代码和本地数据已达到“发布候选”条件，但尚未达到“立即生产上线”条件。

生产上线前仍有两项硬门槛：

1. 使用生产数据库备份副本完整演练 34 条迁移，并核对迁移前后订单、收货、库存和账号数据。
2. 由业务方完成店长、厨师长和供应商三个角色的 UAT，并确认发布窗口与回滚责任人。

## 已完成

- Prisma 迁移：34 条全部应用，本地 `migrate status` 正常。
- Schema 校验：本地现有库与 `schema.prisma` 零差异。
- 空库演练：34 条迁移从头执行成功，最终 schema 零差异。
- API 单元/集成测试：42/42 通过。
- API TypeScript 构建：通过。
- Web TypeScript 检查：通过。
- API/Web 生产构建：通过。
- 端到端：订货不可变、分批配送两次收货、供应商商品、供应商分类、门店预计库存、盘点主数据绑定全部通过。
- 浏览器：供应商订单、商品、分类、库存和店长工作台/库存页面均可正常加载，无运行时错误。
- 瑶海 7·13 盘点：167/167 品项已在本地建立并绑定商品主数据；预计库存金额 31,649.586 元；供应商库存仍为 0。

## 本轮修复的发布阻塞

- 修复本地 Prisma 迁移账本缺失。
- 修正供应商分类表的更新时间默认值和索引名漂移。
- 修复美团同步游标在模块加载时固化机构 ID，导致配置更新后游标写入错误机构的问题。
- 为库存主数据导入/验证脚本补齐环境变量加载，避免安全护栏误报。
- 忽略本地 `.next.stale-*` 构建缓存，避免误提交。

## 已知非阻塞项

- Web 当前没有 Vitest 测试文件，因此以前端生产构建、TypeScript 检查、API 端到端和真实浏览器回归替代。
- Next.js 构建存在 OpenTelemetry 动态依赖警告，但构建成功，不影响本轮页面功能。
- 167 个盘点商品的真实供应商、采购单位换算、安全库存和保质期仍待业务补齐；当前不会虚构这些数据。

## 生产发布门禁

任何一项不满足即停止发布：

- 已生成可恢复的生产数据库备份，并完成恢复抽检。
- 已在生产备份副本完成迁移演练，且 `migrate status` 和 schema diff 正常。
- 已记录待发布 Git commit，代码审查无凭证和业务数据泄露。
- 店长、厨师长、供应商 UAT 签字确认。
- 发布窗口、观察人和回滚责任人明确。
- 发布后依次验证健康检查、登录、订货、配送、收货、供应商库存、门店库存和营业指标。

## 回滚原则

- 迁移以向前修复为主，不在生产执行 `prisma migrate dev`、`db push` 或 reset。
- 发布失败先停止新版本服务并恢复上一代码版本；涉及数据不一致时使用已验证备份恢复方案。
- 未完成生产备份副本演练前，不合并到可能自动部署的 `main`。

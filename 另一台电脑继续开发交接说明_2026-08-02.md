# 滇界 V4 供应链真实库存改造：跨电脑交接说明

交接日期：2026-08-02
正确项目目录：`/Users/weiyi/Documents/Codex/dianjie-V4`
当前分支：`feature/20260801-meituan-warehouse-snapshot-codex`

## 1. 必须复制哪个目录

复制完整的 `/Users/weiyi/Documents/Codex/dianjie-V4` 文件夹，包括隐藏的 `.git`。

桌面的 `/Users/weiyi/Desktop/dianjie-V4-main` 是旧下载版：没有 `.git`，也不包含本轮真实库存改造，不能作为交接版本。

当前改动尚未提交、尚未推送。只在另一台电脑重新 `git clone` 会丢失本轮新增代码。使用 Finder、移动硬盘或局域网复制完整文件夹最稳妥。`node_modules` 和 `.next` 可以不复制，在目标电脑重新安装生成。

注意：当前仓库是带 promisor remote 的部分克隆，个别历史对象可能需要联网向 GitHub 补取；但当前工作树和本轮新增文件都在本机。复制 `.git` 后，目标电脑应先联网执行一次 `git status`，必要时再 `git fetch`。

7月31日库存 Excel 和方案报告不在项目仓库内，需要一并复制：

- `/Users/weiyi/Downloads/%E4%BE%9B%E5%BA%94%E9%93%BE7.31%E6%97%A5%E5%BA%93%E5%AD%98.xlsx`
- 本文件及同目录下两份供应链方案报告。

## 2. 已确认的业务口径

1. 门店原则上向总仓订货；只有 `Supplier.sourceType=HEADQ_WAREHOUSE` 的订单进入总仓库存账，外部直采不进入。
2. 点击“发货”就是货物实际离开总仓的时点，在此扣减总仓物理库存。
3. 当前没有分批送货；一次有效发货后关闭未发余量并释放相应预占，未来可独立扩展分批履约。
4. 供应商暂不介入本轮库存管理，后续可能通过小程序管理。
5. 总仓入库当前采用单条手工录入，不做入库文件导入。
6. 7月31日美团表是当日出入库结算后的历史盘点；由于缺少8月1日起连续库存事实，只用于商品映射、单位核验和历史差异分析，不能作为当前期初或采购入库。

## 3. 本轮已经实现

- 新建独立总仓真实库存内核，余额唯一维度为“租户 + 仓库 + 商品”，不按供应商拆库存。
- 库存余额、预占、批次、流水统一使用库存单位；订单按行级冻结换算扣账。
- 单条手工入库：采购数量、入库单位、换算、库存数量、金额、移动平均成本、批次、效期、备注和幂等键原子入账。
- 手工入库冲销：只追加反向流水，不删历史；相关批次已消耗时拒绝整笔冲销。
- 门店订单库存钩子：总仓接单预占，撤回/拒单释放，实际发货扣实发数量；门店收货和到货报损不再次扣总仓。
- SHADOW 模式失败旁路：影子库存异常不会阻断现有门店订货、配送、收货和报损闭环；失败写操作日志。
- 历史订单事实可按稳定游标分页、幂等重放，单张坏单不阻断后续补记。
- 单 SKU 实盘校准，支持显式零库存基准。
- 四账审计：余额、累计流水、活动预占、批次数量及单位/负数/预占穿透/成本异常。
- 美团历史快照页面只保留预检；确认与撤销接口永久返回 `410 Gone`。
- 总仓库存页面已调整为仓库视角，移除供应商选择，增加手工入库、实盘校准、冲销、最近流水和审计状态。

## 4. 当前未提交文件

已修改：

- `apps/api/src/index.ts`
- `apps/api/src/routes/orders.ts`
- `apps/web/src/app/v2/supply-chain/inventory/import/page.tsx`
- `apps/web/src/app/v2/supply-chain/inventory/page.tsx`
- `packages/db/prisma/schema.prisma`

新文件：

- `apps/api/src/routes/warehouseInventory.ts`
- `apps/api/src/routes/warehouseInventoryImports.ts`
- `apps/api/src/services/warehouseInventoryImport.ts`
- `apps/api/src/services/warehouseLedger.ts`
- `apps/api/src/services/warehouseLedgerAudit.ts`
- `apps/api/src/services/warehouseLedgerReconciliation.ts`
- `apps/api/tests/routes/warehouseInventory.test.ts`
- `apps/api/tests/services/warehouseInventoryImport.test.ts`
- `apps/api/tests/services/warehouseLedger.test.ts`
- `apps/api/tests/services/warehouseLedgerAudit.test.ts`
- `apps/api/tests/services/warehouseLedgerReconciliation.test.ts`
- `apps/api/tests/services/warehouseShadowLedgerMigration.test.ts`
- `apps/web/src/app/v2/supply-chain/inventory/import/page.test.tsx`
- `apps/web/src/app/v2/supply-chain/inventory/page.test.tsx`
- `packages/db/prisma/migrations/20260801193000_warehouse_inventory_imports/migration.sql`
- `packages/db/prisma/migrations/20260802090000_warehouse_shadow_ledger/migration.sql`

## 5. 已完成验证

- API 非数据库回归：72 个测试文件、818 项测试，全部通过。
- Web 回归：32 个测试文件、549 项测试，全部通过。
- API TypeScript：通过。
- Web TypeScript：通过。
- Prisma schema validate：通过。
- Prisma client generate：通过。
- 新库存专项覆盖单位换算、权限、手工入库、冲销、零库存实盘、四账审计、迁移护栏和订单钩子。

本机没有 PostgreSQL、Docker 或 `DATABASE_URL`，因此尚未完成真实 PostgreSQL 迁移、数据库并发集成测试和完整登录页面验收。前端曾成功启动在 `http://localhost:3200`，但没有数据库时无法走完整登录与业务操作。

## 6. 专家评审结论

三位独立评审结论一致：

- Schema-only 部署：**Go**。
- 显式单租户 SHADOW 试点：**Go**。
- OFF/SHADOW 下的单条手工入库与受控冲销：**Go**，前提是单位已核验。
- STRICT 正式库存：**No-Go**。

STRICT 剩余阻断：

1. 缺少“仓库锁定 → 游标重放追平 → 事务内最终审计 → 双人审批 → CAS 原子写入 `STRICT + inventoryActivatedAt`”的受控激活入口。
2. 尚未完成下一次总仓同一 T0 的全 SKU 实盘，必须覆盖所有启用 SKU，包括零库存。
3. 尚未取得真实数据库下连续投影无失败、游标无积压、四账审计 `readyForStrict=true` 的验收证据。

## 7. 另一台电脑的运行条件

- Node.js 20.x
- pnpm 10.32.1
- Docker Desktop，或独立 PostgreSQL 16 和 Redis 7
- 必须使用隔离本地数据库，禁止连接生产 RDS

建议启动步骤：

```bash
cd /目标路径/dianjie-V4
nvm use
corepack enable pnpm
pnpm install --frozen-lockfile
docker compose up -d
```

随后创建明确命名的本地预览数据库，例如 `dianjie_v4_local`，并仅在当前终端显式传入：

```bash
export DATABASE_URL='postgresql://本地用户:本地密码@localhost:5432/dianjie_v4_local'
export PREVIEW_MODE=true
export NODE_ENV=development
export API_PORT=4000
export NEXT_PUBLIC_API_URL=http://localhost:4000
```

不要照抄或携带生产 `.env`。先核实 `DATABASE_URL` 的主机是 localhost、数据库名包含 `local`，再执行迁移：

```bash
pnpm --filter @dianjie/db exec prisma generate
pnpm --filter @dianjie/db exec prisma migrate deploy --schema prisma/schema.prisma
```

首次本地演示库需要使用安全 seed。基础 seed 已有非生产护栏，口令至少12位：

```bash
ALLOW_DEMO_SEED=true DEMO_SEED_PASSWORD='自定义本地测试口令' \
  pnpm --filter @dianjie/db exec tsx src/seed.ts
```

基础管理员账号为 `admin@dianjie.com`，密码就是上一步自行设置的 `DEMO_SEED_PASSWORD`。不要使用或传播生产账号。

启动：

```bash
pnpm --filter @dianjie/api dev
pnpm --filter @dianjie/web dev
```

页面：

- 登录：`http://localhost:3200/v2/login`
- 总仓库存：`http://localhost:3200/v2/supply-chain/inventory`

## 8. 接手后的执行顺序

1. 先确认复制后的 `git status` 与本文件第4节一致，不要清理未跟踪文件。
2. 在全新本地数据库执行完整 migration chain，检查新表、唯一约束和默认仓库。
3. 运行 seed，启动 API/Web，使用本地管理员登录库存页面。
4. 对手工入库、冲销、接单预占、取消释放、发货扣减、重复请求幂等和并发冲突做数据库集成验收。
5. 只在指定测试租户启用 SHADOW；默认保持 OFF，绝不能切 STRICT。
6. 记录页面问题和真实数据库验证结果，再决定是否整理提交。

## 9. 当前明确不做

- 不把7月31日快照写成当前库存。
- 不启用 STRICT。
- 不建设供应商小程序、完整采购入库单或文件导入。
- 不实现分批配送。
- 不删除或覆盖旧库存字段和历史流水。
- 不让影子库存异常阻断现有门店闭环。

详细设计与复核报告：

- `供应链真实库存改造总体方案_专家评审版_2026-08-01.md`
- `供应链真实库存改造_实施与复核报告_2026-08-02.md`

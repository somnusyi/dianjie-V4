# 仓库错发/漏发/质量问题 → 库存不准 的解决方案

> 编制：2026-08-15（接手评审 AI）
> 状态：方案待审批，未动代码
> 适用范围：门店 ← 供应链总仓的订货履约链（错发、漏发、质量问题三类）

## 0. 先说结论

库存"快速不准"的系统侧根因不是"发现不了差异"——收货按实收入库、差异单、48h 补报、证据/仲裁这些都已有。真正的洞是三个：

1. **差异结案不回写仓库账**：漏发时仓库出库按发货数记账（发 10 记 10），门店收 8 差异 2，应付口径闭环了，但仓库 ledger 上多记的 2 永远不冲回——错误沉淀在账里，只能等下次实盘对账（08-11 式）才浮出来，周期以天/周计。（已核实 `lossClaims.ts` 全程不写 `warehouseLedgerMovement`。）
2. **错发没有类型化通道**：`LossClaimKind` 只有 ARRIVAL_SHORTAGE / ARRIVAL_DAMAGE / INTERNAL_WASTE，没有"错品"。错发一旦被门店按订单行签收（数量对、品不对），**双账同时错且数量平衡**——门店 A 多 B 少、仓库 A 少 B 多，任何求和校验都发现不了，是最隐蔽的污染源。收货更正（ReceiptCorrection）只支持价格（已核实：仅 unitPrice/amount/inventoryUnitCost 的 before/after），改不了品项。
3. **补发是纯线下行为**：全代码无补发流程。差异确认后仓库重新发一批，不生成配送单、不出库流水——门店白收、仓库白出，双账各错一次。

修这三点，差异从"沉淀到下次盘点"变成"即时出清"。防错（扫码、称重）是第二优先级——先保证错了能立刻清账，再降低出错率。

## 1. 三类错误 × 现有捕获点 × 漏洞

| 错误类型 | 现有捕获 | 漏洞 |
|---|---|---|
| 漏发 | 收货按实收 → ARRIVAL_SHORTAGE，payableBasis NET/GROSS 闭环应付 | 仓库出库不冲回（见上） |
| 错发 | 无类型；门店发现只能文字描述塞进短缺/破损 | 无 WRONG_ITEM 类型；无品项级更正；签收后双账污染且不可自检 |
| 质量 | 当场拒收 / ARRIVAL_DAMAGE + 48h 补报 + 图片视频证据 + 仲裁 | 使用时才发现无通道；责任归属（仓库/运输/门店储存）无结构化字段；补发不记账 |

## 2. 方案分五层

### 第 1 层：账务闭环（止血，直接解决"快速不准"）

**1.1 差异结案自动回写仓库账**
ARRIVAL_SHORTAGE 差异单 APPROVED/AUTO_APPROVED 时，在同一事务（或幂等补偿事务）为涉及的订单行生成冲回流水：`WarehouseLedgerMovement(type=REVERSAL, reversalOfId=原出库流水, idempotencyKey=lossclaim-reversal:{claimId}:{itemId}, physicalDelta=+差异量)`，并重算该商品移动平均。
- 幂等三件套沿用现有模式：幂等键 + `reversalOfId` 唯一约束 + 事务内 replay 双查（`warehouseLedger.ts` 已有全部先例，纯复制模式）。
- 拒绝/仲裁金额调整（resolvedDeductAmount ≠ 全额）时按认可比例冲回。
- 注意与"每仓只保留一个出库来源"闸（8e7a365）的关系：冲回是 REVERSAL 不是 ORDER_OUTBOUND，不触发该闸。

**1.2 品项级收货更正（错发的纠正通道）**
扩展现有 ReceiptCorrection（复用其"发起 → 厨师长终审落地 → 行级 CAS"的成熟链路），从仅价格扩展为三种更正类型：
- `PRICE`（现状）
- `QTY`：数量更正（验收后发现的点数错误）
- `ITEM_SWAP`：品项替换——原行 A 冲销、新行 B（实际收到的 SKU）按当时冻结单位/因子补记；门店库存、应付、仓库账（A 冲回 + B 补记两笔调整流水）三方联动。
落地时再次校验原单 CONFIRMED/ACCOUNTED 的现有防护全部保留。

**1.3 补发流程**
差异单 APPROVED 后提供"生成补发"动作：以 claimId 为来源创建新 DeliveryOrder（normal 发货链路，出库记账幂等键 `delivery-outbound:{newDeliveryId}:{itemId}` 与现有发货完全一致），差异单记 `reshipDeliveryOrderId`。补发到货走正常收货。拒绝线下白送。

### 第 2 层：把错发变成显式事件（收货侧捕获）

**2.1 LossClaimKind 增加 `WRONG_ITEM`**，收货界面行级验收从"改实收数"升级为四态：
- 核对通过（默认）
- 数量差（实收数 < 应收 → 现有 ARRIVAL_SHORTAGE 路径）
- **错品**（实际 SKU ≠ 订单行 SKU → 选实际 SKU → WRONG_ITEM 差异 + 收货按实际 SKU 入库，或拒收）
- 质量问题（→ ARRIVAL_DAMAGE 现有路径）
错品行不进"按订单行签收"的默认流，杜绝污染。

**2.2 收货拍照/称重留证**：kg 类 BOM 毛重口径商品，关键 SKU 收货必称重、拍照（OSS 证据体系已有）。错发争议时"发货照 vs 收货照"可对质。

### 第 3 层：源头防错（发货侧）

**3.1 发货逐行确认 + 拍照**（低成本，先行）：ship 时每行可附照片；发货单打印带行级条码。
**3.2 拣货扫码校验**（分阶段）：商品外部编码已有 646 条映射基础。高价值/易错/规格相近 SKU 强制扫码（扫错即阻断），农产品散货暂豁免。收货侧同理：扫的商品必须匹配收货行。

### 第 4 层：质量问题分级

- 当场拒收（现有）→ 48h 拆包补报（现有）→ **使用时发现**：ARRIVAL_DAMAGE 增加责任判定字段（`仓库责任/运输损坏/门店储存不当`）与处理方式（`补发/扣款/折价`），窗口按商品保质期策略而非一刀切 48h。
- 处理方式联动：补发 → 1.3；扣款 → 现有应付调整；折价 → 价格更正 PRICE。

### 第 5 层：可度量、可追责

- **发货-收货一致率报表**：按仓库/商品/门店统计（发货-实收）/发货、错发次数、质量投诉率、补发时效 → 仓库 KPI，差异率超阈值自动预警（复用现有通知体系）。
- 现有"每日流水 + 次日实盘对账"保持，差异解释 SLA 化（当前 08-11 挂 36 项待处理）。
- 门店在线盘点落地：InventoryCount 目前 0 条真实记录；高价值 SKU 日盘、全量周盘，盘点差异确认后自动成为新基准。

## 3. 落地优先级

| 批次 | 内容 | 量级 | 理由 |
|---|---|---|---|
| P0-a | 1.1 差异结案回写仓库账 | 纯后端，模式全有现成 | 止血：漏发即时出清，不等实盘 |
| P0-b | 2.1 WRONG_ITEM 类型 + 行级验收四态 + 3.1 发货拍照 | 前后端轻量 | 把最隐蔽的错发变成显式事件 |
| P1 | 1.2 品项级收货更正、1.3 补发流程、第 5 层报表 | 中 | 闭环纠错与管理抓手 |
| P2 | 3.2 拣货/收货扫码、第 4 层质量分级、门店周盘落地 | 流程+硬件 | 降低出错率本身 |

## 4. 与现有机制的衔接（不重复建设）

- 幂等/冲销：`reversalOfId` 唯一约束 + 幂等键 + Serializable 重试（warehouseLedger.ts 现成）。
- 更正审批：ReceiptCorrection 的发起-终审-CAS 链路直接复用扩展。
- 证据：OSS 图片/视频 + 差异单打印已有。
- 对账：每日流水对账（47acd27/e0e3b83）继续作为兜底，P0-a 落地后其差异量应显著收敛——这本身就是验收指标。
- 注意：仓库 inventoryMode 仍为 OFF，以上全部是"记账+提醒"，不做发货硬拦截，与 STRICT 准入条件（交接文档 §9）不冲突。

## 5. 验收口径

1. 漏发场景端到端：订 10 发 10 收 8 → 差异 2 确认后，仓库 ledger 当日出现 +2 冲回流水，余额=流水累计审计通过，无重复冲回。
2. 错发场景端到端：订 A 发 B → 收货标错品 → 门店库存 A 不增 B 增、仓库账 A 冲回 B 补记、应付按 B 结算、差异单 WRONG_ITEM 留痕。
3. 补发场景端到端：差异确认 → 补发配送 → 补发出库记账 → 二次收货入库，全程单据链可追溯（claimId ↔ deliveryOrderId ↔ receiptId）。
4. 报表：任选一个月能给出各仓错发率/漏发率/质量投诉率。

## 6. 自审修正记录（2026-08-15 v2，已核实代码）

对初版方案逐场景推演后，修正以下六点：

1. **【补充·P0】拒收场景同样不冲回仓库账**（已核实 `/reject` 只改状态、不写 ledger）：当场拒收的货实物退仓，但发货时已记的出库流水不冲，仓库账虚少。1.1 的冲回范围必须覆盖"收货拒收（整单或行）"，不只是 ARRIVAL_SHORTAGE 结案。
2. **【补充·P1】错品退回的逆向物流**：错发 B 入门店后门店用不掉，实物退仓需要"门店出库 + 仓库入库"的退回记账（幂等键 `wrong-item-return:{claimId}:{itemId}`），初版只做了账面品项替换，漏了实物逆向流。与补发（A 补发 + B 退回）组合成错发的完整闭环。
3. **【修正·P1】补发的外键前提**（已核实 `DeliveryOrder.purchaseOrderId` 为非空 String）：配送单必须挂采购单。补发落地需二选一：生成免审批的"补发子采购单"（推荐，复用现有发货/收货全链路，金额 0 或按差异额）或放宽 schema。初版低估了此项改动面。
4. **【补充·P0】历史数据回补策略**：当前已有 22 张 APPROVED/AUTO_APPROVED 差异单未冲回仓库账。上线时必须决定：一次性回补（幂等脚本可重放，按 1.1 同样规则）或明确划线从上线日起生效。不回补则仓库账继续带着历史沉淀差，1.1 的效果打折。
5. **【补充·P0】现有数据的差异率基线报表提前**：差异单 29 张、发货-收货数据都在库里，出一张"按仓库/SKU/月份的短缺率、破损率"报表是纯查询零新功能，应作为 P0 一部分先行——既是管理抓手（立刻暴露问题集中的仓/SKU），又是后续所有改动的效果基线。
6. **【降权】扫码类防错降级**：该仓以农产品/散货为主，条码覆盖率天然有限，扫码只能覆盖预包装 SKU。主线是"称重 + 拍照 + 四态验收"，扫码作为预包装长尾增强，不作为主要手段。

另核实两点修正初版认知：
- `Product.shelfDays`（保质期天数，默认 7）**已存在**，第 4 层质量分级窗口有数据基础，无需先补主数据；但需抽查该字段真实维护率（默认值 ≠ 真实保质期）。
- LossClaimKind 无"超发/多送"类型：订 8 发 10 的场景目前无结构化通道（危害小于错发——账是平的，只影响应付与需求口径），列入 P1 随 WRONG_ITEM 一起补 `OVER_DELIVERY`。

### 仍需业务侧确认的两个前提（方案有效性的边界）

1. **仓库实际拣货作业方式**：若仓库是"按系统拣货单拣货"，扫码/行级校验类手段有效；若实际是"凭经验抓货、事后补录系统"，则一切发货侧校验都是纸面的，方案重心应完全压在收货侧捕获 + 账务闭环 + 盘点兜底。此点代码无法判断，需要现场确认。
2. **差异判定的信任模型**：1.1 的冲回以差异单判定为准，含 24h 超时自动同意（AUTO_APPROVED）。冲回的正确性 = 判定的正确性。若仓库侧对"超时即认可"有异议，应把冲回限定在人工 APPROVED，AUTO 的挂差异报表人工复核后冲。

## 7. 业务确认结果与方案收敛（2026-08-15 v3）

与项目负责人确认了上述两个前提，方案按以下结论收敛：

### 7.1 拣货方式：按单拣货，扫码出局

仓库实际按系统订单拣货（非事后补录），发货单即作业依据；商品以食材散货为主，条码不可行。据此：

- **扫码类防错整体移出方案**（原第 3.2 条作废）。
- 发货侧防错收敛为三件贴着拣货单的轻量手段：
  1. **拣货单行级勾选**：每拣完一行勾一行，错发的主来源（看错行、SKU 相近、拿错规格）在拣货时打断；
  2. **相近 SKU 警示**：同名不同规格/易混商品在拣货单上标记，易混对可从差异历史自动学习；
  3. **称重 + 拍照**：kg 类拣货称重留照，是散货唯一可靠的数量真值来源。

### 7.2 质量补报窗口：统一 48 小时（业务拍板）

所有差异类型（数量差、错品、质量）补报窗口统一 48h，不做长窗口。**取舍已知情**：使用时才暴露的质量问题（周一收货周四下锅发现变质）将无法向仓库追责，走门店内部报损（INTERNAL_WASTE）自行消化。

**配套补偿（可选，成本近零）**：内部报损单增加可选标记"疑似来货质量"——不开差异单、不调应付、不追责，仅进入仓库质量报表统计列。这样 48h 窗口保持简单，而"延迟暴露的质量问题"在仓库 KPI 画像中仍可见（区分"追责成功的质量差"与"门店消化的质量嫌疑"）。若不加此标记，第 5 层质量投诉率指标将系统性低估。

原第 4 层"按保质期动态窗口"作废；`Product.shelfDays` 保留为分析字段，不作为流程依据。责任判定字段（仓库/运输/门店储存）仍建议保留——48h 内的质量单同样需要归因。

### 7.2.1 差异发起方设计（2026-08-15 补充，与项目负责人讨论定稿）

**原则：货在谁手里，事实谁录入，对方确认。**

| 时点 | 差异类型 | 发起方 | 确认方 | 说明 |
|---|---|---|---|---|
| 发货前 | 货不够只能少发、规格替代 | **供应链改实发数** | 门店确认改单（现状） | 货还在供应链手里，供应链是事实方；改单价格不得静默修改（现状保持） |
| 收货时/收货后 | 少发、多发、错发、质量 | **店铺录入实收/差异** | 供应链确认（24h 超时视同认可，可拒绝进仲裁） | 货到了门店，门店是事实方；供应链不在现场，不应遥控改别人的实收数 |

**多发（超发）的处理——三个出口，按门店现场决定**：

1. **当场退车**（默认推荐）：实收录实际签收数（发 12 收 8 退 4），多发部分不进任何账，差异单标 OVER_DELIVERY 留痕。
2. **店铺留下多发部分**：实收按签收数 12 录入——门店库存 +12、应付按订单行单价 ×12、仓库出库按实发 12，**三账全平**；OVER_DELIVERY 差异照留（多发率是仓库拣货质量的信号，发 12 往往就是数错，账平了作业质量信号也不能丢）。
3. **入库后退回**：走错品退回同款逆向物流。

**出口 2 需要放开现有超收拦截**（已核实 `receipts.ts:495` 硬禁止实收 > 应收，此场景当前被 400 挡死），但必须有管理控制，不能开"门店随便多签"的口子：
- 前端：实收 > 应收时明确提示"多发 N 件、金额 ¥X 将计入应付"，二次确认；
- 后端：超收比例/金额设阈值（建议 ≤ 应收 20% 或单行金额上限，可配置），超限需店长角色确认；
- 价格：多收部分按订单行单价（同批同价）；供应商若按赠品处理，走价格调整/差异单，不是默认白送。

实施上与 P1 的 `OVER_DELIVERY` 枚举同批：差异类型 + 超收放开（含阈值与确认）一起交付。

**不允许供应链单方改收货结果**的理由：① 供应链是差异的责任方，责任方单方定案无制衡；② 供应链不在收货现场，不具备事实条件；③ 项目原则"已生效单据不静默改写"——差异走差异单而非改单据。

### 7.4 供应链负责人对话确认的需求（2026-08-15 补充）

与供应链负责人实际沟通后确认的事实与拍板：

1. **送达卡单根因 = 代点**："点送达"目前由老板/会计代点（送货员年纪大用不了系统），经常忘点导致门店收不了货（PO202608000011 卡 2 天即此原因）。送货人为正式员工。
2. **已拍板：送达 24h 超时自动推进**——业务确认"发货后 24h 内必达"（夜间发货、次日早到）。人工点送达仍是默认，超 24h 系统自动送达兜底。
   - 实现：调度器新增扫描 `DeliveryOrder.status='SHIPPED' && shippedAt < now-24h` → 复用 /deliver 的状态推进（rowVersion CAS + PO DELIVERING→PENDING_CONFIRM），打 `autoDelivered` 标记 + opLog。
   - **安全约束：系统自动送达的单必须跳过既有的"24h 自动收货"**，收货只认门店人工确认——防止"货未到 → 自动送达 → 自动收货 → 幽灵入账"的串联路径。自动送达解决卡状态，不代替收货事实。
   - 调度频率：现有 runDailyCheck 每天 01:00 一次，最坏需等约 48h 才触发；送达扫描很轻，建议提高到每小时。
3. **多发的真实习惯是"加单"**：门店留下多发货物时，供应链目前手工补一张采购单凑平账。方案 7.2.1 出口 2（放开超收直接签收）上线后应替代该习惯——实收直录、三账自动平、免走加单全流程。
4. **送达权限暂不下放**：维持负责人/会计点（权限模型已支持，无需开发）；仓库有专人管出入库后再过渡。
5. **作业画像（更正）**：仓库 4 个夜间发货员为正常年龄（仓储端界面无特殊约束）；**年龄偏大的是送货司机**——司机不使用系统，送达由负责人/会计点 + 24h 系统兜底，此现状已确认可接受。
6. **排期信号**：供应链负责人计划下周开始使用系统——P0 批次（冲回、四态验收、送达兜底）赶在此前后上线，并借机做带真实用户的 UAT。

### 7.5 收敛后的实施清单（含 7.4 更新）

| 批次 | 内容 | 变化 |
|---|---|---|
| P0 | 差异结案 + 拒收冲回仓库账；历史 22 张已确认差异单回补；WRONG_ITEM 类型 + 收货行级四态；现有数据差异率基线报表；**送达 24h 超时自动推进（跳过自动收货）**；（可选）内部报损质量嫌疑标记 | **新增送达兜底（已拍板，生产正在卡单）** |
| P1 | 品项级收货更正（含错品退回逆向物流）；补发子采购单流程；OVER_DELIVERY 超发类型 + **放开超收（阈值 + 店长确认，替代手工加单习惯）**；拣货单行级勾选 + 相近 SKU 警示 + 称重拍照（大龄夜班界面约束） | 超收与 OVER 同批 |
| P2 | 门店在线盘点落地（高价值日盘/全量周盘）；差异率阈值预警 | 扫码移除 |
